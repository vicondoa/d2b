# Per-Zone bundle emitter (ADR 0046).
#
# Reserved for ADR-046 work item ADR046-pstate-010. This is a scaffold: it
# declares no option, emits no artifact, and states no contract. The item that
# fills it owns this file.
#
# Note this is a distinct file from `zone-resources-json.nix`, which is landed
# and emits the per-Zone resource bundle JSON.
{ ... }:

{ }
