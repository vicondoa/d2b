# Generic schema-derived resource options (ADR 0046).
#
# Reserved for ADR-046 work item ADR046-credential-007. The option surface here
# is generic rather than ResourceType-specific. This is a scaffold: it declares
# no option and states no contract. The item that fills it owns this file.
{ ... }:

{ }
