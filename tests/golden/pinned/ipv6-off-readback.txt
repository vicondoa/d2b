# ipv6-off-readback — nixling-host netlink IPv6-off five-step sequence
# #[test]s. Successor of tests/ipv6-off-readback.sh, which only ran these
# #[test]s via substring filters (`cargo test -p nixling-host --all-features
# -- <name>`); each name resolves to exactly one netlink::tests test, and
# they already run in the gated `cargo test --workspace` pass in
# tests/tools/rust-workspace-checks.sh. Pinned fail-closed via
# tests/tools/assert-pinned-tests.sh.
netlink::tests::ipv6_off_sequence_runs_in_order
netlink::tests::drift_after_write_fails_closed
netlink::tests::bridge_nf_sysctls_applied_when_loaded
netlink::tests::ipv6_off_sequence_fails_closed_on_nm_precondition
