# runner-shape-preflight — d2b-host runner_shape #[test]s.
# Successor of tests/runner-shape-preflight.sh: that gate only looped
# `cargo test -p d2b-host --all-features --lib runner_shape::tests::<t>`
# over these names (d2b-host's fake-backends is #[cfg(any(test, feature))]
# so they run under the default `cargo test --workspace` pass in
# tests/tools/rust-workspace-checks.sh). Pinned fail-closed via
# tests/tools/assert-pinned-tests.sh.
runner_shape::tests::happy_path_yields_only_ok_finding
runner_shape::tests::missing_declared_runner_fails_closed
runner_shape::tests::empty_argv_hash_is_runner_shape_drift
runner_shape::tests::capability_drift_surfaces
runner_shape::tests::non_unix_vsock_transport_rejected
runner_shape::tests::sidecar_dag_mismatch_surfaces
runner_shape::tests::parity_drift_fixture_fails_closed
runner_shape::tests::ch_help_with_fd_selects_tap_fd
runner_shape::tests::ch_help_with_tap_only_selects_persistent_tap
runner_shape::tests::ch_help_without_fd_or_tap_fails_closed
