# broker-socket-acl — nixling-priv-broker integration test successor.
# Runs under the broker workspace `cargo test --features layer1-bootstrap`
# pass already exercised by tests/rust-workspace-checks.sh and enumerated by
# tests/tools/assert-pinned-tests.sh. One bare libtest path per line.
socket_acl_rejects_non_nixlingd_peers_and_accepts_daemon
