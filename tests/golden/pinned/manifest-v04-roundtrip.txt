# manifest-v04-roundtrip — d2b-core ManifestV04 golden round-trip
# #[test] successor of tests/manifest-v04-roundtrip.sh, which built xtask
# manifest_v04_check and ran tests/golden/manifest_v04/assert-roundtrip.sh
# against tests/golden/manifest_v04/baseline-vms.json.
# The integration test uses ManifestV04::from_path + to_compact_json on the
# same golden, asserts semantic compact JSON equality, and preserves every
# required networking path/value assertion from the retired shell gate.
# Pinned fail-closed via tests/tools/assert-pinned-tests.sh. One bare libtest
# path per line.
#
manifest_v04_roundtrip::baseline_vms_json_round_trips_semantically
