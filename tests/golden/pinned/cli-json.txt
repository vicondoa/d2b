# cli-json — CLI-contract integration tests for the unique behaviours of the
# retired tests/cli-json.sh bash gate (packages/d2b/tests/cli_json_contract.rs).
# Spawns the real `d2b` binary against the D2B_FIXTURES rendered bundle
# (copied into a hermetic temp bundle so the host's deployed /etc/d2b
# artifacts cannot shadow the fixture) plus synthetic state-root / daemon-state /
# system-state fixtures, and validates the pending-restart, keys-list daemon-down,
# and audit-on-a-TTY daemon-down JSON contracts strictly against the committed
# DTOs (deny_unknown_fields). One libtest path per line; checked by
# tests/tools/assert-pinned-tests.sh.
list_reports_pending_restart_when_booted_differs_and_active
status_reports_pending_restart_with_consistent_current_booted
keys_list_daemon_down_returns_structured_envelope
audit_json_stays_json_on_tty_with_daemon_down_envelope
