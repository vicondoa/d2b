# cli-auth-status — CLI-contract integration test (nixling crate, tests/*.rs).
# Successor for the retired cli-rust-native-auth-status.sh bash gate: spawns the
# real `nixling` binary and drives `auth status` across the launcher/none/admin
# roles via the env-file fixture contract, validating the JSON envelope strictly
# against nixling::AuthStatusOutputV2 (deny_unknown_fields) + the per-role authz
# surface. One libtest path per line; checked by tests/tools/assert-pinned-tests.sh.
auth_status_roles_match_schema_and_authz
