# Rust dry-run snapshot only; bash side comparison gated on Tier-2 self-hosted KVM runner.
{
  "busId": "1-2",
  "command": "usb attach",
  "mode": "dry-run",
  "notes": "USBIP dry-run reports the daemon → broker bind/lock, firewall, backend/proxy ensurement, reconcile plan, and guest import without mutating host or guest state.",
  "planned": [
    "UsbipBind",
    "UsbipBindFirewallRule",
    "SpawnRunner(sys-<env>-usbipd/backend)",
    "SpawnRunner(sys-<env>-usbipd/proxy)",
    "UsbipProxyReconcile",
    "GuestUsbipAttach(ssh sudo -n usbip attach)"
  ],
  "vm": "corp-vm"
}
