# Rust dry-run snapshot only; bash side comparison gated on Tier-2 self-hosted KVM runner.
{
  "command": "keys rotate",
  "mode": "dry-run",
  "notes": "Keys rotate --dry-run is a planned operation. --apply uses the daemon-first bridge and broker audit when the daemon handles the request.",
  "planned": [],
  "vm": "corp-vm"
}
