# Rust dry-run snapshot only; bash side comparison gated on Tier-2 self-hosted KVM runner.
{
  "command": "rollback",
  "mode": "dry-run",
  "notes": "d2b rollback --dry-run reports the planned operation; --apply uses the daemon-first bridge and falls back to the legacy bash path only when needed.",
  "planned": [],
  "vm": "bm6ccueaqlr7wd2cskza"
}
