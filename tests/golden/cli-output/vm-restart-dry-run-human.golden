# Rust dry-run snapshot only; bash side comparison gated on Tier-2 self-hosted KVM runner.
vm restart --dry-run: would drive the 5-node DAG for vm 'corp-vm' (host-reconcile → store-preflight → virtiofsd-ro-store → ch → ssh-ready)
