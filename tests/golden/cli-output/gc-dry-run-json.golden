# Rust dry-run snapshot only; bash side comparison gated on Tier-2 self-hosted KVM runner.
{
  "command": "gc",
  "mode": "dry-run",
  "notes": "nixling gc --dry-run reports the planned operation; --apply uses the daemon-first bridge and falls back to the legacy bash path only when needed.",
  "planned": [],
  "vm": null
}
