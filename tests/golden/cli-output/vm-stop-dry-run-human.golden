# Rust dry-run snapshot only; bash side comparison gated on Tier-2 self-hosted KVM runner.
vm stop --dry-run: would drive the 5-node DAG for vm 'bm6ccueaqlr7wd2cskza' (host-reconcile → store-preflight → virtiofsd-ro-store → ch → guest-control-health)
