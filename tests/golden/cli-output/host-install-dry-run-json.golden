# Rust dry-run snapshot only; bash side comparison gated on Tier-2 self-hosted KVM runner.
{
  "command": "host install",
  "mode": "dry-run",
  "notes": "W15: dry-run preview retained; --apply routes through the daemon → broker RunHostInstall path.",
  "planned_steps": [
    {
      "step": 1,
      "what": "place systemd units at /etc/systemd/system/d2bd.service + d2b-priv-broker.socket"
    },
    {
      "step": 2,
      "what": "write daemon-config.json to /etc/d2b/daemon-config.json with paths matching the daemon's compiled-in defaults"
    },
    {
      "step": 3,
      "what": "bind /run/d2b/public.sock + /run/d2b/priv.sock with the W3 socket ACLs (launcher / admin groups)"
    },
    {
      "step": 4,
      "what": "neither --enable nor --start specified: leave service inactive"
    },
    {
      "step": 5,
      "what": "smoke: d2b auth status against /run/d2b/public.sock"
    }
  ]
}
