# tests/migration-ledger.toml — AUTO-GENERATED by tests/tools/gen-migration-ledger.sh.
# W0 seed: one row per script. W1+ refines to one row per assertion.
# exercised_today is a W0 script-level heuristic (yes|skip|manual); W1 refines it per-assertion.
# Migration state (status, successor_ids, exercised_today) is preserved across regenerations.
# Retired rows are append-only coverage history and must keep non-empty successor_ids.
# Do not edit classification fields by hand; run make ledger-regen. Seed of the check-inventory gate.
schema_version = 0
generated_for = "test-rearchitecture W0"

[[script]]
name = "tests/adr-index-coverage.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "yes"

[[script]]
name = "tests/audio.sh"
group = "GCI"
make_target = "test-integration"
tier = "ci-kvm"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "skip"

[[script]]
name = "tests/audit-forwarding.sh"
group = "GCI"
make_target = "test-integration"
tier = "ci-kvm"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "manual"

[[script]]
name = "tests/bridge-isolation-runtime.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "skip"

[[script]]
name = "tests/ci-coverage.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "manual"

[[script]]
name = "tests/ci-uses-make.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "yes"

[[script]]
name = "tests/cli-contract-coverage.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "yes"

[[script]]
name = "tests/deliverable-gate-inventory.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "yes"

[[script]]
name = "tests/drift-check.sh"
group = "B"
make_target = "test-drift"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "yes"

[[script]]
name = "tests/examples-with-observability-eval.sh"
group = "F"
make_target = "test-flake"
tier = "ci-l1"
status = "porting"      # legacy | porting | ported
successor_ids = [
exercised_today = "skip"

[[script]]
name = "tests/guest-control-token-materializer.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "yes"

[[script]]
name = "tests/hardware-smoke-gpu-yubikey.sh"
group = "GHW"
make_target = "test-hardware"
tier = "manual-hw"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "manual"

[[script]]
name = "tests/harness-ubuntu-eval.sh"
group = "F"
make_target = "test-flake"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "yes"

[[script]]
name = "tests/ifname-nix-rust-parity.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "porting"      # legacy | porting | ported
successor_ids = [
exercised_today = "yes"

[[script]]
name = "tests/l1c-privilege-oracle.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "skip"

[[script]]
name = "tests/layer1-self-inventory.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "yes"

[[script]]
name = "tests/live-vm-smoke.sh"
group = "GHW"
make_target = "test-hardware"
tier = "manual-hw"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "manual"

[[script]]
name = "tests/manifest-fuzz-bounded.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "skip"

[[script]]
name = "tests/multi-env-daemon-backed.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "porting"      # legacy | porting | ported
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/multi-env-daemon-backed.nix — 16 host.json env-propagation (mtu/mssClamp/east-west/bridgePortFlags + personal negative control), manifest no-microvm@/nixling@ unit reference, and ADR-0015 daemon-only processes.json zero-unit-field cases via mkEval over both reconstructed example variants)", "UNMIGRATED: step 1 `nix flake check --no-build --all-systems` of examples/multi-env (realized flake check; needs a derivation-backed successor, not nix-unit)"]
exercised_today = "yes"

[[script]]
name = "tests/network-isolation.sh"
group = "GCI"
make_target = "test-integration"
tier = "ci-kvm"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "manual"

[[script]]
name = "tests/nixlingd-startup-smoke.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "manual"

[[script]]
name = "tests/nixling-store.sh"
group = "GCI"
make_target = "test-integration"
tier = "ci-kvm"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "skip"

[[script]]
name = "tests/no-new-deferral.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "skip"

[[script]]
name = "tests/performance-budgets.sh"
group = "PERF"
make_target = "perf"
tier = "scheduled"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "manual"

[[script]]
name = "tests/pidfd-handoff.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "yes"

[[script]]
name = "tests/pr-checklist-gate.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "yes"

[[script]]
name = "tests/rust-workspace-checks.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "yes"

[[script]]
name = "tests/state-dir-acl-runtime.sh"
group = "GCI"
make_target = "test-integration"
tier = "ci-kvm"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "manual"

[[script]]
name = "tests/stub-no-socket.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "manual"

[[script]]
name = "tests/swtpm-persistence-smoke.sh"
group = "GCI"
make_target = "test-integration"
tier = "ci-kvm"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "manual"

[[script]]
name = "tests/video-binary-contract.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "manual"

[[script]]
name = "tests/vms-json-parity.sh"
group = "B"
make_target = "test-drift"
tier = "ci-l1"
status = "legacy"      # legacy | porting | ported
successor_ids = []
exercised_today = "skip"

[[script]]
name = "tests/broker-enum-disposition.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo test -p nixling-contract-tests --test policy_broker_dispositions broker_disposition_doc_matches_schema_and_dispatcher"]
exercised_today = "yes"

[[script]]
name = "tests/guest-static-consumption-eval.sh"
group = "F"
make_target = "test-flake"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.guest-static-consumption (flake.nix:722; the check's `${evidence}` interpolation forces the eval-time `assert lib.all (pkg: elem pkg guestSystemPackages) expected` in tests/guest-static-consumption-eval.nix, so `nix flake check --no-build --all-systems` — already run by tests/static.sh — covers the assertion on every system; the retired bash gate only additionally realized the marker-writing runCommand, which asserts nothing beyond the eval)"]
exercised_today = "yes"

[[script]]
name = "tests/static-invariant-deny-unknown-fields.sh"
group = "F"
make_target = "test-flake"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_deny_unknown_schemas.rs (faithful structural reduction of the python+jsonschema gate: sensitive_schema_roots_close_unknown_fields asserts all 7 sensitive schema roots set additionalProperties:false; deny_unknown_fixtures_isolate_an_unknown_field asserts the bundle/host/closures valid fixtures carry no unknown top-level field and the invalid fixtures carry exactly the unknown field additionalProperties:false rejects; guest_control_object_schemas_all_close_unknown_fields walks every object sub-schema in guest-control.json (the bash assert_nested_unknowns_rejected); and guest_control_{string,chunk,terminal}_bounds_did_not_drift port the explicit definition bound checks; gated via `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh)"]
exercised_today = "skip"

[[script]]
name = "tests/activation-helper-eval.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-host/tests/activation_helper_fs_posture.rs (drives the real nixling-activation-helper binary via CARGO_BIN_EXE for the ensure-regular-file / enforce-dir-posture / setfacl-on-path / clear-acl-on-path / chown-if-orphan verbs: happy paths with uid/gid/mode/size assertions, existing-file re-assert, and the openat2+RESOLVE_NO_SYMLINKS exit-2 refusals for symlink, intermediate-symlink, directory, and no-hang FIFO; gated via `cargo test --workspace` in tests/rust-workspace-checks.sh)", "packages/nixling-host/tests/activation_helper_build_farm.rs (the sibling build-store-view{,-farm} verb coverage)"]
exercised_today = "skip"

[[script]]
name = "tests/l3-pin-consistency.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_l3_pin.rs::l3_matrix_pins_parse_and_carry_required_keys (reads the committed tests/golden/l3-matrix/*.txt distro-matrix pins and asserts each parses as key=value with the required keys, a placeholder-or-64-char-lowercase-hex sha256, an https:// image_url, and panel_approval_required_for_change=true; gated via the NL_FIXTURES-free contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh)"]
exercised_today = "yes"

[[script]]
name = "tests/cli-json-drift.sh"
group = "B"
make_target = "test-drift"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = [
exercised_today = "yes"

[[script]]
name = "tests/host-json-drift-gate.sh"
group = "B"
make_target = "test-drift"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = [
exercised_today = "skip"

[[script]]
name = "tests/runner-shape-snapshot.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/runner_shape_contract.rs::cloud_hypervisor_runner_shape_matches_rendered_snapshot, ::virtiofsd_runner_shape_matches_rendered_snapshot, ::swtpm_runner_shape_matches_rendered_snapshot (gated via the NL_FIXTURES contract step in tests/rust-workspace-checks.sh; x86_64 also sets NL_FIXTURES_FULL for the rendered runner roles)"]
exercised_today = "skip"

[[script]]
name = "tests/bundle-drift.sh"
group = "B"
make_target = "test-drift"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["tests/drift-check.sh (runs xtask gen-schemas and checks git diff --exit-code -- docs/reference/schemas/)"]
exercised_today = "skip"

[[script]]
name = "tests/daemon-api-drift.sh"
group = "B"
make_target = "test-drift"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["tests/drift-check.sh (runs xtask gen-daemon-api and checks git diff --exit-code -- docs/reference/daemon-api.md)"]
exercised_today = "skip"

[[script]]
name = "tests/error-codes-drift.sh"
group = "B"
make_target = "test-drift"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["tests/drift-check.sh (runs xtask gen-error-codes and checks git diff --exit-code -- docs/reference/error-codes.md; preserves generated error-table marker and row assertions)"]
exercised_today = "yes"

[[script]]
name = "tests/guest-control-proto.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_guest_control_proto.rs::guest_control_proto_declares_service_methods_and_descriptor_sentinels (source-lints packages/nixling-ipc/proto/guest_control.proto for the GuestControl service, required RPC names, descriptor sentinel field names, optional scalar/string field count, TerminalStatus.outcome oneof, required enum values, and the retired capability/root-signal negatives)", "packages/nixling-contract-tests/tests/policy_guest_control_proto.rs::guest_control_proto_field_numbers_and_reserved_slots_are_stable (ports the bash field-number parser checks for HelloRequest.host_nonce=2, HelloResponse.guest_nonce=1, AuthenticateRequest.host_auth_tag=6, AuthenticateResponse guest_auth_tag=1 optional/capabilities_hash=2 optional/health=3/capabilities=4, plus HelloResponse retired pre-auth field negatives and reserved slots)", "packages/nixling-contract-tests/tests/policy_guest_control_proto.rs::guest_control_proto_schema_camelcase_fields_limits_and_shapes_match (asserts docs/reference/schemas/v2/guest-control.json exists and that generated schema message definitions, camelCase field mapping, scalar/bytes/repeated/ref shapes, optional nullability, required scalar fields, and GuestEffectiveLimits minima/maxima match the proto)", "packages/nixling-contract-tests/tests/policy_guest_control_proto.rs::guest_control_proto_schema_terminal_status_and_enums_match (asserts generated schema enum values and TerminalStatus oneOf payload variants match the proto)", "tests/guest-proto-bindings.sh / xtask gen-guest-proto generated-bindings drift gate (prost-build compiles packages/nixling-ipc/proto/guest_control.proto, so protoc well-formedness coverage remains outside this structural policy lint)"]
exercised_today = "skip"

[[script]]
name = "tests/guest-proto-bindings.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["tests/drift-check.sh (runs xtask gen-guest-proto and checks git diff --exit-code -- packages/nixling-ipc/src/generated)", "packages/nixling-contract-tests/tests/policy_guest_bindings.rs::guest_proto_bindings_are_message_only_and_codegen_free (generated guest_control.rs has no unsafe/unsafe_code/broad lint bypasses; message-only no ttrpc/service/client/server symbols; nixling-ipc has no build.rs, no ttrpc, and no protobuf-codegen/prost-build/tonic-build/protoc build-dependency)"]
exercised_today = "yes"

[[script]]
name = "tests/guest-ttrpc-bindings.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["tests/drift-check.sh (runs xtask gen-guest-ttrpc and checks git diff --exit-code -- packages/nixling-guestd/src/generated)", "packages/nixling-contract-tests/tests/policy_guest_bindings.rs::guest_ttrpc_bindings_are_xtask_only_and_ipc_stays_ttrpc_free (generated guest_control_ttrpc.rs has no unsafe/unsafe_code/broad lint bypasses including clipto_camel_casepy; nixling-guestd has no build.rs and no ttrpc-codegen/ttrpc-compiler/prost-build/protoc build-dependency; nixling-ipc remains ttrpc-free)"]
exercised_today = "yes"

[[script]]
name = "tests/manpage-completion-drift.sh"
group = "B"
make_target = "test-drift"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["tests/drift-check.sh (runs xtask gen-cli-shell-artifacts and checks git diff --exit-code -- docs/manpages/ docs/completions/)"]
exercised_today = "yes"

[[script]]
name = "tests/net-vm-network-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/net-vm-network.nix — 61 net-VM/network value cases covering catch-all DHCP neutralization, work/safe/obs static addressing, MTU propagation, MSS clamp, peer/host CIDR drops, nftables ordering, east-west controls, host bridge/tap wiring, and observability env safe defaults)"]
exercised_today = "manual"

[[script]]
name = "tests/observability-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/observability.nix — 24 observability batch cases: 23 success extracted-JSON parity cases plus the obs-vm-without-framework failure-substring assertion; reconstructs tests/eval-cases/observability.nix with direct nixpkgs/nixlingModule injection for hermetic flake evaluation)"]
exercised_today = "skip"

[[script]]
name = "tests/per-vm-state-ownership-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/per-vm-state-ownership.nix — per-VM ownership matrix structure/presence/owner-group-mode/recursive-required/store-view layout via mkEval)", "packages/nixling-contract-tests/tests/policy_ownership_preflight.rs (gated via the NL_FIXTURES contract step in tests/rust-workspace-checks.sh — the source-lint store-sync/daemon-preflight regression guards: store.nix keeps chown nixlingd:users + chmod 0755 META_DIR fix-ups and carries no chown root:kvm / chmod 2775; no nixos-modules file enforces store/store-meta 2775; ownership_preflight.rs declares no store* path at mode 0o2775)"]
exercised_today = "yes"

[[script]]
name = "tests/cli-nix-consumers-eval.sh"
group = "F"
make_target = "test-flake"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_cli_consumers.rs::cli_nix_live_consumer_bindings_absent (repo *.nix/*.sh/*.rs live pre-comment grep over git-listed files, excluding nixos-modules/cli.nix, the retired gate, this policy test, and tests/static.sh only for nixling.store.{package,generations}; guards nixling.cliBin, audioStateHelperPath, _desktopWrappers, nixling.store.package, nixling.store.generations); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh", "packages/nixling-contract-tests/tests/policy_cli_consumers.rs::cli_nix_imports_absent (repo *.nix import/imports payload grep rejects live ./cli.nix imports outside nixos-modules/cli.nix / retired gate / this policy test while allowing comment-only historical references); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/host-validate-verb-eval.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = [
exercised_today = "skip"

[[script]]
name = "tests/manifest-v04-roundtrip.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-core/tests/manifest_v04_roundtrip.rs::manifest_v04_roundtrip::baseline_vms_json_round_trips_byte_identically (cargo test -p nixling-core --test manifest_v04_roundtrip[ --features test-support])", "tests/golden/pinned/manifest-v04-roundtrip.txt + tests/tools/assert-pinned-tests.sh"]
exercised_today = "yes"

[[script]]
name = "tests/broker-export-audit.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["broker-workspace cargo test --workspace --features layer1-bootstrap (nixling-priv-broker broker_export_audit::export_audit_requires_admin_and_exports_op_audit_records) — already gated in tests/rust-workspace-checks.sh", "tests/golden/pinned/broker-export-audit.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin across the broker workspace layer1-bootstrap list)"]
exercised_today = "yes"

[[script]]
name = "tests/broker-socket-acl.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["broker-workspace cargo test --workspace --features layer1-bootstrap (nixling-priv-broker broker_socket_acl::socket_acl_rejects_non_nixlingd_peers_and_accepts_daemon) — already gated in tests/rust-workspace-checks.sh", "tests/golden/pinned/broker-socket-acl.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin across the broker workspace layer1-bootstrap list)"]
exercised_today = "yes"

[[script]]
name = "tests/daemon-metrics-eval.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_metrics.rs::daemon_metrics_inventory_descriptors_match_policy + daemon_metrics_histogram_buckets_match_policy + daemon_metrics_reference_doc_rows_match_retired_gate_policy (descriptor inventory names/kinds/label-keys/buckets + retired doc-row parity; gated via the NL_FIXTURES contract step in tests/rust-workspace-checks.sh)"]
exercised_today = "yes"

[[script]]
name = "tests/daemon-socket-acl.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixlingd/tests/daemon_socket_acl.rs::daemon_socket_acl::{non_launcher_uid_is_rejected,wheel_non_launcher_is_rejected,configured_launcher_is_accepted,daemon_self_client_is_rejected} — default cargo test --workspace via tests/rust-workspace-checks.sh", "tests/golden/pinned/daemon-socket-acl.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "yes"

[[script]]
name = "tests/daemon-state-lock.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixlingd/tests/daemon_state_lock.rs::daemon_state_lock::{lock_file_created_with_current_user_0640,second_lock_holder_exits_already_running,symlink_lock_parent_is_rejected} — default cargo test --workspace via tests/rust-workspace-checks.sh", "tests/golden/pinned/daemon-state-lock.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "yes"

[[script]]
name = "tests/daemon-state-persistence.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixlingd/tests/daemon_state_persistence.rs::daemon_state_persistence::restores_pidfd_table_and_clears_after_vm_stop — default cargo test --workspace via tests/rust-workspace-checks.sh", "tests/golden/pinned/daemon-state-persistence.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "yes"

[[script]]
name = "tests/daemon-version-negotiation.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixlingd/tests/daemon_version_negotiation.rs::daemon_version_negotiation::{version_mismatch_is_rejected,unknown_feature_flags_are_accepted,unknown_hello_field_is_rejected,invalid_ifname_is_rejected} — default cargo test --workspace via tests/rust-workspace-checks.sh", "tests/golden/pinned/daemon-version-negotiation.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "yes"

[[script]]
name = "tests/ch-net-handoff-canary.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["tests/golden/pinned/runner-shape-preflight.txt (runner_shape::tests::{ch_help_with_fd_selects_tap_fd,ch_help_without_fd_or_tap_fails_closed}) + tests/tools/assert-pinned-tests.sh — existing primary pin owner; not duplicated", "tests/cli-contract-coverage.sh + tests/golden/cli-output/host-check-ch-net-handoff-not-supported.{json,txt} guard the retired golden-envelope ch-net-handoff-not-supported residual", "packages/nixling-contract-tests/tests/policy_contracts.rs::tap_dag_contract_doc_matches_implementation (gated via tests/rust-workspace-checks.sh NL_FIXTURES contract step; recorded in tests/migration-state.d/tap-dag-contract-doc-eval.toml) guards the retired CreateTapFd broker-wire/docs residual"]
exercised_today = "yes"

[[script]]
name = "tests/usbip-firewall-skeleton.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo test --workspace (nixling-host nftables::tests::{usbip_carveout_inserted_before_generic,comment_marker_prefix_on_every_managed_rule,fake_backend_refuses_carveout_after_generic}) — already gated in tests/rust-workspace-checks.sh", "broker-workspace cargo test --workspace [+ --features layer1-bootstrap] (nixling-priv-broker ops::usbip_firewall::tests::{bind_firewall_rule_produces_audit_with_busid_and_hash,carveout_ordering_invariant_via_op,w6_ops_refused_with_unknown_operation_audit}) — already gated in tests/rust-workspace-checks.sh", "tests/golden/pinned/usbip-firewall-skeleton.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin across both workspaces)", "retired script's UsbipBind/UsbipUnbind/UsbipProxyReconcile static grep is covered by ops::usbip_firewall::tests::w6_ops_refused_with_unknown_operation_audit, which enumerates those variants and asserts unknown-operation refusal audit payloads"]
exercised_today = "yes"

[[script]]
name = "tests/video-sidecar-hardening-eval.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/minijail_swtpm_video.rs (gated via the NL_FIXTURES / NL_FIXTURES_FULL contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh -> static.sh) — video_rendered_profile_shape + video_source_nvidia_gating_namespace_principal + video_host_activation_runtime_and_session_acls assert the surviving runner-side video hardening (empty caps, seccompPolicyRef w1-video, masked /dev device allowlist, private PID namespace, dedicated nixling-<vm>-video principal, cgroup leaf, runtime-dir ACL exclusion) that the broker applies at SpawnRunner{role: Video} fork time", "tests/broker-caps-eval.sh (broker canonical cap-set + seccomp profile for the video runner)"]
exercised_today = "yes"

[[script]]
name = "tests/broker-validate-bundle.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_broker_schema.rs::broker_delegates_bundle_validation_to_core (asserts the broker delegates bundle validation to nixling-core only: packages/nixling-priv-broker/src exists, some source under it imports `nixling_core::manifest` (delegation target), and some source calls `validate_bundle` (the single validation entry point) — the gate's two positive `grep -R -q` delegation checks ported as substring scans over the recursively-collected broker src tree. Spec correction (existing code is canon): the bash gate's 4th assertion forbidding ALL `serde_json::from_str`/`from_value` under the broker src is red against current code — the broker legitimately parses subprocess JSON (nft / `ip route` / store-view runner output / audit lines) in ops/{store_view_farm,route,tap,store_sync_*}.rs (pre-existing breakage per tests/README.md), so the over-broad blanket negative is dropped while the gate's stated delegation purpose is preserved); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/cgroup-delegation-oracle.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo test --workspace (nixling-host cgroup::tests::* delegation canaries) + broker test pass (nixling-priv-broker ops::cgroup::tests::* delegation/audit canaries) — already gated in tests/rust-workspace-checks.sh", "tests/golden/pinned/cgroup-delegation-oracle.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "yes"

[[script]]
name = "tests/host-prepare-idempotency.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo test --workspace (nixling-host nftables::tests::{idempotency_hash_table_stable,idempotency_hash_volatile_stripped} — drift-digest stability invariant; fake-backends cfg active under plain cargo test) — already gated in tests/rust-workspace-checks.sh", "tests/golden/pinned/host-prepare-idempotency.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin; supersedes the retired gate's zero-test grep guard)"]
exercised_today = "skip"

[[script]]
name = "tests/host-prepare-network.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo test --workspace (nixling-host netlink::tests::{ipv6_off_sequence_runs_in_order,bridge_port_readback_matches_defaults}, bridge_port::tests::readback_matches_defaults, routes::tests::{no_default_route_fails_closed,foreign_default_route_fails_closed,dnsmasq_not_bound_fails_closed,host_lan_cidr_ambiguous_for_vpn}) — already gated in tests/rust-workspace-checks.sh", "broker-workspace cargo test --workspace [+ --features layer1-bootstrap] (nixling-priv-broker ops::nm::tests::{managed_foreign_conflict_detected,reload_command_select_by_version}, ops::tap::tests::set_bridge_port_flags_readback_drift_fails_closed) — already gated in tests/rust-workspace-checks.sh", "tests/golden/pinned/host-prepare-network.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin across both workspaces)", "ch-net-handoff-not-supported docs-anchor (retired gate's trailing grep) independently guarded by tests/cli-contract-coverage.sh + committed host-check goldens (ch-net-handoff-canary.sh retired W3fu10)"]
exercised_today = "yes"

[[script]]
name = "tests/ipv6-off-readback.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo test --workspace (nixling-host netlink::tests::{ipv6_off_sequence_runs_in_order,drift_after_write_fails_closed,bridge_nf_sysctls_applied_when_loaded,ipv6_off_sequence_fails_closed_on_nm_precondition}) — already gated in tests/rust-workspace-checks.sh", "tests/golden/pinned/ipv6-off-readback.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "yes"

[[script]]
name = "tests/nft-coexistence.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo test --workspace (nixling-host nftables::tests::{coexistence_matrix_all_7_rows,detector_clean_host,detector_single_manager_unambiguous,detector_multiple_managers_unknown}) + broker test pass (nixling-priv-broker ops::nft::tests::{refuses_when_firewalld_declared_coexist,applies_on_clean_host_with_coexist,nft_script_includes_all_four_chains}) — already gated in tests/rust-workspace-checks.sh", "tests/golden/pinned/nft-coexistence.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "yes"

[[script]]
name = "tests/nft-foreign-rule-preservation.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo test --workspace (nixling-host nftables::tests::{fake_backend_preserves_foreign_rules,drift_detection_strips_volatile_fields,drift_detection_catches_real_change}) — already gated in tests/rust-workspace-checks.sh", "tests/golden/pinned/nft-foreign-rule-preservation.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "yes"

[[script]]
name = "tests/path-safety-violation-fs.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["broker test pass (nixling-priv-broker ops::hosts::tests::{refuses_symlink_target,writes_managed_block_into_fresh_file,idempotent_when_block_matches}, ops::nm::tests::reload_failure_rolls_back, ops::state_dir::tests::{refuses_absolute_relative_path,refuses_parent_dir_escape,idempotent_reuses_existing_dirs,creates_base_and_relative_paths}) — already gated in tests/rust-workspace-checks.sh", "tests/golden/pinned/path-safety-violation-fs.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "skip"

[[script]]
name = "tests/privileges-matrix-completeness.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_broker_schema.rs::privileges_matrix_covers_declared_operations (asserts every declared public/broker operation has a privileges row: declared_ops = CLI/API verbs parsed from the backtick `nixling ...` spans of docs/reference/cli-contract.md via the bash gate's python `record_cli` folding (host/vm aliasing incl. konsole->exec, audio on|off / mic / speaker, audit --human/--json, status --check-bridges, '/'-split recursion, version/NOTICE/--help filtering) UNION the operation-enum variants parsed from every `*.rs` source under packages/ naming `DelegateCgroupV2` (enum-name suffix Operation/Request/Command/Op, excluding Error/Err/Kind; row(\"Op\") fallback) — reproduced byte-for-byte (94 ops, verified equal to the python extraction); then asserts declared_ops non-empty, rendered_ops non-empty, and declared_ops is a subset of rendered_ops. Spec correction (per the disk-hygiene contract + task directive): rendered_ops is read directly from the canonical source-of-truth `OperationAuthz.operation` enum in docs/reference/schemas/v2/privileges.json, which nixling-core privileges::operation_schema defines as exactly sorted(union(PUBLIC_OPERATION_AUTHZ, BROKER_OPERATION_AUTHZ).operation) == the bash gate's nix-rendered publicOperations ∪ brokerOperations, avoiding the nix-eval render; the dropped JSON-Schema validation of the rendered artifact is covered by the bundle/manifest drift gate); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "skip"

[[script]]
name = "tests/runner-shape-preflight.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo test --workspace (nixling-host runner_shape::tests::*) — already gated in tests/rust-workspace-checks.sh", "tests/golden/pinned/runner-shape-preflight.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "yes"

[[script]]
name = "tests/static-invariant-deny-unknown-fields-w3.sh"
group = "F"
make_target = "test-flake"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_deny_unknown.rs::host_schema_dtos_carry_deny_unknown_fields (every security-sensitive nixling-core::host_w3 DTO — IfNameMapping/BridgePortFlagsW3/KernelModuleEntry/RouteIntent/SysctlIntent/HostsEntry/NmUnmanagedEntry/FirewallCoexistencePolicy — plus the HostChConfig addition to HostJson in host.rs carries #[serde(deny_unknown_fields)], asserted by selecting the declaring source (host_w3.rs first, then host.rs) and requiring the attribute within the 10 lines preceding `pub struct <DTO>`); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "skip"

[[script]]
name = "tests/ssh-host-key-preflight-eval.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo test --workspace (nixlingd ssh_host_key_preflight::tests::* + typed_error::tests::{sshd_host_key_drift_envelope_shape,envelope_kind_matches_expected_discriminant}) — already gated in tests/rust-workspace-checks.sh", "tests/golden/pinned/ssh-host-key-preflight.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "yes"

[[script]]
name = "tests/minijail-validator-audio.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/minijail_audio_usbip.rs::audio_profile_source_layer1_shape (nixos-modules/minijail-profiles.nix audio Layer-1 shape: profile block present, host capabilities kept empty (no `capabilities =`), seccompPolicyRef = \"w1-audio\", namespaces = defaultNamespaces // { net = true; }, userNamespace = { ... } — per-line grep-faithful regex + the bash awk block extraction from `profileIdFor name \"audio\"` through the first `};` line)", "packages/nixling-contract-tests/tests/minijail_audio_usbip.rs::audio_rendered_profile_broker_pre_ns_shape (REAL rendered fixture-smoke-full RoleProfile vm-corp-full-audio: EMPTY host caps, seccompPolicyRef = w1-audio, net + user namespaces isolated with ipc/mount isolated and pid/uts shared, single-entry userNamespace mapping in-NS 0 -> the nixling-<vm>-snd principal uid/gid (== profile uid/gid, != 0), umask 0o007 for the shared snd.sock, cgroup nixling.slice/<vm>/audio; early-returns SKIP when NL_FIXTURES_FULL is unset)", "tests/rust-workspace-checks.sh (NL_FIXTURES / NL_FIXTURES_FULL contract step `cargo test -p nixling-contract-tests` -> static.sh)"]
exercised_today = "manual"

[[script]]
name = "tests/minijail-validator-gpu.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/minijail_gpu.rs (gated via the NL_FIXTURES / NL_FIXTURES_FULL contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh -> static.sh) — gpu_rendered_device_bind_matrix_and_wayland_target (static closed-set: the rendered Gpu RoleProfile pins the exact 6-entry DEVICE_BINDS matrix /dev/kvm,/dev/dri/renderD128,/dev/nvidiactl,/dev/nvidia0,/dev/nvidia-uvm,/dev/udmabuf each under /dev, plus the in-sandbox wayland BIND_TARGET parent /run/nixling-gpu/<vm> as a writable path), gpu_rendered_jail_shape (empty host caps, w1-gpu seccompPolicyRef, nixling.slice/<vm>/gpu cgroup subtree, mount+ipc namespaces with no net/user NS, hideDeviceNodesByDefault, nixStoreReadOnly, umask 0o007, no broker-pre-NS userNamespace), gpu_broker_role_device_claim_source (role_device_classes(Gpu) arm in packages/nixling-core/src/bundle_resolver.rs includes kvm/dri/nvidia-ctl/nvidia-uvm/nvidia-render/udmabuf and excludes vfio), gpu_render_node_minijail_profile_source_shape (D5/P2.3 gpu-render-node mkProfile block in nixos-modules/minijail-profiles.nix: present, userNamespace referencing the gpu principal, seccompPolicyRef = \"w1-gpu-render-node\", empty deviceBinds, umask = 7, gated on vm.graphics.renderNodeOnly), gpu_render_node_broker_processes_wiring_source (D5/P2.3 wiring: live_handlers.rs maps w1-gpu-render-node, sys.rs declares RENDER_NODE_INHERITED_FD + pre_opened_device_fds on RunnerIsolationSpec, processes-json.nix defines gpuRenderNodeRunner + emits \"gpu-render-node\" role node + carries --gpu-device-node in argv). The bash gate's opt-in live arms (cc-compiled virtgpu_probe; minijail0 positive arm DRM_IOCTL_VIRTGPU_GET_CAPS must not SIGSYS; negative arm ptrace(PTRACE_TRACEME) must SIGSYS; NL_LIVE=1 bare GET_CAPS hardware smoke on the host render node; /var/lib/nixling/validated/p1-gpu.json evidence write) are live host-execution layers and do not port to the contract layer."]
exercised_today = "manual"

[[script]]
name = "tests/minijail-validator-otel-host-bridge.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/minijail_relay_otel.rs (gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh -> static.sh) — otel_host_bridge_profile_source_reference (Layer-1 source-grep over nixos-modules/minijail-profiles.nix: references the host-scoped otel-host-bridge profile via `otel-?host-?bridge|otelHostBridge|OtelHostBridge`), otel_host_bridge_rendered_profile_shape (rendered RoleProfile over the FULL fixture `host-otel-host-bridge`: role = OtelHostBridge, caps == [], NO /dev binds in writablePaths and empty deviceBinds, seccompPolicyRef = \"w1-otel-host-bridge\", documented RW bind set /run/nixling/otel + obs VM CH vsock dir under /var/lib/nixling/vms, host-scoped cgroup subtree nixling.slice/host/otel-host-bridge, mount+ipc namespaces with no net/user — the bash gate's NL_LIVE=1 jq checks over the live /etc/nixling/minijail-profiles/host-otel-host-bridge.json port here as rendered checks that always run and are strictly stronger), otel_host_bridge_rendered_argv_socat_shape (rendered socat runner argv: socat binaryPath, nixling-otel-host-bridge title, UNIX-LISTEN:/run/nixling/otel/host-egress.sock listen + EXEC ch-vsock-connect forward leg). The bash gate's NL_LIVE=1 Layer-2 (minijail0 positive pre-opened-fd path, python3 socket(AF_VSOCK) SIGSYS/EPERM negative probe, /var/lib/nixling/validated/p1-otel-host-bridge.json evidence write) is a live host-execution layer and does not port to the contract layer."]
exercised_today = "manual"

[[script]]
name = "tests/minijail-validator-swtpm.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/minijail_swtpm_video.rs (gated via the NL_FIXTURES / NL_FIXTURES_FULL contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh -> static.sh) — swtpm_rendered_empty_caps_both_profiles (Phase-1 S2: rendered swtpm + swtpm-flush RoleProfiles declare EMPTY caps and the closed w1-swtpm seccompPolicyRef; strictly stronger than the bash awk that relied on the mkProfile empty-caps default), swtpm_rendered_user_namespace_long_lived_sidecar_only (Phase-1 S3/S4-rendered: the long-lived swtpm sidecar declares an ADR-0021 userNamespace mapping in-NS uid/gid 0 to the swtpm principal's own uid/gid with a user namespace requested, and swtpm-flush carries NO userNamespace and no user namespace), swtpm_rendered_umask_state_dir_cgroup (Phase-1 S5/S6 + cgroup intent: swtpm sidecar retains umask = 0o007, both profiles bind /var/lib/nixling/vms/<vm>/swtpm as a writable path (no tmpfs), and the nixling.slice/<vm>/{swtpm,swtpm-flush} cgroup subtrees), swtpm_source_principal_and_no_tmpfs (Phase-1 S1/S4-source/S7 over nixos-modules/minijail-profiles.nix: module present, swtpm userNamespace references stablePrincipalId \"nixling-${name}-swtpm\" not the runner principal, and the negative no-tmpfs-for-swtpm-state per-line regex ^\\s*[^#/\\s].*(tmpfs.*swtpm|swtpm.*tmpfs) matches zero non-comment lines). The bash gate's opt-in NL_LIVE=1 Phase 2 (boot swtpm under a hand-rolled minijail0 profile, tpm2_nvdefine/tpm2_nvwrite a TPM 2.0 NVRAM index, restart swtpm and read the index back to prove byte-identical persistence, ptrace SIGSYS seccomp-kill negative probe, and the /var/lib/nixling/validated/p1-swtpm.json evidence write) is a root + live-host + swtpm/tpm2-tools/minijail0 runtime layer and does not port to the contract layer."]
exercised_today = "manual"

[[script]]
name = "tests/minijail-validator-usbip.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/minijail_audio_usbip.rs::usbip_profile_source_layer1_shape (nixos-modules/minijail-profiles.nix usbip Layer-1 shape pins: role = \"usbip\" declared, capabilities = [ \"CAP_NET_RAW\" ] (kernel-r2-4 cap matrix), seccompPolicyRef = \"w1-usbip\" (the closed allowlist; ptrace not in it — the bash gate's negative-path Layer-1 equivalent) — file-scoped per-line grep-faithful regex covering the per-VM vm-<name>-usbip corp-full block AND the usbipd backend block)", "packages/nixling-contract-tests/tests/minijail_audio_usbip.rs::usbip_rendered_usbipd_backend_proxy_shape (REAL rendered fixture-smoke-full RoleProfiles on the env net side: vm-sys-work-usbipd-backend — caps == [CAP_NET_RAW], seccompPolicyRef = w1-usbip, uid/gid 0 with the host-root usbip_sockfd adr_carve_out, private pid namespace, cgroup nixling.slice/sys-work-usbipd/backend; vm-sys-work-usbipd-proxy — EMPTY caps, seccompPolicyRef = w1-usbip-proxy, non-root principal uid, cgroup nixling.slice/sys-work-usbipd/proxy; both: no net/user namespace, no broker-pre userNamespace; early-returns SKIP when NL_FIXTURES_FULL is unset)", "tests/rust-workspace-checks.sh (NL_FIXTURES / NL_FIXTURES_FULL contract step `cargo test -p nixling-contract-tests` -> static.sh)"]
exercised_today = "manual"

[[script]]
name = "tests/minijail-validator-video.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/minijail_swtpm_video.rs (gated via the NL_FIXTURES / NL_FIXTURES_FULL contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh -> static.sh) — video_rendered_profile_shape (Layer-1 V1/V2/V3/V4-device-mask/V5: rendered video RoleProfile exists with EMPTY caps, seccompPolicyRef = \"w1-video\", masked /dev (hideDeviceNodesByDefault = true) exposing exactly [/dev/dri/renderD128] when videoNvidiaDecode is off, a private PID namespace, umask = 0o007 for CH socket-ACL inheritance, the nixling.slice/<vm>/video cgroup leaf, and a DEDICATED video principal whose uid does not collide with any gpu/gpu-render-node uid — AGENTS nixling-<vm>-video NOT gpu), video_source_nvidia_gating_namespace_principal (Layer-1 V1/V4-source over nixos-modules/minijail-profiles.nix, scoped to the `grep -A 45 'profileIdFor name \"video\"'` window: NVIDIA nodes /dev/nvidiactl + /dev/nvidia0 + /dev/nvidia-uvm gated behind lib.optionals (vm.graphics.videoNvidiaDecode or false), namespaces = defaultNamespaces // { pid = true; }, and principal = \"nixling-${name}-video\"), video_host_activation_runtime_and_session_acls (Layer-1 V6 over nixos-modules/host-activation.nix: ten verbatim grep -q substrings — video_media_uids= over cloud-hypervisor-runner/video, gpu_session_uids= over gpu/gpu-render-node, audio_session_uids= over audio, setfacl -d -x \"u:$uid\" /run/nixling-video, the u:$uid:--- deny entries, and stale_video_uid= / u:$stale_video_uid:--- stale-video reaping — so the video runtime dir ACL grants only cloud-hypervisor/video runtime access and video is excluded from host session sockets). The bash gate's opt-in NL_LIVE=1 Layer-2 (live render-node/cgroup-leaf/CH-uid preconditions, a benign vhost-user-media bind probe under minijail0 positive path, an undeclared-syscall ptrace SIGSYS negative probe, the socket-inherits-CH-uid-ACL assertion, and the /var/lib/nixling/validated/p1-video.json evidence write) is a root + live-host + minijail0 runtime layer and does not port to the contract layer."]
exercised_today = "manual"

[[script]]
name = "tests/minijail-validator-vsock-relay.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/minijail_relay_otel.rs (gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh -> static.sh) — vsock_relay_profile_source_shape (Layer-1 source-grep over nixos-modules/minijail-profiles.nix: profile block exists for `profileIdFor name \"vsock-relay\"`, explicit `capabilities = [ ]` empty-cap branch / no `\"CAP_*\"` token via the bash if/elif short-circuit, seccompPolicyRef = \"w1-vsock-relay\"), vsock_relay_rendered_profile_shape (rendered RoleProfile over the FULL fixture `vm-corp-full-vsock-relay`: role = VsockRelay, caps == [], seccompPolicyRef = \"w1-vsock-relay\", per-VM + obs-VM RW bind set under /var/lib/nixling/vms with NO /dev binds and empty deviceBinds, cgroup subtree nixling.slice/<vm>/vsock-relay, mount+ipc namespaces with no net/user — strictly stronger than the bash gate's jq/grep over a live host profile), vsock_relay_rendered_argv_socat_shape (rendered socat runner argv: socat binaryPath, nixling-otel-relay@<vm> title, UNIX-LISTEN + EXEC ch-vsock-connect pipe). The bash gate's NL_LIVE=1 Layer-2 (minijail0 + socat pre-opened-fd positive path, socket(AF_VSOCK) SIGSYS/EPERM negative probe, PTRACE_TRACEME probe, /var/lib/nixling/validated/p1-vsock-relay.json evidence write) is a live host-execution layer and does not port to the contract layer."]
exercised_today = "manual"

[[script]]
name = "tests/minijail-validator-wayland-proxy.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/minijail_gpu.rs (gated via the NL_FIXTURES / NL_FIXTURES_FULL contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh -> static.sh) — wayland_proxy_profile_source_shape (Layer-1 assert_profile_source over nixos-modules/minijail-profiles.nix: role = \"wayland-proxy\" declaration, mandatory seccompPolicyRef = \"w1-wayland-proxy\", empty capabilities/no CAP_ in the profile block, requiresStartRoot never true, no userNamespace, writable path /run/nixling-wlproxy present, no PipeWire/Pulse bind in the block, umask = 7, deviceBinds = [ ] empty), wayland_proxy_policy_ref_device_classes_source (Layer-1 assert_policy_ref_entry: \"w1-wayland-proxy\" in packages/nixling-priv-broker/src/live_handlers.rs policy_ref_device_classes), wayland_proxy_rust_variants_declared_source (Layer-1 assert_rust_variant_exists: ProcessRole::WaylandProxy in packages/nixling-core/src/processes.rs and RunnerRole::WaylandProxy in packages/nixling-ipc/src/broker_wire.rs), wayland_proxy_rendered_profile_shape (Layer-1 assert_installed_profiles_consistent applied to the REAL rendered RoleProfile — strictly stronger than the bash gate which scanned /etc/nixling/minijail-profiles/*wayland-proxy*.json and skipped when absent: empty caps, w1-wayland-proxy seccomp, empty deviceBinds + bindMounts, dedicated runtime dir /run/nixling-wlproxy/<vm> as the sole writable path, no PipeWire/Pulse path access, umask 0o007, no broker-pre-NS userNamespace). The bash gate's NL_LIVE=1 Layer-2 (minijail0 /bin/true positive probe; /var/lib/nixling/validated/p1-wayland-proxy.json evidence write) is a live host-execution layer and does not port to the contract layer."]
exercised_today = "manual"

[[script]]
name = "tests/minijail-validator-cloud-hypervisor.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/minijail_roles.rs (gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh -> static.sh) — cloud_hypervisor_tap_fd_profile_declares_empty_capabilities (Phase-1a: rendered tap-fd runner profile declares EMPTY caps), cloud_hypervisor_persistent_tap_cap_net_admin_source_logic (Phase-1b: persistent-tap CAP_NET_ADMIN conditional in nixos-modules/minijail-profiles.nix — fixture renders only the default tap-fd mode, so the persistent-tap branch is verified as a source-grep), cloud_hypervisor_rendered_jail_shape (rendered device binds incl. no /dev/net/tun in tap-fd, cgroup subtree, mount/ipc/net/user namespaces, w1-cloud-hypervisor-runner seccompPolicyRef). The bash gate's opt-in NL_LIVE=1 phase (minijail0 + `cloud-hypervisor --version` positive path, ptrace SIGSYS negative probe, /var/lib/nixling/validated/p1-cloud-hypervisor.json evidence write) is a live host-execution layer and does not port to the contract layer."]
exercised_today = "manual"

[[script]]
name = "tests/minijail-validator-virtiofsd.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/minijail_roles.rs (gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh -> static.sh) — virtiofsd_profile_source_shape_matches_adr_0021 (Layer-1 assert_profile_source: carve-out marker string + exceptionRef = virtiofsdRootException, zero host CAP_* tokens in the profile block, requiresStartRoot = false, userNamespace { hostUidForZero, hostGidForZero }, seccompPolicyRef = \"w1-virtiofsd\" over nixos-modules/minijail-profiles.nix), virtiofsd_rendered_profiles_match_broker_pre_ns_shape (Layer-1 assert_installed_profiles shape applied to the REAL rendered RoleProfiles — role=virtiofsd, caps==[], numeric single-entry userNamespace, w1-virtiofsd seccomp, user namespace requested; strictly stronger than the bash gate which skipped when no /etc/nixling/minijail-profiles/ JSONs existed), virtiofsd_ro_store_rendered_adr_0021_invariants (rendered ADR-0021 ro-store invariants: nixStoreReadOnly + /nix/store read-only + --sandbox=chroot/--inode-file-handles=never/--readonly argv). The bash gate's NL_LIVE=1 Layer-2 (minijail0 virtiofsd --version positive path; ptrace probe — itself skipped by the gate as the w1-virtiofsd seccomp blob is not materialized; p1-virtiofsd.json evidence write) is a live host-execution layer and does not port to the contract layer."]
exercised_today = "manual"

[[script]]
name = "tests/legacy-unit-denylist-eval.sh"
group = "F"
make_target = "test-flake"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_units.rs::legacy_unit_denylist (ADR 0015 fail-closed denylist scan over every *.nix/*.md under nixos-modules/: no retired per-VM systemd template or host-singleton framework unit name reappears as live wiring, honoring the pure-comment / inline+preceding `# obituary:`/`# retired:` marker / per-file allowlist / `*Service = \"nixling-...\"` content carve-outs); legacy_unit_denylist_classify_semantics (negative coverage of the classify() skip/live escape hatches); legacy_unit_denylist_patterns_match_representative_refs (denylist needle integrity); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "skip"

[[script]]
name = "tests/supervisor-option-absent-eval.sh"
group = "E"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_supervisor.rs::supervisor_option_absent (asserts the productive `supervisor = lib.mkOption` declaration is absent from nixos-modules/options-vms.nix, and the top-level defense-in-depth assertion in nixos-modules/assertions.nix fires on any per-VM `vm ? supervisor` / `nixling.vms.${name}.supervisor` definition with the friendly ADR-0015 daemon-only-clean-break message); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/usbip-state-machine-eval.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_supervisor.rs::usbip_state_machine_surface (asserts packages/nixlingd/src/{usbip_state_machine.rs,lib.rs,typed_error.rs} + docs/reference/usbip-state-machine.md are present, lib.rs declares `pub mod usbip_state_machine;`, the CANONICAL_STEPS const pins the bring-up order Modprobe Lock Withhold Firewall Backend Bind Proxy, typed_error.rs wires TypedError::UsbipStepFailed with the 'usbip-step-failed' kind and exit code 67, and the doc names the canonical order verbatim); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/loki-label-cardinality-eval.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_observability.rs::loki_native_otel_resource_attributes (native SigNoz/OTel resource-attribute contract over nixos-modules/components/observability/{host,stack,guest}.nix: bounded resource-attribute key allowlist + required identity keys + no retired Loki/Alloy/Grafana/Tempo/Prometheus stack surfaces + no secret/high-cardinality/value-leaking key + upsert/identity tokens + StoreSync target_vm/target_env not promoted to resource attributes); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/minijail-version-check.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_misc.rs::minijail_version_comparison_canary (deterministic minijail version-refusal canary: observed >= required => ok else too-old, over the pinned-current/newer-accepted/older-refused/ancient-refused cases against the Tier-0 pinned minimum v17); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh", "packages/nixling-contract-tests/tests/policy_misc.rs::minijail_version_parser_canary (parser-shape canary: extracts the integer revision from a minijail0-shaped version banner); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/polkit-allowlist-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_misc.rs::polkit_allowlist_daemon_only_singletons (nixos-modules/host-polkit.nix: launcher-group allowlist names ONLY the daemon-only singletons nixlingd.service/nixling-priv-broker.service/nixling-priv-broker.socket, the executable region (awk '/^in$/,0' scope) carries no retired per-VM/per-env unit shapes, and the surviving grant keeps the manage-units action-id guard, the `nixling` group guard, the start/stop/restart verb allowlist, and exactly one polkit.addRule callback); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/tempo-budget-eval.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_observability.rs::tempo_stack_signoz_backend_and_collector (native SigNoz stack shape in nixos-modules/components/observability/stack.nix: ClickHouse + Keeper coordinator + signoz/signoz-otel-collector/signoz-schema-migrate-sync units + collector processor/exporter tokens + per-source receiver ingress tokens); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh", "packages/nixling-contract-tests/tests/policy_observability.rs::tempo_guest_collector_shape (guest collector in nixos-modules/components/observability/guest.nix: lib.mkIf/optionalAttrs conditionals resolved before YAML, self-metrics on dedicated resource/self pipeline, workload resource block preserves application service.name, journald receiver+access+severity+restart-safe cursor under scrapeJournal); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh", "packages/nixling-contract-tests/tests/policy_observability.rs::tempo_security_observability_and_retired_backends (collector self-telemetry tokens, source-specific pipelines vs shared otlp receiver, URL-encoded ClickHouse DSN passwords, ClickHouse default-user local-only auth, no OpAMP manager mode, obs secrets readable through ro virtiofs, host-activation obs vsock-relay ACL wiring, loopback-only backend binds + SigNoz UI port, no retired grafana/prometheus/loki/tempo/alloy backend, host options surface in options-observability.nix, ADR 0026 manifestVersion Spec-corrections record); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/vm-submodule-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_misc.rs::vm_submodule_compose_vm_shape (nixos-modules/vm-submodule.nix exists and carries the composeVm ownership shape via the unanchored `composeVm\\s*=` grep, which matches the current `_composeVm =` binding; toplevel-hash parity covered separately); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/wave-evidence-schema-eval.sh"
group = "B"
make_target = "test-drift"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_observability.rs::wave_evidence_schema_cross_check (readiness-wave doc/schema drift gate: every wave key parsed from nixos-modules/options-daemon.nix:readinessWaveSpecs has a matching per-wave inventory row in docs/reference/wave-evidence-schema.md, and docs/reference/wave-evidence-schema.json .required declares exactly [operatorSignature, timestamp, wave]); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/host-prep-dag-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_source.rs::host_prep_dag_module_surface (packages/nixling-host/src/host_prep_dag.rs: all 10 HostPrepStepKind enum variants declared, builder ordering-edge presence, the 9-symbol public API surface, and the depends_on dep-edges ApplySysctl<-BringUpTapInterface / SetBridgePortFlags<-ApplySysctl / PreOpenVhostNetFd<-SetBridgePortFlags via the ported `grep -B 3` window), host_prep_dag_nixling_host_reexport (nixling-host/src/lib.rs `pub mod host_prep_dag;`), host_prep_dag_broker_wire_scaffolds (broker_wire.rs BrokerRequest variant/struct/op_name arms + runtime.rs dispatch arms for SeedDnsmasqLease/BindMountFromHardlinkFarm/OwnershipMatrixCheck/SshHostKeyPreflight, the two live OperationFields audit rows, and the two still-deferred typed Unimplemented labels), host_prep_dag_nixlingd_wiring (nixlingd/src/lib.rs build_/log_/execute_host_prep_dag + NIXLING_HOST_PREP_DAG_EXECUTE markers), host_prep_dag_documentation (docs/reference/host-prep-dag.md: all 10 step kinds, ordering phrases incl. `AFTER \\`ApplySysctl\\``, and the Canonical step set section completeness for all 10 slugs; the bash gate's `grep -qF host-prep-dag` self-reference check is asserted via the doc's canonical slug path rather than its body to avoid the retired-.sh self-referential doc trap); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/no-bash-exec-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_source.rs::no_bash_exec_check (ADR 0017 — the Rust CLI must never invoke bash; per-line scan of git-listed files under packages/ excluding any target/, tests/, .git/ directory component for the verbatim-ported BASH_EXEC_PATTERN, each hit filtered against tests/fixtures/no-bash-exec-exempt-paths.json exempt_paths) + no_bash_exec_fixture_coverage (no stale allow-list entries); gated via the NL_FIXTURES contract step in tests/rust-workspace-checks.sh", "tests/tools/no-bash-ast-walker (ADR 0017 syn-AST-level Command::new bash-literal scan, catching cross-line/obfuscated sites the per-line regex cannot) — now run as a dedicated gated step in tests/rust-workspace-checks.sh, preserving the retired gate's default all-mode syn-ast-walk coverage"]
exercised_today = "yes"

[[script]]
name = "tests/privileges-doc-completeness-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_privileges_doc.rs::legacy_systemd_surface_obituary_completeness (docs/reference/privileges.md legacy-systemd obituary completeness scan vs nixos-modules/: per the 20 LEGACY_UNITS patterns, each must have a live broker-op row OR an obituary row — never a self-contradictory both; obituary region = '## Legacy systemd surface obituary' heading through the next top-level '## ' heading; reproduces all three hard-fail modes [emitted-but-undocumented, deleted-but-no-obituary, contradictory live+obituary] plus the transitional emitted+obituary warning, with the emitted grep / doc_pat / LIVE_OBIT_MARKERS regexes and the ±3-line marker window ported verbatim); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/agents-md-rewrite-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_docs.rs::agents_md_reflects_daemon_only_end_state (AGENTS.md daemon-only end-state invariant: positive — '## Daemon-only end-state (P6 onward)' section + ADR 0015 cross-ref + nixlingd / nixling-priv-broker.socket / SpawnRunner mentions; negative — per-line forbidden legacy-as-live scan exempting lines that carry an explicit historical/retired marker, forbidden_re + allowed_marker_re ported verbatim); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/guest-exec-runtime-static.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_contracts.rs::guest_exec_runtime_static (guest exec runtime static guard over packages/nixling-guestd/src + packages/nixling-exec-runner/src + nixos-modules/guest-control.nix: ATTACHED exec.rs/exec_linux.rs scope held — no userd path, no low-level TTY/PTY syscalls, no retained-log file writes, stdin Stdio::null never piped, no vsock listener, no CH CONNECT/relay/host-network/observability surface; DETACHED path present-and-bounded — slot-keyed nixling-exec-<NN> units with no opaque exec_id in production unit name/argv, nixling-exec.slice scoping, filering.rs truncation-bounded logs, conditionally-advertised EXEC_DETACHED/EXEC_LOGS, guest-module-declared /run/nixling-exec parent dir + slice; INTERACTIVE TTY path present-and-confined — exec_pty.rs owns openpt master alloc, tty_helper.rs owns the setsid+ioctl_tiocsctty handshake, exec_pty.rs non-comment code performs no handshake, service.rs wires TtyStderrUnavailable); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/kernel-module-matrix-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_docs.rs::kernel_module_matrix_source_doc_parity, kernel_module_missing_typed_error_contract (REQUIRED/OPTIONAL module-matrix parity between the constants in packages/nixlingd/src/kernel_module_check.rs — quoted module string literals + canonical `pub const` idents — and the backticked module names in docs/reference/kernel-module-check.md, plus the fatal-typed-error contract: packages/nixlingd/src/typed_error.rs carries HostKernelModulesMissing at exit code 64 with kind host-kernel-modules-missing); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/manpage-completeness-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_docs.rs::manpage_documents_every_top_level_subcommand (every top-level clap subcommand declared in packages/nixling/src/lib.rs `enum NativeCommand` — including #[command(name = ...)] overrides and the PascalCase→kebab-case default conversion — is documented as a `nixling-<name>(1)` entry under the SUBCOMMANDS block of docs/manpages/nixling.1; fails closed on manpage drift without a cargo toolchain); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/tap-dag-contract-doc-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_contracts.rs::tap_dag_contract_doc_matches_implementation (docs/reference/tap-dag-contract.md doc/code drift gate: every `../../` doc-referenced path resolves; ifname.rs DEFAULT_PREFIX=\"nl-\"/BRIDGE_TAG='b'/TAP_TAG='t'/HASH_SUFFIX_LEN=8/derive_from_env_vm/looks_nixling_owned cross-refs; tap.rs broker ops CreateTapFd/CreatePersistentTap/SetBridgePortFlags + nm-unmanaged-pre-create-required + TUNSETPERSIST/TUNSETOWNER/TUNSETGROUP; host_prep_dag.rs BringUpTapInterface variant/bring-up-tap-interface slug/broker_op_name==CreateTapFd/HostPrepStepFailed + apply-nftables-rules->pre-open-vhost-net-fd ordering; host.rs ChNetHandoffMode enum TapFd/PersistentTap; host-daemon.nix nixling group); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/tracing-contract-lint.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_contracts.rs::tracing_contract_lint (bounded-cardinality tracing-attribute allowlist enforcement over packages/**/*.rs excluding target/vendor/generated, including tests: fails closed on forbidden high-cardinality/leakable attr classes — bundle/keys_dir .display() path attrs, bundle_path alias, argv/cmdline/command_line, secret/password/token/private_key, stdout/stderr — plus a faithful awk-state-machine port flagging store-path string literals inside tracing-macro arg lists; see docs/reference/tracing-contract.md); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/broker-systemd-unit-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_daemon.rs::broker_systemd_unit_declarations (nixos-modules/host-broker.nix: nixling-priv-broker socket/service shape — config NOT gated behind cfg.daemonExperimental.enable, ListenSequentialPacket=/run/nixling/priv.sock, SocketGroup=nixlingd, SocketMode=0660, serviceConfig.Type=notify, socket unit wantedBy sockets.target); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/group-migration-fresh-install-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_state.rs::group_migration_fresh_install (fresh-install legacy-gid migration activation block in nixos-modules/host-activation.nix: nixlingGroupMigration stringAfter ordering, root-existence guard, chgrp-by-numeric-gid helper routing, --skip-while-lock-held, the fd-safe O_DIRECTORY|O_NOFOLLOW / F_OFD_SETLK / AT_SYMLINK_NOFOLLOW helper invariants, and the no-raw-chgrp-in-migration-block awk port); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "skip"

[[script]]
name = "tests/group-rename-semantic-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_state.rs::group_rename_semantic (nixling lifecycle-group rename semantics in nixos-modules: users.groups.nixling declaration, launcherUsers extraGroups join, daemon publicSocketGroup=nixling, state-dir ACL LAUNCHER_GROUP=nixling, the g:$LAUNCHER_GROUP traversal render in state-dir-acl.sh, the empty legacy-group tombstones, and the no-legacy-group-in-extraGroups scan over nixos-modules); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/kernel-modules-parity-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_daemon.rs::kernel_modules_parity_evaluator_shape (nixos-modules/vm-evaluator.nix calls the standard NixOS eval-config.nix entrypoint that computes requiredKernelModules, and nixos-modules/lib.nix routes nl.vmRunner through config.nixling._computed so per-VM microvm.* incl. microvm.kernel resolves); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/processes-json-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_daemon.rs::processes_json_consumers_route_through_helpers (nixos-modules/{processes-json,closures-json,minijail-profiles,store}.nix do NOT read config.microvm.vms.<name>.config.config.* directly, and nixos-modules/lib.nix defines the nl.vmRunner/vmToplevel/vmDeclaredRunner helpers); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/state-dir-acl-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_state.rs::state_dir_acl (/var/lib/nixling state-dir declaration + ACL invariants in nixos-modules: 0750 root nixlingd tmpfiles decl (not 0755), nixlingStateDirAcl activation script, per-user/per-group --x traversal grants incl. g:kvm:--x and the g:nixling:--x grant (direct or via the sourced state-dir-acl.sh helper), and the no-`setfacl -d -m`-default-ACL-on-state-dir-root negative with the comment-line filter); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/stop-dag-reconcile-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_daemon.rs::stop_dag_reconcile_surface (packages/nixlingd/src/supervisor/stop_dag.rs StopDagOwner/ObservedHostState/ReconcileReport/Nftables+Usbip reconcile actions+drift reasons/reconcile_on_restart/reconcile surface, supervisor/mod.rs `pub mod stop_dag;`, broker_wire.rs reuses ApplyNftables/UsbipBind/UsbipUnbind variants with no new *Request type in stop_dag.rs, and docs/reference/stop-dag-reconcile.md marker coverage); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/store-sync-export-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_state.rs::store_sync_export (StoreSync-only observability export static invariants in nixos-modules/components/observability/host.nix: storeSyncExportDir/Glob shape, the filelog/store_sync_audit receiver + json_parser + logs/store_sync_audit pipeline forwarded over otlp, the host-singleton resource attrs plus source=store-sync-audit with target_vm/target_env never promoted, the nixling-host-otel-collector export-dir-only ACL grants, and the deny scans for broker audit-log/glob, priv.sock, and any collector ACL on an audit path; over a comment-stripped view); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/changelog-v1-cut-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_release.rs::changelog_keeps_unreleased_above_latest_dated_release, changelog_v1_0_0_section_enumerates_breaking_changes_with_adr_and_migration_xref (CHANGELOG.md Unreleased/latest-cut-date hygiene + [1.0.0] breaking-changes enumeration with ADR 0015 & v0->v1 migration-guide cross-refs); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/guest-control-auth-nongoals.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_guest.rs::retired_guest_control_nongoals_are_now_shipped_goals (the W15 guest-control-health readiness non-goal and the W16 `nixling exec` CLI non-goal are now shipped goals: ProcessRole::GuestControlHealth + the readiness predicate in nixling-core/src/processes.rs, and the admin-only `vm exec` / `vm konsole` verbs in nixling/src/lib.rs); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh", "packages/nixling-contract-tests/tests/policy_guest.rs::guest_control_auth_core_token_share_contract_present (the auth-core token-share / LoadCredential contract the retired nix-eval smoke exercised — nl-gctl share + /run/nixling-guest-control-host mount + guest_control_token LoadCredential + auth.tokenFile absolute/outside-/nix/store validation — is present in the module source; the nix eval itself remains covered by tests/nix-unit/cases/guest-control-auth.nix); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/guest-control-vsock-helper-static.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_guest.rs::guest_control_vsock_helper_stays_transport_confined (recursively scans packages/nixlingd/src/**/*.rs and asserts the raw connect_guest_control_vsock* helper appears only in its transport module guest_control_vsock.rs and its sanctioned consumers guest_control_bridge.rs + exec_session_real.rs); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/legacy-group-name-denylist-self-test.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_modules.rs::legacy_group_name_denylist_rejects_forbidden_line (negative coverage: a forbidden legacy launcher group-name reference in a non-allowlisted source path is flagged, while allowlisted migration-tombstone lines are not); gated via the NL_FIXTURES contract step in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/legacy-group-name-denylist.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_modules.rs::legacy_group_name_denylist (full-line allowlisted denylist scan over nixos-modules/packages/tests rejecting retired legacy launcher group-name references); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/microvm-nix-absent-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_release.rs::microvm_nix_input_absent_from_flake (flake.nix declares no microvm input / inputs.microvm reference per ADR 0018); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "skip"

[[script]]
name = "tests/otel-acl-migration-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_guest.rs::otel_relay_acl_module_not_publicly_imported_and_broker_handler_present (asserts nixos-modules/default.nix carries no uncommented ./host-otel-relay-acl.nix import and that packages/nixling-priv-broker/src/runtime.rs still carries the RunnerRole::OtelHostBridge spawn handler); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/release-tag-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_release.rs::release_v1_1_tag_is_annotated_and_names_the_release (refs/tags/v1.1 is annotated, resolves to a commit, and its tag message first line names the release version); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/static-rust-dependency-direction.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_modules.rs::static_rust_dependency_direction (static Cargo.toml parse of the workspace dep graph WANT map + asserts nixling/nixlingd import nixling_ipc from their src trees); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "skip"

[[script]]
name = "tests/vfsd-watchdog-retired-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_release.rs::vfsd_watchdog_units_retired_from_store_module (nixos-modules/store.nix declares no nixling-vfsd-watchdog@ service/timer template or per-VM enable unit per ADR 0018); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/vm-submodule-cutover-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_modules.rs::vm_submodule_cutover (asserts no non-exempt nixos-modules/ consumer reads config.microvm.vms.${name}.config.config directly; lib.nix/host.nix/vm-submodule.nix exempt); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh"]
exercised_today = "yes"

[[script]]
name = "tests/adr-0015-presence-eval.sh"
group = "H"
make_target = "test-policy"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_lints.rs (gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh -> static.sh) — adr_0015_present_with_header_and_cross_references (ADR 0015 file/header/section invariants + AGENTS.md & docs/adr/README.md cross-refs)"]
exercised_today = "yes"

[[script]]
name = "tests/daemon-experimental-warning-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_lints.rs (gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh -> static.sh) — daemon_experimental_option_documents_default_with_migration (options-daemon.nix + v1.0->v1.1 migration guide literal-string lints)"]
exercised_today = "yes"

[[script]]
name = "tests/v1.1-kernel-floor-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/policy_lints.rs (gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh -> static.sh) — v1_1_kernel_floor_declared_in_adr_and_migration_guide (ADR 0008 + migration guide >=6.9 kernel-floor regex lints)"]
exercised_today = "yes"

[[script]]
name = "tests/static-invariant-broad-caps.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["nixling_core::static_invariants validators (production logic): is_broad_cap_violation", "packages/nixling-contract-tests/tests/static_invariants.rs (synthetic positive/negative logic cases + REAL rendered fixture-smoke contract cases; gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh -> static.sh) — broad_caps_* (synthetic) + rendered_profiles_with_broad_caps_carry_an_adr_carve_out (real RoleProfile.caps vs adr_carve_out)"]
exercised_today = "skip"

[[script]]
name = "tests/static-invariant-opaque-key-ids.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["nixling_core::static_invariants validators (production logic): path_bearing_key_violations", "packages/nixling-contract-tests/tests/static_invariants.rs (synthetic positive/negative logic cases + REAL rendered fixture-smoke contract cases; gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh -> static.sh) — opaque_key_ids_* (synthetic) + rendered_manifest_has_no_path_bearing_key_or_secret_fields (real vms.json key/secret path scan)"]
exercised_today = "skip"

[[script]]
name = "tests/static-invariant-world-readable-leak.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["nixling_core::static_invariants validators (production logic): world_readable_field_leaks", "packages/nixling-contract-tests/tests/static_invariants.rs (synthetic positive/negative logic cases + REAL rendered fixture-smoke contract cases; gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh -> static.sh) — world_readable_* (synthetic) + rendered_manifest_exposes_only_public_safe_fields (real vms.json field allowlist)"]
exercised_today = "manual"

[[script]]
name = "tests/static-invariant-writable-paths.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["nixling_core::static_invariants validators (production logic): undeclared_writable_paths", "packages/nixling-contract-tests/tests/static_invariants.rs (synthetic positive/negative logic cases + REAL rendered fixture-smoke contract cases; gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/rust-workspace-checks.sh -> static.sh) — writable_paths_* (synthetic logic). Spec correction: the bash schema-vocabulary grep is subsumed by the typed MountPolicy.writable_paths DTO + the bundle-drift gate (schema generated from the DTO); no separate bundle-level declared-writable-paths list exists in the real DTO to contract-test"]
exercised_today = "skip"

[[script]]
name = "tests/cli-json.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling/tests/cli_json_contract.rs::list_reports_pending_restart_when_booted_differs_and_active (spawns the real nixling binary against NL_FIXTURES; copies the fixture-smoke bundle into a hermetic temp bundle with the absolute /etc/nixling processesPath/hostPath rewritten to relative basenames; per-VM state-root with corp-vm current!=booted symlinks + daemon-state pidfd-table marking ch-runner running + system-state fixture pinning nixlingd.service active; asserts `list --json` deserialized strictly into nixling::ListOutputV2 (deny_unknown_fields == the bash jq key-set check) reports corp-vm status==pending-restart)", "packages/nixling/tests/cli_json_contract.rs::status_reports_pending_restart_with_consistent_current_booted (same pending-restart env; `status corp-vm --json` deserialized strictly into nixling::StatusVmOutputV2 (deny_unknown_fields == the bash jq key-set check for status/services/runnerParity/livePoolIntegrity); asserts pendingRestart=true, current=/nix/store/nixling-current, booted=/nix/store/nixling-booted, services.nixling=active, services.microvm=running, runtime=unknown, runnerParity.runnerParityOk=true)", "packages/nixling/tests/cli_json_contract.rs::keys_list_daemon_down_returns_structured_envelope (`keys list --json` with NIXLING_PUBLIC_SOCKET pointed at a missing socket: exit 1, empty stderr, structured daemon-down envelope on stdout with the exact key set [code,docsAnchor,exitCode,kind,observedState,remediation,whatWasChecked], kind==\"nixling keys list requires nixlingd\", code=daemon-down, exitCode=1, docsAnchor=docs/reference/error-codes.md#daemon-down)", "packages/nixling/tests/cli_json_contract.rs::audit_json_stays_json_on_tty_with_daemon_down_envelope (reproduces the bash `script`-PTY case via rustix::pty: spawns `audit --json` with stdin/stdout/stderr on a real PTY slave and a missing public socket; asserts the CLI stays JSON on a TTY — not the human-on-stderr form — exit 1, daemon-down envelope with kind==\"nixling audit requires nixlingd\" and the exact key set)", "tests/rust-workspace-checks.sh (CLI-contract step: builds fixture-smoke, runs `cargo test -p nixling --tests` with NL_FIXTURES — gated in static.sh)", "tests/golden/pinned/cli-json.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "manual"

[[script]]
name = "tests/cli-rust-native-host-check.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling/tests/host_check_contract.rs::host_check_pass_fixture_exits_zero_and_matches_schema (pass fixture -> exit 0, stdout strictly deserializes into nixling::HostCheckOutputV2 (deny_unknown_fields == host-check schema check), summary.fail==0 warn==0)", "packages/nixling/tests/host_check_contract.rs::host_check_built_in_kernel_modules_pass (kvm module in builtInModules not loadedModules -> exit 0, kernel-module severity=pass message 'built into the running kernel')", "packages/nixling/tests/host_check_contract.rs::host_check_ufw_active_warns (NIXLING_TEST_UFW_ACTIVE=1 -> exit 1, firewall-coexistence warn ufw_active=true)", "packages/nixling/tests/host_check_contract.rs::host_check_systemctl_unavailable_warns (NIXLING_TEST_SYSTEMCTL_UNAVAILABLE=1 -> exit 1, firewall-coexistence warn 'could not be fully determined' + systemctl-unavailable detail)", "packages/nixling/tests/host_check_contract.rs::host_check_runner_parity_drift_warns_without_strict (runner-drift bundle no --strict -> exit 1, summary.warn>0 fail==0, runner-parity warn)", "packages/nixling/tests/host_check_contract.rs::host_check_runner_parity_drift_fails_under_strict (runner-drift bundle + --strict -> exit 2, summary.fail>0, runner-parity fail)", "packages/nixling/tests/host_check_contract.rs::host_check_hard_failure_exits_two (kernelRelease=6.5.0-nixling old-kernel hard failure -> exit 2, summary.fail>0)", "packages/nixling/tests/host_check_contract.rs::host_check_modules_probe_error_envelope (loadedModulesError -> exit 1, stderr internal-io envelope owningCommand 'host check' code 50 'forced /proc/modules read failure')", "packages/nixling/tests/host_check_contract.rs::host_check_nft_probe_error_envelope (nftError -> exit 1, stderr internal-io envelope 'forced nft probe failure')", "packages/nixling/tests/host_check_contract.rs::host_check_ufw_probe_error_envelope (ufwError -> exit 1, stderr internal-io envelope 'forced ufw probe failure')", "packages/nixling/tests/host_check_contract.rs::host_check_usage_error_exits_three (host check --bogus clap usage error -> exit 3)", "packages/nixling/tests/host_check_contract.rs::host_check_human_groups_findings_by_severity (pass fixture --human -> exit 0, stdout 'summary: pass=' + PASS severity grouping)", "packages/nixling/tests/host_check_contract.rs::daemon_host_check_pass_runs_full_probe_battery (real nixlingd serve --once + test-client hostCheck -> summary.failures==0 warnings==0, checks[] has kernel-module:kvm + nftables-table + firewall-coexistence + runner-parity; via tests/common spawn_nixlingd_host_check)", "packages/nixling/tests/host_check_contract.rs::daemon_host_check_fails_closed_on_old_kernel_and_missing_cgroup_v2 (kernelRelease=6.5.0-nixling + cgroupV2Present=false -> summary.failures>=2, kernel-version=fail + cgroup-v2=fail)", "packages/nixling/tests/host_check_contract.rs::daemon_host_check_strict_runner_parity_fails (runner-drift bundle + strict=true -> summary.failures>0, runner-parity=fail)", "tests/rust-workspace-checks.sh (CLI-contract step builds fixture-smoke + nixlingd, exports NL_FIXTURES + NIXLING_TEST_NIXLINGD_BIN, runs cargo test -p nixling --tests — gated in static.sh)", "tests/golden/pinned/cli-host-check.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "yes"

[[script]]
name = "tests/cli-rust-native-audit.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling/tests/audit_contract.rs::audit_reports_daemon_down_without_bash_fallback (missing socket + NIXLING_LEGACY_* poison-pill -> typed daemon-down envelope exit 1, never execs bash/exit 99)", "packages/nixling/tests/audit_contract.rs::audit_strict_returns_not_yet_implemented_envelope (audit --strict -> exit 78 not-yet-implemented envelope)", "packages/nixling/tests/audit_contract.rs::audit_relays_daemon_auditresponse_frames (in-process SOCK_SEQPACKET mock daemon hello/helloOk/audit/auditResponse -> lines relayed to stdout)", "packages/nixling/tests/audit_contract.rs::audit_admin_rejected_against_live_daemon_without_fallback (real nixlingd serve --once, launcher peer -> exit 32 authz-audit-requires-admin, no daemon-down; via tests/common daemon-spawn harness)", "tests/rust-workspace-checks.sh (CLI-contract step builds nixlingd + exports NIXLING_TEST_NIXLINGD_BIN, runs cargo test -p nixling --tests with NL_FIXTURES — gated in static.sh)", "tests/golden/pinned/cli-audit.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "skip"

[[script]]
name = "tests/cli-rust-native-auth-status.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling/tests/auth_status_contract.rs::auth_status_roles_match_schema_and_authz (spawns the real nixling binary and drives `auth status` across the launcher/none/admin roles via the env-file fixture contract (NIXLING_AUTH_STATUS_FIXTURE + NIXLING_TEST_{LAUNCHER,ADMIN}_UIDS + --test-uid); `auth status --json` deserialized strictly into nixling::AuthStatusOutputV2 (deny_unknown_fields == the docs/reference/cli-output/auth-status.schema.json check) + asserts per-role authz: launcher allows `up`/denies `audit`, none stays read-only [auth status,host check,list,status], admin gains `audit`/denies nothing; --human summarizes role + denied audit)", "tests/rust-workspace-checks.sh (CLI-contract step: cargo test -p nixling --tests — gated in static.sh)", "tests/golden/pinned/cli-auth-status.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "yes"

[[script]]
name = "tests/cli-rust-native-host-doctor.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling/tests/host_doctor_contract.rs::host_doctor_without_read_only_exits_78_usage_envelope (usage gate: `host doctor --json` without --read-only exits 78 with a JSON envelope .code == \"--read-only-required\")", "packages/nixling/tests/host_doctor_contract.rs::host_doctor_baseline_no_state_reports_broker_fail_exit_2 (no daemon-state: broker unreachable -> exit 2, envelope command/mode/broker_ready=false, exact 11-check-name set with signoz-ui-endpoint suppressed by the observability-disabled manifest, summary fail>=1 warn>=4)", "packages/nixling/tests/host_doctor_contract.rs::host_doctor_pidfd_table_reports_otel_and_usbipd_runners (pidfd-table.json -> otel-host-bridge-runner pass count==1, usbipd-runners pass count==2)", "packages/nixling/tests/host_doctor_contract.rs::host_doctor_clean_kernel_module_report_passes (clean kernel-module-report.json -> kernel-module-matrix pass)", "packages/nixling/tests/host_doctor_contract.rs::host_doctor_missing_required_kernel_module_fails_exit_2 (missing_required kernel module -> kernel-module-matrix fail, exit 2, envelope exitCode==2)", "packages/nixling/tests/host_doctor_contract.rs::host_doctor_autostart_failed_outcome_fails_exit_2 (autostart-report Failed outcome -> autostart-status fail, exit 2, data.failed==1, data.degradedTotal==1)", "packages/nixling/tests/host_doctor_contract.rs::host_doctor_autostart_degraded_outcome_warns (autostart-report Degraded outcome -> autostart-status warn, data.degraded==1)", "packages/nixling/tests/host_doctor_contract.rs::host_doctor_metrics_endpoint_unreachable_warns (closed loopback NIXLING_METRICS_URL -> metrics-endpoint warn, detail contains \"unreachable\")", "packages/nixling/tests/host_doctor_contract.rs::host_doctor_human_renderer_emits_summary_and_markers (`host doctor --read-only --human` prints the summary line + [PASS] per-check markers)", "packages/nixling/tests/host_doctor_contract.rs::host_doctor_live_sockets_report_broker_and_daemon_ready (live AF_UNIX SOCK_SEQPACKET broker+public listeners -> broker-ready pass, daemon-ready pass, top-level broker_ready==true)", "tests/rust-workspace-checks.sh (CLI-contract step: cargo test -p nixling --tests — gated in static.sh)", "tests/golden/pinned/cli-host-doctor.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "skip"

[[script]]
name = "tests/cli-rust-native-status.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling/tests/status_contract.rs::status_vm_json_matches_schema_and_static_state (spawns the real nixling binary against NL_FIXTURES; copies the fixture-smoke bundle into a hermetic temp bundle with the absolute /etc/nixling processesPath/hostPath rewritten to relative basenames so the host's deployed manifest cannot shadow the fixture; `status --vm corp-vm --json` classified as StatusOutputV2::Vm and deserialized strictly into nixling::StatusVmOutputV2 (deny_unknown_fields == the docs/reference/cli-output/status.schema.json check); asserts env=work, runtime=unknown, services.nixling=inactive, services.microvm=stopped, services.virtiofsd=stopped, declaredRoles contains cloud-hypervisor-runner, runnerParity.runnerParityOk=true, pendingRestart=false)", "packages/nixling/tests/status_contract.rs::status_vm_flag_and_positional_json_are_equivalent (`status --vm corp-vm --json` and positional `status corp-vm --json` produce byte-identical stdout)", "packages/nixling/tests/status_contract.rs::status_check_bridges_returns_frozen_not_yet_implemented_envelope (`status --check-bridges --json` classified as StatusOutputV2::CheckBridges + strictly deserialized into nixling::StatusBridgeCheckOutputV2; asserts mode=check-bridges, status=not-yet-implemented)", "packages/nixling/tests/status_contract.rs::status_human_renders_runner_parity_and_bridge_sections (`status --vm corp-vm --human` stdout contains 'runner parity: ok' and 'Bridge health')", "tests/rust-workspace-checks.sh (CLI-contract step: cargo test -p nixling --tests with NL_FIXTURES — gated in static.sh)", "tests/golden/pinned/cli-status.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "yes"

[[script]]
name = "tests/cli-rust-native-usb.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling/tests/usb_contract.rs::usb_help_matches_golden (spawns the real nixling binary; `usb --help` stdout matches tests/golden/cli-output/usb-help.txt byte-for-byte)", "packages/nixling/tests/usb_contract.rs::usb_attach_dry_run_matches_golden (`usb attach corp-vm 1-2 --dry-run` against the NL_FIXTURES manifest/bundle matches tests/golden/cli-output/usb-attach-dry-run.txt)", "packages/nixling/tests/usb_contract.rs::usb_detach_dry_run_matches_golden (`usb detach corp-vm 1-2 --dry-run` against the NL_FIXTURES manifest/bundle matches tests/golden/cli-output/usb-detach-dry-run.txt)", "tests/rust-workspace-checks.sh (CLI-contract step: cargo test -p nixling --tests with NL_FIXTURES — gated in static.sh)", "tests/golden/pinned/cli-usb.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "yes"

[[script]]
name = "tests/cli-vm-verbs-eval.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling/tests/vm_verbs_contract.rs::mutating_verbs_emit_daemon_down_without_bash_fallback (up/down/restart + vm start/stop/restart against a missing public socket all surface the typed daemon-down envelope (.code==daemon-down, .exitCode==1) and exit 1; the exit-99 poison-pill legacy CLI is never exec'd even with NIXLING_LEGACY_BASH_OPT_IN=1 set)", "packages/nixling/tests/vm_verbs_contract.rs::legacy_bash_opt_in_is_a_no_op (spot-check: NIXLING_LEGACY_BASH_OPT_IN=1 + poison-pill legacy path never reaches the poison-pill; escape hatch removed)", "packages/nixling/tests/vm_verbs_contract.rs::vm_list_is_daemon_native_json (vm list --json returns the rust-native command==\"vm list\" envelope, exit 0, no bash fallback)", "packages/nixling/tests/vm_verbs_contract.rs::top_level_list_is_daemon_native_json (nixling list --json returns the native manifest array including the synthetic test-vm, exit 0, no bash fallback)", "packages/nixling/tests/vm_verbs_contract.rs::vm_konsole_dry_run_emits_guest_control_shape (vm konsole --dry-run --json emits command/mode/vm/terminal=konsole/transport=guest-control + argv hosting `vm exec -it konsole-vm -- /run/current-system/sw/bin/bash -l`; retired SSH host/user/key fields absent)", "packages/nixling/tests/vm_verbs_contract.rs::vm_konsole_terminal_override_reflected (--terminal xterm reflected in .terminal and argv[0])", "packages/nixling/tests/vm_verbs_contract.rs::vm_konsole_rejects_retired_ssh_flags (retired --host/--user/--key each exit non-zero)", "packages/nixling/tests/vm_verbs_contract.rs::vm_konsole_rejects_unknown_vm (unknown vm name exits non-zero)", "packages/nixling/tests/vm_verbs_contract.rs::vm_exec_missing_command_emits_cli_usage_envelope (vm exec with no command exits 2 with the cli/usage envelope via clap+dispatch)", "packages/nixling/tests/vm_verbs_contract.rs::vm_exec_no_daemon_emits_transport_unavailable_envelope (vm exec -- /bin/true with no daemon exits non-zero with source=transport reason=guest-control-transport-unavailable; never SSH/bash)", "packages/nixling/tests/vm_verbs_contract.rs::vm_exec_rejects_interactive_without_tty (vm exec -i without -t exits 2 and cites the -t/--tty requirement)", "tests/rust-workspace-checks.sh (CLI-contract step: cargo test -p nixling --tests — gated in static.sh)", "tests/golden/pinned/cli-vm-verbs.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "manual"

[[script]]
name = "tests/cli-rust-native-list.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling/tests/cli_contract.rs::list_json_matches_smoke_inventory_and_schema (spawns the real nixling binary against NL_FIXTURES bundle + hermetic system-state/daemon-state fixture; `list --json` deserialized strictly into nixling::ListOutputV2 (deny_unknown_fields == the docs/reference/cli-output/list.schema.json check) + asserts the 2-VM smoke inventory: corp-vm stopped + runnerParityOk, sys-work-net net-vm running + runnerParityOk)", "tests/rust-workspace-checks.sh (CLI-contract step: builds fixture-smoke, runs `cargo test -p nixling --tests` with NL_FIXTURES — gated in static.sh)", "tests/golden/pinned/cli-contract.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
exercised_today = "yes"

[[script]]
name = "tests/broker-default-features-build.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["tests/rust-workspace-checks.sh (gated `cargo check --workspace --manifest-path nixling-priv-broker default features = real wire dispatch` — the broker default-features build is already a gate step)"]
exercised_today = "skip"

[[script]]
name = "tests/broker-scm-rights-fd-lifecycle.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo test --workspace --features layer1-bootstrap (nixling-priv-broker fd_passing::tests::scm_rights_fd_lifecycle_* — runs in tests/rust-workspace-checks.sh broker layer1-bootstrap test pass)", "tests/tools/assert-pinned-tests.sh (fail-closed pin via the broker --features layer1-bootstrap nextest list)"]
exercised_today = "yes"

[[script]]
name = "tests/static-invariant-uid0.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-core/src/bundle_resolver.rs validate_minijail_profiles unit tests (rejects uid0/gid0 without a real carve-out, rejects empty/whitespace carve-out, accepts uid0 WITH carve-out)", "packages/nixling-contract-tests/tests/minijail_profiles.rs::rendered_bundle_passes_all_minijail_profile_invariants (the real rendered bundle passes the uid0/root-carve-out invariant — gated via NL_FIXTURES in rust-workspace-checks.sh)", "validate_minijail_profiles doc + RoleProfile.adr_carve_out structural coupling + bundle-drift (schema-shape: root-capable shapes carry the ADR field); requiresStartRoot coupling superseded by ADR 0021"]
exercised_today = "skip"

[[script]]
name = "tests/privileges-json-rust-vs-nix-eval.sh"
group = "C"
make_target = "test-contract"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["packages/nixling-contract-tests/tests/privileges_parity.rs::rendered_privileges_matches_rust_matrix (compares NL_FIXTURES privileges.json vs nixling_core::privileges::PrivilegesJson::w1 — operation set + allowedGroups parity, public + broker)", "tests/rust-workspace-checks.sh (W3 fixture-contract step: builds fixture-smoke, runs the contract crate with NL_FIXTURES — gated in static.sh)"]
exercised_today = "yes"

[[script]]
name = "tests/assertions-eval.sh"
group = "E"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/assertions.nix — 26 eval-rejection cases via the minimal lib.evalModules fast evaluator; 23 Bucket-A preserve the message-substring check, 3 Bucket-B platform/throw cases assert rejection)"]
exercised_today = "skip"

[[script]]
name = "tests/broker-bundle-path-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/broker-bundle-path.nix — 12 cases: host-broker/host-daemon bundle-path source-grep via readFile + bundle.json artifact ACL contract via mkEval introspection)"]
exercised_today = "manual"

[[script]]
name = "tests/broker-caps-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/broker-caps.nix — 4 cases: broker CapabilityBoundingSet exact-set match + AmbientCapabilities empty-string sentinel via mkEval introspection)"]
exercised_today = "manual"

[[script]]
name = "tests/broker-socket-activation-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/broker-socket-activation.nix — 5 cases: host-broker ExecStart no --socket-path source-grep via readFile + socket-unit FileDescriptorName/ListenSequentialPacket via mkEval introspection)"]
exercised_today = "yes"

[[script]]
name = "tests/daemon-default-compat-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/daemon-default-compat.nix — 5 daemonExperimental.enable default-flip + operator-override (mkForce true/false) cases via mkEval; the historical evidence-dir writes are inert because the default is no longer evidence-gated)"]
exercised_today = "manual"

[[script]]
name = "tests/guest-config-containment-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/guest-config-containment.nix — 13 guestConfigFile containment cases: contained clean/reads-standard fixtures produce no failing assertion; sets-microvm / sets-nixling / imports / builtins.toFile / _file-spoof bypass fixtures fire the containment assertion naming the host-owned option, via mkEval config.assertions introspection)"]
exercised_today = "manual"

[[script]]
name = "tests/guest-control-auth-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/guest-control-auth.nix — 5 auth-token-delivery cases: default parameterization asserts the exact nl-gctl token share + LoadCredential JSON; /nix/store, /nix/store/<child>, and relative auth.tokenFile each reject at eval time, as does a tokenFile set without guest.control.enable; imports tests/eval-cases/guest-control-auth-eval.nix via a mkEval flake shim)"]
exercised_today = "yes"

[[script]]
name = "tests/guest-control-vsock-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/guest-control-vsock.nix — 6 base-vsock allocation cases: positive base scenario asserts exact CID/socket parity + ladder + readiness JSON; user-vsock-cid/socket, --vsock split/equals extraArgs, and long-AF_UNIX-socket override guards each reject at eval time; imports tests/eval-cases/guest-control-vsock-eval.nix via a mkEval flake shim)", "flake.checks.<system>.guest-control-vsock (existing derivation independently builds the positive base scenario)"]
exercised_today = "yes"

[[script]]
name = "tests/guest-exec-policy-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/guest-exec-policy.nix — 13 exec-policy cases: enabled/default/allow-root-only/allow-root-ceiling/allow-root-interactive-ceiling positive scenarios assert exact userd + ceiling JSON; exec-no-control, allowRoot/users-without-enable, empty-target, duplicate/root/wildcard/unknown user, and guest-internal exec.users override each reject at eval time; imports tests/eval-cases/guest-exec-policy-eval.nix via a mkEval flake shim)", "flake.checks.<system>.guest-exec-policy (existing derivation independently builds the positive enabled scenario)"]
exercised_today = "yes"

[[script]]
name = "tests/principal-uid-collision-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/principal-uid-collision.nix — 6 stablePrincipalId UID-collision/range/no-system-user-collision cases + non-vacuity count guards, via mkEval over examples/multi-env)"]
exercised_today = "yes"

[[script]]
name = "tests/readiness-waves-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/readiness-waves.nix — 11 defaultSwitchReadiness p0..p7 wave-presence, p0 implemented/validated default, and daemonExperimental.enable default cases via mkEval)"]
exercised_today = "manual"

[[script]]
name = "tests/video-contract-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/video-contract.nix — 28 video-sidecar DAG/socket/principal/device-bind/virgl-opt-in/guard-assertion cases plus the graphics.nix use_video source-wiring checks, via mkEval; x86_64-only guarded)"]
exercised_today = "manual"

[[script]]
name = "tests/autostart-wiring-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/autostart-wiring.nix — 7 daemon autostart-wiring value cases via mkEval introspection)"]
exercised_today = "manual"

[[script]]
name = "tests/bridge-ipv6-boot-sysctl-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/bridge-ipv6-boot-sysctl.nix — 12 cases)"]
exercised_today = "manual"

[[script]]
name = "tests/daemon-autostart-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/daemon-autostart.nix — 24 cases: Rust/doc source-grep via readFile + parallelism option via mkEval)"]
exercised_today = "manual"

[[script]]
name = "tests/restart-policy-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/restart-policy.nix — 14 restartIfChanged value cases via mkEval introspection)"]
exercised_today = "manual"

[[script]]
name = "tests/store-overlay-emit-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/store-overlay-emit.nix — 9 cases)"]
exercised_today = "manual"

[[script]]
name = "tests/umask-roundtrip-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/umask-roundtrip.nix — 4 cases)"]
exercised_today = "manual"

[[script]]
name = "tests/usbip-gating-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/usbip-gating.nix — 40 usbip gating cases)"]
exercised_today = "manual"

[[script]]
name = "tests/niri-vm-borders-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/niri-vm-borders.nix — 10 niri KDL window-rule cases via mkEval introspection)"]
exercised_today = "yes"

[[script]]
name = "tests/volume-mounts-eval.sh"
group = "D"
make_target = "test-nix-unit"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/volume-mounts.nix — 19 volume-helper value cases)", "flake.checks.<system>.module-helper-wiring (processes-json/vm-guest-base shared-helper callsite invariant)"]
exercised_today = "yes"

[[script]]
name = "tests/dag-topo.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo nextest run --workspace (nixlingd supervisor::dag/supervisor::state/daemon_version #[test]s)", "tests/tools/assert-pinned-tests.sh (fail-closed pin)"]
exercised_today = "skip"

[[script]]
name = "tests/guest-static-elf.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["flake.checks.<system>.guest-static-elf (nix flake check / make test-flake builds the static-ELF derivation gate)"]
exercised_today = "yes"

[[script]]
name = "tests/net-vm-bundle-gate-eval.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo nextest run --workspace (nixlingd net_vm_bundle_gate + typed_error envelope #[test]s)", "tests/tools/assert-pinned-tests.sh (fail-closed pin)"]
exercised_today = "yes"

[[script]]
name = "tests/device-node-matrix.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo nextest run --workspace (nixling-host devices #[test]s)", "tests/rust-workspace-checks.sh (nixling-priv-broker ops::device #[test]s via broker-workspace cargo test --workspace)", "tests/tools/assert-pinned-tests.sh (fail-closed pin across both workspaces)"]
exercised_today = "yes"

[[script]]
name = "tests/ifname-collision.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo nextest run --workspace (net canary #[test]s)", "tests/tools/assert-pinned-tests.sh"]
exercised_today = "yes"

[[script]]
name = "tests/ioctl-negative.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo nextest run --workspace (nixling-host ioctl_policy #[test]s)", "tests/rust-workspace-checks.sh (nixling-priv-broker ops::device #[test]s via broker-workspace cargo test --workspace)", "tests/tools/assert-pinned-tests.sh (fail-closed pin across both workspaces)"]
exercised_today = "manual"

[[script]]
name = "tests/kernel-module-matrix.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo nextest run --workspace (nixling-host modules #[test]s)", "tests/rust-workspace-checks.sh (nixling-priv-broker ops::modprobe #[test]s via broker-workspace cargo test --workspace)", "tests/tools/assert-pinned-tests.sh (fail-closed pin across both workspaces)"]
exercised_today = "skip"

[[script]]
name = "tests/audio-argv-shape.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo nextest run --workspace --exclude nixling-contract-tests (argv #[test]s: audio_argv, ch_argv, gpu_argv, hardlink_farm, otel_host_bridge_argv, ssh_keygen, supervisor::state::reconcile_and_adopt, swtpm_argv, usbip_argv, video_argv, virtiofsd_argv, vsock_relay_argv)", "tests/tools/assert-pinned-tests.sh"]
exercised_today = "yes"

[[script]]
name = "tests/ch-argv-shape.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo nextest run --workspace --exclude nixling-contract-tests (argv #[test]s: audio_argv, ch_argv, gpu_argv, hardlink_farm, otel_host_bridge_argv, ssh_keygen, supervisor::state::reconcile_and_adopt, swtpm_argv, usbip_argv, video_argv, virtiofsd_argv, vsock_relay_argv)", "tests/tools/assert-pinned-tests.sh"]
exercised_today = "yes"

[[script]]
name = "tests/gpu-argv-shape.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo nextest run --workspace --exclude nixling-contract-tests (argv #[test]s: audio_argv, ch_argv, gpu_argv, hardlink_farm, otel_host_bridge_argv, ssh_keygen, supervisor::state::reconcile_and_adopt, swtpm_argv, usbip_argv, video_argv, virtiofsd_argv, vsock_relay_argv)", "tests/tools/assert-pinned-tests.sh"]
exercised_today = "yes"

[[script]]
name = "tests/otel-host-bridge-argv-shape.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo nextest run --workspace --exclude nixling-contract-tests (argv #[test]s: audio_argv, ch_argv, gpu_argv, hardlink_farm, otel_host_bridge_argv, ssh_keygen, supervisor::state::reconcile_and_adopt, swtpm_argv, usbip_argv, video_argv, virtiofsd_argv, vsock_relay_argv)", "tests/tools/assert-pinned-tests.sh"]
exercised_today = "yes"

[[script]]
name = "tests/sidecar-argv-shape.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo nextest run --workspace --exclude nixling-contract-tests (argv #[test]s: audio_argv, ch_argv, gpu_argv, hardlink_farm, otel_host_bridge_argv, ssh_keygen, supervisor::state::reconcile_and_adopt, swtpm_argv, usbip_argv, video_argv, virtiofsd_argv, vsock_relay_argv)", "tests/tools/assert-pinned-tests.sh"]
exercised_today = "yes"

[[script]]
name = "tests/usbip-argv-shape.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo nextest run --workspace --exclude nixling-contract-tests (argv #[test]s: audio_argv, ch_argv, gpu_argv, hardlink_farm, otel_host_bridge_argv, ssh_keygen, supervisor::state::reconcile_and_adopt, swtpm_argv, usbip_argv, video_argv, virtiofsd_argv, vsock_relay_argv)", "tests/tools/assert-pinned-tests.sh"]
exercised_today = "yes"

[[script]]
name = "tests/video-argv-shape.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo nextest run --workspace --exclude nixling-contract-tests (argv #[test]s: audio_argv, ch_argv, gpu_argv, hardlink_farm, otel_host_bridge_argv, ssh_keygen, supervisor::state::reconcile_and_adopt, swtpm_argv, usbip_argv, video_argv, virtiofsd_argv, vsock_relay_argv)", "tests/tools/assert-pinned-tests.sh"]
exercised_today = "yes"

[[script]]
name = "tests/virtiofsd-argv-shape.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo nextest run --workspace --exclude nixling-contract-tests (argv #[test]s: audio_argv, ch_argv, gpu_argv, hardlink_farm, otel_host_bridge_argv, ssh_keygen, supervisor::state::reconcile_and_adopt, swtpm_argv, usbip_argv, video_argv, virtiofsd_argv, vsock_relay_argv)", "tests/tools/assert-pinned-tests.sh"]
exercised_today = "yes"

[[script]]
name = "tests/w6-argv-shape.sh"
group = "A"
make_target = "test-rust"
tier = "ci-l1"
status = "retired"      # legacy | porting | ported | retired
successor_ids = ["cargo nextest run --workspace --exclude nixling-contract-tests (argv #[test]s: audio_argv, ch_argv, gpu_argv, hardlink_farm, otel_host_bridge_argv, ssh_keygen, supervisor::state::reconcile_and_adopt, swtpm_argv, usbip_argv, video_argv, virtiofsd_argv, vsock_relay_argv)", "tests/tools/assert-pinned-tests.sh"]
exercised_today = "yes"

