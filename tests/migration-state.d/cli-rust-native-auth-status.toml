status = "retired"
successor_ids = ["packages/d2b/tests/auth_status_contract.rs::auth_status_roles_match_schema_and_authz (spawns the real d2b binary and drives `auth status` across the launcher/none/admin roles via the env-file fixture contract (D2B_AUTH_STATUS_FIXTURE + D2B_TEST_{LAUNCHER,ADMIN}_UIDS + --test-uid); `auth status --json` deserialized strictly into d2b::AuthStatusOutputV2 (deny_unknown_fields == the docs/reference/cli-output/auth-status.schema.json check) + asserts per-role authz: launcher allows `up`/denies `audit`, none stays read-only [auth status,host check,list,status], admin gains `audit`/denies nothing; --human summarizes role + denied audit)", "tests/tools/rust-workspace-checks.sh (CLI-contract step: cargo test -p d2b --tests — gated in static.sh)", "tests/golden/pinned/cli-auth-status.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
