status = "retired"
successor_ids = ["packages/d2b/tests/audit_contract.rs::audit_reports_daemon_down_without_bash_fallback (missing socket + D2B_LEGACY_* poison-pill -> typed daemon-down envelope exit 1, never execs bash/exit 99)", "packages/d2b/tests/audit_contract.rs::audit_strict_returns_not_yet_implemented_envelope (audit --strict -> exit 78 not-yet-implemented envelope)", "packages/d2b/tests/audit_contract.rs::audit_relays_daemon_auditresponse_frames (in-process SOCK_SEQPACKET mock daemon hello/helloOk/audit/auditResponse -> lines relayed to stdout)", "packages/d2b/tests/audit_contract.rs::audit_admin_rejection_frame_has_no_fallback (in-process SOCK_SEQPACKET mock daemon maps authz-audit-requires-admin to exit 32 without daemon-down)", "packages/d2bd/src/lib.rs::audit_authorization_tests::launcher_is_denied_audit_before_broker_access (daemon role enforcement rejects launcher before broker access)", "tests/test-rust.sh (CLI-contract step runs cargo test -p d2b --tests with D2B_FIXTURES and daemon unit coverage)", "tests/golden/pinned/cli-audit.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
