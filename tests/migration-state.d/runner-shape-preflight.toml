status = "retired"
successor_ids = ["cargo test --workspace (nixling-host runner_shape::tests::*) — already gated in tests/tools/rust-workspace-checks.sh", "tests/golden/pinned/runner-shape-preflight.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
