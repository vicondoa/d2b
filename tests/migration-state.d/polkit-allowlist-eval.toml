status = "retired"
successor_ids = ["flake.checks.<system>.nix-unit-state (tests/unit/nix/cases/principal-uid-collision.nix — exact local-root and child-realm principal shapes, lifecycle/public-group separation, least-privilege controller and broker memberships, socket ownership/ACL rows, and deterministic UID/GID collision checks)"]
