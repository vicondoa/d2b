status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_guest.rs::retired_guest_control_nongoals_are_now_shipped_goals (the guest-control-health readiness and admin-only guest execution surfaces are shipped goals); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh", "packages/d2b-contract-tests/tests/policy_guest.rs::guest_session_credential_contract_present (the runtime-only authenticated ComponentSession credential replaces the long-lived token share and caller-selected token path); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh"]
