status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_guest.rs::retired_guest_control_nongoals_are_now_shipped_goals (the W15 guest-control-health readiness non-goal and the W16 `d2b exec` CLI non-goal are now shipped goals: ProcessRole::GuestControlHealth + the readiness predicate in d2b-core/src/processes.rs, and the admin-only `vm exec` / `vm konsole` verbs in d2b/src/lib.rs); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh", "packages/d2b-contract-tests/tests/policy_guest.rs::guest_control_auth_core_token_share_contract_present (the auth-core token-share / LoadCredential contract the retired nix-eval smoke exercised — d2b-gctl share + /run/d2b-guest-control-host mount + guest_control_token LoadCredential + auth.tokenFile absolute/outside-/nix/store validation — is present in the module source; the nix eval itself remains covered by tests/unit/nix/cases/guest-control-auth.nix); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh"]
