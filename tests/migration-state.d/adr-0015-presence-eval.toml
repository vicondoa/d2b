status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_lints.rs (gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh) — adr_0015_superseded_with_historical_invariants (ADR 0015 superseded header plus retained sections/cross-references) and adr_0045_accepted_with_realm_and_delivery_contracts (accepted ADR 0045 parent-spawned realm-process and concurrent-lane invariants)"]
