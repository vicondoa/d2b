status = "retired"
successor_ids = ["cargo test --workspace (d2b-host cgroup::tests::* delegation canaries) + broker test pass (d2b-priv-broker ops::cgroup::tests::* delegation/audit canaries) — already gated in tests/tools/rust-workspace-checks.sh", "tests/golden/pinned/cgroup-delegation-oracle.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
