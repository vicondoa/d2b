status = "retired"
successor_ids = ["packages/d2b/tests/cli_json_contract.rs::keys_list_daemon_down_returns_structured_envelope (`keys list --json` with a missing public socket: exit 1, empty stderr, and the exact typed daemon-down envelope)", "packages/d2b/tests/cli_json_contract.rs::audit_json_stays_json_on_tty_with_daemon_down_envelope (runs `audit --json` on a real PTY with a missing public socket and requires the typed JSON daemon-down envelope)", "packages/d2b/src/service_v2.rs::tests::typed_status_preserves_json_and_human_fields (typed daemon projection preserves pendingRestart/current/booted and service/readiness fields)", "tests/test-rust.sh (runs cargo test -p d2b --tests)", "tests/golden/pinned/cli-json.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
