status = "retired"
successor_ids = ["packages/d2b/tests/cli_json_contract.rs::list_reports_pending_restart_when_booted_differs_and_active (spawns the real d2b binary against D2B_FIXTURES; copies the fixture-smoke bundle into a hermetic temp bundle with the absolute /etc/d2b processesPath/hostPath rewritten to relative basenames; per-VM state-root with corp-vm current!=booted symlinks + daemon-state pidfd-table marking ch-runner running + system-state fixture pinning d2bd.service active; asserts `list --json` deserialized strictly into d2b::ListOutputV2 (deny_unknown_fields == the bash jq key-set check) reports corp-vm status==pending-restart)", "packages/d2b/tests/cli_json_contract.rs::status_reports_pending_restart_with_consistent_current_booted (same pending-restart env; `status corp-vm --json` deserialized strictly into d2b::StatusVmOutputV2 (deny_unknown_fields == the bash jq key-set check for status/services/runnerParity/livePoolIntegrity); asserts pendingRestart=true, current=/nix/store/d2b-current, booted=/nix/store/d2b-booted, services.d2b=active, services.microvm=running, runtime=unknown, runnerParity.runnerParityOk=true)", "packages/d2b/tests/cli_json_contract.rs::keys_list_daemon_down_returns_structured_envelope (`keys list --json` with D2B_PUBLIC_SOCKET pointed at a missing socket: exit 1, empty stderr, structured daemon-down envelope on stdout with the exact key set [code,docsAnchor,exitCode,kind,observedState,remediation,whatWasChecked], kind==\"d2b keys list requires d2bd\", code=daemon-down, exitCode=1, docsAnchor=docs/reference/error-codes.md#daemon-down)", "packages/d2b/tests/cli_json_contract.rs::audit_json_stays_json_on_tty_with_daemon_down_envelope (reproduces the bash `script`-PTY case via rustix::pty: spawns `audit --json` with stdin/stdout/stderr on a real PTY slave and a missing public socket; asserts the CLI stays JSON on a TTY — not the human-on-stderr form — exit 1, daemon-down envelope with kind==\"d2b audit requires d2bd\" and the exact key set)", "tests/tools/rust-workspace-checks.sh (CLI-contract step: builds fixture-smoke, runs `cargo test -p d2b --tests` with D2B_FIXTURES — gated in static.sh)", "tests/golden/pinned/cli-json.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
