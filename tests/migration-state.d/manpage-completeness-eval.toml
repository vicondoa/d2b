status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_docs.rs::manpage_documents_every_top_level_subcommand (every top-level clap subcommand declared in packages/d2b/src/lib.rs `enum NativeCommand` — including #[command(name = ...)] overrides and the PascalCase→kebab-case default conversion — is documented as a `d2b-<name>(1)` entry under the SUBCOMMANDS block of docs/manpages/d2b.1; fails closed on manpage drift without a cargo toolchain); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh"]
