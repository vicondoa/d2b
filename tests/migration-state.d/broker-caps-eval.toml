status = "retired"
successor_ids = ["flake.checks.<system>.nix-unit (tests/unit/nix/cases/broker-caps.nix — 4 cases: broker CapabilityBoundingSet exact-set match + AmbientCapabilities empty-string sentinel via mkEval introspection)"]
