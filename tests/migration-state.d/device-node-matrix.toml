status = "retired"
successor_ids = ["cargo nextest run --workspace (d2b-host devices #[test]s)", "tests/tools/rust-workspace-checks.sh (d2b-priv-broker ops::device #[test]s via focused broker package cargo test)", "tests/tools/assert-pinned-tests.sh (fail-closed pin across the workspace and focused broker passes)"]
