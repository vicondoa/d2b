status = "retired"
successor_ids = ["flake.checks.<system>.nix-unit (tests/unit/nix/cases/per-vm-state-ownership.nix — per-VM ownership matrix structure/presence/owner-group-mode/recursive-required/store-view layout via mkEval)", "packages/d2b-contract-tests/tests/policy_ownership_preflight.rs (gated via the D2B_FIXTURES contract step in tests/tools/rust-workspace-checks.sh — the source-lint store-sync/daemon-preflight regression guards: store.nix keeps chown d2bd:users + chmod 0755 META_DIR fix-ups and carries no chown root:kvm / chmod 2775; no nixos-modules file enforces store/store-meta 2775; ownership_preflight.rs declares no store* path at mode 0o2775)"]
