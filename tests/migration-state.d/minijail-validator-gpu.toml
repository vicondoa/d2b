status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/minijail_gpu.rs (gated via the D2B_FIXTURES / D2B_FIXTURES_FULL contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh -> static.sh) — gpu_rendered_device_bind_matrix_and_wayland_target (static closed-set: the rendered Gpu RoleProfile pins the exact 6-entry DEVICE_BINDS matrix /dev/kvm,/dev/dri/renderD128,/dev/nvidiactl,/dev/nvidia0,/dev/nvidia-uvm,/dev/udmabuf each under /dev, plus the in-sandbox wayland BIND_TARGET parent /run/d2b-gpu/<vm> as a writable path), gpu_rendered_jail_shape (empty host caps, w1-gpu seccompPolicyRef, d2b.slice/<vm>/gpu cgroup subtree, mount+ipc namespaces with no net/user NS, hideDeviceNodesByDefault, nixStoreReadOnly, umask 0o007, no broker-pre-NS userNamespace), gpu_broker_role_device_claim_source (role_device_classes(Gpu) arm in packages/d2b-core/src/bundle_resolver.rs includes kvm/dri/nvidia-ctl/nvidia-uvm/nvidia-render/udmabuf and excludes vfio), gpu_render_node_minijail_profile_source_shape (D5/P2.3 gpu-render-node mkProfile block in nixos-modules/minijail-profiles.nix: present, userNamespace referencing the gpu principal, seccompPolicyRef = \"w1-gpu-render-node\", empty deviceBinds, umask = 7, gated on vm.graphics.renderNodeOnly), gpu_render_node_broker_processes_wiring_source (D5/P2.3 wiring: live_handlers.rs maps w1-gpu-render-node, sys.rs declares RENDER_NODE_INHERITED_FD + pre_opened_device_fds on RunnerIsolationSpec, processes-json.nix defines gpuRenderNodeRunner + emits \"gpu-render-node\" role node + carries --gpu-device-node in argv). The bash gate's opt-in live arms (cc-compiled virtgpu_probe; minijail0 positive arm DRM_IOCTL_VIRTGPU_GET_CAPS must not SIGSYS; negative arm ptrace(PTRACE_TRACEME) must SIGSYS; D2B_LIVE=1 bare GET_CAPS hardware smoke on the host render node; /var/lib/d2b/validated/p1-gpu.json evidence write) are live host-execution layers and do not port to the contract layer."]
