status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_state.rs::activation_storage_repair_is_absent (the destructive realm cutover removes legacy group migration and all activation repair below fixed storage anchors; realm/workload paths are broker-created from opaque storage ids)"]
