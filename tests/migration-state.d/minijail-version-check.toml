status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_misc.rs::minijail_version_comparison_canary (deterministic minijail version-refusal canary: observed >= required => ok else too-old, over the pinned-current/newer-accepted/older-refused/ancient-refused cases against the Tier-0 pinned minimum v17); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh", "packages/d2b-contract-tests/tests/policy_misc.rs::minijail_version_parser_canary (parser-shape canary: extracts the integer revision from a minijail0-shaped version banner); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh"]
