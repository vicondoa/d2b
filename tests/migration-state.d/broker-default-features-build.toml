status = "retired"
successor_ids = ["tests/tools/rust-workspace-checks.sh (gated `cargo check --workspace --manifest-path d2b-priv-broker default features = real wire dispatch` — the broker default-features build is already a gate step)"]
