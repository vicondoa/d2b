status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/privileges_parity.rs::rendered_privileges_matches_rust_matrix (compares D2B_FIXTURES privileges.json vs d2b_core::privileges::PrivilegesJson::w1 — operation set + allowedGroups parity, public + broker)", "tests/tools/rust-workspace-checks.sh (W3 fixture-contract step: builds fixture-smoke, runs the contract crate with D2B_FIXTURES — gated in static.sh)"]
