status = "retired"
successor_ids = ["flake.checks.<system>.eval-multi-env (existing root flake check imports examples/multi-env/configuration.nix against the in-tree framework module for the demo variant; realized by `nix flake check`)", "flake.checks.<system>.eval-multi-env-daemon (root flake check imports examples/multi-env/configuration.nix plus the daemon-backed variant overlay; realized by `nix flake check`)", "flake.checks.<system>.nix-unit (tests/nix-unit/cases/multi-env-daemon-backed.nix — 16 host.json env-propagation (mtu/mssClamp/east-west/bridgePortFlags + personal negative control), manifest no-microvm@/nixling@ unit reference, and ADR-0015 daemon-only processes.json zero-unit-field cases via mkEval over both reconstructed example variants)"]
