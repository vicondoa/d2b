status = "retired"
successor_ids = ["packages/nixling/tests/cli_contract.rs::list_json_matches_smoke_inventory_and_schema (spawns the real nixling binary against NL_FIXTURES bundle + hermetic system-state/daemon-state fixture; `list --json` deserialized strictly into nixling::ListOutputV2 (deny_unknown_fields == the docs/reference/cli-output/list.schema.json check) + asserts the 2-VM smoke inventory: corp-vm stopped + runnerParityOk, sys-work-net net-vm running + runnerParityOk)", "tests/tools/rust-workspace-checks.sh (CLI-contract step: builds fixture-smoke, runs `cargo test -p nixling --tests` with NL_FIXTURES — gated in static.sh)", "tests/golden/pinned/cli-contract.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
