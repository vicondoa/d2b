status = "retired"
successor_ids = ["packages/d2b/tests/cli_contract.rs::list_json_missing_daemon_emits_typed_daemon_down (spawns the real d2b binary with a missing public socket and requires one typed daemon-down JSON envelope, exit 1, no stderr, and no static inventory fallback)", "tests/test-rust.sh (CLI-contract step runs cargo test -p d2b --tests)", "tests/golden/pinned/cli-contract.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
