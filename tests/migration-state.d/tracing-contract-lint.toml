status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_contracts.rs::tracing_contract_lint (bounded-cardinality tracing-attribute allowlist enforcement over packages/**/*.rs excluding target/vendor/generated, including tests: fails closed on forbidden high-cardinality/leakable attr classes — bundle/keys_dir .display() path attrs, bundle_path alias, argv/cmdline/command_line, secret/password/token/private_key, stdout/stderr — plus a faithful awk-state-machine port flagging store-path string literals inside tracing-macro arg lists; see docs/reference/tracing-contract.md); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh"]
