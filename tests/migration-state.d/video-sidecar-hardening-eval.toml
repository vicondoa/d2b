status = "retired"
successor_ids = ["packages/nixling-contract-tests/tests/minijail_swtpm_video.rs (gated via the NL_FIXTURES / NL_FIXTURES_FULL contract step `cargo test -p nixling-contract-tests` in tests/tools/rust-workspace-checks.sh -> static.sh) — video_rendered_profile_shape + video_source_nvidia_gating_namespace_principal + video_host_activation_runtime_and_session_acls assert the surviving runner-side video hardening (empty caps, seccompPolicyRef w1-video, masked /dev device allowlist, private PID namespace, dedicated nixling-<vm>-video principal, cgroup leaf, runtime-dir ACL exclusion) that the broker applies at SpawnRunner{role: Video} fork time", "tests/broker-caps-eval.sh (broker canonical cap-set + seccomp profile for the video runner)"]
note = "tests/video-sidecar-hardening-eval.sh was a no-op stub (exit 0, zero assertions) after the daemon-only cutover removed the per-VM nixling-<vm>-video systemd unit it once hardened. The surviving runner-side hardening moved to the broker SpawnRunner{role: Video} cap-set + seccomp profile, already covered by the migrated minijail video contract tests + broker-caps-eval. No coverage is lost by retiring the stub."
