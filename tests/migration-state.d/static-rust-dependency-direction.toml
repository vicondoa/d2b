status = "retired"
successor_ids = ["packages/nixling-contract-tests/tests/policy_modules.rs::static_rust_dependency_direction (static Cargo.toml parse of the workspace dep graph WANT map + asserts nixling/nixlingd import nixling_contracts from their src trees); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/tools/rust-workspace-checks.sh"]
