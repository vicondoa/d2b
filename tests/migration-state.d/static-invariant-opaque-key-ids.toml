status = "retired"
successor_ids = ["nixling_core::static_invariants validators (production logic): path_bearing_key_violations", "packages/nixling-contract-tests/tests/static_invariants.rs (synthetic positive/negative logic cases + REAL rendered fixture-smoke contract cases; gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/tools/rust-workspace-checks.sh -> static.sh) — opaque_key_ids_* (synthetic) + rendered_manifest_has_no_path_bearing_key_or_secret_fields (real vms.json key/secret path scan)"]
