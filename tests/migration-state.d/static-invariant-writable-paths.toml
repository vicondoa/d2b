status = "retired"
successor_ids = ["d2b_core::static_invariants validators (production logic): undeclared_writable_paths", "packages/d2b-contract-tests/tests/static_invariants.rs (synthetic positive/negative logic cases + REAL rendered fixture-smoke contract cases; gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh -> static.sh) — writable_paths_* (synthetic logic). Spec correction: the bash schema-vocabulary grep is subsumed by the typed MountPolicy.writable_paths DTO + the bundle-drift gate (schema generated from the DTO); no separate bundle-level declared-writable-paths list exists in the real DTO to contract-test"]
