status = "retired"
successor_ids = ["packages/nixling-contract-tests/tests/policy_misc.rs::vm_submodule_compose_vm_shape (nixos-modules/vm-submodule.nix exists and carries the composeVm ownership shape via the unanchored `composeVm\\s*=` grep, which matches the current `_composeVm =` binding; toplevel-hash parity covered separately); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/tools/rust-workspace-checks.sh"]
