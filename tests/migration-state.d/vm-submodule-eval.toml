status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_misc.rs::workload_composition_shape (legacy vm-submodule.nix and vm-options.nix stay deleted and vm-evaluator.nix owns `_composeWorkload`); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh"]
