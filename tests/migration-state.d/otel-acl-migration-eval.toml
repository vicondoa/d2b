status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_guest.rs::otel_relay_acl_module_not_publicly_imported_and_broker_handler_present (asserts nixos-modules/default.nix carries no uncommented ./host-otel-relay-acl.nix import and that packages/d2b-priv-broker/src/runtime.rs still carries the RunnerRole::OtelHostBridge spawn handler); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh"]
