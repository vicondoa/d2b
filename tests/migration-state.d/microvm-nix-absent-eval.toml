status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_release.rs::microvm_nix_input_absent_from_flake (flake.nix declares no microvm input / inputs.microvm reference per ADR 0018); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh"]
