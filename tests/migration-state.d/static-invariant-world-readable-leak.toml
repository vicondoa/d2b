status = "retired"
successor_ids = ["d2b_core::static_invariants validators (production logic): world_readable_field_leaks", "packages/d2b-contract-tests/tests/static_invariants.rs (synthetic positive/negative logic cases + REAL rendered fixture-smoke contract cases; gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh -> static.sh) — world_readable_* (synthetic) + rendered_manifest_exposes_only_public_safe_fields (real vms.json field allowlist)"]
