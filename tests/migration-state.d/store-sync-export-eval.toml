status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_state.rs::store_sync_export (StoreSync-only observability export static invariants in nixos-modules/components/observability/host.nix: storeSyncExportDir/Glob shape, the filelog/store_sync_audit receiver + json_parser + logs/store_sync_audit pipeline forwarded over otlp, the host-singleton resource attrs plus source=store-sync-audit with target_vm/target_env never promoted, the d2b-host-otel-collector export-dir-only ACL grants, and the deny scans for broker audit-log/glob, priv.sock, and any collector ACL on an audit path; over a comment-stripped view); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh"]
