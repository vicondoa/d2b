status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_state.rs::store_sync_export (the host collector consumes only the realm observability row's broker-owned bounded StoreSync projection, forwards it through the canonical OTLP endpoint, grants ACLs only on that projection, and never reads raw broker audit or legacy host-global export paths)"]
