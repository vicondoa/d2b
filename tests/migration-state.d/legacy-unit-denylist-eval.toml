status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_units.rs::legacy_unit_denylist (ADR 0015 fail-closed denylist scan over every *.nix/*.md under nixos-modules/: no retired per-VM systemd template or host-singleton framework unit name reappears as live wiring, honoring the pure-comment / inline+preceding `# obituary:`/`# retired:` marker / per-file allowlist / `*Service = \"d2b-...\"` content carve-outs); legacy_unit_denylist_classify_semantics (negative coverage of the classify() skip/live escape hatches); legacy_unit_denylist_patterns_match_representative_refs (denylist needle integrity); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh"]
