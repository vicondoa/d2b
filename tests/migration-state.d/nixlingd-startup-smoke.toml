status = "retired"
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/nixlingd-startup-smoke.nix — 17 cases: daemon/broker tmpfiles + systemd socket/service surface + evidence-record shape)", "packages/nixlingd/tests/daemon_socket_acl.rs::daemon_socket_acl::configured_launcher_is_accepted (spawns CARGO_BIN_EXE_nixlingd over AF_UNIX, waits for public.sock, and asserts Hello -> helloOk)", "packages/nixlingd/tests/daemon_version_file.rs::daemon_version_file::startup_writes_version_file_next_to_public_socket (spawns CARGO_BIN_EXE_nixlingd over AF_UNIX and asserts the startup version file fields)", "packages/nixlingd/tests/bundle_tampered_envelope.rs::daemon_vm_start_returns_bundle_tampered_envelope (spawns CARGO_BIN_EXE_nixlingd over AF_UNIX and asserts vmStart returns kind=bundle-tampered for a tampered bundle.json)", "tests/golden/pinned/nixlingd-startup-smoke.txt + tests/golden/pinned/daemon-socket-acl.txt (fail-closed native-test presence pins)", "packages/nixling-priv-broker/tests/socket_activation.rs::broker_adopts_socket_activated_fd_and_serves_hello (real LISTEN_FDS fd-3 socket-activation handoff + Hello round-trip; covers the live .socket -> .service activation path natively, replacing the dropped systemd-boot container — see tests/containers/README.md)"]
