status = "retired"
successor_ids = ["packages/d2b-core/src/bundle_resolver.rs validate_minijail_profiles unit tests (rejects uid0/gid0 without a real carve-out, rejects empty/whitespace carve-out, accepts uid0 WITH carve-out)", "packages/d2b-contract-tests/tests/minijail_profiles.rs::rendered_bundle_passes_all_minijail_profile_invariants (the real rendered bundle passes the uid0/root-carve-out invariant — gated via D2B_FIXTURES in rust-workspace-checks.sh)", "validate_minijail_profiles doc + RoleProfile.adr_carve_out structural coupling + bundle-drift (schema-shape: root-capable shapes carry the ADR field); requiresStartRoot coupling superseded by ADR 0021"]
