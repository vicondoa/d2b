status = "retired"
successor_ids = ["flake.checks.<system>.nix-unit (tests/nix-unit/cases/guest-control-auth.nix — 5 auth-token-delivery cases: default parameterization asserts the exact nl-gctl token share + LoadCredential JSON; /nix/store, /nix/store/<child>, and relative auth.tokenFile each reject at eval time, as does a tokenFile set without guest.control.enable; imports tests/guest-control-auth-eval.nix via a mkEval flake shim)"]
