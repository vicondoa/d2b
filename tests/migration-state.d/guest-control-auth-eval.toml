status = "retired"
successor_ids = ["flake.checks.<system>.nix-unit (tests/unit/nix/cases/guest-control-auth.nix — authenticated runtime credential cases cover exact public-key/generation/channel bindings, optional operation PSK binding, private owner/modes, read-only delivery, fail-closed rotation/adoption, child-realm confinement, and absence from bundle artifacts)"]
