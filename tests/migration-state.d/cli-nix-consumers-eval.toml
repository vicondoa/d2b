status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_cli_consumers.rs::cli_nix_live_consumer_bindings_absent (repo *.nix/*.sh/*.rs live pre-comment grep over git-listed files, excluding nixos-modules/cli.nix, the retired gate, this policy test, and tests/static.sh only for d2b.store.{package,generations}; guards d2b.cliBin, audioStateHelperPath, _desktopWrappers, d2b.store.package, d2b.store.generations); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh", "packages/d2b-contract-tests/tests/policy_cli_consumers.rs::cli_nix_imports_absent (repo *.nix import/imports payload grep rejects live ./cli.nix imports outside nixos-modules/cli.nix / retired gate / this policy test while allowing comment-only historical references); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh"]
