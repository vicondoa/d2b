status = "retired"
successor_ids = ["packages/nixling/tests/usb_contract.rs::usb_help_matches_golden (spawns the real nixling binary; `usb --help` stdout matches tests/golden/cli-output/usb-help.txt byte-for-byte)", "packages/nixling/tests/usb_contract.rs::usb_attach_dry_run_matches_golden (`usb attach corp-vm 1-2 --dry-run` against the NL_FIXTURES manifest/bundle matches tests/golden/cli-output/usb-attach-dry-run.txt)", "packages/nixling/tests/usb_contract.rs::usb_detach_dry_run_matches_golden (`usb detach corp-vm 1-2 --dry-run` against the NL_FIXTURES manifest/bundle matches tests/golden/cli-output/usb-detach-dry-run.txt)", "tests/tools/rust-workspace-checks.sh (CLI-contract step: cargo test -p nixling --tests with NL_FIXTURES — gated in static.sh)", "tests/golden/pinned/cli-usb.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
