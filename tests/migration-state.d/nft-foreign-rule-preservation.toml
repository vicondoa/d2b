status = "retired"
successor_ids = ["cargo test --workspace (d2b-host nftables::tests::{fake_backend_preserves_foreign_rules,drift_detection_strips_volatile_fields,drift_detection_catches_real_change}) — already gated in tests/tools/rust-workspace-checks.sh", "tests/golden/pinned/nft-foreign-rule-preservation.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
