status = "retired"
successor_ids = ["packages/nixling-contract-tests/tests/video_binary_contract.rs::patched_video_binaries_expose_required_command_surface (gated via the NL_FIXTURES_FULL contract step `cargo test -p nixling-contract-tests` in tests/tools/rust-workspace-checks.sh -> static.sh) — loads the rendered feature-rich processes.json graphics/video VM, requires exactly one video node and cloud-hypervisor node, asserts both binaryPath values resolve to executable files, then executes the rendered crosvm binary with `device video-decoder --help` and the rendered Cloud Hypervisor binary with `--help` to require the patched `--backend` and `--vhost-user-media` command surfaces.", "tests/golden/pinned/video-binary-contract.txt"]
