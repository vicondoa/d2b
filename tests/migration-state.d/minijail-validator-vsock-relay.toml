status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/minijail_relay_otel.rs (gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh -> static.sh) — vsock_relay_profile_source_shape (Layer-1 source-grep over nixos-modules/minijail-profiles.nix: profile block exists for `profileIdFor name \"vsock-relay\"`, explicit `capabilities = [ ]` empty-cap branch / no `\"CAP_*\"` token via the bash if/elif short-circuit, seccompPolicyRef = \"w1-vsock-relay\"), vsock_relay_rendered_profile_shape (rendered RoleProfile over the FULL fixture `vm-corp-full-vsock-relay`: role = VsockRelay, caps == [], seccompPolicyRef = \"w1-vsock-relay\", per-VM + obs-VM RW bind set under /var/lib/d2b/vms with NO /dev binds and empty deviceBinds, cgroup subtree d2b.slice/<vm>/vsock-relay, mount+ipc namespaces with no net/user — strictly stronger than the bash gate's jq/grep over a live host profile), vsock_relay_rendered_argv_socat_shape (rendered socat runner argv: socat binaryPath, d2b-otel-relay@<vm> title, UNIX-LISTEN + EXEC ch-vsock-connect pipe). The bash gate's D2B_LIVE=1 Layer-2 (minijail0 + socat pre-opened-fd positive path, socket(AF_VSOCK) SIGSYS/EPERM negative probe, PTRACE_TRACEME probe, /var/lib/d2b/validated/p1-vsock-relay.json evidence write) is a live host-execution layer and does not port to the contract layer."]
