status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/minijail_gpu.rs (gated via the D2B_FIXTURES / D2B_FIXTURES_FULL contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh -> static.sh) — wayland_proxy_profile_source_shape (Layer-1 assert_profile_source over nixos-modules/minijail-profiles.nix: role = \"wayland-proxy\" declaration, mandatory seccompPolicyRef = \"w1-wayland-proxy\", empty capabilities/no CAP_ in the profile block, requiresStartRoot never true, no userNamespace, writable path /run/d2b-wlproxy present, no PipeWire/Pulse bind in the block, umask = 7, deviceBinds = [ ] empty), wayland_proxy_policy_ref_device_classes_source (Layer-1 assert_policy_ref_entry: \"w1-wayland-proxy\" in packages/d2b-priv-broker/src/live_handlers.rs policy_ref_device_classes), wayland_proxy_rust_variants_declared_source (Layer-1 assert_rust_variant_exists: ProcessRole::WaylandProxy in packages/d2b-core/src/processes.rs and RunnerRole::WaylandProxy in packages/d2b-contracts/src/broker_wire.rs), wayland_proxy_rendered_profile_shape (Layer-1 assert_installed_profiles_consistent applied to the REAL rendered RoleProfile — strictly stronger than the bash gate which scanned /etc/d2b/minijail-profiles/*wayland-proxy*.json and skipped when absent: empty caps, w1-wayland-proxy seccomp, empty deviceBinds + bindMounts, dedicated runtime dir /run/d2b-wlproxy/<vm> as the sole writable path, no PipeWire/Pulse path access, umask 0o007, no broker-pre-NS userNamespace). The bash gate's D2B_LIVE=1 Layer-2 (minijail0 /bin/true positive probe; /var/lib/d2b/validated/p1-wayland-proxy.json evidence write) is a live host-execution layer and does not port to the contract layer."]
