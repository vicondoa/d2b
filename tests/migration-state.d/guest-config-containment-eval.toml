status = "retired"
successor_ids = ["flake.checks.<system>.nix-unit (tests/unit/nix/cases/guest-config-containment.nix — 13 guestConfigFile containment cases: contained clean/reads-standard fixtures produce no failing assertion; sets-microvm / sets-d2b / imports / builtins.toFile / _file-spoof bypass fixtures fire the containment assertion naming the host-owned option, via mkEval config.assertions introspection)"]
