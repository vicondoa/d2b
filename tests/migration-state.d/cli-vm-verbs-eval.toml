status = "retired"
successor_ids = [
  "packages/d2b/tests/vm_verbs_contract.rs::mutating_verbs_emit_daemon_down_without_bash_fallback (up/down/restart + vm start/stop/restart against a missing public socket all surface the typed daemon-down envelope and never execute the retired bash poison-pill)",
  "packages/d2b/tests/vm_verbs_contract.rs::legacy_bash_opt_in_is_a_no_op (D2B_LEGACY_BASH_OPT_IN remains a no-op and never reaches the poison-pill path)",
  "packages/d2b/tests/vm_verbs_contract.rs::vm_list_requires_authenticated_daemon_session (vm list fails closed when no authenticated ComponentSession is available)",
  "packages/d2b/tests/vm_verbs_contract.rs::top_level_list_emits_typed_daemon_down_without_static_fallback (top-level list with a missing daemon emits the typed daemon-down envelope and no static inventory)",
  "packages/d2b/tests/vm_verbs_contract.rs::vm_konsole_dry_run_emits_guest_control_shape (konsole dry-run emits the guest-control terminal shape without retired SSH fields)",
  "packages/d2b/tests/vm_verbs_contract.rs::vm_konsole_terminal_override_reflected (terminal override is reflected in the generated argv)",
  "packages/d2b/tests/vm_verbs_contract.rs::vm_konsole_rejects_retired_ssh_flags (retired host/user/key flags fail closed)",
  "packages/d2b/tests/vm_verbs_contract.rs::vm_konsole_rejects_unknown_vm (unknown VM names fail closed)",
  "packages/d2b/tests/vm_verbs_contract.rs::vm_exec_missing_command_emits_cli_usage_envelope (missing exec command emits the typed usage envelope)",
  "packages/d2b/tests/vm_verbs_contract.rs::vm_exec_no_daemon_emits_transport_unavailable_envelope (exec without a daemon emits transport unavailable and never SSH/bash)",
  "packages/d2b/tests/vm_verbs_contract.rs::vm_exec_rejects_interactive_without_tty (interactive exec without tty emits usage)",
  "tests/test-rust.sh (runs cargo test -p d2b --tests)",
  "tests/golden/pinned/cli-vm-verbs.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)",
]
