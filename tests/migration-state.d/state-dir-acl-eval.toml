status = "retired"
successor_ids = ["packages/nixling-contract-tests/tests/policy_state.rs::state_dir_acl (/var/lib/nixling state-dir declaration + ACL invariants in nixos-modules: 0750 root nixlingd tmpfiles decl (not 0755), nixlingStateDirAcl activation script, per-user/per-group --x traversal grants incl. g:kvm:--x and the g:nixling:--x grant (direct or via the sourced state-dir-acl.sh helper), and the no-`setfacl -d -m`-default-ACL-on-state-dir-root negative with the comment-line filter); gated via the NL_FIXTURES contract step `cargo test -p nixling-contract-tests` in tests/tools/rust-workspace-checks.sh"]
