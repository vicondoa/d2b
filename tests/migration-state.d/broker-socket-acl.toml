status = "retired"
successor_ids = ["packages/d2b-priv-broker/tests/broker_socket_acl.rs::broker_endpoint_requires_host_local_directional_identity (pins the host-local Unix seqpacket endpoint to directional identity and the local-root controller initiator; default workspace pass in tests/tools/rust-workspace-checks.sh)", "tests/golden/pinned/broker-socket-acl.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
