status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_source.rs::host_prep_dag_module_surface (packages/d2b-host/src/host_prep_dag.rs: all 10 HostPrepStepKind enum variants declared, builder ordering-edge presence, the 9-symbol public API surface, and the depends_on dep-edges ApplySysctl<-BringUpTapInterface / SetBridgePortFlags<-ApplySysctl / PreOpenVhostNetFd<-SetBridgePortFlags via the ported `grep -B 3` window), host_prep_dag_d2b_host_reexport (d2b-host/src/lib.rs `pub mod host_prep_dag;`), host_prep_dag_broker_wire_scaffolds (broker_wire.rs BrokerRequest variant/struct/op_name arms + runtime.rs dispatch arms for SeedDnsmasqLease/BindMountFromHardlinkFarm/OwnershipMatrixCheck/SshHostKeyPreflight, the two live OperationFields audit rows, and the two still-deferred typed Unimplemented labels), host_prep_dag_d2bd_wiring (d2bd/src/lib.rs build_/log_/execute_host_prep_dag + D2B_HOST_PREP_DAG_EXECUTE markers), host_prep_dag_documentation (docs/reference/host-prep-dag.md: all 10 step kinds, ordering phrases incl. `AFTER \\`ApplySysctl\\``, and the Canonical step set section completeness for all 10 slugs; the bash gate's `grep -qF host-prep-dag` self-reference check is asserted via the doc's canonical slug path rather than its body to avoid the retired-.sh self-referential doc trap); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh"]
