status = "retired"
successor_ids = ["broker test pass (nixling-priv-broker ops::hosts::tests::{refuses_symlink_target,writes_managed_block_into_fresh_file,idempotent_when_block_matches}, ops::nm::tests::reload_failure_rolls_back, ops::state_dir::tests::{refuses_absolute_relative_path,refuses_parent_dir_escape,idempotent_reuses_existing_dirs,creates_base_and_relative_paths}) — already gated in tests/tools/rust-workspace-checks.sh", "tests/golden/pinned/path-safety-violation-fs.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin)"]
