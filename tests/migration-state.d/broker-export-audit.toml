status = "retired"
successor_ids = ["focused broker package cargo test --features layer1-bootstrap (d2b-priv-broker broker_export_audit::export_audit_requires_admin_and_exports_op_audit_records) — already gated in tests/tools/rust-workspace-checks.sh", "tests/golden/pinned/broker-export-audit.txt + tests/tools/assert-pinned-tests.sh (fail-closed presence pin across the focused broker layer1-bootstrap list)"]
