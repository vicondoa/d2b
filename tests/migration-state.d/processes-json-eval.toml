status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_daemon.rs::processes_json_consumers_route_through_helpers (nixos-modules/{processes-json,closures-json,minijail-profiles,store}.nix do NOT read config.microvm.vms.<name>.config.config.* directly, and nixos-modules/lib.nix defines the d2bLib.vmRunner/vmToplevel/vmDeclaredRunner helpers); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh"]
