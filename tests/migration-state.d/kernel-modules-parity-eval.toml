status = "retired"
successor_ids = ["packages/d2b-contract-tests/tests/policy_daemon.rs::kernel_modules_parity_evaluator_shape (nixos-modules/vm-evaluator.nix calls the standard NixOS eval-config.nix entrypoint that computes requiredKernelModules, and nixos-modules/lib.nix routes d2bLib.vmRunner through config.d2b._computed so per-VM microvm.* incl. microvm.kernel resolves); gated via the D2B_FIXTURES contract step `cargo test -p d2b-contract-tests` in tests/tools/rust-workspace-checks.sh"]
