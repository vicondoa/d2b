status = "retired"
successor_ids = ["cargo test --workspace (tests/tools/rust-workspace-checks.sh): broker default + layer1-bootstrap + the NEW fake-backends pass run every pidfd test the wrapper ran — broker ops::pidfd::tests::* (default), the #![cfg(feature = \"fake-backends\")] integration tests scm_rights_transports_pidfd_with_cloexec_preserved + reconciliation_refuses_start_time_drift (fake-backends pass), and daemon supervisor::pidfd_table::tests::* (main workspace)", "tests/golden/pinned/pidfd-handoff.txt (fail-closed assert-pinned-tests.sh presence pin for all 12 named pidfd tests; the broker listing now uses --features layer1-bootstrap,fake-backends so the integration tests are enumerable)"]
