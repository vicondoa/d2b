# Installer artifacts the W15 RunHostInstall broker op must
# materialize on the Ubuntu 24.04 host. Lines starting with `#` are
# comments; everything else is matched as an exact path the smoke
# harness asserts is present + non-empty after `d2b host install
# --apply` completes.
/etc/systemd/system/d2bd.service
/etc/d2b/daemon-config.json
/run/current-system/sw/share/d2b/vms.json
/var/lib/d2b/runtime/host-runtime.json
