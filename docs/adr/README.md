# Architecture decision records

Architecture decision records (ADRs) capture load-bearing design
choices for d2b. They complement the Diataxis documentation tree;
for the broader design narrative, see
[docs/explanation/design.md](../explanation/design.md).

| ADR | Status | Date | Summary |
| --- | --- | --- | --- |
| [0000. Repository layout and Rust bootstrap](0000-repository-layout-and-rust-bootstrap.md) | Accepted | 2026-05-25 | Establishes `packages/` as the Rust workspace home while preserving the v0.4.0 bash CLI as the only user-facing entry point. |
| [0001. Systemd-free VM orchestration](0001-systemd-free-vm-orchestration.md) | Accepted | 2026-05-25 | Daemon-owned VMs produce no per-VM systemd units, with a `/run/d2b/locks/<vm>` single-writer lock and `[pending restart]` drift surface. |
| [0002. Non-root d2bd plus minimal privileged broker](0002-non-root-daemon-and-privileged-broker.md) | Accepted | 2026-05-25 | `d2bd` runs as a non-root user; a small AF_UNIX broker handles only non-delegable kernel/filesystem mutations and writes an append-only root-owned audit log. |
| [0003. Minijail provisioning and sandbox interface](0003-minijail-provisioning-and-sandbox-interface.md) | Accepted | 2026-05-25 | Nix-pinned minijail behind a typed Rust `d2b-sandbox` API; no `ioctl: 1` catch-alls; `requiresStartRoot` only for audited roles. |
| [0004. Cloud Hypervisor runner shape](0004-cloud-hypervisor-runner-shape.md) | Accepted | 2026-05-25 | Generate d2b-owned CH argv from evaluated microvm/d2b config; keep declaredRunner as the parity oracle; reserve future W2 crate names. |
| [0005. Network, firewall, and TAP model](0005-network-firewall-and-tap-model.md) | Accepted | 2026-05-25 | Validated `IfName` newtypes, broker-opened TAP/vhost fds via `SCM_RIGHTS`, named `inet d2b` nftables table with explicit firewall coexistence policy, br_netfilter sysctl pinning, daemon-owned USBIP. |
| [0006. Manifest bundle versioning](0006-manifest-bundle-versioning.md) | Accepted | 2026-05-25 | `bundleVersion` + per-artifact `schemaVersion`; preserve `vms.json` `manifestVersion = 2`; `deny_unknown_fields` on security-sensitive artifacts; private 0640 bundle paths. |
| [0007. Bash coexistence and migration](0007-bash-coexistence-and-migration.md) | Accepted | 2026-05-25 | Three modes (`legacy-systemd`, `daemon-experimental`, `daemon-default`); per-VM `supervisor` field + per-VM filesystem lock; W9 `d2b migrate`. |
| [0008. Supported platforms and rejected targets](0008-supported-platforms-and-rejected-targets.md) | Accepted | 2026-05-25 | Tier 0 NixOS x86_64; Tier 1 alpha Ubuntu 24.04 LTS x86_64. v1.0 kernel floor ≥ 6.6; **v1.1 kernel floor uplifted to ≥ 6.9** (pidfs required for the BootedNotify identity check per [ADR 0018 § set-booted race-free serialization](0018-microvm-nix-removal.md#set-booted-race-free-serialization)). Explicit rejected list; deferred `/dev/vhost-vsock`; telemetry posture: none. |
| [0009. Rust toolchain, MSRV, and supply-chain policy](0009-rust-toolchain-msrv-and-supply-chain.md) | Accepted | 2026-05-25 | Pins the W0a Rust toolchain/MSRV and gates the workspace with reproducible formatting, lint, test, deny, and audit checks. |
| [0010. Wire protocol and typed errors](0010-wire-protocol-and-typed-errors.md) | Accepted | 2026-05-26 | Freezes the W2 Unix-socket wire protocol, negotiation rules, typed-error envelope, and audited broker read path. |
| [0011. cgroup v2 delegation and pidfd handoff](0011-cgroup-v2-delegation-and-pidfd-handoff.md) | Accepted | 2026-05-27 | Single `d2b.slice`, 8-step v2 delegation (Phase A privileged setup steps 1-6, Phase B non-root runtime mutation steps 7-8 per the v1.1 update), leaf-only **broker-mediated** `CgroupKill` (v1.1-P10 op - daemon never writes `cgroup.kill` directly), non-root Phase B delegation invariant, and pidfd handoff over SCM_RIGHTS. **v1.1 update**: the v1.0 `PR_SET_CHILD_SUBREAPER` clause is explicitly SUPERSEDED for the SpawnRunner-child population (NEITHER broker NOR daemon claims subreaper for SpawnRunner children per ADR 0018 § "set-booted race-free serialization"). |
| [0012. W3 IPv6-off sysctl set, hash-derived IfName, bridge-port defaults](0012-w3-ipv6-off-sysctl-set-and-hash-ifname.md) | Accepted | 2026-04-29 | The exact 5-step IPv6-off ordering, the FNV-1a + Crockford base32 IfName derivation + emitter-time collision detection, and the per-role bridge-port flag defaults (with double opt-in for east-west). |
| [0013. W3 firewall coexistence policy matrix + `inet d2b` chain layout](0013-w3-firewall-coexistence-policy.md) | Accepted | 2026-06-09 | Defines the 4-chain `inet d2b` layout (no raw/mangle/nat), the 7-row detector→policy matrix, USBIP carve-out ordering, and the no-libnftnl fallback for the W3 reconcile path. |
| [0014. W3 `kernel.modules_disabled=1` behavior, module probe order, CH net handoff selection, and runner-shape preflight](0014-w3-modules-devices-runner-shape.md) | Accepted | 2026-06-08 | Documents the four-step kernel-module probe order, the fail-closed posture under `modules_disabled=1`, the `tap-fd` / `persistent-tap` selection rule, and the runner-shape preflight contract owned by W3 scope s4. |
| [0015. Daemon-only clean break (v1.0)](0015-daemon-only-clean-break.md) | Accepted | 2026-05-31 | P6 collapses every persistent root surface to `d2bd` + `d2b-priv-broker`; deletes per-VM systemd templates, host singletons (`d2b-{ch-exporter,otel-host-bridge,…}`), the `cli.nix` bash package, the W14c bash fallback bridge, and the `d2b-launcher` polkit rules. The `supervisor` option's hard removal + eval-time rejection assertion is SCHEDULED for v1.1-P2 (v1.0 retains the option for backward-compat with consumer flakes pinning pre-v1.0 manifests); see [ADR 0017](0017-no-bash-fallbacks-invariant.md) + [ADR 0018](0018-microvm-nix-removal.md) for the v1.1 follow-through. No v0 → v1 compat window otherwise; supersedes the migration-mode plumbing in [ADR 0007](0007-bash-coexistence-and-migration.md). |
| [0017. No bash fallbacks invariant (v1.1)](0017-no-bash-fallbacks-invariant.md) | Accepted | 2026-05-31 | v1.1-P1 deletes `exec_legacy_passthrough` + `should_fallback_to_legacy` and converts the 6 residual call sites (audit `--strict`/unreachable, keys list/show unavailable, the two passthrough helpers) to typed envelopes per [ADR 0010](0010-wire-protocol-and-typed-errors.md). CI grep gate `tests/no-bash-exec-eval.sh` enforces the invariant. |
| [0018. Removal of the microvm.nix flake dependency (v1.1)](0018-microvm-nix-removal.md) | Accepted | 2026-05-31 | v1.1-P8 → P11 re-homes every `config.microvm.vms.<vm>.config.config.microvm.*` read to `d2b.vms.<vm>.runner.*`, introduces a d2b-owned per-VM NixOS submodule evaluator (`nixos-modules/vm-submodule.nix`), retires the `microvm@*` / `microvm-virtiofsd@*` / `d2b-<vm>-store-sync.service` template surfaces in favour of broker `SpawnRunner` variants, drops the `microvm` flake input, and updates the tagline to drop "on microvm.nix". |
| [0021. Broker-pre-established user namespace for virtiofsd](0021-broker-user-namespace-for-virtiofsd.md) | Accepted | 2026-06-02 | v1.1.1 retires the [ADR 0003](0003-minijail-provisioning-and-sandbox-interface.md) virtiofsd `requiresStartRoot` carve-out. The broker pre-creates a single-entry user namespace via `clone3(CLONE_NEWUSER)` and writes `0 host_uid_for_zero 1` to `uid_map` so virtiofsd runs fake-root inside the namespace while exposing **zero** host capabilities. Extends [ADR 0001](0001-systemd-free-vm-orchestration.md)'s broker-owned privilege quarantine premise to virtiofsd specifically. |
| [0023. Runner-role lifecycle matrix](0023-runner-role-lifecycle-matrix.md) | Accepted (v1.2) | 2026-06-09 | Every new runner role must fill out an 11-field lifecycle matrix (fork model, wait/reap owner, in-NS mount-action, capability bounding set, ambient capability set, seccomp profile, FD lifetime, umask, RLIMIT_NPROC, oom_score_adj, CLONE_INTO_CGROUP) before being added to `nixos-modules/minijail-profiles.nix`. Backfills matrices for the 5 existing roles (cloud-hypervisor, virtiofsd, swtpm, gpu, audio). Motivated by the vx-kernel retrospective finding that fu27 + fu34 should have been caught at v1.1.2 review. |
| [0022. Stabilization-mode releases](0022-stabilization-mode-releases.md) | Accepted | 2026-06-10 | Defines the stabilization-release flavour of SemVer MINOR: no new public surface, no new deferrals (I3), every tracked deferral closes. Documents the grep-based I3 enforcement in `tests/static.sh` + `make pre-tag`, the required pre-tag live-smoke gate (D1), the R1→R2→R3 panel-signoff cadence, and the annotated-only (no GPG) tag-signing decision for v1.2. |
| [0024. In-VM guest config editing, sync, and containment](0024-in-vm-guest-config-sync.md) | Accepted | 2026-06-07 | Adds `d2b.vms.<vm>.guestConfigFile` (a contained guest-editable OS layer) + the `d2b config` sync/diff/approve workflow. Containment is a fail-closed host-eval namespace policy lint (no `microvm.*`/`d2b.*` from the guest file, detected by definition-existence over the real NixOS module set so imports/`builtins.toFile`/`_file`-spoofing are caught - a best-effort lint, not a sound sandbox: an approved file's eval-time host reads and `mkIf`-on-divergent-context cases are bounded by operator review/approve, with a sound restricted-evaluator as deferred future work); the canonical config stays host-side; the transport is a bounded host-initiated SSH copy reusing the per-VM key (no virtiofs, no new socket); review/approve is host-operator-only and never auto-writes the config tree; guest-built store paths are never trusted. **Superseded transport (W16):** the SSH `cat` copy described here was replaced by the typed bounded `ReadGuestFile` read over the authenticated guest-control vsock (ADR 0029); the rest of the containment/review model still holds. |
| [0025. Host-jailed Wayland filter proxy role](0025-wayland-proxy-host-jailed-role.md) | Accepted (v1.3-dev) | 2026-06-10 | Adds the `wayland-proxy` process role (`d2b-<vm>-wlproxy` principal, empty caps, mandatory `w1-wayland-proxy` seccomp, no PipeWire/Pulse access, dedicated `/run/d2b-wlproxy/<vm>` runtime dir, host compositor socket bind-mounted at in-jail upstream path). Extends the d2bd pidfd watchdog so unexpected proxy death triggers `StopRunnerRequested { runner_role: Gpu }`. GPU runners no longer hold direct access to the real host compositor socket once Wave 2 integration is complete. |
| [0026. Native SigNoz observability backend](0026-native-signoz-observability.md) | Accepted | 2026-06-10 | Replaces the Grafana/Prometheus/Loki/Tempo observability backend with native SigNoz services in the renamed `sys-obs` VM, keeps d2b-owned vsock transport, and records the manifestVersion bump for the new SigNoz observability metadata. |
| [0027. Hardlink-backed store-view live pool](0027-store-view-hardlink-live-pool.md) | Draft | 2026-06-09 | Makes `store-view/live` the canonical hardlink-backed per-VM lower store, with Rust broker `StoreSync` as the sole writer and metadata-only generations. |
| [0028. Guest control plane over virtio-vsock](0028-guest-control-plane-over-vsock.md) | Accepted | 2026-06-08 | Selects ttRPC/protobuf for guest-control APIs and Kata-style chunked stdio RPCs for Docker-like exec I/O, with a representative static/no-unsafe guest dependency proof, LoadCredential token auth, and SSH compatibility only for existing SSH-backed commands on old running VMs; production guest binaries remain an implementation gate. Supporting evidence: [guest-control feasibility dossier](guest-control-feasibility-dossier.md). **Shipped (W16):** guest-control plane landed (`ReadGuestFile` config sync + guest exec/konsole over vsock, no SSH); the SSH compatibility path for old running VMs is not yet wired and `config sync` fails closed there. |
| [0029. Migrate framework SSH operations to typed guest-control RPCs](0029-framework-ssh-to-typed-guest-rpc.md) | Accepted | 2026-06-09 | Migrates framework readiness (raw TCP-22 → fail-closed authenticated guest-control Health) and `config sync` (SSH `cat` → typed bounded `ReadGuestFile` over the authenticated vsock channel) off SSH, adds the negotiated `ReadGuestFile` guest capability, deprecates `vm konsole`, and confines remaining SSH to delimited operator conveniences guarded by a fail-closed source allowlist. **Shipped (W16):** readiness (`guest-control-health`) and `config sync` (`ReadGuestFile`) migrated off SSH; `vm konsole` now runs over guest-control via the shipped admin-only `vm exec`; the SSH compatibility transport for old generations is not yet wired (`config sync` fails closed). |
| [0030. Guest exec runs as the workload user in a PAM login session](0030-guest-exec-as-workload-user.md) | Accepted | 2026-06-13 | Replaces root-only guest exec with the host-fixed per-VM workload user (`ssh.user`, never root) in a real PAM login session (`systemd-run --uid=<uid> -p PAMName=login` through the user login shell), reproducing the retired SSH `vm konsole` environment (`XDG_RUNTIME_DIR` + `WAYLAND_DISPLAY`). Guestd ignores the wire `user` field, names each transient unit so teardown SIGKILLs the workload cgroup after a local kill, removes `guest.exec.allowRoot`/`guest.exec.users` (tombstoned) + the `d2b-userd` stub + `vm konsole`. |
| [0031. Bare commands and detached workload-user exec](0031-bare-command-and-detached-exec.md) | Accepted (Unreleased) | 2026-06-15 | Allows bare command names behind the workload user's login shell, maps invalid program names to the usage-class `guest-control-invalid-program` error, enables non-interactive detached exec, and adds VM-first detached-management verbs with bounded per-stream retained-log offsets. |
| [0032. d2b v2 constellation control plane](0032-d2b-v2-constellation-control-plane.md) | Superseded | 2026-06-14 | Superseded by [ADR 0043](0043-realm-native-control-plane.md); its semantic operation, stream, capability, idempotency, relay-as-reachability, and bounded audit invariants carry forward into the realm-native model. |
| [0033. Host collector parity and hostname identity](0033-host-collector-parity.md) | Accepted | 2026-06-17 | Brings the host OTel edge collector to parity with the guest collector (opt-in host journald + UDS-only host OTLP ingest, mirroring guest pipelines with an identity-only `resource` processor) and stamps host-origin telemetry with the real hostname (`vm.name`/`host.name` via `host.identityName`, default `networking.hostName`) at the trusted ingress boundary while `vm.role` stays `host`. |
| [0034. Storage lifecycle, restart adoption, and synchronization](0034-storage-lifecycle-restart-and-synchronization.md) | Accepted | 2026-06-19 | Makes managed paths, process restart/adoption behavior, lock ownership, cleanup, degraded-state reporting, and migration cutover explicit generated contracts owned by `d2bd` and the broker. |
| [0035. Efficiency and simplification roadmap](0035-efficiency-and-simplification-roadmap.md) | Accepted | 2026-06-19 | Defines the multi-wave cleanup plan for shrinking duplicated Rust/Nix/test surfaces, consolidating generated contracts, normalizing VM/env indexes, and keeping v2 provider work on one efficient architecture. |
| [0036. qemu-media runtime](0036-qemu-media-runtime.md) | Accepted | 2026-06-20 | Documents the current qemu-media implementation: local QEMU provider, paused QMP boot media attach, broker-owned media operations, registry-backed physical USB refs, direct image-file sources, redaction, manual-only posture, and unsupported guest features. |
| [0037. Local hypervisor runtime seam](0037-local-hypervisor-runtime-seam.md) | Accepted | 2026-06-20 | Defines a shared runtime/service abstraction for qemu-media and Cloud Hypervisor/crosvm workloads, including provider-neutral status/capability reporting, operation denials, lifecycle/reap semantics, adapter boundaries, qemu-media USB cleanup, autostart policy, and Wayland capability consumption. |
| [0038. Persistent named guest shell sessions](0038-persistent-guest-shell-sessions.md) | Accepted | 2026-06-21 | Selects top-level `d2b shell`, shpool-backed named persistent workload-user shells, shared terminal-v1 reuse with exec, static guest helper isolation, and staged implementation constraints. |
| [0039. Constellation persistent shell routing](0039-constellation-persistent-shell-routing.md) | Accepted | 2026-06-22 | Defines the ADR 0032 routing contract for ADR 0038 persistent shells: shell operations route as semantic constellation operations, provider sandboxes need guestd-compatible agents, and provider-native `executeShellCommand` is not a persistent-shell channel. |
| [0040. Graceful local VM shutdown before VMM termination](0040-graceful-vm-shutdown.md) | Accepted | 2026-06-22 | Requires provider-aware guest shutdown for Cloud Hypervisor and QEMU before host-side VMM termination, with configurable timeouts, explicit force-stop semantics, status reconciliation, and desktop control-surface constraints. |
| [0041. Console and audio controls across runtime providers](0041-console-and-audio-controls.md) | Accepted | 2026-06-27 | Selects provider-capability-aware console and audio controls for Cloud Hypervisor, qemu-media, and ACA sandboxes, including persistent console draining, qemu fd-backed console posture, guestd-backed ACA routing, host-only qemu audio, and wlcontrol UX constraints. |
| [0042. d2b clipboard authority and picker split](0042-d2b-clipboard-authority-and-picker-split.md) | Accepted | 2026-06-28 | Splits trusted clipboard authority into d2b-owned `d2b-clipd` plus Wayland bridge virtualization, keeps `d2b-clip-picker` UI-only in a separate GPL repository, and requires a no-patch Niri paste-intent hook for host cross-realm native paste. |
| [0043. Realm-native control plane](0043-realm-native-control-plane.md) | Accepted | 2026-07-05 | Supersedes ADR 0032 with realm-as-control-plane architecture: one `d2bd` plus broker/socket/state/audit boundary per realm, strict parent/child routing, dynamic relay discovery, realm-qualified VM addresses, and migration from current `home`/`dev`/`work` groups into first-class realms. |
| [0044. Unsafe-local runtime provider](0044-unsafe-local-runtime-provider.md) | Accepted | 2026-07-09 | Adds an explicit `unsafe-local` provider for host-user/session/network workloads with Wayland-proxy identity rails, first-class desktop launch metadata, and host-local persistent shell UX while preserving the no-isolation security warning. |
| [0045. Provider and transport framework](0045-provider-and-transport-framework.md) | Proposed | 2026-07-10 | Separates provider authorities from transport and protocol seams, selects Noise-authenticated component sessions with ttrpc/protobuf control services, models controllers as parent-owned workloads, and authorizes direct peer streams over shared transport fabrics without bypassing realm-tree policy. |
| [0046. d2b 3.0 Provider control plane](0046-d2b-3-provider-control-plane.md) | Proposed | 2026-07-22 | Rebuilds d2b 3.0 from the pre-ADR45 v3 baseline around Zone-local resources over 19 standard ResourceTypes (incl. `Endpoint`), independently packaged multi-process Providers selected by `{artifactId,config}`, an async embedded redb resource plane with owner-driven reconciliation, status-first component state with optional Volumes, layered base+provider ResourceType specs, and Noise/d2b-bus ComponentSession channels on the `d2bus.org` namespace. |
| [0047. Window identity chrome](0047-window-identity-chrome.md) | Proposed | 2026-07-29 | Replaces the wayland-proxy's 9 px rotated-text rail with a reserved top band containing one accessible, clickable identity tab; derives layout and hit-testing from a single measured parts list, adds labelled two-tier action disclosure, WCAG-correct colour selection, bidi-safe identity text, and a typed fail-closed contract. |
| [0048. Copilot-native agent surface](0048-copilot-native-agent-surface.md) | Accepted | 2026-07-31 | Makes Copilot the canonical definition surface: thirteen `.github/agents/` roles with ten independent panel seats, five d2b skills, dispatch-parameter model binding measured against CLI 1.0.75, read-only-by-construction panel lanes, spec-kit in skills mode, a standalone ADR process, and qualified wave identifiers that leave the legacy namespace untouched. |
| [0049. Store-owned mutation seal](0049-store-owned-mutation-seal.md) | Accepted | 2026-08-01 | Closes the ADR 0046 D106 verified-only mutation requirement by moving the mutation authorization capability down into `d2b-resource-store` as a private concrete `SealedMutation` minted only by a paired `MutationSealIssuer`. Deletes the publicly implementable `VerifiedMutationView` trait and the `std::any::type_name` runtime guard, both measured forgeable; makes `commit_verified` non-generic and by-value so single use, exact store binding, and cross-store replay rejection are compiler-enforced. Records that the seal binds evidence to a store instance but cannot bind a store instance to the evaluator, and guards that residual with two counted mint call sites. |
| [0050. Provider derivation artifact layout](0050-provider-derivation-artifact-layout.md) | Proposed | 2026-08-02 | Fixes the on-disk shape of a Provider Nix derivation so the ADR 0046 Phase 2 required-outputs check becomes writable: one Nix output carrying evidence it was chosen, asserted at eval over `outputs`, `outputSpecified` and `outputName` because `package.all` both rejects a correctly pinned `pkgs.foo.dev` and throws on a store-path-valued `types.package`; the signed manifest, its detached Ed25519 signature, and the root config JSON Schema as regular files at fixed identity-independent paths under `share/d2b/provider/`; the executable set located by enumerating `bin/`, name-checked as read from the directory, required to be ELF, and cross-checked against the manifest per-binary digest map; explicit preimages for every pinned digest, with a new `d2b:v3:provider-executable-set` tag binding the whole sorted name-to-digest map; anchored `openat2` resolution under `RESOLVE_BENEATH`, `RESOLVE_NO_SYMLINKS` and `RESOLVE_NO_MAGICLINKS` with `O_CLOEXEC` throughout, in two least-authority handle modes because an `O_PATH` descriptor cannot be read, `fstat` on the descriptor and `execveat` from it, behind an injectable trait boundary so sequencing and errno mapping are hermetically testable; a framework-owned `d2b.lib.buildProviderElfShim` that resolves an interpreter through a bounded same-output symlink chain ending at a verified ELF image, so `pkgs.python3` works and wrapper scripts are honestly refused; a validated `BinaryRef` newtype deserialized through its parser, with launchability modelled as an enum over an unchanged flat `binaryRef` wire field so no schema version moves and a twenty-one-code bounded failure taxonomy, split so a Phase 2 content check and a Phase 3 `execveat` result are never conflated, naming actionable remediations without emitting store paths or key material. Adds twenty-one Phase 1 and Phase 2 conformance scenarios plus one Phase 3 launcher scenario, and corrects five scenario identifiers cited as validation that exist nowhere in the spec set. |
