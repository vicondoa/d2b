# Error envelope guidance

This page moved to [`../reference/error-envelope-guidance.md`](../reference/error-envelope-guidance.md).

The reference copy is now canonical because the daemon, broker, and
CLI envelope rules are part of the stable public contract surface.
