# U34 d2b-provider-audio-pipewire
net: -242 lines, -0 deps

- `delete:` audio-pipewire: `src/argv.rs` whole-surface `AudioComponentTemplate` — `AudioComponentTemplate::new`/`render()`, `RenderedAudioTemplate`, `AudioTemplateError` have **zero production callers workspace-wide** (caller search: `grep -rn "AudioComponentTemplate\|RenderedAudioTemplate\|AudioTemplateError\|AudioTemplateError::"` across every crate's `src/`+`tests/`, all `BUILD.bazel` dep lists, `tests/support/`, `nixos-modules/`, `docs/`, `integration/` — excluding the crate's own `src/argv.rs`, its `tests/argv.rs`, and the `lib.rs` re-export arm → empty for every production file; the only remaining references are the crate's own unit test `tests/argv.rs` and the re-export arm in `lib.rs`). The daemon composes the live vhost-user-sound worker argv itself (`d2bd/src/audio_dispatch.rs`, `d2bd/src/audio_host_controller.rs` build the sidecar launch via `AudioMediator`/wire vocabulary; `d2bd/src/audio_host_controller.rs:21-40` asserts the daemon-side launch, not this crate's template). `src/argv.rs` (86 ln) + `tests/argv.rs` (25 ln) + the `argv` `d2b_rust_test` target and its lib `pub mod argv;`+re-export arm. Same leaf family already applied in this crate at `54b7948f2` (#S42/S43/S45 deleted `src/audio_argv.rs`, `nix/host.nix`+`nix/guest.nix`, the `audio_policy.rs` shim, and the `AudioRunnerContract` holder — `argv.rs` is the same-surface survivor that remained because a dossier anchor row at ADR-046 `src/argv.rs:72` pinned the template projection; re-checked: that row pins the appended-container argv shape bounded at admission, not a live `render()` surface, and no daemon path calls `render()`). [packages/d2b-provider-audio-pipewire/src/argv.rs] (leaf)
- `delete:` audio-pipewire: `src/telemetry.rs` record surface — `AudioTelemetryOperation`/`AudioTelemetryRecord`/`record()`/`AudioTelemetryError` have **zero production readers workspace-wide** (caller search across every crate's src+tests, BUILD dep lists, nixos-modules, docs, integration — excluding the crate's own `src/telemetry.rs` + `tests/audio_telemetry.rs` + lib arm → empty; daemon reads `AudioMediator` readiness/grant state via the crate's mediator/authority/state surfaces, never the closed telemetry record). Same unproduced-telemetry family as the applied cross-crate finding at #G98/#G99 (supervisor/metrics+tracing, qemu-media, azure-vm, ACA, cloud-hypervisor all cut label-set telemetry with zero in-tree consumers). Delete `src/telemetry.rs` (62 ln) + `tests/audio_telemetry.rs` (33 ln) + the `audio_telemetry` test target block. [packages/d2b-provider-audio-pipewire/src/telemetry.rs] (leaf)
- `delete:` audio-pipewire: `src/manifest.rs` `AudioManifest` — `AudioManifest::canonical()` and the `AudioManifest` type have **zero production consumers workspace-wide** (caller search excluding the crate's `src/manifest.rs` + `lib.rs` re-export arm; the ADR-046 row at docs pinning "component template" does not name `AudioManifest`; no daemon/broker/policy/zone crate reads it). Delete `src/manifest.rs` (36 ln) + the `pub use manifest::AudioManifest;` re-export arm. [packages/d2b-provider-audio-pipewire/src/manifest.rs] (leaf, lower-confidence than finding 1)

## Consistency notes
N/A — non-types-layer crate; no cross-crate type drift to report.

## Reopened refusals
None. Prior refusals for this crate (`#S44` — `AudioMediator` compat defaults, `AudioReadiness`, in-src `FakeAudioMediator`) remain standing: the mediator default is consumed by a test implementor, the readiness type is read by the daemon, and the fake is consumed by another crate's tests — none of the three files below is that surface picks. Only new-evidence reopenings: none.

## Checked
Read `src/argv.rs` (86), `src/telemetry.rs` (62), `src/manifest.rs` (36) in full, plus `src/lib.rs` (re-export arms), `tests/argv.rs` (25), `tests/audio_telemetry.rs` (33), and `BUILD.bazel` target spans (`argv` L50-x, `audio_telemetry` L80-x), plus the crate's other live surfaces (`controller.rs`, `authority.rs`, `mediator.rs`, `resource_type.rs`, `state.rs`, `tests/controller.rs`) to confirm the refused mediator/readiness surface. Ran workspace-wide caller search for every public symbol on the three candidate surfaces across all `packages/**/*.rs` (src+tests+dev), every `BUILD.bazel`/`BUILD` dep list, `nixos-modules/`, `tests/support/`, `docs/` (incl. the ADR-046 dossier + ponytail audit record + U1 constraints packet), `integration/`, and Nix projections; all three returned empty outside the crate's own modules and their own test files. Honored the U1 refusal ledger: no applied-crate prior finding covers `argv.rs` or `telemetry.rs` (the `#S42-#S45` audio family cut the `audio_argv.rs` builder, `nix/host.nix`+`nix/guest.nix`, and the `audio_policy.rs` shim, and refused the mediator/readiness/fake surface); `#S44` refusal intact. `src/generated/` untouched; no policy scaffold (README-only ratchet: this crate is on the README-only integration list, so no `integration/*.rs` obligation) — deletions are within crate-layout policy. No dossier row pins any of the three files; this is a provider crate on the README-only ratchet, so no ADR-dossier-destination evidence applied.
