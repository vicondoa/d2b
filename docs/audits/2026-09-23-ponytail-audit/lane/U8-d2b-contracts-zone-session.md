# U8 d2b-contracts-zone-session

net: -303 lines, -0 deps

- delete five dead `*StatusResource` projections - EmergencyPolicy, ResourceExport, ResourceImport, Role, RoleBinding - each projection is struct + `impl` + `Deserialize` + `redacted_debug!` + alias + in-file self-test, zero workspace callers on every surface, unpinned (no xtask policy pin, no golden wire pin, no nix/BUILD ref; the record's line 121 `#B5` "six *StatusResource projections" is a miscount - Zone + ZoneLink are LIVE, read at d2bd/resource_runtime.rs:3057,4810, d2b-provider-zone/zone_status.rs:114, d2b-provider-provider/providers.rs:357, d2b-provider-zone-link/zone_links.rs:1261, and the only policy pin for this crate is services.rs). Measured regions: emergency_policy.rs:220-265 (46), resource_export.rs:728-819 (92), resource_import.rs:412-500 (89), role.rs:872-901 (30), role_binding.rs:435-480 (46). Replaces each with nothing. Caller method: workspace-wide `grep -rn "\b<T>StatusResource\b" --include=*.rs` over `packages/` minus this crate (five structs, all 0 external refs), plus `grep -rn "\b<T>Status"` over `packages/xtask/src/*.rs` (only services.rs pinned, token `nixos`, at provider_crate_policy.rs:1795-1798), plus `tests/contracts.rs` (golden surface touches only component_session + Zone/ZoneLink wire) and `nixos-modules/resource-schemas/*.nix`. Deletion removes each projection's in-file `#[cfg(test)]` self-test (emergency_policy.rs:307 asserts `EmergencyPolicyStatusResource::new`, resource_import.rs:579) and mod.rs re-export name arm; BUILD is glob-based (no target edits), contracts.rs suite pin untouched. [packages/d2b-contracts-zone-session/src/v3/{emergency_policy,resource_export,resource_import,role,role_binding}.rs] (family)

## Consistency notes
- The U1 record's `#B5` claimed "the dead half of services.rs and **six** *StatusResource projections stay" - astronomically that set is **five**, not six. The miscount was caused by treating the LIVING `ZoneStatus`/`ZoneLinkStatus` projections as dead candidates; workspace caller census proves both live (enumerated above). Any future re-audit must not re-add Zone/ZoneLink to the deletion set.
- `services.rs` stays - refused as declared: golden test `service_package_wire_values_are_frozen` at v3/services.rs:385 pins `V3Service::package()` wire d2b.config-nixos.v3, xtask `SharedFamilyKnowledgeExemption` at provider_crate_policy.rs:1795 (token `nixos`, family `activation-nixos`) pins the exact closed package, and the bus routes an exact closed package; that half of services.rs is frozen wire, not knowledge. Left untouched here (matches record's `#B5` refusal).
- `#C4` `[not applied]` macro/boilerplate consolidation (76 Wire deserialize blocks, identity macros, facade copies) - not applied this pass; cross-crate consolidation is out of per-crate scope (record's `#C4`).

## Reopened refusals
- `#B5` projection limb reopened on new evidence: the record's refusal kept the projections on the implicit premise that a golden wire test pins them (grouped with the refused services.rs wire pin). Caller census shows the golden pin covers only `V3Service::package()` at services.rs:385 - the five projections have no wire test, no xtask policy row, and zero workspace callers. That is a changed blocker (pin scope was narrower than assumed); the five are now safely deletable, hence the -303.

## Checked
Read all of `packages/d2b-contracts-zone-session` - `lib.rs`, `src/v3/mod.rs`, all five dead-projection files (emergency_policy, resource_export, resource_import, role, role_binding) plus the two live siblings (zone, zone_link), `services.rs` with its frozen-wire golden test, and `tests/contracts.rs`. Ran grep/rg caller census across `packages/` (Rust), `packages/xtask/src/*.rs` (policy rows, incl. provider_crate_policy.rs:1795-1798 pin), BUILD.bazel, `nixos-modules/resource-schemas/`, and `tests/contracts.rs`; measured all line regions with sed/awk, not estimates. Ledger rows honored: `#B5` [partial] remainder verified and revised (five, not six), `#C4` [not applied] untouched, services.rs wire pin respected, generated/ + xtask golden test surfaces excluded as policy-required.
