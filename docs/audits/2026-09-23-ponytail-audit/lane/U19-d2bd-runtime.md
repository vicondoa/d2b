# U19 d2bd-runtime
(daemon runtime, 43,646 LOC / 48 modules. Redundancy + dead-surface audit over 38 scout-verified files. This is the provider-neutral daemon runtime: admission, target_runtime, guest/zone runtime, supervisor (pidfd_table/state/dag/readiness), daemon_audit, exec/console sessions, concurrency, metrics, otel bridge. Cross-referenced against U1 packet rows for this crate - #P7 [partial] and #S1 [applied] are the dash-grouped uid-to-UUIDv4 renderer and are NOT re-reported. Nothing refused; nothing reopened.)

## Dead surface (verified zero-callers workspace-wide)

1. host_mode.rs:14-55 (~42) - whole module: `HostRuntime`, `HostRuntimeConfig` + 5 accessors. Zero callers. d2bd composes `ProviderDeployment` directly. search: workspace grep `\bHostRuntime\b|\bHostRuntimeConfig\b|host_mode` - only hits are (a) the crate's own decl + BUILD.bazel glob, (b) d2b-broker/src/live_handlers.rs:39 `use d2b_core::bundle_resolver::HostRuntime` - that is d2b_core::bundle_resolver::HostRuntime, a *different live type* in another crate, not this crate's decl, plus (c) runtime_boundary.rs test include_str pin + lib.rs `pub mod host_mode;`.
2. resource_runtime_support.rs:74-148 (~70) - `TargetResourceLifecycle` (5 methods: new/mode/admit_controller/revoke_session/active_assignments) + `FIXED_BOOTSTRAP_PROVIDER_IDS`. Zero callers. Convenience wrapper over `ProviderDeployment`. search: workspace grep `\bTargetResourceLifecycle\b|\bFIXED_BOOTSTRAP_PROVIDER_IDS\b` - decls only (incl. BUILD glob). Refuted-lane caution honored: the wrapper's *inner* type `ProviderDeployment` is live (used at target_runtime.rs:1122), only the wrapper is dead.
3. daemon_audit.rs:1022-1031,1241-1292 (~55) - `verify_daemon_audit_lines`, `DaemonAuditChainReport::is_clean`, `DaemonAuditLog::{sink_health_report, sink_health_report_with_floor}`. Test-only callers. search: workspace grep `\bverify_daemon_audit_lines\b|\bis_clean\b|\bsink_health_report\b` - tests + decls only; production re-verify uses private `verify_daemon_audit_files`.
4. exec_session.rs:122-137,165-178 (~30) - `ExecOpError::{metric_kind}` + `ExecEstablishError::{metric_kind}`. Closed-enum metric labels never emitted. search: workspace grep `\.metric_kind\(\)` - decls + tests only.
5. autostart.rs:168-190,272-279 (~23) - `AutostartReport::{count_where, started, already_running, failed, degraded}` + `execute_autostart` wrapper. Test-only. search: workspace grep `\.started\(\)|execute_autostart\b` - own tests only.
6. guest_mode.rs:63-72 (~10) - `reject_legacy_guest_prelude`. Test-only. search: workspace grep `reject_legacy_guest_prelude` - decls + tests only.
7. target_runtime.rs:139-146 (~8) - `ModeSurfaces::{host, guest}` ctors. Zero callers. search: workspace grep `\.host\(\)|\.guest\(\)|\bModeSurfaces::\b` - decl only.
8. concurrency.rs:97-105 (~8) - `ConnSemaphore::{in_flight, cap}`. Test-only. search: workspace grep `\.in_flight\(\)|\.cap\(\)` - decls + tests only.

## Duplication (in-crate, canonical exists - fair new findings)

9. civil-date (Hinnant 719_468/146_097) hand-rolled 3x in-crate: daemon_audit.rs:1963 `ymd_from_unix`, runtime_process.rs:519 `days_to_ymd` (civil variant), resource_runtime_support.rs:1174 `civil_from_days`. Replacement: single shared in-crate `civil_from_days` (no new dep - crate deliberately avoids chrono, per module comments; keep one canonical Hinnant copy). ~35 lines saved. search: workspace grep `719_468|146_097`.
10. hex renderers hand-rolled 6x in-crate vs canonical in-crate `runtime_util::hex_bytes` (runtime_util.rs:24): daemon_audit.rs:1763 `hex_lower`, exec_session.rs:1585 `hex_encode`, guest_component_session.rs:587 `hex_digest`, guest_mode.rs:353 `hex_digest`, console_session.rs:124 inline, resource_runtime_support.rs:1756 inline. ~30 lines saved. search: workspace grep `fn hex_|{:02x}`.
11. /proc/<pid>/stat tail parse hand-rolled 3x vs shared supervisor/state.rs parser: supervisor/state.rs:203 `parse_proc_stat_starttime` canonical vs supervisor/pidfd_table.rs:783-798 and readiness.rs:360 `read_proc_state` re-parsing with `rfind(')')`. ~25 lines saved. search: workspace grep `rfind(')')|read_proc_state`.
12. metrics request-head parse duplicated within metrics.rs (metrics_handler:759-764 vs metrics_handler_with_ch_stats:790-795) - one `parse_metrics_request_head` helper. ~8 lines saved. search: in-file grep `split_whitespace`.

net: -246 lines, -0 deps.

## U3 outcome (2026-09-24)
- applied: host_mode.rs whole module (+ lib.rs arm + runtime_boundary.rs include_str pin); TargetResourceLifecycle + FIXED_BOOTSTRAP_PROVIDER_IDS (+ orphaned target_runtime imports); daemon_audit verify_daemon_audit_lines + DaemonAuditChainReport::is_clean + DaemonAuditLog::{sink_health_report, sink_health_report_with_floor} (+ unread state_dir field, unused unavailable() ctor, 7 subject tests, orphaned two_chained_records helper; concurrent_writes/async_seat tests re-pointed at production verify_daemon_audit_files); ExecOpError/ExecEstablishError::metric_kind; reject_legacy_guest_prelude (+ its in-crate test + d2bd/tests/guest_mode_component_session.rs test + import arm); ModeSurfaces::{host,guest} ctors; ConnSemaphore::{in_flight,cap} (+ 2 tests reworked to behavioral asserts).
- skip (stale claim): autostart AutostartReport::{count_where, started, already_running, failed, degraded} + execute_autostart - production callers exist at HEAD: d2bd/src/composition.rs:5324-5327 (`report.started()/already_running()/failed()/degraded()`); count_where is their substrate.
- Duplication items 9-12 (civil-date/hex/proc-stat/metrics-head) are dedup refactors, not class-(b) leaf deletes; not part of the lane's net -246; left for the dedup wave.
