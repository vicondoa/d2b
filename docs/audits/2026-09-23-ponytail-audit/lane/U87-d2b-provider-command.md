# U87 d2b-provider-command
net: -0 lines, -0 deps

Lean already. Ship.

Audited the whole crate at HEAD (3f2664794): src/{command.rs 400, driver.rs 20, lib.rs 22}, tests/registration.rs, integration/command.rs + README.md, Cargo.toml, BUILD.bazel. The declaration-only driver was already folded into the shared metadata driver of the #A1 finding (driver.rs is the 20-line `command_descriptor()` -> `metadata_descriptor(WellKnownType::COMMAND)` shell), the registration test already delegates to the shared `assert_metadata_registration`, and the crate's one real asset — the `Command` contract types (CommandExec, CommandArgvSlot, CommandSpec, CommandIntent, CommandContractError, plus the four MAX_* capacity consts) — is the type identity this crate exists to own. Every public accessor and const is live: exec/argv/params/roleRef/intent are read by d2bd/src/foundation_seed.rs (rows materialize there), command_descriptor() is consumed at d2bd/src/resource_plane_v3.rs:140 + zone_schema.rs:1235 and the BUILD deps, and the per-type parse/schema/round-trip tests in command.rs exercise real contract behavior (placeholder/role/exec rejection, wire round-trip with deny_unknown_fields), not tautologies. The fallible Deserialize/JsonSchema impls are live admission gates on the declared launch shape — the refused hand-written-Deserialize class — so they stay. No unused deps: d2b-contracts-resource and d2b-resource-types both referenced; serde_json/tokio dev/cargo-only. Caller search: workspace grep across packages/*/src, integration/, tests/, BUILD.bazel dep lists, and xtask/src/zone_schema.rs for COMMAND_RESOURCE_TYPE / command_descriptor / CommandSpec confirms every claim above; nothing zero-caller, no dead surface, no hand-rolled stdlib equivalent found.

## Consistency notes
types-layer crate. No divergence to report: the type's wire shape is spelled exactly once here (CommandSpec + its camelCase deny_unknown_fields wire), the driver is the single shared declaration-only metadata driver (canonical home d2b-resource-runtime/src/metadata.rs via the #A1 fold), and registration flows through the shared assertion in d2b-resource-types/src/metadata.rs. No duplicate type definitions, naming drift, hand-rolled copies of shared constructors, or wire-shape skew within this crate or against its siblings. Command is unique across the provider plane (only one command-type crate), so no cross-crate twins to reconcile. No U97 cross-crate consistency finding.

## Reopened refusals
None. #PR13 (controller-family template — declaration-only half removed with the shared metadata driver; the duplicated fences/verb-list template was refused as a collision with another lane's shared-engine restructuring) is not reopened: no new evidence that the shared engine lane landed, and the in-crate declaration-only half is already gone at HEAD so there is nothing left here to cut. #A1 remains applied and verified as such.

## Checked
Read full command.rs (400 lines), driver.rs, lib.rs, tests/registration.rs, integration/command.rs and its README.md, Cargo.toml, BUILD.bazel. Searches: workspace grep for command_descriptor / COMMAND_RESOURCE_TYPE / CommandSpec across all packages/*/src + integration/ + tests/ + BUILD dep lists + xtask/src/zone_schema.rs + d2bd/src/{foundation_seed.rs, resource_plane_v3.rs}; razor check (`.exec()`/`.argv()`/`.params()`/`.role_ref()`/`.intent()` callers); confirmed every MAX_* const and all four public accessors have live readers/tests. Ledger honored: #PR13 refused stays refused (no new evidence), #A1 verified applied. No findings warranted.
