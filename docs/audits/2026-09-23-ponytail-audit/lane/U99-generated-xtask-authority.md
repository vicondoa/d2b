# U99 Generated code + xtask authority alignment

- shrink: replace the ~220-line hand-rolled Rust-source parser behind `gen-daemon-api` with `syn`. `parse_rust_items`, `parse_fields`, `parse_variants`, `split_top_level_entries`, `strip_non_code_lines`, `normalize_ws`, `extract_name`, `extract_body`, `brace_delta` (main.rs:1025-1257) re-implement brace-counted `pub struct`/`pub enum` extraction over the d2b-contracts{, -broker, -control} IPC sources, which `syn` (three versions already in Cargo.lock, pulled transitively by the xtask dep `schemars`) does in ~40 lines. The doc output (`docs/reference/daemon-api.md`) and the `gen_daemon_api_drift` gate stay byte-identical;the hand-rolled lexer is the only complexity being cut. A syn rewrite needs no new lockfile node (syn becomes a direct dep, no new supply-chain edge),and the generated doc's shape assertions (render_handshake/request/response/enum/error sections) survive unchanged. [packages/xtask/src/main.rs:1025-1257] (leaf)
- delete: `gen-resource-schemas`: its JSON half is a dormant second generator for zone_schema-owned artifacts,and its Nix half emits six orphaned wrapper modules. gen_resource_schemas.rs (76 lines) writes `docs/reference/schemas/v3/core.d2bus.org_{Host,Guest,Process,EphemeralProcess,User,Endpoint}.schema.json` only when the file is absent (`if !schema_path.exists()`, gen_resource_schemas.rs:29-53) - but those six files are committed and owned by `gen-zone-schemas` (zone_schema.rs `standard_resource_schemas()` + `gen_zone_schemas` (lines 1141-1288, written via `core_schema_artifact_name()`), so on any committed tree the fallback never runs,and its drift gate compares the same files zone_schemas already pins. The six `nixos-modules/resource-schemas/{Endpoint,EphemeralProcess,Guest,Host,Process,User}.nix` wrapper modules it does write have **zero imports** workspace-wide (grep `resource-schemas` + wrapper filenames across nixos-modules/, nix/, packages/, tests/, bazel/, Makefile, flake.nix, scripts/, .github/: only the root BUILD.bazel `generated_resource_schemas` filegroup (12 srcs) and docs context;`options-zones.nix` imports `./lib.nix`, `./resources.nix`, `./resources-bundle.nix`, not the wrappers;`create-provider.md:81-83` and ADR-046 "imported by options-zones.nix" are stale claims.). Delete: gen_resource_schemas.rs (76), the six wrapper .nix (~40), the 12 `generated_resource_schemas` filegroup entries (the six JSON files stay covered by the `generated_zone_schemas` glob + `gen_zone_schemas_drift`),the `gen_resource_schemas_drift` target (xtask/BUILD.bazel:127-131),and the usage-string arm.;the two JSON-farm references (`d2b-resource-schemas` linkFarm in resource-compiler.nix/resource-schema-validation.nix) build from docs/reference/schemas/v3 JSON files, not these modules. [packages/xtask/src/gen_resource_schemas.rs;nixos-modules/resource-schemas/;BUILD.bazel:24-38;docs/how-to/create-provider.md:81-83] (family)
- delete: the byte-identical authority-scaffold triplication: `collect_rs_files` appears in operation_row_authority.rs:340, provider_registration_authority.rs:220,and resource_type_authority.rs:686 (13 lines each, diff-clean save one formatting wrap),and `verify_committed` in the same three modules (15 lines each, differ only in the error-message prefix: "operation-row"/"provider-registration"/"resource-type-authority"). Share one authority-common helper (or fold `collect_rs_files` into the already-shared `resource_type_authority` home)and one parameterized `verify_committed`;the drift/parity/idempotence gates,and their error text (reworded once),stay equivalent. [packages/xtask/src/{operation_row_authority.rs:340,provider_registration_authority.rs:220,resource_type_authority.rs:686} for collect_rs_files;{operation_row_authority.rs:116,provider_registration_authority.rs:113,resource_type_authority.rs:340} for verify_committed] (leaf)
- delete: zone_schema.rs's duplicate emitter for `nixos-modules/generated/resource-types.nix`. Two independent generators write the same committed artifact: `gen-zone-nix-options` renders `generated_resource_types_module()` (zone_schema.rs:1541-1560, write arm 1575-1578) from the committed `d2b_contracts::identity::STANDARD_RESOURCE_TYPES` const,while the resource-type authority renders `render_nix_resource_types()` (resource_type_authority.rs:404-420, wired into check-provider-crate-layout via NIX_RESOURCE_TYPES_OUT)from the per-crate `resource-types.json` declarations. Both are drift-gated today (gen_zone_nix_options_drift + the layout check)and coincide byte-for-byte only because `COMMITTED_NIX_STANDARD_ORDER` copies `STANDARD_RESOURCE_TYPES` order (identity.rs:36-59) and the authority copies zone_schema's header text (resource_type_authority.rs:404-406, including the provenance "gen-zone-nix-options" which is wrong for its own emission). Canonical home:the resource-type authority (declarations-drive, zero-outside-edit fixture;zone_schema's copy requires editing d2b-contracts when a standard type appears). Delete `generated_resource_types_module()` + its `gen_zone_nix_options` write arm + the `nixos-modules/generated/resource-types.nix` entry in filegroup `generated_zone_nix_options`;the layout check's own gate (bazel/checks/policy:provider_crate_layout) keeps pinning the artifact. [packages/xtask/src/zone_schema.rs:1541-1560,1575-1578;resource_type_authority.rs:404-420] (family)
- delete: `string_array` - a byte-identical twin of `string_slice` in gen_layer_catalogs.rs (diff clean save the fn name;both render `pub const NAME: &[&str] = &[...]`). Collapse to one name;the six call sites of `string_array` (MUTATION_VERBS, PROCESS_PROVIDERS, RESOURCE_TYPE_VALUES, API_VERBS, BROKER_OPERATION_VALUES, RESOURCE_TYPES,) rename onto `string_slice` (or vice versa. [packages/xtask/src/gen_layer_catalogs.rs:151-187] (leaf)
- delete: the dead no-op replace in `sanitize_generated_rust`: `generated.replace("#![allow(clipto_camel_casepy)]\n", "")` (main.rs:453) references a corrupted lint name that never occurs in any generator output orany `.rs` in the workspace (grep `clipto_camel_casepy` across packages:main.rs:453 only;the protobuf/ttrpc outputs carry `clippy::bool_comparison`/`rustfmt::skip` etc., never this string). One stale line in the sanitization chain. [packages/xtask/src/main.rs:453] (leaf)
- fold:the hand-maintained `packages/d2b-resource-api/src/generated/mod.rs` (5 lines, no @generated header, no drift target:filegroup `resource_ttrpc_generated` lists only `d2b_resource_v3_ttrpc.rs`) into `gen_resource_ttrpc`'s emission (like `write_contract_generated_mod` already does for the d2b-contracts-resource side, main.rs:408-414), so the generated dir is fully generator-owned,and the alias (`pub mod d2b_resource_v3 { pub use d2b_contracts_resource::resource_proto::*; }`, required by the generated `super::d2b_resource_v3::GetRequest` refs at d2b_resource_v3_ttrpc.rs:30+) rides the generator + `gen_resource_ttrpc_drift` data (+1 filegroup entry), closing a hand-editable gap in a generated surface. [packages/d2b-resource-api/src/generated/mod.rs;packages/xtask/src/main.rs:355-377] (leaf)

net: -~400 lines, +1 direct dep (syn - already three versions in Cargo.lock via schemars/ttrpc-codegen;no new lockfile node)

## Verified

- gen_resource_schemas dormancy:read gen_resource_schemas.rs (76 lines) + zone_schema.rs `standard_resource_schemas()`/`gen_zone_schemas`/`core_schema_artifact_name` (1141-1288);globbed docs/reference/schemas/v3/core.d2bus.org_*.schema.json (all six present);diffed core_schema_artifact_name naming. Wrapper-consumer census:`grep "resource-schemas|Endpoint\.nix|EphemeralProcess\.nix|Host\.nix|Process\.nix|User\.nix|Guest\.nix"` across nixos-modules/, nix/, packages/, tests/, bazel/, Makefile, flake.nix, scripts/, .github/ -> only root BUILD.bazel filegroup + docs context;read options-zones.nix (imports lib.nix/resources.nix/resources-bundle.nix only)and resource-compiler.nix/resource-schema-validation.nix (`d2b-resource-schemas` linkFarm built from docs/reference/schemas/v3/*.json entries only, lines 32-50).
- resource-types.nix duplicate:read zone_schema.rs gen_zone_nix_options + generated_resource_types_module (1541-1589), resource_type_authority.rs render_artifacts + render_nix_resource_types (404-426);byte-compared COMMITTED_NIX_STANDARD_ORDER (resource_type_authority.rs:74-99) vs STANDARD_RESOURCE_TYPES (d2b-contracts/src/identity.rs:36-59):identical order;read committed nixos-modules/generated/resource-types.nix header/comment (matches both renderers' bytes, incl.the "Binding before Service" provider list).
- collect_rs_files/verify_committed triplication:awk-extracted all three bodies per fn and diffed (collect:one-line formatting wrap differs;verify:only the "operation-row"/"provider-registration"/"resource-type-authority" message prefixes differ).
- string_slice/string_array:extracted both bodies and diffed (only the fn name differs);call-site counts via grep (each live).
- clipto typo:grep "clipto_camel_casepy" across packages/ -> single hit at main.rs:453;read sanitize_generated_rust chain (448-464) and the generated files' allow headers (d2b_resource_v3.rs, d2b_resource_v3_ttrpc.rs - no such string).
- daemon-api parser:read main.rs:1025-1257 (all nine parser helpers) + render helpers (1324-1498);Cargo.lock has three `name = "syn"` packages;xtask already depends on schemars (Cargo.toml:55) which pulls syn transitively.
- resource-api mod.rs:read d2b-resource-api/src/generated/mod.rs (5 lines, no provenance header), gen_resource_ttrpc (main.rs:355-377, writes only d2b_resource_v3_ttrpc.rs), BUILD.bazel `resource_ttrpc_generated` filegroup ((d2b-resource-api/BUILD.bazel:19-23, lists only the ttrpc file), andthe generated file's `super::d2b_resource_v3::` refs (d2b_resource_v3_ttrpc.rs:30+, dozens of sites).
## Checked

Read (in full or structural + targeted ranges):`packages/xtask/src/gen_broker_operations.rs` (1669, incl. its whole test module), `operation_row_authority.rs` (842 incl. tests), `provider_registration_authority.rs` (634 incl. tests), `resource_type_authority.rs` (1731, through load/parity/render/authority-bound/, `blocking_census.rs` (1778, scan lexer + run wiring), `gen_layer_catalogs.rs` (725], `service_catalog.rs` (259], `gen_resource_schemas.rs` (76], `zone_schema.rs` (structural, via targeted reads of the schema lists, gen_zone_schemas, gen_zone_nix_options, generated_*_module renderers), `nix_inventories.rs` (structural], `deadcode.rs` (112], `main.rs` gen_* functions (354-973];all 16 `src/generated/` artifacts' provenance headers + regeneration/drift paths (the seven parent-listed crates + d2b-resource-api, d2b-audit, d2b, d2b-contracts-provider/src/v3/generated];root + xtask BUILD.bazel drift/filegroup tables;Makefile check-census/check-dead-code/check-async-gate + bazel/checks/policy:provider_crate_layout wiring;prior-record #A2 honored (gen_resource_type_catalog.rs already deleted];no refusals reopened - no refusal-ledger item covers any current generator. Generated shapes themselves were not flagged (per U1 constraint 3);only generator logic and boundary gaps above. No generator emits a file with no committed counterpart;one hand-written file lives un-gated inside a generated dir (finding 7];every other generated artifact hat a live regeneration command + a drift gate (layout-check authorities included).
## Addendum (post-audit tree movement)

- PR #600 (4d26998d8) added ~+1,300 lines to packages/xtask/src/async_gate.rs plus data/async-gate-inventory.json after this pass; treat the async-gate scanner as unaudited new surface.

## U99 outcome (2026-09-24)

- applied: gen-daemon-api's hand-rolled brace-counted parser (parse_rust_items line scan, brace_delta, extract_name, macro_rules skip) replaced with a syn visitor (IpcItemCollector, slice_source, line_col_to_offset); each item's braced body is span-sliced from the original source and fed to the unchanged text-based field/variant extraction, so `docs/reference/daemon-api.md` stays byte-identical (regenerated, git diff clean). syn 2.0.119 + proc-macro2 (span-locations) become direct xtask deps; no new lockfile nodes.
- applied: `collect_rs_files`/`verify_committed` triplication folded into one shared `packages/xtask/src/authority_common.rs`; the three authorities call the shared helpers with their own authority name, preserving the exact error strings and walk order.
- applied: `gen-resource-schemas` deleted (module, dispatch arm, usage row, GENERATOR_COMMANDS row, GENERATED_ARTIFACT_COMMANDS row, gen_resource_schemas_drift bazel target + test_suite entry, root `generated_resource_schemas` filegroup) and the six orphaned `nixos-modules/resource-schemas/*.nix` wrappers deleted (workspace grep: zero consumers outside the deleted generator + docs prose).
- applied: zone_schema's duplicate `resource-types.nix` emitter deleted (generated_resource_types_module, write arm, read-back assertion, and the two tests that rendered it now read the committed authority-owned file); `generated_zone_nix_options` bazel filegroup trimmed to the generator's 3-file output. Canonical emission = resource_type_authority via check-provider-crate-layout.
- applied: `packages/d2b-resource-api/src/generated/mod.rs` folded into `gen_resource_ttrpc`'s emission (`// @generated` header + alias + ttrpc module), committed file updated to match, `resource_ttrpc_generated` filegroup +1 entry.
- not in this work order (lane rows only): `string_array`/`string_slice` collapse and the `clipto_camel_casepy` sanitize no-op remain open.
