# U41 d2b-provider-guest-cloud-hypervisor

net: -~980 lines, -0 deps

- delete: D091 upgrade executor — `CloudHypervisorController::{plan_upgrade,execute_upgrade,observe_upgrade_state}` (controller.rs:2334-2637), the `upgrade_progress` field (1721, 1753), `shutdown::plan_upgrade` (shutdown.rs:587-676), and `GuestUpgradePlan` (shutdown.rs:528-585) have zero production callers; `reconcile` only projects `UpgradeRequired` (controller.rs:1926-1951) and never executes, and d2bd calls only `with_lifecycle_intent` + `reconcile` (resource_runtime.rs:5209-5333). Delete with the two test blocks that only pin them (reconcile_state_machine_test.rs:1058-1197, finalize_ordering_test.rs:277-298); `UpgradeReason` stays (read by live `assess_update`). [packages/d2b-provider-guest-cloud-hypervisor/src/controller.rs:2334] (family)
- delete: 10 dead `GuestControlEndpoint` accessors — `endpoint_ref/guest_ref/zone/uid/endpoint_uid/resource_generation/endpoint_generation/provider_generation/schema_digest/ready` (guest_local.rs:99-146) have zero callers; d2bd uses only `new` + `validate_for` (composition.rs:10723-10736). [packages/d2b-provider-guest-cloud-hypervisor/src/guest_local.rs:99] (leaf)
- delete: 8 dead `GuestSessionEvidence` accessors — `guest_ref/boot_identity_digest/capabilities/controller_ready/endpoint_ready/binding` (health.rs:283-318) and `validate_against/ready_for` (379-395) have zero callers; `session_generation`/`seed_ready`/`health` stay (controller.rs:2360, 3000-3005). [packages/d2b-provider-guest-cloud-hypervisor/src/health.rs:283] (leaf)
- delete: `CloudHypervisorControllerRegistration` write-only fields + dead accessors — `child_roles/watched_types/dependency_types/finalizer` (controller.rs:157-159) are constructed but never read in production (d2bd Register handler reads only provider_ref/provider_generation/descriptor_digest, resource_runtime.rs:2116-2121); `child_roles()`/`finalizer()` have zero callers, `watched_types()`/`dependency_types()` are test-only (tests/controller.rs:132,141). Cut fields, accessors, and the construction + Debug rows that render them. [packages/d2b-provider-guest-cloud-hypervisor/src/controller.rs:157] (leaf)
- delete: state.rs dead surface — `GuestStatusPhase::as_str` (state.rs:24-31), `GuestStatusObservation::ready` (102-118), and `finalization_eligible` (189-195) have zero callers (finalization_eligible is test-only, state_status_test.rs:218); its only readers `session_active`/`descendants_present` (93,95) are then write-only, so cut them with the writes at controller.rs:3027-3028 and the test rows. [packages/d2b-provider-guest-cloud-hypervisor/src/state.rs:24] (leaf)
- delete: identity.rs dead accessors — `ProcessCreateBody::{provider_ref,execution_ref,template,desired_lifecycle}` (246-265), `EndpointCreateBody::{provider_ref,producer_ref,purpose}` (329-342), `VolumeCreateBody::{provider_ref,execution_ref,system_artifact_id,view}` (396-414), `CommittedChild::{resource_ref,owner_ref,zone}` (723-736), `ChildRoleSet::contains` (167-170) — zero callers; constructors stay (`ProcessCreateBody::new` is live at d2bd/resource_runtime.rs:11735). [packages/d2b-provider-guest-cloud-hypervisor/src/identity.rs:246] (leaf)
- delete: descriptor.rs dead accessors — `DescriptorSignature::{algorithm,key_fingerprint}` (162-170), `GuestSeedContract::{schema,schema_version}` (223-231), `BootstrapHandoff::{class,expiry_ms}` (286-294), `GuestSetupDescriptor::{system_artifact_commitment,bootstrap_handoff}` (482-500), `VerifiedGuestSetupDescriptor::canonical_bytes` (370-373) — zero callers; `GuestSeedContract::fingerprint` stays (d2bd/composition.rs:10731). [packages/d2b-provider-guest-cloud-hypervisor/src/descriptor.rs:162] (leaf)
- delete: shutdown.rs dead accessors — `GuestFinalizationInput::{with_verified_process_identity,session,guest_local_drained,transitive_descendants_present,host_backed_volume_present,foreign_children_present}` (201-247), `GuestUpgradePlan::previous_session_generation` (566-569), `GuestFinalizationPlan::guest_uid` (319-322) — zero callers (fields stay, read internally by `plan_finalization`). [packages/d2b-provider-guest-cloud-hypervisor/src/shutdown.rs:201] (leaf)
- delete: `GuestStatusProjection::conditions` (controller.rs:982-985) — zero callers; `status`/`has_condition` stay (d2bd tests read them). [packages/d2b-provider-guest-cloud-hypervisor/src/controller.rs:982] (leaf)
- delete: `BootstrapGraph::readiness` (bootstrap_graph.rs:132-141) — zero callers; `vmm_readiness`/`vmm_lifecycle` stay (controller.rs:662). [packages/d2b-provider-guest-cloud-hypervisor/src/bootstrap_graph.rs:132] (leaf)
- dup: #C1 still open — `GuestControlEndpoint` remains byte-identical in d2b-resource-client/src/zone_client.rs:129 and this crate's guest_local.rs:49 (verified via struct-body diff, identical=True); the client copy has zero production callers (only zone_client.rs:1058 test + lib.rs:69 re-export) while the provider copy is live (d2bd/composition.rs:10723). Fix direction is cross-crate (delete the test-only client copy or add a dep edge), so it stays as recorded. [packages/d2b-provider-guest-cloud-hypervisor/src/guest_local.rs:49] (family)
- shrink: #G9 still open — BUILD.bazel repeats the same deps tail across 7 `d2b_rust_test` targets (BUILD.bazel:86-188); prior BUILD-file function was rejected by Bazel at analysis time and reverted (4422fe6ab), so no new fix direction exists. [packages/d2b-provider-guest-cloud-hypervisor/BUILD.bazel:86] (leaf)

## Reopened refusals

None. #G7 (fd10 handshake), #G10 (AttachmentRef/attachments write-only), #G11 (state_status_test is_exact rows) re-checked: blockers unchanged — #G10's `attachments` is still written by `BootstrapGraph::new` and passed as `Vec::new()` from d2bd/src/resource_runtime.rs:6163 with no reader, requiring an edit in another lane's file.

## Checked

Read every src file (adoption, bootstrap_graph, config, controller 3216, controller_session 666, descriptor, guest_local, health, identity, lib, shutdown, state), all six test binaries, tests/common, BUILD.bazel, Cargo.toml, nix/default.nix, nix/tests/default.nix, README, integration/README. Ledger rows honored: applied findings (#G1-#G6, #G8, #G12-#G20, #G98, #G99) verified as landed (guest_local.rs is 180 lines; metrics/audit modules gone; runner-contract structs gone); refused rows (#G7, #G10, #G11) left refused; not-applied rows (#G9 BUILD tail, #C1 dup) re-verified still present. Every zero-caller claim in this lane was verified workspace-wide by grep over packages/ `*.rs` plus BUILD.bazel, Cargo.toml, nix/, nixos-modules/, and docs/ (method: `grep -rn "<item>" packages/ --include="*.rs"` then in-crate and cross-crate hit review; d2bd call sites read directly for the controller, registration, descriptor, endpoint, and status surfaces). Scout false positives caught and rejected: `GUEST_SEED_RESOURCE_TYPES` is live (d2bd/resource_runtime.rs:1902), `CLOUD_HYPERVISOR_REPAIR_INTERVAL_SECS` was deliberately kept by #G99, `GuestRuntimeStatus` fields are serde wire output read by d2bd (resource_plane_v3.rs:4354), and test-only accessors with d2bd test consumers (`GuestDependencySnapshot::ready`, `GuestStatusProjection::status`, `CloudHypervisorReconcileOutcome::{is_pending,status}`, `OwnedChildSnapshot::with_desired_lifecycle`) were left alone as cross-crate test surface.