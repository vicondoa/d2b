# U78 d2b-provider-zone
Lean already. Ship.

Checked: crate is 223 LOC (lib.rs 21, driver.rs 21, zone_status.rs 181) plus policy-required scaffolds (integration/zone.rs + README, tests/registration.rs). The driver is the shared declaration-only metadata driver (d2b-resource-runtime metadata, ledger #P1/#A1 applied) — 21 lines, nothing to cut. zone_status.rs emitter has live callers in d2bd/src/resource_runtime.rs (SystemCoreStatusEmitter, ZoneStatusInput, ZoneRuntimeMetadata all referenced there); its emit_handler_status helper (zone_status.rs:7-24) is called by the live emitter at zone_status.rs:149 — ledger #P8 [refused] stays (no new evidence; deleting would change the status a live emitter publishes). Ledger #P9 [refused] integration/README scaffolds honored (crate-layout policy). #P5 [partial] zone-side emitter stays with its live callers. No dead code, no hand-rolled stdlib, no duplication beyond the shared driver the ledger already consolidated. Zero-caller sweep: zone_descriptor called from d2bd/src/foundation_seed.rs, d2bd/src/resource_plane_v3.rs, packages/xtask/src/resource_type_authority.rs; emitter types from d2bd/src/resource_runtime.rs.