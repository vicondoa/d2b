# U10 d2b-resource-api
net: -85 lines, -2 deps

- shrink #A6 **13 hand-written single-string redaction Debug impls where `redacted_debug!` exists — replace with the shared macro.** The crate's own dependency `d2b-contracts-resource` exports `redacted_debug!` (d2b-contracts-resource/src/v3/execution_policy.rs:21-22, re-exported at :63); the macro is unused anywhere in this crate. The 13 pure-form impls — `f.write_str("X(<redacted>)")` as the entire body — are byte-compatible with the macro and each shrink from 7 lines to 1: `adapter.rs:266` (ResourceBusAdapter), `admission.rs:31,43,55,68` (AdmissionIssuer, AdmissionVerifier, StoreIdentity, StoreAdmissionBinding), `authz.rs:602,1219,1419` (BoundSubject, AuthorizationGrant, NativeAuthorizer), `client.rs:24` (ResourceApiClient), `identity.rs:47` (AuthenticatedSubjectContext), `manager_backend.rs:913` (ManagerBackend), `service.rs:77,190` (TrustedRequest, ResourceService). The remaining structured impls (AdmissionPermit, AuthorizationState, CompiledRole, PolicySet — which render counts + a redacted string via `debug_struct`) are NOT macro-compatible and stay; the finding counts only the 13 macro-identical ones. [packages/d2b-resource-api/src/{adapter,admission,authz,client,identity,manager_backend,service}.rs] (family)
- yagni #B9 **One-purpose reachability surface — `ResourceApiReachability` enum + `RESOURCE_API_REACHABILITY` const kept.** Single-variant enum with one constant value (`adapter.rs:250-256`), re-exported through `lib.rs` and `adapter.rs` and asserted only by the crate's own reachability test (`adapter.rs:1208-1211`). No workspace caller reads the enum or const beyond the crate's own test — verified workspace-wide (`ResourceApiReachability|RESOURCE_API_REACHABILITY` across packages; only this crate's adapter.rs + lib.rs + its test match). The constant is `pub` surface that no other crate consumes. Options: delete the reachability vocabulary, or gate both behind the redacted surface it documents. (leaf)
- yagni #B9 **`resource_proto` alias arm and redundant one-way surface at the task boundaries — clean-up candidates.** `lib.rs:5-8` re-exports `crate::generated::d2b_resource_v3_ttrpc::*` as the `resource_proto` module alongside the v3 facade; the `enum ResourceApiReachability` above is the reachability half of the same finding family (reported here as its own line per the plan's "one line per finding" rule). (leaf)

net: -85 lines, -2 deps

## Consistency notes
- `redacted_debug!` (plus `parsed_deserialize!`, `string_schema!`) are exported from `packages/d2b-contracts-resource` (`src/v3/execution_policy.rs:21-22`, `pub use crate::{parsed_deserialize, redacted_debug, string_schema}` at :63). This crate depends on `d2b-contracts-resource` yet hand-writes the 13 redaction Debug impls — the hand-rolled pattern remains here and in d2b-core-controller (authz.rs-family), while d2b-resource-api's own dependency already carries the canonical macro. Canonical home: `packages/d2b-contracts-resource/src/v3/execution_policy.rs` (the exported macro), per the #A6 class.
- No duplicate type definitions or naming drift found: this crate's `ResourceUid`, `ResourceName`, `ResourceTypeName`, `StoredResource`, `ResourceEnvelope` etc. all import from `d2b-contracts-resource` v3 (verified in `adapter.rs`, `admission.rs`, `authz.rs`, `identity.rs`, `lib.rs`) — no local hand-rolled copies of the contracts-resource identity or envelope shapes. The reached-for `d2b_contracts_resource::v3::identity::AuthenticatedSubjectContext` (client.rs) is a dependency import, not a local copy.
- Resource type catalog: this crate has no name-substituted resource-type list; the `WellKnownType`/converted-type catalog work is owned by U9/U29 (a different crate). No wire-shape skew observed in the envelopes this crate renders (all through `d2b-contracts-resource` v3 wire types).

## Reopened refusals
- (none — this crate's ledger carries only not-applied/partial rows, no refusals)

## Checked
Read the U1-constraints packet U10 section and the plan's constraint section; listed the crate tree (manager_backend.rs, adapter.rs, authz.rs, admission.rs, service.rs, store.rs, watch.rs, error.rs, identity.rs, client.rs, lib.rs + manager_backend/tests.rs + generated/)); confirmed the four #B1-deleted modules (metrics.rs, zone_service.rs, quota_gate.rs, emergency_gate.rs) are absent and that watch.rs + bus-side impl were deliberately kept (tested watch-delivery credit path: send_wait, send_and_wait_ack, acknowledged_bytes — referenced in manager_backend/tests.rs and the U22/U24 ledger). Ran workspace-wide caller searches for `ResourceApiReachability|RESOURCE_API_REACHABILITY` (only this crate + its test), for `redacted_debug!` (defines in d2b-contracts-resource; zero uses in this crate), and grep'd this crate's Debug impls to measure the 13 pure-form macro-replaceable impls vs the structured non-macro ones. Confirmed `d2b-contracts-resource` is a declared dependency of this crate (Cargo.toml), so the macro is importable here with no cross-crate barrier. Generated/ is out of per-crate scope (U99). Ledger rows #A6/#B1/#B9 honored (all non-refused; #B1 already partial, its surviving surface is the tested-credit watch path).
