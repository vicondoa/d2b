# U53 d2b-provider-credential-secret-service
net: -50 lines, -0 deps

- shrink deadline/absolute-unix-ms helper trio triplicated across the credential family — `ABSOLUTE_UNIX_MS_THRESHOLD` + `now_unix_ms` + `is_absolute_unix_ms` + `operation_deadline` + `deadline_remaining` (lib.rs:49,1501-1527,1596-1603; ~35 lines) are byte-identical to the entra twin (lib.rs:49,1165-1212) and managed-identity twin (lib.rs:60,938-952,1124-1130); the dossier (§7.2 ABSOLUTE_UNIX_MS_THRESHOLD/operation_deadline invariants) pins the *semantics*, not the helper location; the PR1 shared home `d2b-provider-toolkit/src/credential.rs` owns the three crates' common credential machinery but has no time helper (toolkit carries only the `Clock` trait + `DeterministicClock` test clock). Fold the trio into the shared toolkit credential module and call it from all three crates; keep the per-crate `SecretServiceCredentialProvider::operation_deadline` wrapper that preserves the crate's own error type. [packages/d2b-provider-credential-secret-service/src/lib.rs:49,1501-1527,1596-1603] (family)
- shrink `reject_process_environment_credential_chain` env-scan body duplicated across the credential family — lib.rs:52-66 (~15 lines) is the same `std::env::vars_os` scan + shared-contract call as entra (lib.rs:52-65) and managed-identity (lib.rs:63-76); the shared core already exists in `d2b-contracts-provider::v3::credential_controller::reject_ambient_credential_chain`, so the per-crate copy is only the error-type mapping. Move the env-scan to the toolkit credential module; keep the one-line crate-specific error wrapper (dossier-pinned startup guard). [packages/d2b-provider-credential-secret-service/src/lib.rs:52-66] (family)

## Checked
Read all src files (lib.rs 2192, service.rs 910, controller.rs 476, audit.rs 69, telemetry.rs 910, main.rs 5) and tests/ (canary, lifecycle, session, faults, conformance, delivery, faults, canary, placement, session, faults, lifecycle, session, faults, lifecycle, conformance, common, faults, placement, delivery, faults, lifecycle, faults, delivery, session, lifecycle, faults, delivery, lifecycle, canary, README), Cargo.toml, BUILD.bazel, README.md, integration/. Workspace-wide caller searches (grep across packages/, nixos-modules/, docs/, tests/, xtask BUILD records): `PROVIDER_REF`/`BACKEND_REF`/`PROVIDER_KIND`/`PROVIDER_REVOKE_FINALIZER` have live readers in d2b-provider-credential/src/{lib.rs:64,70, session.rs:288-289,352-353} and are pinned by the dossier and ADR-046 dossier row; `run_from_fd10`/`run_from_fd10` is the binary entry consumed by main.rs + d2bd BUILD.bazel deps; `SecretServiceController`/`SecretServiceStatusProjection`/`SecretServiceOwner`/`SecretServicePlacement`/`LockPolicy`/`SecretServiceConfig`/`Oo7SecretServicePort`/`SecretServiceCredentialProvider` et al. are pinned by the dossier dossier (ADR-046-provider-credential-secret-service.md) and its file-inventory/work-item rows; deadline/absolute-unix-ms helper trio confirmed live (deadline_remaining/operation_deadline/now_unix_ms called only within crate) and is the one family-triplicated small item; env-chain wrapper confirmed folded-able family-wide since shared contract call exists in pr (d2b-contracts-provider v3). Ledger honored: U53 dossier rows PR1/PR2/PR3 all applied/kept (deadline observation authority kept, session-capability authority kept, entra sync twins deleted) — no refused rows, no reopened refusals. `src/generated/`, integration tools, and dossier rows are out of per-crate scope. No new evidence to reopen any prior refusal.
