# U3 d2b-contracts

net: -785 lines, -0 deps

- delete src/opaque_payload.rs (135) + src/constellation_error.rs (358) + their lib.rs mod/pub-use arms (4). Zero consumers anywhere: d2b-realm-core carries byte-identical live copies (payload.rs, error.rs) and nothing imports `d2b_contracts::{OpaquePayload, ConstellationError}`. If U2 consolidates realm-core to re-export d2b-contracts (canonical home), this cut is reversed - see Consistency notes. [packages/d2b-contracts/src/opaque_payload.rs, packages/d2b-contracts/src/constellation_error.rs] (family)
- delete W3BrokerOperation::flags() (69 lines, 24-arm match) + W3OperationFlags struct (10) + the three tests that only pin them (26). Only consumers are the crate's own tests; production uses `all()`/`wire_tag()` only (d2b-broker/src/catalog.rs:742, d2b-contracts-broker/src/lib.rs:29), and the broker's audit/destructive/secret_access posture lives in its own generated catalog rows (d2b-broker/src/generated/broker_operation_catalog.rs). Keep `wire_tags_are_unique_pascalcase`. [packages/d2b-contracts/src/privileges_w3.rs] (leaf)
- delete src/store_verify_wire.rs (55) + lib.rs mod arm (1). Zero Rust consumers of StoreVerifyRequest/Response/Status/UnknownReason; the broker op it described does not exist in d2b-broker (docs/ADR-0027 and nixos-modules/privileges-json.nix:1547 are stale; d2b-core's BROKER_OPERATION_AUTHZ has no StoreVerify row). [packages/d2b-contracts/src/store_verify_wire.rs] (leaf)
- delete UsbipClaimSource (29) + UsbipDaemonClaimRecord struct+impl (25) except `lock_path_for_busid`, which becomes a free fn (4). The record is never constructed; only `lock_path_for_busid` is called (d2b-broker/src/runtime.rs:4574). The live copy of UsbipClaimSource is d2b-provider-device-usbip/src/state_machine.rs:100 (byte-identical). [packages/d2b-contracts/src/usbip.rs] (leaf)
- delete negotiate_hello (34) + the handshake test that only exercises it (31). Zero production callers; d2bd inlines the same negotiation (d2bd/src/composition.rs:5417-5466). Keep `retired_unsafe_local_shell_feature_is_not_negotiated` (tests live FeatureFlag::known). [packages/d2b-contracts/src/lib.rs] (leaf)
- delete WorkloadSelector (12) + the re-export arm at d2b-realm-core/src/lib.rs:153 (U2 lane file, one line). Zero callers; ADR-046-current-code-migration-map.md:431 marks it dead-reachable with no production caller. [packages/d2b-contracts/src/workload.rs] (family)

## Consistency notes

- **RealmControllerPlacement declared twice in the same crate with different wire shapes**: realm.rs:26 (`#[serde(tag="kind", rename_all="kebab-case", deny_unknown_fields)]`, 3 unit + 3 payload variants) vs controller_config.rs:256 (plain `rename_all="kebab-case"`, 6 unit variants). realm-controllers.json (live: d2bd-runtime, xtask schema) uses the controller_config copy; d2b-realm-core access/registry/route DTOs use the realm.rs copy. Canonical home: realm.rs (richer); consolidation is a wire-shape change, out of scope.
- **OpaquePayload duplicated byte-identically**: d2b-contracts/src/opaque_payload.rs vs d2b-realm-core/src/payload.rs (same doc, impls, tests, line numbers). Canonical home: d2b-contracts (see finding 1).
- **ConstellationError + ErrorKind duplicated byte-identically**: d2b-contracts/src/constellation_error.rs vs d2b-realm-core/src/error.rs. Canonical home: d2b-contracts (see finding 1).
- **RealmIdentityConfigJson/Summary/validate_metadata_only duplicated byte-identically**: d2b-contracts/src/identity_config.rs vs d2b-realm-core/src/identity_config.rs. Live consumers (d2bd-runtime/src/daemon_config.rs:10, xtask/src/main.rs:16) use the d2b-contracts copy. Canonical home: d2b-contracts.
- **SecurityKeySessionId declared three times with different shapes**: d2b-contracts/src/security_key.rs:50 (transparent String), d2b-provider-device-security-key/src/lease.rs:13 (`[u8;16]`), d2b-contracts-control UsbSkSession (String `session_id`). Wire-shape skew across the feature's three boundaries.
- **UsbipClaimSource byte-identical double**: d2b-contracts/src/usbip.rs:99 vs d2b-provider-device-usbip/src/state_machine.rs:100 (see finding 4).
- **v3/ifname.rs canonical home is d2b-contracts** (re-exported by d2b-contracts-resource/src/v3/mod.rs:41-43, consumed by d2b-broker/d2b-core). d2b-host/src/ifname.rs is a drifted near-duplicate (fnv crate vs hand-rolled FNV-1a, different IfNameError shapes) - U91 lane.
- **TargetName/LegacyNodeQualifiedTarget/THIS_NODE_ALIAS/TargetParseError** kept per #B4: only references are the d2b-realm-core/src/lib.rs:144-146 re-export (never used there). No new evidence; stays.
- **WorkloadSelector doc drift**: ADR-046-current-code-migration-map.md:431 documents variants `All | ByTarget`; code has `All | One(WorkloadId)` (see finding 6).
- **d2b-contracts-control/src/public_wire.rs:2204 carries a local validate_audit_page** duplicating d2b-contracts/src/audit_wire.rs:52 - U5 lane.
- **security_key.rs DTOs are declared-but-unproduced wire surface**: broker_wire variants SecurityKeyOpenDevice/SecurityKeyApplyUdevRules are typed stubs never constructed; d2bd uses provider-crate types (SkSessionTable etc.). Pinned by the contract enums + generated daemon-api.md table; not cut here.

## Reopened refusals

None. #B4 kept items (target.rs node-era compat types, refuse_wrong_plane/refuse_legacy_subject test-only wrappers) re-verified - still only the d2b-realm-core re-export references the compat types, and the two `#[cfg(test)]` wrappers remain pinned by their own tests; no new evidence.

## Checked

Read all 31 src files (structural + targeted ranges), src/generated/* (evidence only), src/v3/mod.rs, Cargo.toml, BUILD.bazel, tests/fixtures. Ledger honored: #A2/#A9 applied (V3_CONVERTED_RESOURCE_TYPES is the single plane authority via `include!`; generated catalog gone); #B4 kept items honored (see Reopened); #C4 verified - the remaining hand-written `Deserialize` blocks in this crate are live admission gates (fail-closed decode: FeatureFlag, ConfiguredArgv, OpaqueAzureRef/CredentialLeaseHandle, OpaquePayload, ProtocolToken, label/digest identities, ResourceTypeName/ResourceUid/ResourceRef, RealmPath, TargetName/RealmTarget, CapabilitySet/CapabilityNegotiation, KeyFingerprint/RealmIdentityFingerprint, LevelPercent, IfName) and the identity macros (label_identity!, digest_identity!, id_newtype!, opaque_id!, contract_string!, opaque_credential_value!) already consolidate the boilerplate; the "facade copies" live in d2b-realm-core and feed the Consistency notes above. Caller searches (Rust + BUILD.bazel + nixos-modules + tests + docs/reference/policy): `StoreVerify*`, `WorkloadSelector`, `UsbipDaemonClaimRecord`/`UsbipClaimSource`, `W3OperationFlags`/`.flags()`, `negotiate_hello`, `OpaquePayload`/`ConstellationError`, `RealmControllerPlacement`, `TargetName`/`LegacyNodeQualifiedTarget`, `SecurityKey*`, `FailureKinds`, `Runtime*Capabilities`, `Realm*Json`, `validate_bus_id`/`sanitize_usb_hex_id`/`format_usb_hex_id`, `encode_frame`/`decode_frame`, `KnownFeatureFlag`, `MAX_FRAME_SIZE`/socket-path consts, `resource_plane`/`STANDARD_RESOURCE_TYPES` - all live surfaces confirmed against 60+ dependent crates. LOC measured with wc (11,119 src lines total).
## U4 execution (2026-09-24)
- F5 negotiate_hello + handshake test: APPLIED (zero production callers at HEAD; d2bd inlines negotiation).
- F6 WorkloadSelector: APPLIED (zero callers at HEAD; realm-core gone). ADR-046-current-code-migration-map.md:431 row corrected to "deleted".
- F1 opaque_payload/constellation_error: SKIP (reversed per U97 - realm-core deleted by U104; contracts copies are the only home and must stay).
- F2 flags()/W3OperationFlags, F3 store_verify_wire, F4 usbip twins: already applied at HEAD (pre-existing); no-op.
- daemon-api.md regenerated (gen-daemon-api) - dropped WorkloadSelector + StoreVerify + UsbipClaimSource rows.
