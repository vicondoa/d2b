# U90 d2b

- stdlib: hand-rolled FIPS-180-4 SHA-256 duplicating the crate's own shared helper. host_validate.rs re-implements sha256 from scratch (K-table + 8-round compression, 551-631) plus a local bare-hex `sha256_hex` wrapper (541-548) and the "Minimal in-tree SHA-256 (no extra crate dep)" comment (533-539), even though this crate already ships a tested, sha2-crate-backed `pub(crate) fn sha256_hex` at lib.rs:203 (returns the identical `sha256:`-prefixed string; the local wrapper returns bare hex that the one caller at 517 re-wraps with `format!("sha256:{}", ...)` - byte-identical output). Repoint line 517 to `crate::sha256_hex(input.as_bytes())` and delete lines 533-631 (comment + local wrapper + hand-rolled impl). Keep the two FIPS-180-4 known-vector tests (757-773) but point them at `crate::sha256_hex` (adjust expected value for the `sha256:` prefix) so the operator-signature label keeps its vector pin. [packages/d2b/src/host_validate.rs] (leaf)

net: -99 lines, -0 deps

## U2 execution (2026-09-24)

- applied: repointed host_validate.rs:517 to `crate::sha256_hex(input.as_bytes())`; deleted lines 533-631 (comment + local bare-hex wrapper + hand-rolled FIPS impl). Both FIPS-180-4 known-vector tests kept, re-pointed at `crate::sha256_hex` with `sha256:`-prefixed expectations ("sha256:ba7816…", "sha256:e3b0c4…"). Verified passing at HEAD (cargo test -p d2b --lib sha256_known_vector: 2 passed). -99 lines.
