# U82 d2b-provider-role-binding

net: -0 lines, -1 deps

- yagni `tokio` as a normal dependency: only the policy-required registration test uses it (`#[tokio::test]` in tests/registration.rs; zero `tokio` references in src/ - the crate's own README admits "tokio for its registration test"), and its `sync`/`time` features are unused. Move to `[dev-dependencies]` trimmed to `macros` + `rt-multi-thread` (the default multi-thread `#[tokio::test]` flavor); the bazel registration target already pulls `normal_dev` deps and the `test_support`/library targets need no tokio (precedent G85, U89's serde_json finding). [packages/d2b-provider-role-binding/Cargo.toml] (leaf)

## Checked

Read every crate file (src/lib.rs, src/driver.rs, tests/registration.rs, integration/role-binding.rs, integration/README.md, Cargo.toml, BUILD.bazel, README.md, resource-types.json) and the U1 packet's U82 ledger. Ledger honored: #P1/#A1 applied shape confirmed (driver.rs is the 20-line `metadata_descriptor(WellKnownType::ROLE_BINDING)` floor); #P2 applied shape confirmed (tests/registration.rs calls the shared `assert_metadata_registration`); #P10 applied confirmed (Cargo.toml carries no d2b-contracts-resource; only d2b-resource-types + tokio remain); #P9 refused scaffolds untouched - integration/*.rs + README are policy-required (xtask/src/provider_crate_policy.rs:9048 commits role-binding as a Provider-class crate; no new evidence the ratchet changed). Caller verification (workspace-wide grep over *.rs, BUILD.bazel, nixos-modules/, tests/, docs/reference/): live consumers are d2bd/src/foundation_seed.rs:1111 and d2bd/src/resource_plane_v3.rs:150 (register `role_binding_descriptor`), d2bd/BUILD.bazel:63,176 (library) and :249 (`d2b_provider_role_binding_test_support` - so the empty `test-support` feature and its bazel target are wired, not dead), and xtask's resource_type_authority reads resource-types.json as the per-crate declaration (policy layout doc). No dead code, no hand-rolled stdlib, no unused flexibility beyond the tokio placement; the crate sits at the post-fold metadata-crate floor.