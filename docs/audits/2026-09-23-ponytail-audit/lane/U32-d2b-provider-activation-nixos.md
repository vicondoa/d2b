# U32 d2b-provider-activation-nixos
net: 0 lines, 0 deps

- `shrink` (refused) `RunnerRequest.runner_name` - write-only in production (set at controller.rs:786, read only by tests/reconcile.rs:52,122). Refused: the dossier commits the deterministic runner child-name vocabulary derived from the qualified generation reference (ADR-046 §OKD13 runner plating; "the deterministic child resource name, derived from the generation"), the driver re-derives the same name (driver.rs:1768,1782-1785 via `activation_runner_ref`), and the field carries exactly that committed vocabulary - a dossier-pinned wire duplicate, same refused class as the pinned wire-field refusals in the ledger. [packages/d2b-provider-activation-nixos/src/controller.rs:786] (leaf)
- `delete` (refused) `SignedActivationApplicationVerifier` + trust-epoch/revocation/Ed25519/digest machinery - production binds `FailClosedActivationVerifier` (driver.rs:1543 `Arc::new(crate::FailClosedActivationVerifier)`); the signed surface is constructed only by tests (reconcile.rs:496). Refused: the dossier commits the fail-closed trust epoch/revocation-reference/Ed25519/signature/artifact-digest vocabulary as ADR-047's preserved trust surface, and the signed verifier is the committed declared-provider trust seam of the same refused class (#S/#B refused rows). [packages/d2b-provider-activation-nixos/src/controller.rs:209] (leaf)
- `caller-role` (refused) `CallerRole::{Lifecycle,User,Admin,Provider}` variants - only `Lifecycle` is constructed in production (driver.rs:805 `RunnerCaller::new(CallerRole::Lifecycle, …)`); User/Admin/Provider appear only in tests. Refused: the caller-role vocabulary is the dossier's committed activation-caller admission surface (ADR-046 caller-role plane; "authenticated caller role"), same class as the refused aptitude-constants ledger rows. [packages/d2b-provider-activation-nixos/src/controller.rs:13-16] (leaf)

## Checked
Read all seven src files in full (controller.rs 858, driver.rs 1855, effects_service.rs 356, facets.rs 50, lib.rs 47, test_support.rs 115, vocabulary.rs 106, test_support 115) + both test binaries (reconcile.rs 512, registration.rs 134); workspace-wide caller census for every exported symbol (RunnerRequest/RunnerResult/RunnerRequest fields, `declared_runner_step`, `is_declared_runner_step`, `ACTIVATION_RUNNER_STEPS`, `SignedActivationApplicationVerifier`, `FailClosedActivationVerifier`, trust vocabulary) over packages/, nix/, tests/, docs/specs/, docs/reference/, dossier ADR-046/ADR-047. The prior ledger row #PR8 [applied] already deleted the three orphan subsystems (runner.rs, diagnostics/, manifest.rs, RetentionPlan surface, tests/runner.rs, BUILD target). Everything remaining is either headed by a production caller or refused as dossier-pinned vocabulary. Lean already; no further surface to cut.

net: 0 lines, 0 deps - checked, lean, ship.
