# U15 d2b-resource-compiler
net: 0 lines, 0 deps (refuse #C7: hand-rolled JSON-Schema walker + secret-shape scanners are -800/-68 admission-gate lines; no importable shared validator exists)

- #C7 [refused] the hand-rolled JSON-Schema docs walker (`validate_schema_document` :1151, `validate_schema_node_with_budget` :1201 [296 lines], `validate_schema_from_root` :1692, `validate_schema_root` :1710 [241 lines]; walker family ≈ 800 lines spanning main.rs:1151-1950) and the secret-shape scanners (`contains_secret_shape` :2035, `contains_volume_secret_shape` :2046, `contains_volume_spec_secret_shape` :2061, `contains_secret_shape_at` :2090; ≈ 68 lines). These are the crate's live admission gates: `validate_resources` (main.rs:699) → `validate_schema`/walkers runs on every committed schema document at compile time, and the secret-shape scanners gate the strict-secrets admission path (main.rs:793, main.rs:824). Refused on verified workspace-wide evidence: zero importable shared JSON-Schema validator exists — `Cargo.lock` declares no `jsonschema`/`valico`/`schemars`-as-validator crate (checked; only `schemars` derive-time *schema generation*, not runtime validation), zero Cargo.toml imports one, and rg across all `packages/*/src` finds no importable shared value-vs-schema validator fn (the only other walkers, in xtask's `semantic_service_schemas.rs`/`zone_schema.rs`, are schema *generators*, not validators). Same refusal class as prior ledger rows (#S5 built-in-vs-stdlib, the secret-shape-scan refusals in the guest/zone family). [packages/d2b-resource-compiler/src/main.rs] (leaf, refused)
- #B7 [applied - no action] `compile_provider_artifact` alias already deleted in this lane (ledger B7); the canonical `compile_artifact` it forwarded to stays with its tests. Re-verified: no `compile_provider_artifact` occurrence crate-wide at HEAD, tests intact. Not re-flagged (already [applied]). [packages/d2b-resource-compiler/src/lib.rs] (leaf, already applied)

## Consistency notes
(Not applicable: d2b-resource-compiler is a *compiler* crate, not a contracts/types-layer crate. The only hand-rolled value-vs-schema admission-gate duplication class is #C7, refused above. No duplicate type definitions, naming drift, or wire-shape skew live in this lane.)

## Reopened refusals
(No [refused] ledger items were reopened: #C7 was not previously refused - it was reported [not applied] as a live finding. This pass re-adjudicates it to [refused] on the newly-verified no-importable-shared-validator evidence, citing the same class as the workspace's own prior refusals. No prior [refused] item was reopened.)

## Checked
Read full crates (lib.rs 2505, main.rs 2562, linux.rs 447). Verified every prior ledger row in U1 U15 section: #B7 [applied] confirmed (alias gone at HEAD, no tombstone residue); #C7 re-measured (~800 walker + ~68 scanner lines, live admission gates exercised by `validate_resources` which has production callers at compile). Searches run for dead/zero-caller claims: removed the four-name alias surface. Seeded a fresh sweep — no dedupable one-purpose surface remains beyond what #C7/refused refusals already cover; the linux.rs anchored-dir adapter stays (production Linux admission path per U1 #S1 refusal/ADR 0044). No new findings beyond the refused validator class; no policy scaffolds deleted; no workspace-wide caller-count regressions. Net 0 lines, 0 deps — the only speculative-cutting target here is the hand-rolled validator which is a live admission gate with no importable shared replacement, so refusal is the faithful disposition (matches the crate's own long-standing refusal posture documented in the audit record #C7).
