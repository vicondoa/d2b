# U84 d2b-provider-emergency-policy
Lean already. Ship.
- Checked: driver.rs is a 20-line declaration-only wrapper over the shared metadata_descriptor (the #P1 consolidation target); lib.rs re-exports it; tests/registration.rs is one assert_metadata_registration call (#P2's shared assert). Zero-caller claims: emergency_policy_descriptor has a live consumer at packages/d2bd/src/resource_plane_v3.rs:141 (family_declaration("emergency-policy") + descriptor), and d2bd/BUILD.bazel links both the library and the test_support target — so no dead surface to cut. integration/emergency-policy.rs + integration/README.md + README.md are the #P9 policy-required scaffolds (refused). resource-types.json is the copied declaration of the category the crate owns.
## Consistency notes
- Sibling of nine other declaration-only metadata provider crates (role, role-binding, quota, resource-export, resource-import, display-wayland, endpoint, guest, command); all ten share the single metadata_descriptor driver from d2b-resource-runtime (U1 #P1) and the shared assert_metadata_registration (#P2). The declaration-only `test-support` feature is empty across the family (no cfg-gated code in any crate) — consistent, not a per-crate divergence.
## Checked
- Read src/lib.rs + src/driver.rs + tests/registration.rs + BUILD.bazel + Cargo.toml + resource-types.json + integration/*. Ledger rows honored: #P1/#P2 already applied (crate is now the shared-driver shape), #P9 refused (policy-required scaffold, not flagged). Caller verification via grep across workspace (`emergency_policy_descriptor`, `d2b_provider_emergency_policy_test_support`) plus d2bd resource_plane_v3.rs:141 and d2bd/BUILD.bazel deps list. No new evidence contradicting any ledger row; no prior [refused] item reopened.
