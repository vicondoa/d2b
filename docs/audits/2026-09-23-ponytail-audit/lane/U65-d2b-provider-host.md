# U65 d2b-provider-host

net: -24 lines, -0 deps

- shrink Usbip capability arm builds the same `/sys/module/<name>` path twice through `format!` — one `any()` over `[USBIP_CORE_MODULE, USBIP_HOST_MODULE]` (12 lines -> 4). [packages/d2b-provider-host/src/probe.rs:190-201] (leaf)
- shrink `has_render_node` / `has_primary_drm_node` are the same `/dev/dri` scan differing only in the name prefix — one `has_dri_node(prefix: &str)` called with `"renderD"` / `"card"` (20 lines -> 13). [packages/d2b-provider-host/src/probe.rs:118-138] (leaf)
- delete seven lib.rs re-export arms with zero external consumers — `HostDriver`, `HostDriverEffects`, `HostDriverError`, `HostDriverFactory`, `HostDriverStatus`, `host_spec_decoder`, `HostEffectsService` are used only inside the crate (driver/effects/tests); keep `host_descriptor`, `HOST_EFFECTS_SERVICE`, `HostEffectsServiceFactory` (7 lines -> 2). Search: grep of each symbol across packages/ (all *.rs + BUILD.bazel), tests/, nixos-modules/, docs/, bazel/ — only in-crate hits; d2bd names only `host_descriptor`, `HOST_EFFECTS_SERVICE`, `HostEffectsServiceFactory`, `HostEffectFacets`, `MinijailPlatformGateSource`, `MinijailPlatformGate`, `production_probe`, `PIPEWIRE_RUNTIME_SOCKET`, `USBIP_*_MODULE`, `test_support::{recording_facets, RecordingMinijailGate}`. [packages/d2b-provider-host/src/lib.rs:43-49] (leaf)
- delete `RecordingMinijailGate::set` — no caller anywhere (every use constructs the double with `new` and never re-scripts the gate); drop the method and its doc (4 lines). Search: `RecordingMinijailGate` + `.set(` across packages/d2b-provider-host and packages/d2bd — only `new` sites. [packages/d2b-provider-host/src/test_support.rs:183-186] (leaf)

## Checked

Read all of src/{lib,driver,effects_service,facets,probe,test_support}.rs, tests/registration.rs, BUILD.bazel, Cargo.toml, README.md, integration/{host.rs,README.md}, registrations.json, resource-types.json. Searches: `d2b_provider_host::` across packages/ (consumers: d2bd only — process_provider_runtime.rs:59,169-196; provider_lifecycle.rs:43; resource_plane_v3.rs:64,2259-2260,3762-3766,4255-4266; shared_provider_effects.rs:3308-3311,3443-3446), each public symbol across packages/ + tests/ + nixos-modules/ + docs/ + bazel/, `VERBS`/`allow(dead_code)`/`todo!` in-crate. Ledger honored: #S2 [applied] verified — `verbs: CONVERTED_TYPE_VERBS` at driver.rs:459, no local verb list survives; #S6 [refused] not reopened — host/user driver merge still blocked by the same structural facts (toolkit is the only shared home and is off-limits; host-to-user edge is a new cross-family dependency), no caller or tree change since. Exclusions kept: integration/host.rs + README (policy-required scaffold), registrations.json (policy artifact), tests/registration.rs (policy-pinned suite). Deps all live (nix for `Uid::current`, serde_json, tokio fs/sync, async-trait, five workspace deps). Remaining test doubles (RecordingManager/RecordingRequeue inline in driver.rs tests) are the cross-crate shared-double consolidation the 09-18 residual-scaffolding plan already scoped — left to U98, not re-flagged here.