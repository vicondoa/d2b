# U94 d2b-sk-frontend

net: -54 lines, -0 deps

- delete: three tautological/self-pin tests in uhid.rs - `uhid_event_type_values_match_kernel_uapi` (11 lines: re-asserts each `UHID_*` const against the same literal it is defined with two lines above; the kernel-UAPI pin is the const declaration + its `include/uapi/linux/uhid.h` comment), `fido_descriptor_is_valid_length` (4 lines: asserts `FIDO_HID_DESCRIPTOR.len() == 34` against the descriptor defined in the same file), `short_lifecycle_event_is_zero_extended` (10 lines: writes `UHID_START` into a buffer, reads the same 4 bytes back, asserts they match - never calls `read_event`, tests nothing its name claims). Delete the three tests; the 11 real layout/redaction tests keep the byte-exact coverage. [packages/d2b-sk-frontend/src/uhid.rs:372-382,515-518,520-529] (leaf)
- delete: write-only `UhidEvent` fields - `Output._rtype`, `GetReport.rtype`, `GetReport.rnum` plus their parse lines (`payload[4098]`, `payload[4]`, `payload[5]`) and doc rows. Only reader of `GetReport` is `SecurityKeyFrontend`'s loop with `GetReport { id, .. }` (agent.rs) and the Debug impl (id only); `_rtype` is underscore-declared and never read. Search method: `grep rtype|rnum` over packages/d2b-sk-frontend - hits are only the declarations, parse sites, and the redaction test's construction (that construction drops one line too). Kernel wire stays untouched: the bytes are still skipped, just not recorded. [packages/d2b-sk-frontend/src/uhid.rs:113-114,124-127,200,210-211,203,214-215,507] (leaf)
- delete: `VsockAllocatorLink::host()` - zero production callers; its only consumer is the crate's own test `the_default_endpoint_is_the_hypervisor_host`, which pins `VSOCK_HOST_CID == 2` (a tokio-vsock re-export) and `SK_VSOCK_PORT == 14320` (already cross-pinned in nixos-modules/lib.nix:611 `securityKeyVsockPort`). config.rs defaults via the `VSOCK_HOST_CID`/`SK_VSOCK_PORT` consts directly; main.rs uses `new()`. Delete `host()` + the test. Search method: workspace grep `VsockAllocatorLink::host|AllocatorLink::host|\.host\(` over packages/, nixos-modules/, tests/, docs/reference/policy/ - only hit is link.rs:69 (other `.host()` hits are unrelated types in clipboard-wayland, resource-runtime, d2bd). [packages/d2b-sk-frontend/src/link.rs:33-36,66-73] (leaf)
- shrink: main.rs's two identical fatal-exit matches - `Config::from_env()` and `placement.clone().into_placement()` each repeat `match ... { Ok(v) => v, Err(e) => { eprintln!("[d2b-sk-frontend] fatal: {e}"); exit(1) } }` (16 lines). One `fn exit_on_error<T, E: Display>(Result<T, E>) -> T` helper (10 lines) + two call rows (2 lines) = -4. [packages/d2b-sk-frontend/src/main.rs:43-58] (leaf)

## Checked
- Read all of src/ (lib.rs 29, main.rs 70, agent.rs 194, config.rs 226, link.rs 74, uhid.rs 530 = 1,123 LOC, matches plan U94), Cargo.toml, BUILD.bazel; glob confirms no tests/, integration/, README.
- Searches: `d2b_sk_frontend|HidDevice|VsockAllocatorLink|SecurityKeyFrontend` over packages/ (only this crate + d2b-zone-routing test consumer + unrelated daemon `ProcessRole::SecurityKeyFrontend` enum hits); `sk-frontend|sk_frontend|security-key-guest|D2B_SK_` over nixos-modules/, docs/reference/policy/, tests/, packages/xtask; `VsockAllocatorLink::host` and `rtype|rnum` as above.
- Refuted candidate: `HidDevice` trait + `SecurityKeyFrontend::new()` are LIVE - d2b-zone-routing/tests/guest_enrollment.rs:210,422 implements `d2b_sk_frontend::HidDevice` for `FakeHidDevice` and drives the full guest lifecycle through `new()`; d2b-zone-routing/BUILD.bazel:74 carries the lib as a test dep. Not flagged (caller verification caught the #1 false-positive class).
- Ledger honored: no prior findings for U94; no policy scaffolds (crate is `CommittedScopeClass::Shared` at packages/xtask/src/provider_crate_policy.rs:8884 - no README/integration ratchet applies); no src/generated; ADR-046 dossier pins the crate "implemented-and-reachable" (live guest binary - no existential finding); dossier prose about obsolete `framing.rs`/`vsock.rs` refers to files already absent (current link.rs replaces them).
- Deps all live (async-trait, contracts-resource, contracts-zone-session, toolkit, session, session-unix, libc, rustix, tokio, tokio-vsock each have call sites).
## U2 execution (2026-09-24)

- re-verified `VsockAllocatorLink::host` zero callers at HEAD (only its own test; config.rs/main.rs use consts/`new()`).
- applied: deleted three tautological/self-pin tests in uhid.rs (uhid_event_type_values_match_kernel_uapi, fido_descriptor_is_valid_length, short_lifecycle_event_is_zero_extended).
- applied: deleted write-only fields Output._rtype / GetReport.rtype / GetReport.rnum + parse lines + doc rows + redaction-test construction row; kernel wire bytes still skipped, just not recorded.
- applied: deleted `VsockAllocatorLink::host()` + the_default_endpoint_is_the_hypervisor_host test (VSOCK_HOST_CID/SK_VSOCK_PORT consts stay, cross-pinned in lib.nix).
- applied: main.rs two fatal-exit matches shrunk to `exit_on_error` helper.
- tests: cargo test -p d2b-sk-frontend 13 passed; cargo check -p d2b-sk-frontend green.
