# U33 d2b-provider-config-nixos
net: -112 lines, -0 deps

- delete ConfigServiceDescriptor chain: the struct + `canonical()` + `validate()` (service.rs:296-329), `ConfigService::descriptor()` (controller.rs:279-282), `GuestConfigReader::descriptor()` (ttrpc.rs:74-77), the lib.rs re-export arm, and the `descriptor_is_closed_and_service_only` test that only re-asserts the chain (tests/service_contract.rs:20-33). No production or test caller outside the chain itself; the closed-method surface stays covered by `config_service_publishes_only_the_closed_typed_methods`, which asserts `create_ttrpc_services` directly. Same class as qemu finding G34. [packages/d2b-provider-config-nixos/src/service.rs] (leaf) - search: workspace grep `ConfigServiceDescriptor|GuestConfigReader::descriptor|ConfigService::descriptor` over packages/, nixos-modules/, bazel/, docs/reference/policy/; only crate-internal + the self-asserting test matched.
- delete `ConfigService::stage` (controller.rs:310-328). Zero callers; the live staging path is `ConfigStagingStore::stage` (d2bd/src/composition.rs:6160) and `validate_operation` already re-validates the Stage payload at admission. [packages/d2b-provider-config-nixos/src/controller.rs] (leaf) - search: workspace grep `ConfigService::stage|ConfigService\.stage`; no match outside the definition.
- delete `ConfigService::authorize_admin` (controller.rs:330-344). Zero callers; `authorize_host_operation` in service.rs does the real check. [packages/d2b-provider-config-nixos/src/controller.rs] (leaf) - search: workspace grep `authorize_admin`; only the definition matched (d2b-contracts-provider's `authorize_admin` is a different crate's symbol).
- delete `ConfigService::service_identity` (controller.rs:394-398). Zero callers; SERVICE_PACKAGE/SERVICE_NAME consts stay for ttrpc.rs. [packages/d2b-provider-config-nixos/src/controller.rs] (leaf) - search: workspace grep `service_identity\(\)`; d2bd matches are `session.service_identity()` on RegisteredInteractionSession, a different type.
- delete `GuestConfigDocument::is_empty` (controller.rs:147-151). Zero callers; `len()` stays, and `new()` already rejects empty bytes. [packages/d2b-provider-config-nixos/src/controller.rs] (leaf) - search: workspace grep `is_empty\(\)` in crate + consumers; only the definition matched.
- delete `GuestSessionEvidence::boot_identity()` (controller.rs:81-85). Zero callers; the field stays (bound at construction, rendered redacted in Debug). [packages/d2b-provider-config-nixos/src/controller.rs] (leaf) - search: workspace grep `boot_identity\(\)`; only the definition matched.
- delete `GuestSessionEvidence::reconnect_generation()` (controller.rs:86-90). Zero callers; the field stays (Debug renders it). [packages/d2b-provider-config-nixos/src/controller.rs] (leaf) - search: workspace grep `reconnect_generation\(\)`; d2bd/d2b-bus matches are other crates' session types.
- yagni `ConfigCaller::Lifecycle` and `ConfigCaller::User` variants (controller.rs:29-32). Never constructed in production - d2bd passes only `Admin` (composition.rs:6161-6241) and the guest read path only `Guest` (ttrpc.rs:113); both variants appear only in tests. Collapse `can_read`/`can_stage`/`authorize_host_operation` to the two live roles and rework the three test call sites (config_lifecycle.rs:42,74,220) onto Admin/Guest. [packages/d2b-provider-config-nixos/src/controller.rs] (leaf) - search: workspace grep `ConfigCaller::`; production matches are Admin (d2bd) and Guest (ttrpc.rs) only.

## Checked

Read all of src/{lib,controller,service,ttrpc}.rs (1,530 LOC), tests/{config_lifecycle,redaction,service_contract}.rs, Cargo.toml, BUILD.bazel, service-catalog.json; traced every public export to its consumers. Live surface confirmed: `ConfigNixosClient` + `decode_document` at d2bd/src/composition.rs:11571 (ledger #P6 cited 11508 - same finding, line shifted in tree 3f2664794; stays), `ConfigStagingStore` (composition.rs:578, stage/diff/approve/reject/status at 6154-6242), `GuestConfigReader` + `create_ttrpc_services` (composition.rs:4711-4720), all request/response DTOs + `GuestConfigDocument` + `GUEST_CONFIG_IDENTIFIER` (packages/d2b/src/activation.rs), `ConfigOperation`/`ConfigError::code()` (composition.rs error mapping). Ledger items honored; nothing reopened. Kept deliberately: `ConfigServiceBackend` trait (one production impl - the concrete-reader refactor spans d2bd, refusal class 5), `GuestSessionEvidence::stale()`/`authenticated()` (fail-closed read gate exercised only by tests but security-relevant; out of scope), the R4 bounded dispatch worker, `validate_reader_path` + `read_bounded_file` double path validation (security path). `service-catalog.json` and the xtask committed-scope row are declared-provider artifacts - not findings. No types-layer consistency notes apply (U33 is not U2-U11).
## U1 execution (2026-09-24)
- applied: ConfigServiceDescriptor chain (struct + canonical + validate, ConfigService::descriptor, GuestConfigReader::descriptor, lib.rs re-export arm, descriptor_is_closed_and_service_only test) - R4 re-verified zero external callers at HEAD.
- applied: ConfigService::stage, authorize_admin, service_identity, GuestConfigDocument::is_empty, GuestSessionEvidence::boot_identity, GuestSessionEvidence::reconnect_generation - R4 re-verified zero callers.
- applied: ConfigCaller Lifecycle/User variants collapsed to Guest/Admin; can_read/can_stage/authorize_host_operation reduced to live roles (can_stage deleted as dead after stage/authorize_admin removal); three test call sites reworked onto Admin/Guest.
- tests: cargo test -p d2b-provider-config-nixos pass.
