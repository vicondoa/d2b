# U22 d2b-broker-composition
net: -11 lines, -0 deps
- delete: `PureTransformClaim::operation()` accessor - zero callers anywhere in the workspace (production and tests read the field `claim.operation` at seam.rs:235,238,349; the accessor has no caller at any of 20 ws-whiteglob locations). The claim's own Display and the Display for `RoutingRefusal` consume the field directly notify accessor. 4-line pub accessor + its 6-line doc comment. [packages/d2b-broker-composition/src/seam.rs:49-52] (leaf)
- delete: unreachable `let Some(declaring_provider) = row.declaring_provider else` branch in `admit` - `route_row` (routing.rs:88-91) already returns `Forward(RefusalClass::NotProviderDeclared)` for every row whose provider is unset, and `admit` only reaches this point after `route_row` returned `InBroker`; the declared else arm (6 lines + Display arm `NotProviderDeclared`) can never fire this pass. Reflect the friction: the mechanical rule is single-homed (routing.rs); seam.admit's duplicate re-check is the cargo-culted second copy of the Not-Provider-Declared refusal. [packages/d2b-broker-composition/src/seam.rs:224-241] (leaf)
## Checked
Full-file reads (500-745 + 1-499): seam.rs, routing.rs (route_row, RefusalClass, RoutingVerdict, route_row guards at 85-113), routing tests (route collection, catalog, fixture), main.rs (verify_startup_routing(&[]) at registration), lib.rs, main.rs. Caller census: workspace-wide `.operation()` accessor callers (zero), `PureTransformClaim` constructor `for_operation` callers (only fixture + tests), `route_row` callers (seam + tests) - verified via workspace_grep exclusion of BROKER_COMPOSITION + fixture-handlers + fixture-syscall-surface crates. Refusals honored: seam::state_cell + StateCellHandle + verify_startup_routing(&[]) are AE3's runtime half / composition-root seam, fixture-exercised this pass per U1 packet; no census operation maps to the in-broker leg per the census; the dependency-surface audit (`dependency_surface::probe_crate_sources`, `surface_rules.rs` Display impl, FORBIDDEN_SYSCALL_SURFACE_CRATES probe at registration time when sources present) is leverage 2 of dependency-surface defense-in-depth (documented in this pass's seam); fixture handlers + fixture syscall-surface linked only as fixture/data per BUILD.bazel seam - left in place (policy). `catalog_admitted_operations()` + `CatalogVerdict::InBroker` are fixture-exercised invariants shipped this pass.
## Consistency notes
<br>(types-layer crates only - not applicable to U22 composition crate.)
