# U51 d2b-provider-volume-virtiofs

net: -68 lines, -0 deps

- shrink `VirtiofsRunnerContract` struct + `virtiofs_runner_contract()` const-fn to a bare `VIRTIOFS_REPAIR_INTERVAL_SECS: u64 = 30` constant. Only `repair_interval_secs` has a production reader (`d2b-provider-volume-binding/src/driver.rs:113`); `resource_type`, `finalizer`, `watched_configuration_is_dependency` are read only by the crate's own conformance test (`tests/lifecycle.rs:420-423`), and the same facts are already pinned directly in that test (`VOLUME_BINDING_RESOURCE_TYPE`/`VOLUME_BINDING_FINALIZER` asserts at `tests/lifecycle.rs:411-413`). Same class as applied cross-crate #G99 (runner-contract structs read only by their own tests; FINALIZER/REPAIR_INTERVAL_SECS constants stay); volume-virtiofs was not in G99's applied set because one field had a live reader - shrink to the live constant, point driver.rs at it. [packages/d2b-provider-volume-virtiofs/src/controller.rs:20-41](family)
- delete `binding_phase` const-fn - zero callers anywhere (no in-crate caller, no test caller, no external caller; the controller maps phases via direct `BindingPhase` match arms, not this fn). [packages/d2b-provider-volume-virtiofs/src/controller.rs:53-62](leaf)
- delete `SocketIdentity::short_tag` - zero callers anywhere (only `to_hex` is read, by the Serialize impl and `d2bd/src/resource_plane_v3.rs:322,533`). [packages/d2b-provider-volume-virtiofs/src/bindings.rs:69-77](leaf)
- delete `ScriptedPort::failing_launch` + `launch_error` field + the `launch_worker` error branch - zero callers (no test scripts a launch failure; the error branch is unreachable). [packages/d2b-provider-volume-virtiofs/src/testing.rs:120-124,65,200-202](leaf)
- delete `VirtiofsBindingError::WorkerLaunchFailed` variant + code arm + `ALL` entry - never constructed anywhere (its only would-be producer, `failing_launch`, is itself dead; the crate's closed-set test iterates `ALL`, so removing the variant keeps the test valid). Same class as applied #G39 (never-constructed error variants). [packages/d2b-provider-volume-virtiofs/src/error.rs:24-25,52,69](leaf)
- delete `INODE_FILE_HANDLES` const + lib.rs re-export arm - zero readers anywhere; the argv renderer that consumed it was deleted in #S63 and the worker plan carries no such field. [packages/d2b-provider-volume-virtiofs/src/worker.rs:19-20](leaf)
- delete `fixtures::volume_ref()` - zero callers (fixtures build bindings via `binding_envelope`, never this accessor). [packages/d2b-provider-volume-virtiofs/src/testing.rs:278-281](leaf)
- shrink lib.rs re-export block: drop `resolve_view`, `SANDBOX_MODE`, `USER_NAMESPACE_MAPPING_CLASS`, `WorkerSandbox` arms - all four are live only in-crate (`resolve_view` at controller.rs:157, the worker consts inside worker.rs); no external consumer exists. [packages/d2b-provider-volume-virtiofs/src/lib.rs:44-59](leaf)

## Checked

Read all of `src/{lib,controller,error,bindings,port,socket_path,worker,testing}.rs`, `tests/lifecycle.rs`, `Cargo.toml`, `BUILD.bazel`, `README.md`, `integration/README.md`, `nix/default.nix`, `nix/tests/default.nix`, plus the U1 packet's U51 ledger rows (#S62 partial, #S63 applied, #S64 partial). Caller verification method: workspace-wide `rg` over `packages/`, `docs/`, `nixos-modules/`, `BUILD.bazel` files and `packages/xtask/src` for every candidate symbol (`binding_phase`, `resolve_view`, `short_tag`, `failing_launch`, `WorkerLaunchFailed`, `INODE_FILE_HANDLES`, `SANDBOX_MODE`, `USER_NAMESPACE_MAPPING_CLASS`, `VirtiofsRunnerContract`, `virtiofs_runner_contract`, `volume_ref`, `MAX_SOCKET_PATH_BYTES`, `WORKER_TEMPLATE`, `SocketIdentity`, `StoredBinding`, `VirtiofsBindingError`), then targeted greps of the consumer crates (d2bd, d2b-provider-volume, d2b-provider-volume-binding, d2b-provider-process, d2b-broker, d2b-provider-toolkit, d2b-resource-runtime). Ledger honored: controller.rs (in-crate reconciler) and testing.rs fixtures stay per #S62 - the lifecycle conformance suite exercises them; port.rs stays per #S64. Live surface re-confirmed: `WORKER_TEMPLATE` (d2bd/src/process_provider_runtime.rs:4198, d2b-provider-process/{driver.rs:1154,launch_identity.rs:10}, volume-binding driver), `MAX_SOCKET_PATH_BYTES` (d2bd/src/resource_plane_v3.rs:88), `SocketIdentity`/`StoredBinding` (d2bd + volume-binding driver/effects_service/test_support), `VirtiofsdWorkerPlan` (volume-binding driver.rs:580), `VirtiofsBindingError::{ViewNotFound,BindingNotReady}` (volume-binding driver.rs:855,894,934), `PROVIDER_REF` (volume driver.rs:64, volume-binding driver.rs:79), `virtiofs_runner_contract().repair_interval_secs` (volume-binding driver.rs:113). No prior refusal row is reopened; no new evidence contradicts #S62-#S64.## U1 execution (2026-09-24)

All 7 findings applied. R4 re-verified at HEAD: all zero-caller claims hold (grep-verified workspace-wide; `resolve_view`/`SANDBOX_MODE`/`USER_NAMESPACE_MAPPING_CLASS`/`WorkerSandbox` live only in-crate, as claimed).

- Finding 1: `VirtiofsRunnerContract` struct + `virtiofs_runner_contract()` shrunk to `pub const VIRTIOFS_REPAIR_INTERVAL_SECS: u64 = 30` (controller.rs); external reader `d2b-provider-volume-binding/src/driver.rs:113` repointed to the bare constant (cross-crate, per the finding's own named reader); lifecycle test contract asserts (420-423) deleted - ResourceType/Finalizer facts stay pinned via const asserts at 411-413; `virtiofs_runner_contract` import dropped from the test.
- Finding 2: deleted `binding_phase` const-fn + its now-unused `ResourcePhase` import (controller.rs).
- Finding 3: deleted `SocketIdentity::short_tag` (bindings.rs.

- Finding 4: deleted `ScriptedPort::failing_launch` + `launch_error` field + `launch_worker` error branch + unused `ResourceRef` import in `fixtures` (testing.rs.

- Finding 5: deleted `VirtiofsBindingError::WorkerLaunchFailed` variant + code arm + `ALL` entry (ALL len 12 → 11.
- Finding 6: deleted `INODE_FILE_HANDLES` const (worker.rs) + lib.rs re-export arm.

- Finding 7: shrunk lib.rs re-export blocks: controller arm now `VirtiofsBindingController, VIRTIOFS_REPAIR_INTERVAL_SECS`; worker arm now `VirtiofsdWorkerPlan, WORKER_TEMPLATE` (dropped `resolve_view`, `INODE_FILE_HANDLES`, `SANDBOX_MODE`, `USER_NAMESPACE_MAPPING_CLASS`, `WorkerSandbox` arms.

`cargo test -p d2b-provider-volume-virtiofs -p d2b-provider-volume-binding`: PASS (volume-binding 18;virtiofs 9 unit + 18 lifecycle; doc-tests;0 failures..
