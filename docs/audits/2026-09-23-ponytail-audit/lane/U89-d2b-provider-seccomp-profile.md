# U89 d2b-provider-seccomp-profile

net: -148 lines, -1 deps

- delete Test-only accessor surface on the contract types: `SeccompNamespaces::new` (8-arg, `#[allow(clippy::too_many_arguments)]`) + all 8 getters, `SeccompCgroups::controllers()`, `DeviceBind::{path,kind,major,minor,access}`, `SeccompProfileSpec::{syscalls,namespaces,cgroups,devices}`, `DeviceNodePath::as_str`, and the crate test `a_profile_carries_its_content_inline` that only pins them. No reader outside the crate's own test; fields stay (wire-pinned by the committed schema, drift-asserted at xtask/src/zone_schema.rs:1731-1741); live constructors `SeccompProfileSpec::new`/`DeviceBind::new`/`DeviceNodePath::parse` stay (d2bd/src/foundation_seed.rs:1263-1275); the `profile()` fixture reworks onto defaults/JSON. [packages/d2b-provider-seccomp-profile/src/seccomp_profile.rs] (leaf)
- yagni Cgroup-controller bound machinery: `SeccompCgroups::new`'s bound check, `MAX_SECCOMP_CGROUP_CONTROLLERS`, `SeccompProfileContractError::TooManyCgroupControllers` + its Display arm, and the cgroup half of `list_bounds_fail_closed`. The bound is enforced nowhere on the wire path (`SeccompProfileSpec`'s Deserialize decodes `SeccompCgroups` by derive; `Spec::new` checks only syscalls/devices bounds), so the sole caller is the crate's own test; deleting changes no behavior. [packages/d2b-provider-seccomp-profile/src/seccomp_profile.rs] (leaf)
- yagni `serde_json` as a normal dependency: used only by `#[cfg(test)]` (`json!`, `from_slice`); move to dev-dependencies (precedent G74/G85). [packages/d2b-provider-seccomp-profile/Cargo.toml] (leaf)

## Checked

Read all crate files (src/lib.rs, src/driver.rs, src/seccomp_profile.rs, tests/registration.rs, integration/seccomp-profile.rs, Cargo.toml, BUILD.bazel, README.md, resource-types.json) and the U1 packet's U89 ledger. Ledger honored: P1/A1 applied shape confirmed (driver.rs is 20 lines via `metadata_descriptor(WellKnownType::SECCOMP_PROFILE)`); P2 applied shape confirmed (tests/registration.rs uses `assert_metadata_registration`); P9 refused scaffolds untouched (integration/ + README are policy-required); P10's d2b-contracts-resource dep is now legitimately live (`BoundedToken`, `redacted_debug!`, `canonical_json_bytes` in seccomp_profile.rs) - not re-flagged.

Caller verification (workspace-wide grep over *.rs, BUILD.bazel, nixos-modules/, tests/, docs/reference/, xtask): the only Rust consumers of the public types are d2bd/src/foundation_seed.rs (constructs via `new`/`default`, encodes via `Serialize`, registers `seccomp_profile_descriptor`; no field reads), d2bd/src/resource_plane_v3.rs:151 (descriptor), and xtask/src/zone_schema.rs:1251 (`dto_resource_schema::<SeccompProfileSpec>` - JsonSchema only). The admission gate is the generic `metadata_spec_decoder` (d2b-resource-runtime/src/metadata.rs:90-92 decodes as `Value`), so the typed Deserialize impls are not live gates; they are the family's C4 contract pattern (byte-identical `Wire` + `redacted_debug!` in d2b-provider-command and d2b-provider-operation) and are left alone per the C4 ledger. The hand-written `JsonSchema for DeviceNodePath` feeds the committed, drift-asserted schema - kept. `resource-types.json` feeds the xtask resource-type authority and the generated Nix inventories - kept. Consistency observation for the family lead: d2b-contracts-zone-session/src/v3/role.rs:27 carries a private duplicate of the canonical `SECCOMP_PROFILE_RESOURCE_TYPE` const (U8's lane).## U1 execution (2026-09-24)

All 3 findings applied. R4 re-verified at HEAD: external consumers (d2bd foundation_seed.rs:1263-1275) use only `SeccompProfileSpec::new`/`SeccompNamespaces::default`/`SeccompCgroups::default`/`DeviceBind::new`/`DeviceNodePath::parse`; xtask uses JsonSchema only; no external accessor calls.

- Finding 1: deleted `SeccompNamespaces::new` (8-arg) + all 8 getters; `SeccompCgroups::controllers()`; `DeviceBind::{path,kind,major,minor,access}`; `SeccompProfileSpec::{syscalls,namespaces,cgroups,devices}`; `DeviceNodePath::as_str`; and the `a_profile_carries_its_content_inline` test. Fields stay (wire-pinned, drift-asserted at xtask zone_schema.rs:1731-1741). `profile()` fixture reworked: `SeccompNamespaces` struct literal (fields private but visible in the child tests module) + `SeccompCgroups::new(...)` without `.unwrap()`.
- Finding 2: `SeccompCgroups::new` bound check removed (now returns `Self`); `MAX_SECCOMP_CGROUP_CONTROLLERS` const, `SeccompProfileContractError::TooManyCgroupControllers` variant + Display arm, and the cgroup half of `list_bounds_fail_closed` deleted.
- Finding 3: `serde_json` moved to `[dev-dependencies]`.

`cargo test -p d2b-provider-seccomp-profile`: PASS (3 unit + 1 registration; doc-tests; 0 failures).
