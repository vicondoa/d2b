# U81 d2b-provider-role

net: -17 lines, -1 deps

- stdlib: hand-written redaction `Debug for AuthorizationCacheKey` (9 lines) where `redacted_debug!` exists in an already-linked dep (`d2b_contracts_resource::v3::execution_policy::redacted_debug`, `#[macro_export]`). Replace with `redacted_debug!(AuthorizationCacheKey);`. Disclosed log-only difference: rendered shape changes from `AuthorizationCacheKey { subject_kind, has_subject_uid, has_attributes_digest }` to `AuthorizationCacheKey(<redacted>)` - no production formatter exists (only the in-crate test formats it; d2b-resource-api/src/authz.rs never `{:?}`-formats the key/cache); the redaction test is updated to pin the new shape, keeping the sentinel-absence contract. The `Debug for PositiveDecisionCache` (try_lock-based non-blocking formatter) is genuinely custom and stays. [packages/d2b-provider-role/src/rbac.rs:41-49] (leaf)
- delete: `PositiveDecisionCache::invalidate_revisions` has zero production callers - only its own test (rbac.rs:225) and the sync-surface comment (rbac.rs:144) mention it; the live consumer (d2b-resource-api/src/authz.rs) uses only `contains`/`insert_allow`/`clear` (authz.rs:1529,1535,1607,1609). Delete the method, drop it from the comment's surface list, and drop the test line - the adjacent `contains` assertions already cover revision-change invalidation. [packages/d2b-provider-role/src/rbac.rs:134-137] (leaf)
- yagni: `pub use rbac::*;` glob re-export has zero consumers - the only rbac consumer imports the module path (`use d2b_provider_role::rbac::{AuthorizationCacheKey, PolicyRevisionSet, PositiveDecisionCache}` at d2b-resource-api/src/authz.rs:23), and every other crate-root use is `role_descriptor` (d2bd/src/foundation_seed.rs:1108, d2bd/src/resource_plane_v3.rs:149,2814, tests/registration.rs:13). Delete the re-export line; `pub mod rbac;` stays. [packages/d2b-provider-role/src/lib.rs:20] (leaf)
- shrink: `PositiveEntry` derives `Debug` but nothing formats entries - the custom cache Debug impl never touches them and the tests format only key/cache. Drop `Debug` from the derive list. [packages/d2b-provider-role/src/rbac.rs:52] (leaf)
- yagni: `tokio` as a normal dependency: only the policy-required registration test uses it (`#[tokio::test]` in tests/registration.rs; zero `tokio` references in src/ - rbac.rs uses `std::sync::Mutex`), and its `sync`/`time` features are unused. Move to `[dev-dependencies]` trimmed to `macros` + `rt-multi-thread` (the default multi-thread `#[tokio::test]` flavor); the bazel registration target already pulls `normal_dev` deps and the library/test_support targets need no tokio (precedent: sibling U82 finding, G85). [packages/d2b-provider-role/Cargo.toml] (leaf)

## Checked

Read every crate file (src/lib.rs, src/driver.rs, src/rbac.rs, tests/registration.rs, integration/role.rs, Cargo.toml, BUILD.bazel, README.md, resource-types.json; 497 lines total) and the U1 packet's U81 ledger. Ledger honored: #P1/#A1 applied shape confirmed (driver.rs is the 20-line `metadata_descriptor(WellKnownType::ROLE)` floor; live registration consumers at d2bd/src/foundation_seed.rs:1108 and d2bd/src/resource_plane_v3.rs:149,2814); #P2 applied confirmed (tests/registration.rs calls the shared `assert_metadata_registration` at d2b-resource-types/src/metadata.rs:72); #P9 refused scaffolds untouched - integration/role.rs + README are policy-required (crate not on the README-only ratchet; no new evidence the xtask ratchet changed). Caller searches run workspace-wide over *.rs + BUILD.bazel + nixos-modules/ + tests/ + docs/reference/policy/: `d2b[-_]provider[-_]role`, `role_descriptor`, `PositiveDecisionCache|AuthorizationCacheKey|PolicyRevisionSet` (only rbac.rs + authz.rs), `invalidate_revisions` (only rbac.rs), `d2b_provider_role::(?!rbac)` (only role_descriptor), `d2b_provider_role_test_support` (BUILD twins consumed by d2b-resource-api/BUILD.bazel:65 and d2bd/BUILD.bazel:248), `provider-role/test-support` (no Cargo enabler). The empty `test-support` feature is the Bazel variant discriminator for those consumed twins - left. `d2b-contracts-resource` stays a live normal dep (rbac.rs uses ConfigurationGeneration/ResourceRef/ResourceUid/ZoneRevision), unlike the #P10 crates. No src/generated/ in this crate.## U1 execution (2026-09-24)

All 5 findings applied. R4 re-verified at HEAD: `invalidate_revisions` zero production callers (d2b-resource-api uses only `contains`/`insert_allow`/`clear`); `pub use rbac::*` zero consumers (authz.rs imports the module path); zero `tokio` refs in src/.

- Finding 1: hand-written `Debug for AuthorizationCacheKey` replaced with `redacted_debug!(AuthorizationCacheKey);` (macro imported via `d2b_contracts_resource::v3::execution_policy::redacted_debug`); redaction test re-pinned to the new shape (`AuthorizationCacheKey(<redacted>)` present, `subject_kind` absent) with the sentinel-absence loop kept.
- Finding 2: deleted `PositiveDecisionCache::invalidate_revisions` + its sync-surface comment mention + the test call and its two dependent asserts (the adjacent `contains` revision-mismatch assert already covers revision-change invalidation).
- Finding 3: deleted `pub use rbac::*;` (lib.rs); `pub mod rbac;` stays.
- Finding 4: dropped `Debug` from `PositiveEntry` derive.
- Finding 5: `tokio` moved to `[dev-dependencies]` trimmed to `macros` + `rt-multi-thread`.

`cargo test -p d2b-provider-role`: PASS (2 unit + 1 registration; doc-tests; 0 failures).
