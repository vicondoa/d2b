# U38 d2b-provider-guest

net: -26 lines, -0 deps

- `stdlib` driver.rs:402-414: `resource_uid` hand-rolls the UUIDv4 mapping (version/variant bit forcing + dashed hex render) that `ResourceUid::from_bytes` already ships at d2b-contracts/src/identity.rs:580-592 - the toolkit's own `resource_uid` wraps it (d2b-provider-toolkit/src/shared_provider.rs:400-402). Replace the body with `ResourceUid::from_bytes(bytes).map_err(|_| GuestEffectError::InvalidResource)` (13 -> 3 lines); the public error type stays `GuestEffectError`, so the #G44 refusal (toolkit-helper error-type change) is untouched. This is one of the twelve remaining #P7 sites the ledger counts in d2bd/d2bd-runtime/d2b-broker/provider crates; #P7 is [partial], not refused. Call-site search: `d2b_provider_guest::resource_uid` over packages;tests;nixos-modules;docs/reference/policy - zero external callers; 3 internal call sites (driver.rs reconcile/delete, effects_service.rs:673) unchanged. (leaf)
- `delete` lib.rs:39-63: 27 root re-export arms with zero external consumers - `GUEST_REGISTRATIONS, GUEST_TYPE_NAME, GuestChildObservation, GuestChildSurface, GuestDriverEffects, GuestDriverFactory, GuestDriverStatus, GuestEffectError, GuestEffectOutcome, GuestEffectPhase, GuestEffectRequest, GuestFinalizeStage, GuestKind, GuestRegistration, declared_dependency_refs, decode_metadata, guest_spec_decoder, guest_status_sink, key_ref, resource_uid, view_phase, GuestEffectsService, SessionTargetControlChannel, GuestTargetEffect, GuestTargetEffectError, GuestTargetEffects, GuestTargetRefusal`. Keep the consumed names (`GuestDriverArgs, GuestStatusSink, guest_descriptor, GUEST_EFFECTS_SERVICE, GuestEffectsServiceFactory, CloudHypervisorGuestRuntime, GuestCloudHypervisorOutcome, GuestEffectFacets, GuestManagerView, GUEST_RESOURCE_TYPE, GuestSpec`, the six shutdown names, `GuestTargetSession, guest_target_ref, session_target_control, GuestTargetService, production_guest_target_effects, target_control_services`); the items stay `pub` in their modules (d2bd/tests/guest_target_service.rs imports target_service items by module path; tests/registration.rs can import `driver::GUEST_TYPE_NAME`). Search method: grep `d2b_provider_guest::` and `d2b_provider_guest::<module>::` over packages;tests;nixos-modules;docs/reference/policy plus BUILD.bazel dep sweep (`//packages/d2b-provider-guest:` referenced only by network-local, d2bd, xtask) - the 27 names have no external reader; d2bd uses only the kept 18. (leaf)
- `shrink` effects_service.rs:832-842 + 844-852: `validate_qemu_guest`/`validate_azure_vm_guest` re-parse the same `/spec/provider/settings` bytes that `build_guest_controller` parses again on every reconcile (1104-1113, 1194-1198) and on finalize; have the validators return the parsed settings and pass them into the builder (same class as applied #G72 in the ACA crate). Net ~-4 lines. (leaf)

## Checked

Read all 10 src files (driver.rs 2550, effects_service.rs 2257, test_support.rs 333, target_service.rs 407, shutdown.rs 199, guest_spec.rs 186, facets.rs 126, target_control.rs 110, lib.rs 66), tests/registration.rs (232), integration/guest_family.rs, README, BUILD.bazel, registrations.json, resource-types.json, Cargo.toml. Caller verification: workspace-wide greps (`d2b_provider_guest::`, module paths, per-symbol sweeps over packages;tests;nixos-modules;docs/reference/policy + BUILD.bazel + Cargo.toml dependents: network-local, d2bd, xtask only). Ledger honored: #G45-G48, G50, G52 [applied] verified absent at HEAD (no HOST_REF, no `GuestEffectOutcome::projection`, no `GuestSpecEnvelope::raw`, no module-scope `#![allow(dead_code)]`; `GuestDriverStatus`/`GuestDriver`/`GuestRegistration`/`GuestKind` carry only live fields); #G42/#C6 [refused] still stands (driver implements `ResourceDriver` directly; toolkit lacks the three hooks), #G43 [refused] still stands (`production_guest_target_effects()` returns the empty map; only consumer remains d2bd/tests/guest_target_service.rs), #G44 [refused] still stands (the 20 local `decode_metadata`/`resource_uid`/`key_ref` lines remain; no new evidence), #G49 [refused] still stands (crate not on README-only ratchet; integration/guest_family.rs policy-required), #G51 [refused] still stands (`GuestChildSurface` one-impl + test double; invariance restructure). #P7/#S1 [partial] re-verified: this crate holds one of the twelve remaining hand-rolled UUID renderer sites (flagged above); the three migrated sites are elsewhere. GuestSpec's hand-written Deserialize is a live admission gate (deny_unknown_fields + policy validation) - refusal class 1, not flagged. Live surface confirmed: shutdown vocabulary + `ProviderKind::as_str` (d2bd composition.rs:16276), `CloudHypervisorShutdown` (composition.rs:17453), target-control channel/service (composition.rs:4530-4688, 6663-6700, 11105-11148), facets + factory (resource_plane_v3.rs:2515-2585, 3487), `GuestSpec` (d2bd, network-local, xtask).
## U1 execution (2026-09-24)
- applied: driver.rs resource_uid body replaced with ResourceUid::from_bytes (same error type stays)。
- applied: lib.rs 27 root re-export arms deleted (kept GuestDriverArgs, GuestStatusSink, guest_descriptor, GUEST_EFFECTS_SERVICE, GuestEffectsServiceFactory - and the facets/guest_spec/shutdown full arms;; module items stay pub://d2bd tests consume target_service via module path, unaffected)。R4 re-verified zero root-path external consumers at HEAD.
- applied: effects_service.rs validators now return parsed typed settings; build_guest_controller/run_guest_controller/finalize_guest_controller thread them (reconcile qemu/azure arms parse+pass; ACA passes None; finalize parses per-kind first。R4 confirmed double-parse claim via code reading。
- tests: cargo test -p d2b-provider-guest pass。
