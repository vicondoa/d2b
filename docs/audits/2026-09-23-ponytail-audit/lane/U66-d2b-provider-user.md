# U66 d2b-provider-user
net: -17 lines, -0 deps

- shrink The orphaned 8-line "resource verbs" doc block at driver.rs:391-398 name-drops `USER_VERBS` and the "closed `RoleResourceVerb` set minus the two Credential-scoped verbs" derivation that no longer lives in this crate — the applied #S2 consolidation moved the verb surface to the shared `CONVERTED_TYPE_VERBS` referenced through `d2b_contracts_resource::v3::user`, and `descriptor`'s verbs field serves from it. Delete the stale block (no replacement: the verbs derivation is already documented at the contracts resource plane's `CONVERTED_TYPE_VERBS` home). [packages/d2b-provider-user/src/driver.rs:391-398] (leaf)

- shrink The one-use `USER_EXECUTION_DOMAINS` const (driver.rs:406) is only referenced at driver.rs:426 (`execution: USER_EXECUTION_DOMAINS`). Inline `execution: &["host"]` in `user_descriptor` and fold its 5-line placement rationale ("User names no placement anchor, so a User row never carries the canonical `spec.executionRef` and the plane reconciles it on its own Host domain") into the existing `user_descriptor` doc, which already documents the BUILTIN|STARTUP/verb/domain surface. [packages/d2b-provider-user/src/driver.rs:399-406] (leaf)

- yagni Seven of the ten lib.rs re-export names have zero workspace callers: `UserDriver`, `UserDriverEffects`, `UserDriverError`, `UserDriverFactory`, `UserDriverStatus`, `user_spec_decoder`, and `UserEffectsService` (the concrete type) are consumed by no external crate — d2bd wiring (resource_plane_v3.rs:2114,4293, effects seams, provider_lifecycle.rs:49) and the plane tests import only `user_descriptor`, `USER_EFFECTS_SERVICE`, `UserEffectsServiceFactory`, and `UserEffectFacets`, plus the `test_support` seam (ScriptedProbe/recording_facets) which must stay. These types remain `pub` in their own modules and are reachable via `crate::` from the crate's internal driver/effects_service/tests, so dropping the retries re-export only shrinks the public root. Replace the 8-line export block with the consumed four: `pub use driver::user_descriptor;`, `pub use effects_service::{USER_EFFECTS_SERVICE, UserEffectsServiceFactory};`, `pub use facets::UserEffectFacets;`. [packages/d2b-provider-user/src/lib.rs:42-49] (leaf)

## Consistency notes
(types-layer crates only — none.)

## Reopened refusals
None: #S6 (host/user driver merge) stays refused — no new evidence, same cross-family edge; the lone `host` execution domain and the shared CONVERTED_TYPE_VERBS here are exactly the shapes the refusal cited.

## Checked
Read every file to the end: driver.rs (929 lines: descriptor, decoder, verbs/execution declarations, factory, all reconcile/recover/delete/finalize paths plus the recording-facets registration tests), effects_service.rs (633 lines), probe.rs, facets.rs, test_support.rs, lib.rs, README.md, integration/user.rs (declaration-only scaffold, kept per policy), tests/registration.rs, tests/integration/, BUILD.bazel, Cargo.toml, registrations.json, resource-types.json. Workspace-wide caller search per plan constraint 2 across `packages/` (all src + BUILD.bazel + Cargo.toml + d2bd/src + tests/registration.rs), `nixos-modules/`, `tests/`, and `docs/reference/policy/`: `user_descriptor` via d2bd (resource_plane_v3.rs:2114,4293, shared_provider_effects.rs), `USER_EFFECTS_SERVICE` via d2bd (resource_plane_v3.rs:2264,3938, effects seams, provider_lifecycle.rs:49), `UserEffectsServiceFactory` via d2bd (effects seams + tests), `UserEffectFacets` via d2bd; `ScriptedProbe`/`recording_facets` consumed by both d2bd plane tests and this crate's tests; `user_spec_decoder`, `UserEffectsService`, and the five driver types have no external consumer (searched by crate-qualified name and bare name — the only hits are definitions plus crate-local tests). Ledger rows honored: #S2's applied verbs consolidation makes driver.rs:391-398 stale (new evidence, now removable); #S6 refused stays refused (ledger honored, no new evidence). Exclusions respected: no `src/generated/` in this crate; no labs; README/BUILD/registrations policy scaffolds not findings.
