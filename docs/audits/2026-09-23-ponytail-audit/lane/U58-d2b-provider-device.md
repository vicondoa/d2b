# U58 d2b-provider-device
net: -140 lines, -0 deps
- delete `src/vocabulary.rs` (89 lines incl. its 34-line test arm) + the `pub mod vocabulary;` arm in `src/lib.rs`. `admit_open_class` and `DeviceOpenAdmissionError` are the module's only production surface and have ZERO callers anywhere in the workspace - census: `rg --hidden --glob '!**/.git/**' --glob '!**/target/**' "admit_open_class|DeviceOpenAdmissionError" .` (gitignore off, docs/nixos/BUILD/policy included) returns only vocabulary.rs itself (def + own test). No policy/doc/nixos/xtask pin on `device-open-class`/`admit-open` tokens; the four class-set constants it aggregates (TPM/USBIP/SECURITY_KEY/GPU) are read only inside this module's own test. The daemon plane builds its own admission from `DEVICE_REGISTRATIONS` rows; nothing consumes the family's local "fail-closed open-class" gate. [packages/d2b-provider-device/src/vocabulary.rs] (leaf)
- delete `RecordingEffects` in `src/test_support.rs` (lines 18-67: doc + struct + `impl DeviceDriverEffects` + `impl RecordingEffects`/`call_order`, ~50 lines). Zero callers workspace-wide: d2bd's plane tests build the Device facet set from `RecordingRuntime` + `recording_facets` only (resource_plane_v3.rs:3797-3799, shared_provider_effects.rs:3339-3340), and the crate's own tests/device_family.rs + integration/device_family.rs use `recording_facets`/`RecordingRuntime` too. The `DeviceDriverEffects` recording double is never constructed or referenced - only the `DeviceRuntime` double (`RecordingRuntime`, lines 69-108) and `recording_facets` are live; keep those, drop the dead driver-seam double and its module-doc reference. [packages/d2b-provider-device/src/test_support.rs:18-67] (leaf)

## Checked
Read all of `src/` (lib.rs, driver.rs 305, effects_service.rs, facets.rs, vocabulary.rs, test_support.rs), `tests/device_family.rs`, `integration/device_family.rs`, `Cargo.toml`, `BUILD.bazel`, `registrations.json`, `resource-types.json`, README. Caller census per plan constraint 2: workspace-wide `rg` (gitignore off, `--hidden`, `.git`/`target` excluded) for `admit_open_class`, `DeviceOpenAdmissionError`, `RecordingEffects`, `RecordingRuntime`, `recording_facets` across packages/ + docs/ + nixos-modules/ + BUILD files; plus targeted greps in d2bd (resource_plane_v3.rs, shared_provider_effects.rs, provider_lifecycle.rs) and xtask census/exemption ledgers. Result: `admit_open_class`/`DeviceOpenAdmissionError` - only vocabulary.rs (self); `RecordingEffects` - only test_support.rs (self); `RecordingRuntime`/`recording_facets` - live at d2bd plane test sites + in-crate tests. Ledger honored: U58's only prior row is #S2 [applied] (verbs migrated to CONVERTED_TYPE_VERBS) - nothing refused to reopen; no new evidence on any other row. `DeviceEffectsServiceFactory.facets` (effects_service.rs) checked and NOT flagged: it is the family-consistent R5 respawn carrier (identical `#[allow(dead_code)]` facet fields in the tpm/usbip/security-key/gpu effect-service factories), a declared cross-crate seam, not a one-crate yagni.## U1 execution (2026-09-24)

Both findings applied. R4 re-verified at HEAD: `admit_open_class`/`DeviceOpenAdmissionError` zero hits workspace-wide beyond src/vocabulary.rs (def + own test); `RecordingEffects` zero hits in d2bd and in the crate beyond the test_support defs (tests use `RecordingRuntime`/`recording_facets` only).

- Finding 1: deleted `src/vocabulary.rs` (89 lines) + `pub mod vocabulary;` arm from src/lib.rs.
- Finding 2: deleted `RecordingEffects` (doc + struct + `impl DeviceDriverEffects` + `call_order`, 51 lines) from src/test_support.rs; module doc reference to the deleted double updated (now "recording doubles"); now-unused `DeviceDriverEffects` import dropped from test_support.rs. `RecordingRuntime` + `recording_facets` stay.

 `cargo test -p d2b-provider-device`: PASS (all suites, 0 failures..
