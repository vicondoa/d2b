# U67 d2b-provider-endpoint
net: -14 lines, -0 deps

- delete `EndpointConsumerPolicy::{allowed_subjects, allowed_provider_components}` accessors. The two fields are pinned wire (serde contract, `deny_unknown_fields` decode), so they stay; the accessors have zero callers anywhere (workspace-wide grep for `allowed_subjects()` / `allowed_provider_components()` over *.rs, BUILD.bazel, nixos-modules/, tests/, docs/ — only the definitions match). [packages/d2b-provider-endpoint/src/endpoint.rs:191-199] (leaf)
- delete `EndpointSpec::attachment_policy()` accessor. The field is pinned wire and constructed by d2bd (`EndpointAttachmentPolicy::new` at interaction_composition.rs:2921), so it stays; the accessor has zero callers (workspace-wide grep for `attachment_policy()` — only d2b-bus's own `AttachmentPlan::attachment_policy` and `controller_bootstrap_attachment_policy()` match, both different types). [packages/d2b-provider-endpoint/src/endpoint.rs:354-357] (leaf)
- delete `ENDPOINT_RESOURCE_TYPE` const. Zero workspace references (grep over *.rs, BUILD.bazel, nixos-modules/, tests/, docs/ — definition and the `pub use endpoint::*` re-export only, no consumers); `WellKnownType::ENDPOINT` is the canonical name, the same derivation G94 already applied to the driver's type-name const. [packages/d2b-provider-endpoint/src/endpoint.rs:17-18] (leaf)

## Checked
Read all of `src/` (driver.rs 1283 lines, endpoint.rs 535, effects_service.rs 351, facets.rs, test_support.rs, lib.rs), `tests/registration.rs`, `integration/` (policy-required scaffold, not findings), Cargo.toml, BUILD.bazel, README.md, registrations.json, resource-types.json. Workspace-wide caller search for the whole public surface: EndpointSpec + every accessor, EndpointDriver/Factory/Error/Status, EndpointRealization, EndpointDriverArgs, EndpointPurposeVocabulary, EndpointDriverEffects, the three facet traits + EndpointEffectFacets, ENDPOINT_EFFECTS_SERVICE + EndpointEffectsService(Factory), FakeSocketEffects, VIRTIOFSD_PURPOSE, GuestControlProducer, the four purpose-derivation fns, MAX_ENDPOINT_* consts, EndpointSpecError — via grep over packages/ plus tests/, docs/, nixos-modules/, BUILD.bazel files. Live surface confirmed: d2bd (resource_plane_v3.rs facets/descriptor/factory/effects-service, interaction_composition.rs EndpointSpec construction + service_fingerprint/locality/visibility/consumer_policy/lifecycle_policy reads, composition.rs consumer_policy reads, plane_controller_bridge.rs EndpointRealization + GuestControlProducer + endpoint_realization), display-wayland session_children.rs (EndpointSpec::new, Data/Transport/FdAttachment/Vsock/Zone variants), security-key driver.rs (EndpointSpec decode). Ledger honored: G86/G87/G89/PR15 refused — re-verified no blocker changed: runtime NullRequeue/DeadManager are still `#[cfg(test)] pub(crate)` (d2b-resource-runtime/src/context.rs:894, test_support), d2bd still reads EndpointRealization (resource_runtime/plane_controller_bridge.rs:970,1053) and still constructs EndpointDriverArgs{zone,..} (resource_plane_v3.rs:2969-2971), PR15 systemic doubles unchanged. G88/G90-G96 applied rows verified present in current tree (one parameterized builder, no re-asserting registration test, no envelope wrapper, class() arms gone, ENDPOINT_TYPE_NAME gone, free error fn, no Clone derive). G97's premise no longer holds: tokio "time" feature is live (tokio::time::Instant + sleep in effects_service.rs ensure_socket realize-budget loop), so no finding there. Unused wire variants (EndpointLocality::GuestLocal, EndpointOperation::Attach, EndpointLifecyclePolicy::{Pinned,RecreateOnGeneration}) are pinned wire surface — out of scope, not flagged.