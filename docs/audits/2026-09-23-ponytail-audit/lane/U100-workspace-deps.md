# U100 Workspace deps and features

net: -61 deps, -61 Cargo.toml lines (15 further deps moved normal→dev-deps, 0 net; ~55 rows rewritten to workspace pins,  0 net.

- `shrink` **tokio inline pins bypass the workspace `default-features = false` pin.** 42 crates write `tokio = { version = "1", features = [...] }` directly (`packages/d2b-broker/Cargo.toml:121`, `packages/d2b-provider-device-usbip/Cargo.toml:33`, `packages/d2bd/Cargo.toml:120`, `packages/d2b-provider-supervisor/Cargo.toml`, ...) — every one re-enables tokio's **full default feature set** (rt, rt-multi-thread, macros, net, process, fs, io-util, time, sync, signal, parking_lot,…) regardless of the listed subset, exactly the surface the workspace root pin (`tokio = { version = "1", default-features = false }`) exists to exclude. Replace each row with `tokio = { workspace = true, features = [<same list>] }` — zero behavior change (each crate's explicit feature list stays), one line each. Same class: **ttrpc** 7 inline pins (`d2b-provider-config-nixos/Cargo.toml`, `d2b-provider-guest`, `d2b-provider-toolkit`, `d2bd`, `d2bd-runtime`, `d2b-provider-credential-secret-service` dev, `d2b-host` dev) bypass the workspace ttrpc `default-features = false` pin → switch to `workspace = true`. [Cargo.toml rows in 42 + 7 crates] (wide
- `shrink` **tokio declared as normal dep but used only by the policy-required registration test (12 crates.** `#[tokio::test]` in `tests/registration.rs` is the only tokio reference (zero tokio refs in src),and the declared `sync`/`time` features are unused. Move to `[dev-dependencies]` trimmed to `features = ["macros", "rt-multi-thread"]` (the default multi-thread `#[tokio::test]` flavor). Crates: `d2b-provider-role` (U81 already flags), `d2b-provider-role-binding` (U82 already flags), `d2b-provider-device`, `d2b-provider-zone`, `d2b-provider-zone-link`, `d2b-provider-quota`, `d2b-provider-emergency-policy`, `d2b-provider-resource-export`, `d2b-provider-resource-import`, `d2b-provider-command`, `d2b-provider-operation`, `d2b-provider-seccomp-profile`. The bazel registration targets already pull `normal_dev` deps, so moving keeps them fed (precedent: sibling U81/U82 rows). [12 Cargo.toml rows] (family
- `shrink` **serde_json declared as normal dep but used only by tests (4 crates.** `d2b-provider-notification-desktop` (7 hits in tests/,none src/,`d2b-provider-transport-vsock` (5 hits tests/,none src/,`d2b-provider-shell-pool` (2 hits tests/,none src/,plus U89's `d2b-provider-seccomp-profile` row. Move to `[dev-dependencies]`. [4 Cargo.toml rows] (family
- `shrink` **serde_json / d2b-contracts-provider normal deps used only by tests (d2bd + volume-local.** `d2bd` uses `d2b_provider_guest_azure_container_apps` only in `tests/cloud_composition.rs` (4 hits,zero src/ → move that dep to dev-deps. `d2b-provider-volume-local` uses `d2b_contracts_provider` only in `tests/state.rs` → move to dev-deps. [2 rows] (leaf
- `delete` **`d2b-contracts-zone-session` declared but never imported in 20 crates.** Zero occurrences of `zone_session`/`ZoneSession` (word and path searches over each crate's entire src + tests, incl cfg(test) blocks; only BUILD.bazel dep rows carry the name. Crates: `d2b-provider-process-systemd` (U49's note that "systemd consumes the zone plane" is refuted at HEAD: grep over its src yields zero), `d2b-provider-process-minijail` (U49 already flags,,`d2b-provider-activation-nixos`, `d2b-provider-audio-pipewire`, `d2b-provider-clipboard-wayland`, `d2b-provider-notification-desktop`, `d2b-provider-guest-qemu-media`, `d2b-provider-shell-terminal`, `d2b-provider-transport-unix`, `d2b-process-conformance`, `d2b-provider-volume-virtiofs`, `d2b-provider-credential-entra`, `d2b-provider-credential-managed-identity`, `d2b-provider-network-local`, `d2b-provider-device-gpu`, `d2b-provider-device-security-key`, `d2b-provider-device-tpm`, `d2b-provider-device-usbip`, `d2b-host`, `d2b-telemetry`. Remove the Cargo.toml row from each (drop the matching BUILD.bazel zone-session rows in the same commit — 5 rows per crate (U49's method. [20 Cargo.toml rows] (wide
- `delete` **`d2b-contracts` declared but never imported in 10 crates.** Every crate's code uses `d2b_contracts_resource`/`d2b_contracts_zone_session`/... (the `_resource`/`_zone_session` suffixes make `d2b_contracts` (bare) a distinct word — workspace-wide `\bd2b_contracts\b` scans return zero in: `d2b-bus`, `d2b-zone-routing`, `d2b-resource-client`, `d2b-resource-compiler`, `d2b-process-conformance`, `d2b-session`, `d2b-session-unix`, `d2b-resource-api`, `d2b`, `d2b-telemetry` (U11's core shims are NOT in this set — core genuinely re-exports `d2b_contracts::*`). Remove. [10 Cargo.toml rows] (wide
- `delete` **`d2bd` declares 5 provider crates its Rust never references.** Zero `d2b_provider_*` occurrences for `d2b-provider-observability-otel`, `d2b-provider-guest-azure-virtual-machine`, `d2b-provider-credential-secret-service`, `d2b-provider-credential-entra`, `d2b-provider-credential-managed-identity` (whole-crate word scans incl src/generated/; only d2bd/BUILD.bazel:72-90,157-166,230-238 lists them explicitly). The daemon wires those declared providers over the binary/service contract, not as Rust library deps. Remove the 5 Cargo.toml rows (the BUILD.bazel binary deps rows go separately (they add dead weight to the daemon binary link surface; flag to U101 consolidation. [packages/d2bd/Cargo.toml] (family
- `delete` **`d2b-broker`: tracing-subscriber + zbus unused.** `tracing_subscriber` has zero occurrences in the whole crate; `zbus` appears once — a code comment ("mirroring the zbus SYSTEMD_METHOD_TIMEOUT precedent" runtime.rs:3535),not code. Remove both Cargo.toml rows (broker DOES use `tracing` — that row stays. [packages/d2b-broker/Cargo.toml] (leaf
- `delete` **`d2b-controller-toolkit`: serde + schemars unused.** Zero `serde`/`Serialize`/`Deserialize`/`JsonSchema` occurrences crate-wide (its types are plain structs with hand-written Debug identity projections,per U26). Remove both. [packages/d2b-controller-toolkit/Cargo.toml] (leaf
- `delete` **Single zero-use externals (12 deps.** `d2b-core` semver (no `semver` anywhere in core src/tests),`d2b-contracts` async-trait + unicode-normalization (no `async_trait`/`unicode_normalization` in src;note `error.rs` semver usage is contracts's OWN dep,not core's),`d2b-contracts-provider` unicode-normalization,`d2b-resource-types` serde_json (its src uses serde derive + bitflags,never JSON),`d2b-provider-supervisor` socket2 (supervisor's src uses rustix::event poll,not socket2),`d2b-provider-device-usbip` libc (zero refs in src;U62 did not flag),`d2b-provider-network-local` sha2 (the crate hand-rolls SHA-256 at nftables.rs:496 — the declared crate dep sits unused;U57's fix would consume it — declare-then-consume order stays 0-error),`d2b-provider-process-systemd` d2b-contracts-control (zero refs in src),`d2b-provider-process-minijail` serde (U49 flags;only Debug derives,`d2b-unsafe-local-helper` d2b-core (U93 flags;zero `d2b_core::` src),`d2b-provider-guest-qemu-media` d2b-process-conformance (zero refs in src/tests). [11 Cargo.toml rows] (leaf/family
- `delete` **Zero-use dev-dependencies (10 rows.** `d2b-core` dev regex;`d2b-contracts-resource` dev regex (the only "regex" strings are `#[schemars(regex(pattern = …)))]` attributes — schemars's,not the regex crate);`d2b-bus` dev quote + dev syn;`d2b-provider-toolkit` dev parking_lot;`d2b-host` dev ttrpc (host's tests drive its binaries synchronously,no ttrpc;`d2b-provider-display-wayland` dev d2b-session;`d2b-controller-toolkit` dev serde_json (zero even in tests);`d2b-broker-composition` dev d2b-broker-fixture-syscall-surface + dev d2b-contracts-resource (the fixture tests reach the fixture crate by NAME through `crate_dir("d2b-broker-fixture-syscall-surface")` — a workspace-member path string + `cargo metadata`,not a Rust crate path — so the dev-dep edge is scannable-only,never linked (the file's own comment says the same; deleting it doesn't affect the probes. [10 Cargo.toml rows] (leaf/family
- `shrink` **Tokio under-declared features — production src uses tokio surfaces absent from the crate's own tokio edge (6 crates.** Compiles today only because Cargo unifies features workspace-wide from other crates; declaring them locally removes the hidden cross-crate coupling: `d2bd` uses `tokio::io::{AsyncFd, AsyncReadExt, AsyncWriteExt, Interest}` (composition.rs:10687-10701, forward_rendezvous.rs:98-99, interaction_composition.rs:5371, process_provider_runtime.rs:95) but dep lists no `io-util`;`d2b` uses `tokio::io::unix::AsyncFd` (context.rs:47) but no `io-util`;`d2b-provider-device-security-key` uses the same `tokio::io::*` family (relay_service.rs:23) but no `io-util`;`d2b-session` `tokio::spawn` (client.rs:51) but no `macros`;`d2b-provider` `tokio::spawn` (registry.rs:709) but no `macros`+`rt`;`d2b-provider-transport-azure-relay` `tokio::task::JoinHandle`/`tokio::runtime::Handle`/`tokio::spawn` (relay_transport.rs:1166-1173) but no `macros`+`rt`. Add the missing features to each crate's tokio edge (0 net lines(one-line edits. [6 rows] (leaf
- `shrink` **getrandom pinned at two majors across the workspace.** `d2b-provider-notification-desktop`, `d2b-unsafe-local-helper`, `d2bd`, `d2bd-runtime` pin `getrandom = "0.2"`;`d2b-bus`, `d2b-provider-transport-unix` pin `"0.3"` → Cargo.lock carries both 0.2.17 and 0.3.4. Promote the four 0.2 pins to a `[workspace.dependencies]` row (keep 0.2; lock's 0.3 copy rides via `snow`, and the 0.4 copy via `uuid`/`tempfile` — transitive,not ours). The two 0.3 crates keep their inline pin with a comment (snow-constrained),or move crate-wide after a snow bump. [6 Cargo.toml rows] (leaf
- `yagni` **Same-version inline pins of workspace-managed deps (15 rows.** `serde = { version = "1", features = ["derive"] }` inline in 5 crates,`serde_json = "1"` in 8,`base64 = "0.22"` in 2 — all identical to the workspace rows. Not dupe pins (same version,no lock dupes) but bypass the single-source-of-truth table; switch to `workspace = true` (0 net. [15 Cargo.toml rows] (leaf

## Verified
- **Method:** all 94 member Cargo.tomls parsed (tomllib): full `[dependencies]`/`[dev-dependencies]`/`[build-dependencies]`/`target.*` tables; every dep×crate pair word-scanned + path-scanned (`\b<name>\b`, `<name>::`, `use <name>`, attribute/derive macro names for serde/schemars/thiserror/async-trait/bitflags/clap/ttrpc) over the crate's whole `.rs` tree incl `cfg(test)` blocks, excl `src/generated/` and the pinned guest lock (`packages/Cargo.guest.lock`, excluded per brief).
- Every zero-use claim re-checked crate-wide including `src/generated/` and `build.rs` — only `protobuf` (contracts-resource) is generated-only (excluded; NOT flagged. All 61 zero-use rows above have zero hits in every file incl generated.
- Spot-greps validating the script: d2b-bus `d2b_contracts_resource` ≠ `d2b_contracts` (word-boundary correct); d2b-provider-device-tpm / process-systemd zone-session = BUILD.bazel rows only,zero src hits (refuting U49's "systemd consumes" note); d2b-process-conformance contracts+zone-session = zero; d2b-provider-network-local sha2 = only `sha256:` string literals +the hand-rolled `fn sha256`; d2b-broker zbus = one comment; contracts-resource dev regex = schemars attribute strings only; broker-composition fixture dev-dep = reached by NAME via `crate_dir("…syscall-surface")` + `cargo metadata`,never as Rust crate.
- **Tokio features:** per-crate union of dep∪dev edges vs module usage (`tokio::net/process/fs/io/macros/time/sync/task/signal/runtime`, `#[tokio::main|test]`, `spawn!`). The 6 under-declared rows are production-src verified (file+line citedabove;the remaining `src gap` rows from the union scan are cfg(test)-blocks inside src/ whose dev features already cover them — e.g. d2b-bus session_seam_tests.rs, d2b-session-unix vsock.rs, d2b-core comment mentions — not flagged. Over-broad rows beyond the default-features class were not separately enumerated (the ~42 inline pins dominate;per-feature trims deferred to the inline→workspace rewrite).
- **Duplicate pins:** Cargo.lock duplicate-version clusters reverse-mapped: nix 0.26/0.29/0.31, thiserror 1/2, socket2 0.5/0.6, webpki-roots 0.26/1.0, rand/getrandom/…, bitflags 1/2, rustix 0.38/1.1 — all attributable to third-party pins (ttrpc→nix0.26+thiserror1,vsock/tokio-tungstenite→socket2 0.6/webpki-roots 1.0,command-fds→nix0.31,bolero/tungstenite→rand0.8/0.10,defmt/ttrpc→bitflags1,and zbus/async-*→rustix1.1) EXCEPT getrandom (workspace-owned,flagged above). No workspace Cargo.toml carries a conflicting direct pin for nay other duplicated crate.


## Sources
- Lane cross-checks (already-recorded rows, verified at HEAD): U49 `d2b-provider-process-minijail` drop d2b-contracts-zone-session + serde (confirmed;also applied to systemd per my own scan); U81/U82 `d2b-provider-role`/`role-binding` tokio→dev-deps (confirmed,extended to the 10 sibling metadata crates with the identical test-only shape;U89 `d2b-provider-seccomp-profile` serde_json→dev;(confirmed,extended to notification-desktop, transport-vsock, shell-pool;U93 `d2b-unsafe-local-helper` unused d2b-core (confirmed. No lane claims contradicted beyond U49's "systemd consumes zone-plane" note (refuted by grep — systemd carries zero zone-session refs at HEAD.
- Data: root Cargo.toml + rust-toolchain.toml + 94 member Cargo.tomls + Cargo.lock,packages/Cargo.guest.lock (excluded,workspace members list.

Receipt (for U101)lane path | net | findings count
docs/audits/2026-09-23-ponytail-audit/lane/U100-workspace-deps.md | -61 deps,-61 Cargo.toml lines | 14 findings
