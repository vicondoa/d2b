# d2b-resource-runtime — unit-test audit
tests: 137 · src files: 15
net: -5 tests, -103 lines

Note: census counts 137 test attributes; 136 are real test fns — the extra hit is a
`#[tokio::test(start_paused = true)]` mention inside a doc comment (src/resource.rs:1551).
resource.rs itself has zero test fns (its `test_support` module is shared harness, not tests).

## Findings (biggest net first)
- duplicate: `async_recv_drains_then_missed_then_ends` (src/watch.rs:891) — covered by `slow_subscriber_gets_explicit_missed_never_silent_drop` (src/watch.rs:737). Both pin the same hub behavior: a subscriber whose delivery buffer overflows under publish churn receives an explicit `Missed` delivery (never a silent drop) with at most `delivery_buffer` changes delivered. The keeper pins strictly more: the Missed frontier equals the last delivered change and `events_after(missed)` maps onto the `RevisionExpired` relist path. The deleted test's only extra is consuming via std `recv()` instead of `try_recv()` — a consumer-side API difference, not hub behavior; its "ends" assertion is incidental (the stream closes when the hub drops at test end).
- trivial: `watch_and_manager_shapes_send_and_receive_intact` (src/context.rs:1594) — echoes protocol shapes (`WatchRegistration`, `StubCall::Get`, `WatchSatisfied`) through channels and asserts they arrive field-identical. Moving a value through a channel cannot transform it (Rust move semantics), and the Send bounds are compile-time. Nothing lost.
- trivial: `modules_resolve` (src/lib.rs:69) — echoes each module's `MODULE_NAME` constant against its literal name; the crate compiling already proves the module tree resolves. Nothing lost.
- trivial: `regenerate_failure_kind_reference` (src/error.rs:1026, `#[ignore]`d) — writes the committed reference doc and asserts nothing; the registry↔doc freshness contract is pinned by `failure_kind_reference_doc_matches_the_registry` (src/error.rs:1011). Deleting it would leave the documented `--ignored` regeneration command without a body (manual regeneration needed).
- trivial: `wire_budget_bounds_sequence_for_u32_low_word` (src/revision.rs:183) — asserts `WIRE_SEQUENCE_BUDGET == 1 << 32`, a constant echo of the definition beside it; the wire-format comment carries the intent. Nothing lost.

## Keep
- context.rs remaining 11 tests pin: ensure_child persist-before-commit-ack routing (exactly one EnsureChild, no spawn-shaped call), ManagerRpc surfacing for closed/dropped channels (get and view), classified lookup Present/Absent/Unavailable (row + view planes), requeue exactly-once scheduling and cancel-isolation under paused time, typed spec decode caching/downcast/mismatch, the evaluate-and-register single-mailbox-handler pattern, watch routing with subscriber identity, and ServiceResourceContext reads + fail-closed refusal.
- driver.rs 3 tests pin: erased-boundary failure classification (Transient→Retryable, Fatal→Terminal, satisfied path unchanged), erased finalize gating the driver's own drain behind owned children (children-draining NotYet, child delete re-requested, default no-op), and long-effect non-blocking InProgress + typed EffectCompleted with operation id.
- error.rs remaining 9 tests pin: SpecStoreError→ResourceError mapping (DeletingConflict vs Store wrap), DriverFailure shorthand class/outcome/defers taxonomy (retryable/terminal/not_yet/refused/error), log-line/wire-layer/Display projection equivalence, redacted comparisons, char-boundary note truncation, and the FailureKinds registry invariants (unique kebab codes, both notes, from_code roundtrip) plus the committed reference doc match.
- guest_target.rs 11 tests pin: idempotent per-source realization, session-bound capability fencing across reconnect, adoption rebinding (local + over-the-wire), stale-generation SessionUnavailable with no side effects, protocol-token frames, spec digest commitment, client round-trip symmetry, local stale fence (no channel trip) vs wire SessionUnavailable passthrough, and generation-regression refusal on bind.
- manager.rs 40 tests pin: ensure handle/dedup + commit-before-spawn (top-level and child) with restart recovery, guest assignment session binding + loss/reconnect notification, actor crash respawn + dependent notification, owned-child diff (create/retain/obsolete), child-first teardown holding (2-level, 3-level, crash-resume), deleting-conflict rejection, finalize-before-delete ordering, status generation fidelity (in-flight stale delivery, wire projection, projection publish/invalidate/drop on spec change/delete/failure), reconcile coalescing vs concurrency, watch immediate/exactly-once notification, driver context reads + readiness watches, live-read absent/unpublished semantics, requeue cancellation on delete, retry ladder (doubling/ceiling, deferral cadence vs error escalation, driver-named delay), NotYet/terminal-refusal publishing, zero-store-row status/watches, restart edge rebuild, re-parent refusal, metadata-only child ensure, and mailbox responsiveness under long effects.
- metadata.rs 6 tests pin: MetadataDriver validate (non-object refuse, empty-object admit, no manager calls), recover/delete/reconcile convergence without manager calls or status, child-first finalize drain, and failed child read = retryable.
- provider.rs 6 tests pin: directory register/lookup by type, duplicate-type refusal keeping the original, declared-driver registration + reporting + second-declaration refusal, foreign operation-ref ownership, plane-open gate, and create_driver resolution + UnknownType.
- revision.rs remaining 4 tests pin: lexicographic Ord (epoch then sequence), Display rendering, epoch stamping from clock nanos, and first-of-epoch sequence 1.
- spec_store.rs 13 tests pin: persist/reopen + idempotent migrations, commit durability across transactions, idempotent ensure generation, deleting-mark crash survival, ensure-against-deleting rejection + recreate after cleanup, no status write surface (audit ops only), concurrent writer serialization without SQLITE_BUSY, metadata-only ensure column writes, 0600/0700 file posture incl. suffixed-db side files, selector-filtered list, and the Busy-backpressure vs WriterGone-terminal taxonomy.
- target.rs 16 tests pin: host resolve/assign, host-vs-guest realization authority (directory and binding layers), guest realize idempotence + observe, disconnect keeping desired state, unanswerable guest = Unavailable never Absent, reconnect rebind + adoption, stale-generation refusal (realize/delete/observe, adopt-only crossing), stale/regressed/duplicate/zero session events, ZoneLink isolation (deletion, no synthesis, topology/availability independence), single-assignment-per-resource migration, NotAssigned authority, binding live-session fidelity, and canonical TargetRef parsing.
- watch.rs remaining 12 tests pin: LIST→WATCH no-gap handoff, selector filtering (replay + live), in-epoch ring replay, stale/future/lapped cursor Expired semantics, relist recovery, ring boundedness + subscriber reaping, strictly increasing revisions under status churn, zero-file/no-store-handle hub, and events_after list support.

Nothing else to cut: every remaining test pins an observable behavior, boundary, transition, or error path that no other test in the crate (src or tests/) covers. No `tests/` dir exists for this crate; cross-crate overlap was not resolved here.