# d2b-zone-routing - unit-test audit
tests: 116 · src files: 7
net: -24 tests, -488 lines

## Findings (biggest net first)
- duplicate: `a_second_advertiser_claiming_the_same_descendant_is_refused_as_multi_parent` (src/engine.rs:2879) - covered by `multi_parent_detection_vectors` (tests/route_engine_vectors.rs:775). A second advertiser claiming a live descendant is refused `MultiParent` and the first claim survives; both pin the same outcome/audit/state.
- duplicate: `an_allocation_for_another_edge_or_generation_is_refused` (src/engine.rs:2815) - covered by `advertisement_admission_vectors` (tests/route_engine_vectors.rs:399). Wrong-generation allocation → `NamespaceViolation`; routes beyond the allocation's route count → `NamespaceViolation`.
- duplicate: `a_route_outside_the_allocated_prefix_or_capability_scope_is_refused` (src/engine.rs:2778) - covered by `advertisement_admission_vectors` (tests/route_engine_vectors.rs:399). Route outside the allocated prefix → `NamespaceViolation`; capability above the allocated ceiling → `NamespaceViolation`.
- duplicate: `a_withdrawal_removes_only_the_named_live_routes` (src/engine.rs:3189) - covered by `withdrawal_vectors` (tests/route_engine_vectors.rs:1066). Withdrawal removes exactly the named rows, an unknown id withdraws `Accepted{empty}` idempotently, audit `ZoneAdvertisementWithdrawn`, projection empty.
- duplicate: `relay_forwarding_never_exceeds_the_initial_protocol_budget` (src/engine.rs:3159) - covered by `relay_budget_monotonicity_vector` (tests/route_engine_vectors.rs:1366). Relay chain strictly decreases from the initial budget, ends after exactly `ZONE_ROUTE_INITIAL_HOP_BUDGET` hops with `HopLimitExceeded`.
- duplicate: `a_relay_hop_refuses_an_exhausted_budget_a_dead_link_and_an_attachment` (src/engine.rs:3130) - covered by `relay_grant_vectors` (tests/route_engine_vectors.rs:1191). Exhausted budget → `HopLimitExceeded`; no admissions → `ZoneLinkDisconnected`; attachment offer → `AttachmentNotPermittedOverZoneLink`.
- duplicate: `an_expired_advertisement_and_a_future_dated_one_are_both_refused` (src/engine.rs:2728) - covered by `advertisement_admission_vectors` (tests/route_engine_vectors.rs:399). Window closed at receive → `Expired`; receive before issue → `MalformedAdvert`.
- duplicate: `an_advertisement_that_does_not_advance_its_issue_time_is_refused_as_replay` (src/engine.rs:2704) - covered by `replay_window_vectors` (tests/route_engine_vectors.rs:959). Re-presentation with a fresh signature ref but a non-advancing issue time → `Replay`.
- duplicate: `disjoint_trees_have_no_nearest_common_ancestor` (src/engine.rs:2576) - covered by `unknown_and_disjoint_zone_vectors` (tests/route_engine_vectors.rs:699). Target in a disjoint tree → `UnknownParent`.
- duplicate: `a_two_hop_downward_route_is_allowed_and_pays_its_hops` (src/engine.rs:2498) - covered by `k0_k1_k2_topology_scenario` (tests/route_engine_vectors.rs:1131) and `nearest_common_ancestor_vectors` (tests/route_engine_vectors.rs:570). k0→k2 allowed, 2 hops, NCA k0, budget decremented by 2, both hops down.
- duplicate: `a_replayed_advertisement_is_refused_on_the_exact_window_key` (src/engine.rs:2654) - covered by `replay_window_vectors` (tests/route_engine_vectors.rs:959). Byte-identical re-presentation inside the live window → `Replay`.
- duplicate: `an_advertisement_from_an_unknown_parent_is_refused` (src/engine.rs:2756) - covered by `advertisement_admission_vectors` (tests/route_engine_vectors.rs:399). Advertisement whose parent is not projected → `UnknownParent`.
- duplicate: `a_hop_budget_smaller_than_the_path_is_refused` (src/engine.rs:3045) - covered by `hop_count_boundary_vectors` (tests/route_engine_vectors.rs:1277). 2-hop path with 1 remaining → `HopLimitExceeded`; exactly 2 → allowed with 0 remaining.
- duplicate: `a_future_dated_withdrawal_is_refused_as_malformed` (src/engine.rs:3274) - covered by `withdrawal_vectors` (tests/route_engine_vectors.rs:1066). Future-dated withdrawal → `MalformedAdvert`.
- duplicate: `the_local_root_target_asserts_no_advertised_ceiling` (src/engine.rs:3028) - covered by `effective_capability_reporting_vectors` (tests/route_engine_vectors.rs:912) and `nearest_common_ancestor_vectors` (tests/route_engine_vectors.rs:570). Local-root target → zero-hop path, no advertised ceiling, budget unchanged.
- duplicate: `an_unknown_target_zone_is_refused` (src/engine.rs:2566) - covered by `unknown_and_disjoint_zone_vectors` (tests/route_engine_vectors.rs:699). Unknown target below a known parent → `UnknownParent`.
- duplicate: `a_capability_the_target_never_advertised_is_refused` (src/engine.rs:3018) - covered by `capability_ceiling_vectors` (tests/route_engine_vectors.rs:851). Capability nobody advertised → `MissingCapability`.
- duplicate: `an_exhausted_hop_budget_is_refused_before_the_walk` (src/engine.rs:3067) - covered by `hop_count_boundary_vectors` (tests/route_engine_vectors.rs:1277). Zero budget → `HopLimitExceeded` before the walk.
- duplicate: `remote_route_without_runtime_admission_is_refused` (src/engine.rs:2077) - covered by `request_defaults_refuse_before_any_projection_is_consulted` (src/engine.rs:2430). A request with no runtime admission is refused `PolicyDenial` before any projection; the keeper also pins the default hop budget and the `ZoneRouteDenied` audit event. cross-check: also pinned by `refusal_ordering_vectors` / `refusing_default_vectors` (tests/route_engine_vectors.rs:1407,1440).
- duplicate: `policy_denial_is_reported_before_the_tree_walk` (src/engine.rs:2488) - covered by `request_defaults_refuse_before_any_projection_is_consulted` (src/engine.rs:2430). Same no-admission → `PolicyDenial` behavior as the above; the keeper pins strictly more.
- duplicate: `projection_request_defaults_refuse` (src/service.rs:1503) - covered by `an_unauthenticated_projection_reports_every_remote_row_unreachable` (src/service.rs:1448). A default (no-admission) `ZoneTopologyRequest` leaves every projection row `Unreachable`; the keeper pins the exact `PolicyDenial` reason the weaker `matches!(Unreachable{..})` does not.
- duplicate: `an_unauthenticated_route_projection_refuses_a_remote_entrypoint` (src/resolver.rs:765) - covered by `request_defaults_refuse_before_any_topology_is_consulted` (src/resolver.rs:651). A request with no runtime admission → `PolicyDenial` before any topology is consulted; the keeper also pins the default hop budget and the `ZoneRouteDenied` audit event.
- trivial: `every_reason_the_engine_can_produce_is_covered_by_this_suite` (src/engine.rs:3333) - meta-tautology: asserts only that a literal local array of `ZoneRouteFailClosedReason`s has unique labels; it never consults engine or suite state, so it passes even if a listed reason stops being covered. Nothing lost.
- trivial: `durable_exec_table_is_bounded_to_ephemeral_processes` (src/router.rs:505) - insert/remove/len roundtrip on a `BTreeMap`-backed table; the namesake bound (`WrongExecutionType`, `Capacity`, `DuplicateOperation`) is not asserted anywhere. Nothing lost; the real bound is recorded as a gap.
- gap: `ZoneOperationRouter::begin`/`complete` error paths - `InvalidExpiry` (expiry ≤ now), `InvalidRequestDigest` (empty digest), `Capacity` at `max_rows`, and `complete` `UnknownOperation`/`OperationMismatch` (src/router.rs:282-351) have zero tests. These are closed refusals a caller hits on malformed input; only the happy path and `AlreadyComplete` are pinned.
- gap: `DurableExecTable` bound and rejections - `WrongExecutionType` for a non-`EphemeralProcess` ref, `DuplicateOperation`, and the `Capacity` ceiling (src/router.rs:306-326) are untested, so the table's namesake "bounded to ephemeral processes" promise is unpinned.

## Keep
- `admission_tuple_substitution_fails_closed_before_route_walk` (src/engine.rs:2087) - 8 snapshot-substitution axes (target uid, capability, generation, reconnect gen, verb, policy revision, edge, link uid, forged time) fail closed via the private `validate_snapshot`, unreachable from integration.
- `route_target_substitution_fails_closed_after_admission_consumption` (src/engine.rs:2164) - admission minted for a different target presented on the request → `PolicyDenial` after consumption.
- `remote_paths_without_a_capability_ceiling_fail_closed` (src/engine.rs:2175) - parent rows with no capability ceiling → `MissingCapability` across source/target shapes.
- `a_route_admission_cannot_be_reused_after_a_successful_route` (src/engine.rs:2237) - engine consumes the admission; second `decide_route` → `ZoneLinkDisconnected`.
- `admission_expiry_is_checked_after_an_initial_success` (src/engine.rs:2252) - live-clock admission refused `Expired` after a prior success.
- `admission_revocation_is_checked_after_an_initial_success` (src/engine.rs:2277) - revoked admission refused `ZoneLinkDisconnected` after a prior success.
- `request_defaults_refuse_before_any_projection_is_consulted` (src/engine.rs:2430) - keeper of the no-admission family: default hop budget + `PolicyDenial` + `ZoneRouteDenied` audit.
- `an_authenticated_child_session_seeds_its_exact_direct_edge` + `authenticated_edge_projection_requires_a_runtime_admission` (src/engine.rs:2446,2473) - authenticated-edge projection: exact edge seeds a 1-hop route; missing admission refuses it.
- `an_upward_and_downward_walk_meets_at_the_nearest_common_ancestor` (src/engine.rs:2520) - mixed up+down walk between deep zones in two subtrees meets at k0 with 4 hops; a shape the integration NCA vectors (down-only or up-only) do not cover.
- `a_cyclic_parent_chain_is_refused_as_a_loop` + `an_advertisement_that_would_close_a_parent_cycle_is_refused` (src/engine.rs:2599,2622) - `Loop` refusal reachable only via hand-injected private parent state; integration `loop_detection_vectors` pins only the admissible repeated-label side.
- `a_renewal_with_a_fresh_window_and_signature_reference_is_admitted` (src/engine.rs:2676) - renewal admitted plus superseded replay key dropped and narrowed inventory; the replay-key bookkeeping is beyond `replay_window_vectors`.
- `two_route_rows_for_one_descendant_in_one_advertisement_are_refused` (src/engine.rs:2856) - duplicate route rows in one advertisement → `MalformedAdvert`; no integration vector.
- `capacity_pressure_that_pruning_cannot_relieve_drops_the_new_advertisement` (src/engine.rs:2924) - capacity ceiling → `QueueFullDropNew` with empty inventory; no integration vector.
- `capability_scope_narrows_monotonically_across_a_multi_hop_walk` (src/engine.rs:2949) - narrowing at an intermediate hop (k1's renewal narrows k2's advertised set); effective ceilings reported and the dropped capability refused.
- `a_relay_hop_decrements_the_budget_only_with_both_independent_grants` (src/engine.rs:3078) - includes the `RelayDenied` case (relay grant carrying a non-relay capability), absent from `relay_grant_vectors`.
- `a_relay_admission_cannot_be_reused_after_a_successful_hop` (src/engine.rs:3112) - relay admission single-use; second hop → `ZoneLinkDisconnected`.
- `a_withdrawal_from_another_generation_or_zone_is_refused` (src/engine.rs:3223) - includes the predating-withdrawal → `Replay` case, absent from `withdrawal_vectors`.
- `expiry_sweeps_projection_state_and_makes_the_route_unknown_again` (src/engine.rs:3292) - exact sweep accounting (1 route / 2 parents / 1 replay key) plus route unknown after expiry; integration `expiry_sweep_vectors` asserts only `≥ 1`.
- `pre_auth_admission_drops_new_on_overflow_and_on_the_rate_ceiling` (src/engine.rs:3306) - private `decide_pre_auth_admission` overflow → `QueueFullDropNew`, rate ceiling → `RateLimited`, queued → `Queued` + audit event.
- remaining 37 service.rs tests pin: the wire-inventory/limits family (`every_method_has_a_distinct_kebab_wire_name`, `every_landed_method_dispatches_within_the_window`, `dispatch_admits_exactly_the_in_flight_ceiling…`, `an_unbalanced_release…`, `configured_bounds_refuse…`); projection reachability (sealed row order, root has no row, unsealed zone returns none, unauthenticated/stale/withdrawn rows unreachable); watch (first poll + monotonic revision, consumed admissions cannot be reused); resolution + auditing (served == resolver outcome, refused resolution audited with its reason); relay server wrapper (`a_forwarding_hop_needs…`, server-level audit event); shortcuts (entrypoint-routed authorization, refusal matching route refusal, revoke/report-close exactly-once, capacity bound); audit ring bound and redaction (`the_audit_ring_is_bounded…`, `no_audit_record_can_carry…`, `public_debug_renders…`); construction (`construction_rejects…`, `a_repeated_identical_row_is_projected_once`); and the enrollment handler state machine (bootstrap without admission, PSK burn against replay, substituted link/session refused before burn, unsealed edge, TTL range, expired-not-burned PSK, runtime-issued admission reaching the handler, substituted expectation refused at the handler, commit+enroll with allocator placement, enroll without bootstrap, fingerprint mismatch without downgrade, revoked authority refusing both handlers).
- remaining 19 resolver.rs tests pin: seal invariants (`a_target_can_only_match_one_sealed_entrypoint`, repeated-row idempotence, root-as-child and out-of-scope-subtree rejection), longest-suffix match vectors (exact, nearest sealed ancestor, unsealed-sibling fallback, foreign root → none), and resolution (defaults refuse, local root zero-hop, sealed descendant path, unsealed descendant → sealed ancestor entrypoint, unknown-below-root, out-of-subtree and foreign-root scope violations, absent/stale projection fail-closed, missing capability reported unchanged, public debug redaction).
- remaining 5 router.rs tests pin: dedup namespace (`same_key_replays_and_different_request_conflicts`, `same_key_while_running…`, `completion_is_exactly_once`, `expiry_keeps_a_tombstone…`) and `dedup_namespace_rejects_a_non_user_authenticated_subject`.
- 8 enrollment.rs tests pin: enrolled-guest-profile validation, issued-admission single-use + expiry, substituted sealed tuple refused terminally, stale/revoked fail-closed, cross-authority evidence refused, lifetime bound refused not clamped, admission admits only its identity, synthetic admission single-use + redacted debug.
- 2 serving.rs tests pin: runtime construction over a sealed topology with no links tracked; `decode_call` decodes bootstrap/enroll frames as exactly one call and rejects garbage.

route-out: none.
