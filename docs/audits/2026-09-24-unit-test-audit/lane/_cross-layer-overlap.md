# Cross-layer overlap (C2) - unit-test audit
net: -20 tests, -305 lines

## Findings (biggest net first)
- duplicate: `scm_rights_receipt_fd_does_not_inherit_across_exec` (packages/d2b-broker/src/fd_passing.rs:298) - covered by `scm_rights_transports_pidfd_with_cloexec_preserved` (packages/d2b-broker/tests/pidfd_handoff_scm_rights.rs::scm_rights_transports_pidfd_with_cloexec_preserved). Both pin: an SCM_RIGHTS-delivered fd carries FD_CLOEXEC, so it cannot leak across exec;the integration test asserts the flag directly on the same transport helpers (the live exec probe is a behavioral restatement of the flag invariant).
- duplicate: `storage_lifecycle_report_clean_is_pass` (packages/d2b/src/doctor.rs:2027) - covered by `host_doctor_clean_storage_lifecycle_report_passes` (packages/d2b/tests/host_doctor_contract.rs::host_doctor_clean_storage_lifecycle_report_passes). Both pin: a clean storage-lifecycle-report.json yields storage-lifecycle-report=pass.
.
 
- duplicate: `policy_runs_before_capacity_and_rejects_the_whole_frame` (packages/d2b-provider-observability-otel/src/ingress_policy.rs:855) - covered by `every_ingress_covers_each_policy_failure_and_capacity_rejection` (packages/d2b-provider-observability-otel/tests/ingress_metric_policy.rs::every_ingress_covers_each_policy_failure_and_capacity_rejection). Both pin: a valid frame with capacity unavailable → (Rejected, None) andunchanged quarantined state; an identity-forbidden frame ("vm"/"work") → (Rejected, KeyForbidden) - the integration matrix runs the same two cases per ingress через admit_for_connection (the unit's admit() is a thin wrapper).
- duplicate: `delete_persistent_tap_checks_both_fences_before_mutation` (packages/d2b-broker/src/ops/network.rs:1334) - covered by `delete_persistent_tap_rejects_stale_network_generation` + `delete_persistent_tap_rejects_stale_attachment_generation` (packages/d2b-broker/tests/persistent_tap_lifecycle.rs). Both pin:same fence pairs (3,7)→StaleNetworkGeneration;(4,6)→StaleAttachmentGeneration), each with 0 backend deletes - identical params, errors, and mutation assertions at engine and CLI-driver levels.

- duplicate: `kernel_module_matrix_clean_is_pass` (packages/d2b/src/doctor.rs:1902) - covered by `host_doctor_clean_kernel_module_report_passes` (packages/d2b/tests/host_doctor_contract.rs::host_doctor_clean_kernel_module_report_passes). Both pin:kernel-module-report.json with no missing/optional gaps yields kernel-module-matrix pass (clean→warn→fail status ladder's pass leg re-pinned end-to-end in tests/;optional→warn leg is not,so `kernel_module_matrix_optional_is_warn` stays).
- duplicate: `kernel_module_matrix_required_missing_is_fail` (packages/d2b/src/doctor.rs:1946) - covered by `host_doctor_missing_required_kernel_module_fails_exit_2` (packages/d2b/tests/host_doctor_contract.rs::host_doctor_missing_required_kernel_module_fails_exit_2)。Both pin:missing-required report → kernel-module-matrix fail + process exit 2.
.
 
- duplicate: `process_unique_token_and_identity_canaries_never_render` (packages/d2b-provider-credential-entra/src/audit.rs:39) - covered by `an_authorized_call_never_renders_the_presented_identity` (packages/d2b-provider-toolkit/src/credential.rs::an_authorized_call_never_renders_the_presented_identity). Both pin:the same `authorized_service_record` helper never renders raw identity/credential canary bytes into the wire record or Debug;the Entra delta (provider-kind/method params) changes no asserted behavior (canary hashing is unconditional after authorization).
- duplicate: `autostart_status_failed_is_fail` (packages/d2b/src/doctor.rs:1966) - covered by `host_doctor_autostart_failed_outcome_fails_exit_2` (packages/d2b/tests/host_doctor_contract.rs::host_doctor_autostart_failed_outcome_fails_exit_2). Both pin:autostart-report with a Failed outcome → autostart-status fail, data.failed==1, exit 2 (integration additionally pins degradedTotal==1).
- duplicate: `process_unique_entra_canary_is_rejected_from_closed_values` (packages/d2b-provider-credential-entra/src/telemetry.rs:34) - covered by `audit_denial_is_identity_silent_and_telemetry_values_are_closed` (packages/d2b-contracts-provider/src/v3/credential_controller.rs::audit_denial_is_identity_silent_and_telemetry_values_are_closed)。Both pin:an identity-canary value in the "outcome" field → validate_collector_fields fails, and a frame built by the closed builder passes validation - identical two-half scene, same helper (the Entra builder delta（method/rotation params）asserts nothing extra).
- duplicate: `process_unique_canary_is_rejected_as_an_allowed_key_value` (packages/d2b-provider-credential-secret-service/src/telemetry.rs:32) - covered by `audit_denial_is_identity_silent_and_telemetry_values_are_closed` (+ `a_frame_carries_only_closed_values`,packages/d2b-provider-toolkit/src/credential.rs::a_frame_carries_only_closed_values)。Both halves（canary-rejection + closed-builder-frame-validates) pinned by the contracts-provider test;the SecretService-kind frame validation additionally pinned by toolkit's credential_frame test.
.
 

- duplicate: `delete_persistent_tap_foreign_marker_fails_without_deletion` (packages/d2b-broker/src/ops/network.rs:1365) - covered by `delete_persistent_tap_foreign_marker_fails_closed` (packages/d2b-broker/tests/persistent_tap_lifecycle.rs::delete_persistent_tap_foreign_marker_fails_closed). Both pin:foreign ownership-marker in the realization row → ForeignOwnership, 0 backend deletes, present stays true - identical scenario/assertions(shared-right the kernel-tap unmarked-marker path is also covered at integration level by `delete_persistent_tap_refuses_unmarked_kernel_tap`;unit unpins nothing extra).
- duplicate: `provider_committed_control_shapes_are_admitted` (packages/d2b-provider-endpoint/src/driver.rs:1026) - covered by `desired_child_resource_keeps_the_authored_endpoint_spec` + `desired_child_resource_keeps_the_authored_ch_api_spec` (packages/d2bd/src/resource_runtime/plane_controller_bridge.rs:961,1002。。Both pin:ch-api/guest-control purposes realize as `EndpointRealization::GuestControl` under the committed purposes vocabulary;the d2bd pair additionally pins the materialized-envelope verbatim preservation (purpose/producer/locality) of the same admission, so the endpoint unit adds nothing.
.
 
- duplicate: `forbidden_identity_keys_fail_before_values` (packages/d2b-telemetry/src/metric_label_policy.rs:53) - covered by `forbidden_identity_keys_fail_structurally` (packages/d2b-provider-observability-otel/src/metric_policy.rs::forbidden_identity_keys_fail_structurally)。Both pin:every FORBIDDEN_LABEL_KEYS entry → KeyForbidden from the same re-exported `validate_label_key`, plus the same three identity-suffix probes("resource_name","zone_uid","link_name_hash") rejected - identical loop and vector;two consumer crates re-pin the contracts-provider validator twice.

- duplicate: `delete_persistent_tap_validated_absence_is_idempotent` (packages/d2b-broker/src/ops/network.rs:1354) - covered by `delete_persistent_tap_absent_is_idempotent_after_ownership_validation` (packages/d2b-broker/tests/persistent_tap_lifecycle.rs::delete_persistent_tap_absent_is_idempotent_after_ownership_validation。)Both pin:absent tap → ok result, 0 backend deletes - identical.
- duplicate: `exit_code_is_two_when_fail` (packages/d2b/src/doctor.rs:1812) - covered by `host_doctor_baseline_no_state_reports_broker_fail_exit_2`（+`host_doctor_missing_required_kernel_module_fails_exit_2`,packages/d2b/tests/host_doctor_contract.rs)。Both pin:any Fail check in the host-doctor report → process exit 2 + broker_ready false - the unit's direct mapping is exercised end-to-end by real report types at CLI level (warn→exit1/clean→exit0 legs stay:not re-pinned by tests/）。
- duplicate: `semaphore_refuses_at_cap_then_readmits_after_release` (packages/d2bd/src/composition.rs:24038) - covered by `semaphore_admits_up_to_cap_then_refuses` + `semaphore_permit_released_on_handler_thread_exit` (packages/d2bd-runtime/src/concurrency.rs::semaphore_admits_up_to_cap_then_refuses,packages/d2bd-runtime/src/concurrency.rs::semaphore_permit_released_on_handler_thread_exit)。Both pin:cap-hit refuses without blocking, release re-opens a slot, and all-drained→acquire-ok (the tail is the same release semantics as the sole-permit drop case)。
- duplicate: `autostart_status_degraded_is_warn` (packages/d2b/src/doctor.rs:1988) - covered by `host_doctor_autostart_degraded_outcome_warns` (packages/d2b/tests/host_doctor_contract.rs::host_doctor_autostart_degraded_outcome_warns). Both pin:autostart-report with only Degraded outcomes → autostart-status warn（+ data.degraded==1 per integration）;the all-started pass case stays (not re-pinned in tests/）。
- duplicate: `if_name_rejects_invalid_names` (packages/d2b-core/src/host.rs:553) - covered by `linux_limit_alphabet_and_prefix_reservation_fail_closed` (packages/d2b-contracts/src/v3/ifname.rs::linux_limit_alphabet_and_prefix_reservation_fail_closed)。Both pin:the same three rejections of `d2b_contracts::v3::IfName` (Empty/TooLong/InvalidCharacter) - a consumer crate re-tests the type's home-crate validator verbatim.

- duplicate: `if_name_accepts_safe_linux_names` (packages/d2b-core/src/host.rs:547) - covered by `collision_and_inconsistent_mapping_are_distinct_closed_failures`（+`linux_limit_alphabet_and_prefix_reservation_fail_closed`,packages/d2b-contracts/src/v3/ifname.rs:498,517）。Both pin:valid `IfName` names parse;the acceptance path for the same type is exercised at ifname.rs:517 (parse-unwrap of canonical names);the unit's as_str echo is a trivial field access。
- duplicate: `ambient_sdk_chain_names_are_rejected_without_reading_values` (packages/d2b-provider-credential-secret-service/src/lib.rs:2148) - covered by `ambient_sdk_chain_names_are_rejected_without_reading_values`（packages/d2b-contracts-provider/src/v3/credential_controller.rs::ambient_sdk_chain_names_are_rejected_without_reading_values）。Both pin:["PATH","RUST_LOG"] pass, ["AZURE_CLIENT_SECRET"] refusesthe same re-exported one-line wrapper,contracts adds the AWS_SESSION_TOKEN case。

。


 
## Keep
- d2b-broker `ops/network.rs` realization tests (`v3_realization_persists_and_removes_after_fenced_cleanup`, `v3_realization_reclaims_stale_temp_and_rejects_conflicting_row`, `deleted_realization_tombstone_makes_duplicate_cleanup_idempotent`) - complementary:integration pins persist-idempotence + conflict-no-write;unit pins remove→RealizationUnavailable, stale-.tmp reclaim,, tombstone idempotence (conflict core is double-pinned but each side keeps an extra）。- d2b-broker `protocol.rs` (`async_frames_carry_attached_descriptors`, `request_receiver_preserves_ordinary_fd_free_frames`, `a_bounded_dial_to_an_absent_peer_refuses_instead_of_waiting`) - complementary:w12/pidfd integrations cover the sync fd-carrying path;ers units pin the async path,, fd-free-frame receiver ("ordinary requests carry no SCM_RIGHTS"）, and bounded-dial refusal.`seccomp_compile_tests.rs` (4) - no integration overlap (kernel_surface.rs pins path/fd helpers, not BPF)）。
- d2b-broker `catalog.rs` committed-rows gate family vs `tests/broker_protocol_compatibility.rs` - complementary:integration pins the wire DECODER retirement matrix (old-frame refusals, gate machinery）;unit pins the catalog DATA self-consistency (rows↔views agreement, stub-deferral markers, variant resolution)--no overlapping assertion surface。
- d2b-contracts-broker serde roundtrip families (envelope/forward/context) - keep:no `tests/golden` broker fixtures;tests/wire.rs pins only capabilities + Debug-redaction;no sibling contract-crate re-pins of these wire shapes。
- d2b-contracts-control wire-shape pins（`vm_lifecycle_keeps_booted_variant`,`named_process_stream_frames_round_trip_without_identity_fields`,`console_/audio_public_wire_json_shape_is_stable`,`launch_requests_round_trip_and_correlate`) - keep:no golden fixtures,no consumer-side re-pins(its own tests/public_wire.rs covers only Capabilities + AuditResponse;d2bd consumes the types without re-asserting JSON shapes) - serialization-shape pins are the canonical wire contract。
- d2b-contracts-resource golden-byte vectors (`MINIMAL_*_SPEC`,`GOLDEN_ENVELOPE`,canonical/framed digests) + identity-scalar serde roundtrips - keep:consumer crates define THEIR OWN per-type MINIMAL_* literals (Provider/Guest specs are different types with different bytes);no sibling test re-asserts GOLDEN_ENVELOPE or the scalar serde shapes;tests/schema.rs pins ZoneResourceIdentity (different surface)。
- d2b-contracts-zone-session canonical-JSON/serde round-trips + the ServicePackage tag/wire vector - keep:no sibling contract-crate or golden re-pin of these canonical shapes（tests/contracts.rs covers only validate_enrolled_guest_session/generation-discovery）;the ServicePackage block duplication is inside-crate between two keepers (each pins additional distinct enum vectors),not cross-layer.
.
 
- d2b-contracts serde families (`SecurityKeyStatusResponse`/`SecurityKeySessionResult`,`WorkloadIdentity`/`WorkloadBackend`/`WorkloadRuntimeIntent`,transparent-string ids) - keep:no sibling contract/golden re-pins;consumers reconstruct/parse the types without asserting their standalone wire representations。
- d2b-core `host_json_*` et al - keep（IfName duplicates listed above;remaining host.rs tests are d2b-core-local surfaces this crate alone pins）;d2b-contracts re-export is the covering home,see Findings。
- d2b-host `validation_request_is_bounded_and_typed` - keep:`parse_validation_request` is a distinct helper from the integration-tested `parse_request`;tests/host_generation.rs pins oversize/path/unknown-field on the request variant, not the validation variant's success+typo-reject path。
- d2b-host hardlink-farm split-path unit tests - keep:collision refusal is double-pinned（integration adds typed-error-on-stdout CLI contract）while the unit tests uniquely pin the fast-path readiness gate, swap-order-with-marker-last,, guest meta.json allow-list,, and same-id idempotent rebuild。
- d2b-provider-credential-secret-service `process_unique_identity_canary_is_hashed_only_after_authorization` - keep:denied→no-record and authorized→marker-absent halves are pinned by toolkit, but the `resource_name_digest=sha256:` wire-format assertion is unique to this crate。
- d2b-provider-device-usbip rooted-plan busid tests (`explicit_plan_accepts_valid_busid_shapes`/`rejects_invalid_busid_shapes`) - keep:busid shape vectors overlap contracts' `validate_bus_id` unit tests and tests/conformance.rs, but the Lock-step error tagging on `build_usbip_explicit_plan` is pinned only here;the raw-validator shape lists are the authority at home.
- d2b-provider-endpoint `the_derived_families_follow_the_declaring_providers` - keep:d2bd's inspect-endpoint integration pins only presence of "ch-api"/"swtpm-tpm-socket" in the served report+realization count;unit pins full producer/class mappings + negative exclusions。

- d2b-provider-guest-cloud-hypervisor `guest_child_graph_is_deterministic_name_addressed_and_redacted` - keep:name-addressed/UID-free/redaction halves are pinned by three tests/(integration tests, but порядок creation/deletion determinism + `plan_children ≡ from_descriptor` equivalence are unique to the unit。
- d2b-provider-observability-otel metric-policy triple（`forbidden_identity_keys_fail_structurally`,`descriptor_validation_rejects_identity_canaries`,`data_point_label_set_mismatch_precedes_actual_label_policy`) - keep:d2b-contracts-provider's sole telemetry_policy test pins only descriptor-registry closure;the runtime policy applications (with canary/LabelSetMismatch precedence) are unique to this consumer（the telemetry crate's own version of the forbidden-keys loop is the cross-crate dup listed above）。
- d2b-provider-operation `a_materialized_operation_reuses_the_command_payload_contract` - keep:d2bd foundation_seed integration pins owner_ref+payload+secret-access propagation through the seeding pipeline;unit pins constructor-side contract（no wireTag, audit-join admission）-- overlapping owner_ref only;xtask zone_schema golden pins the JSON SCHEMA generation（a different artifact from value wire bytes）。
- d2b-provider-process `factory_registers_both_process_family_resource_types` + `family_descriptors_register_both_member_types` - keep:integration registration test pins registered-types order + requires-plane-registration + not-exportable;unit pins exact allowed_sources membership（BUILTIN+STARTUP,excludes RUNTIME）+ decoder count + factory.create for EphemeralProcess（integration only creates Process via registry）-- each side keeps extra pins。
- d2b-provider-provider unit-test reconcile semantics - keep:d2bd plane registers the Provider driver with `FailClosedProviderDriverEffects` (fail-closed vtable,so plane tests can't exercise the real validate/recover/reconcile logic）;the unit suite is the only pin of the family's reconcile transitions。
- d2b-provider-seccomp-profile `the_wire_shape_round_trips_and_is_closed` - keep:contracts-resource's golden vectors cover only its OWN spec types(SeccompProfileSpec is defined here);no sibling re-pin of the canonical roundtrip or the unknown-field rejection。
- d2b-resource-types `every_well_known_type_is_distinct` + `names_round_trip_through_the_runtime_name` - keep:`V3_CONVERTED_RESOURCE_TYPES` membership/plane-resolution is pinned by d2b-contracts's converted-registry test,but no test elsewhere asserts the generated list has no duplicates or the runtime `to_resource_type_name` conversion preserves these specific literals。
- d2b-telemetry `trace_context.rs`/`audit_hash.rs` surfaces + remaining metric-label tests - keep:no sibling crate re-tests the telemetry types' canonical-digest/Debug-masking or the consumer-side policy composites（the contracts-provider home pins only registry closure;the telemetry crate's descriptor+canary and resource-attributes tests each carry membership/healthy-pass halves the otel crate lacks）。
- d2bd audio capability constants（`ch_nixos_cap_is_pipewire_target_process`,`qemu_media_cap_is_host_only`）- keep:no provider-crate unit or integration test pins `audio_capability()` at all（the d2bd pins are the only coverage of the capability constants）。
- d2bd host-network-admission family - keep:`HostNetworkAdmissionIndex` is defined in d2bd itself;no network-local test re-pins the admission decisions（network-local's own tests cover its internal helpers,not this index）。
- d2bd-runtime gap cross-checks - no corrections:greps of d2bd/tests found no coverage of `resource_api` non-list parsers,broker_transport fail-closed/timeout,or `resource_operator_activation` selection error paths--the lane's gap claims stand。
- d2b doctor leftover（`kernel_module_matrix_optional_is_warn`,`autostart_status_pass_when_all_started`,`exit_code_is_one_when_only_warn`,`exit_code_zero_when_clean`,degraded/storage variants...) - keep:no tests/ counterpart pins the optional→warn, all-started→pass,or warn→exit1/clean→exit0 legs of the doctor ladder（the d2b lane's "exit-code ladder re-pinned end-to-end" claim is over-broad：warn/clean legs are unit-only）。
- d2b dispatch parser tests（7) - keep:the integration probes cover many of the same command surfaces（incl. retired-namespace rejections→`RETIRED_PARSER_PROBES`）but every unit test carries at least one pin the probes lack（guest-start-with-ambient-zone, positional-zone precedence, bare-list rejection + resource_type field, unknown-provider-command,usb-probe/activation-apply/endpoint-resolve acceptances…）。
- d2b zone_doctor `all_ready_is_zero_and_otel_absence_is_a_warning` - keep:integration warn test has zone_phase "unavailable" as a co-cause,so it cannot isolate otel-absence-on-a-Ready-zone→exit1;the unit's isolated scenario is unique（the ready/warn integration pair re-pins the two separate legs）。- d2b-zone-routing `remote_route_without_runtime_admission_is_refused`（its lane duplicate finding）- supported:the same no-admission→PolicyDenial refusal is also pinned at integration level by `refusal_ordering_vectors`/`refusing_default_vectors`（tests/route_engine_vectors.rs:1407,1440),so the deletion loses nothing。
- xtask `bazel_evidence.rs` unit tests vs `tests/bazel_evidence.rs` - complementary as the lane judged:unit pins exact classification JSON + redaction text-shaping;integration pins real CLI runs + exit codes。