# d2bd — unit-test audit
tests: 474 · src files: 25
net: -13 tests, -164 lines

## Findings (biggest net first)
- duplicate: `qemu_controller_never_calls_target_process_path` (src/audio_dispatch.rs:812) — covered by `fake_controller_success_guest_unavailable_maps_to_host_only` (src/audio_dispatch.rs:756). Both pin `combined_audio_applied(Applied, non-None cap)` → `HostOnly`; the guest-enforcement kind is not consulted by the mapping, and `QemuAudioController.enforce_level` returning Applied is the same unconditional constant the keeper `qemu_controller_grant_is_applied` (src/audio_host_controller.rs:403) pins.
- duplicate: `qemu_controller_applied_maps_to_host_only` (src/audio_dispatch.rs:801) — covered by `fake_controller_success_guest_unavailable_maps_to_host_only` (src/audio_dispatch.rs:756). Same `Applied` → `HostOnly` mapping for the same non-None cap class (ch vs qemu cap is indistinguishable to `combined_audio_applied`); the controller half is pinned by `qemu_controller_grant_is_applied` (src/audio_host_controller.rs:403).
- duplicate: `fake_controller_failure_on_level_maps_to_unsupported` (src/audio_dispatch.rs:789) — covered by `fake_controller_failure_on_off_maps_to_unsupported_not_success` (src/audio_dispatch.rs:771). Both pin `combined_audio_applied(Failed, non-None cap)` → `Unsupported` (cap and op are not consulted).
- duplicate: `qemu_controller_level_is_applied` (src/audio_host_controller.rs:412) — covered by `qemu_controller_grant_is_applied` (src/audio_host_controller.rs:403). The impl returns the constant `Applied` for every input; the level method cannot differ from the grant method.
- duplicate: `qemu_controller_on_grant_is_applied` (src/audio_host_controller.rs:422) — covered by `qemu_controller_grant_is_applied` (src/audio_host_controller.rs:403). Same unconditional constant, same method, only the (ignored) grant input differs.
- duplicate: `a_context_minted_against_an_older_provider_set_revision_is_refused` (src/forward_rendezvous.rs:3435) — covered by `the_daemon_publishes_over_the_origination_leg_and_advances_on_the_ack` (src/forward_rendezvous.rs:3726). The covering test's stale-refusal loop asserts `STALE_CONTEXT` for `context_for(7, "test", 2, 1)` against published revision 1 — the same older-revision refusal this test pins in isolation.
- duplicate: `a_context_minted_against_another_zone_is_refused` (src/forward_rendezvous.rs:3479) — covered by `the_daemon_publishes_over_the_origination_leg_and_advances_on_the_ack` (src/forward_rendezvous.rs:3726). The covering test's stale loop asserts `STALE_CONTEXT` for `context_for(7, "other", 1, 1)` — the same other-zone refusal.
- duplicate: `the_plane_drains_its_providers` (src/resource_plane_v3.rs:5240) — covered by `providers_start_in_the_committed_order_and_drain_in_reverse` (src/resource_plane_v3.rs:5198). The keeper pinned the exact committed startup order and asserts `drain_order == reversed(startup_order)`, which is the entire reverse-drain invariant this test re-asserts through the plane wrapper.
- trivial: `fake_success_returns_applied_for_grant` (src/audio_host_controller.rs:349) — mock-echo of a `#[cfg(test)]`-only fake (configures Applied, asserts Applied). Nothing lost; the fake's grant path is exercised by the kept audio_dispatch mapping tests.
- trivial: `fake_success_returns_applied_for_level` (src/audio_host_controller.rs:358) — mock-echo of configurable fake; nothing lost.
- trivial: `fake_failed_returns_failed_for_grant` (src/audio_host_controller.rs:368) — mock-echo of configurable fake; nothing lost.
- trivial: `fake_failed_returns_failed_for_level` (src/audio_host_controller.rs:377) — mock-echo of configurable fake; nothing lost.
- trivial: `fake_unsupported_returns_unsupported` (src/audio_host_controller.rs:387) — mock-echo of configurable fake; deleting it also makes the unused `FakeHostController::unsupported()` constructor (src/audio_host_controller.rs:275-280) dead → remove with it. Nothing lost.
- gap: the full vm-start supervisor DAG drive is `#[ignore]`d as flaky (src/composition.rs:25679 `vm_start_drives_supervisor_dag_in_topo_order`) — the only end-to-end fake-broker test of spawn order/readiness on a real VM DAG never runs in CI. The three root-gated siblings (`vm_start_broker_unreachable_returns_broker_error` 25421, `vm_start_registers_pidfd_table_entry_from_broker_fd` 25491, `vm_restart_stops_then_surfaces_start_failure` 28204) also skip without `test_root`. Broken promise cluster.
- gap: local SIGTERM→SIGKILL stop escalation is `#[ignore]`d as flaky (src/composition.rs:27132 `vm_stop_escalates_to_sigkill_after_term_timeout`) — a term-ignoring runner's escalation is never pinned in CI; the broker-fallback variants are covered but the daemon-local escalation path is not.
- gap: audio dispatch error paths are untested (src/audio_dispatch.rs:386-680) — `dispatch_audio_status` / `resolve_vm_audio_status` `VmNotFound` / `AudioNotEnabled` / `InternalError`, and the set-volume/mute fail-closed returns (`level_increase && Failed → InternalIo`, grant `On` + `Failed → InternalIo`) have zero tests; only the `combined_audio_applied` mapping helper is pinned.

cross-check (C2): `semaphore_refuses_at_cap_then_readmits_after_release` (src/composition.rs:24037) re-tests `d2bd_runtime::concurrency::ConnSemaphore` — likely duplicated by d2bd-runtime's own tests.
cross-check (C2): audio capability constants pinned at src/audio_dispatch.rs:694-731 (`d2b_provider_guest_cloud_hypervisor::audio_capability`, `d2b_provider_guest_qemu_media::audio_capability`) — likely also pinned in the provider crates.
cross-check (C2): the host-network-admission family (src/resource_runtime.rs:12466-12915) drives `d2b_provider_network_local`'s `HostNetworkAdmissionIndex` directly — likely also pinned in d2b-provider-network-local.

## Keep
- 8 keepers in audio_dispatch: ch-nixos/qemu-media/aca capability rows, `public_enforcement_posture` mapping, Applied→HostOnly + Failed→Unsupported mappings, missing-old-level increase classifier, decrease classifier.
- 7 keepers in audio_host_controller: `qemu_controller_grant_is_applied` (implicit Applied contract), PipeWire construction from runner identity, broker-unavailable fails closed, `find_audio_node` absent/audio-role, pw-dump target selects channel / rejects ambiguity.
- composition.rs 157 keepers: topology commit/refusal; gateway composition session states, key fencing, classification and op-id binding; tap provenance + deletion gate; workload observability metrics/status/launch-refusal audit (exactly-one bounded signal); session-target admission incl. deletion marks; guest-target reconnect adoption + journal publication; cache modes; launcher-broker relabel; public status/list lifecycle family (resolver-load matrix, per-state lifecycle, qemu-media projections, read-model cache + fingerprint race, order preservation, pending-restart); detached-process resource family (ref forms, owner/finalizer payloads, admin authz, fail-closed connector); accept-loop family (feature negotiation, off-loop owner dispatch, permit lifetime, peer classification precedence, busy frames); vm-start family (store-view intake, preflight/lease callers, redaction, destructive-verb authz matrix, sidecar modes, stale-dependency cleanup, pidfd registration, DAG topo, startup adoption/quarantine, snapshot eligibility); vm-stop family (local stop, usbip degradation skips, sidecar drain, pidfd broker-fallback matrix ×10, ECHILD poll/deadline, force-generation, graceful-shutdown + manifest policy, cgroup-kill); readiness family (6 scripted wait_for_readiness verdicts); typed-shell family (error mapping, alias conflict, ref mapping, concurrent reserve, cache bounds/recency/validation, audit digest, helper ids); provider-identity seeding (late/absent/complete); loader-seat refusals (busy + child-death).
- credential_resource_runtime.rs 4 keepers: generation-mismatch Uncertain, rejoin rebind + identical op id, wrong-zone/wrong-guest + reconnect refuse before any resource read.
- effect_service_actors.rs 5 keepers: publish+republish answers, kill→respawn + stale/dead refusal, in-flight refusal, timer requeue, durable-row recovery.
- forward_rendezvous.rs 39 keepers (41 - 2 stale-fence dups): forwarded call/refusal/error code relay; stall/crash/cap/peer refusals; fd-leg count/kind/ceiling refusals; context fencing (zero-epoch, lower guest generation, mutation, broker-restart epoch, budget bound + ceiling); publication ack/refused/unreachable fail-closed; effect-service dispatch through the rendezvous (answer, uncommitted, respawn, in-flight stale, republish, decline, fd return, driver-context read, code mapping); chain records (root-once, nested-correlation, depth cap, attested identity); pidfd kernel leg (hermetic decode + end-to-end mint).
- foundation_seed.rs 7 keepers: commit order + idempotent reseed, unresolved refs (role/seccomp/principal), self-binding-required, operator-binding-insufficient, undeclared verb/type, binding scope fences.
- interaction_composition.rs 26 keepers: durable display payloads/names/annotation, op-id bounds, idempotent stop, ttrpc correlation, endpoint policy, clipboard kinds, listener stop/paths, reactor admit, session lookup ownership/contention, committed reconcile + wrong-guest, finalization, hermetic multi-service dispatch, picker receipt conservation, transport authz, identity validation, display-only readiness, notification queue/authority, handler/effect caps + reap, single-listener flow + replay refusal, peer credentials.
- plane_port.rs 10 keepers: root claim/undeclared/overlap/escape/missing refusals, adapter dependency order, duplicate-claim ledger seat, undeclared service, release precision.
- principal_allocation.rs 7 keepers: committed doc, reload reuse, overlap/exhaustion, invalid names/duplicate ids, host-table ownership checks, malformed tables.
- process_provider_runtime.rs 28 keepers: gate conversion, bootstrap fence, worker argv render/swtpm namespace/path fence/gpu shape/wire shape, stable uid/token, readiness expectation, bootstrap wait ok/timeout, host/guest execution cross-refusals, fixed provider set, identity-change/retire semantics, retry classification, bootstrap context binding, static/device/ephemeral tickets, GPU settings/decoders, observer replace/keep.
- provider_effects.rs 33 keepers: adapter/validate/denied, dispatch once+dedup, pre-effect fences, rejected-not-fallback, start/stop/start sequencing, persistent dedup/restart, stop-intent TTL survival, legacy generation migration ×4, pending reexecute/reconcile/unavailable, opposite pending retention, concurrent opposite + same-op, TTL survival, persist-failure release, generation serialization/restart preserves, expired prune, cycle retirement, identity persistence, quarantine ×3, stop-only lease, substitution refusal, fresh identity not blocked.
- provider_lifecycle.rs 12 keepers: start/drain order, named refusals ×3, service hosting ×4 (answer/respawn/republish/refuse ×2/facets), driver-context read, restart rehost.
- provider_registry.rs 8 keepers: registry resolution, provider-ref gate, typed effect + unknown/caller refusals, unique op ids, unconfigured refusal, capability gate, controller deployment, v3 path.
- resource_plane_v3.rs 48 keepers (49 - 1 drain dup): composition-hosted service + factory refusal; 12 family effects answers; 3 restart rehosts; kernel-name agreement; converted-type authority + committed order; registry-catalog mismatch; identity publish; assembly checklist; store-derived lookup; bundle ingest/applied/refused + api-protection + deleting marks; controller child→driver + owner scope; endpoint realization; anchored walk; zone-local refusal; foundation seed commit + vocabulary read; notice drain family (single/burst/between/uncovered/status/sustained/restart/relist ×3); projection law ×2; role-ful anchor replacement.
- resource_runtime.rs 42 keepers: incarnation fence, catalog gates, spec merge, bootstrap wake, publication marker, session binding match, error-classification matrix ×5, publication gate, retryable projection, finalizer report, bootstrap accept/reject, evidence policy, early fragment, device bindings ×2, tpm path, network admission ×15, volume-binding gate, policy digest/revision, page-bound merge, relist fence.
- shared_provider_effects.rs 4 keepers: phase delegation, bundle replacement observation, tamper journal, same-zone guest admission.
- zone_enrollment.rs 7 keepers: socket path, admission identity, link row facts, zone/guest negatives, enroll flow, stale/live socket handling, non-socket refusal.
- plane_controller_bridge.rs 6 keepers: routing table, CH child set, envelope split, foreign-envelope refusal, endpoint/ch-api spec preservation.

[count reconciliation: 458 keepers = 8+7+157+4+5+39+7+26+10+7+28+33+12+8+48+42+4+7+6; 471 census fns = 458 + 13 cut (8 duplicate + 5 trivial)]
