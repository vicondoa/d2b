# d2b-contracts-zone-session - unit-test audit
tests: 71 · src files: 15
net: -0 tests, -0 lines

## Findings (biggest net first)
- gap: `validate_finalizer` rejects any finalizer other than the drain finalizer (src/v3/zone.rs:423) - the only error path in the Zone contract with no test anywhere in the crate (src or tests/).
- gap: `BootstrapIdentityBinding::validate` rejects subject refs outside Host/Guest/Provider/Zone/Process (src/v3/component_session.rs:2346) - untested error path.
- cross-check: canonical-JSON/serde round-trip tests (`roles_with_posture_round_trip_through_canonical_json`, `bindings_with_every_scope_facet_round_trip_through_canonical_json`, `golden_advertisement_vector_renders_canonical_bytes_and_round_trips`, `golden_route_path_and_failure_labels_are_stable`, `enrollment_payloads_round_trip_within_the_bound`, `bundle_sorts_rows_and_hashes_only_the_resource_array`, `bundle_accepts_nix_omitted_optional_metadata`, `dynamic_process_template_binding_does_not_require_a_bundle_process`, `serde_round_trips_through_the_frozen_wire_string`, `relay_authority_uses_the_authoring_spellings`, `export_spec_is_strict_and_canonicalizes_lists`, `import_is_local_route_and_same_type_projection`) have no in-crate covering test - tests/contracts.rs covers only `validate_enrolled_guest_session`/`validate_generation_discovery`. Flag for C2: check tests/golden/ and sibling contracts crates (d2b-contracts-resource, d2b-contracts-broker) for generated-schema/contract coverage.
- cross-check: `wire_enum_vectors_are_frozen` (src/v3/component_session.rs:3226) and `frozen_tag_and_wire_string_vectors_are_exact` (src/v3/zone_session.rs:788) both pin the identical ServicePackage tag/wire vector (byte-same const); each additionally pins distinct enums (TransportClass/AttachmentKind/AttachmentPurpose vs EndpointPurpose/EndpointRole), so both keep - but the ServicePackage block is duplicated. Flag for C2: sibling contracts crates may re-pin the same ServicePackage vector.

Nothing to cut. Ship. All 71 tests checked against every other test in the crate (same file, same module, other modules) plus the crate's tests/contracts.rs; each pins a distinct observable contract (frozen wire vectors, constructor invariants, fail-closed decode paths, Debug redaction, bounds, round-trip fidelity) with no two tests pinning identical behavior.

## Keep
- zone_session.rs (13 tests) - frozen tag/wire vectors, tag round-trip + uniqueness, wire-string uniqueness, reserved-tag fail-closed, serde via frozen wire string, unknown-string rejection, purpose-class admission matrix, component-session lift/lower totality, Debug variant-name shape, absent guest-credential surface, enrollment payload round-trip/bound, enrollment refusal paths (truncated/empty/oversize/zero-digest/zero-issuance/zero-generation), refusal label stability.
- zone_routing.rs (12 tests) - golden canonical bytes + round-trip, golden route-path bytes + fail-closed reason labels, audit-label uniqueness/zone prefix, zone-path bounds/ancestry/next-hop, advertisement constructor invariants (version/empty/dup/expiry/sibling/ancestor/self/next-hop), capability-scope narrowing + wire privacy + subset ordering, withdrawal + namespace-allocation bounds, hop direction/contiguity/ceiling, opaque token shapes, admission-request intent surface, fail-closed wire decoding (tamper/downgrade/unknown fields), Debug redaction of all routing types.
- role.rs (9 tests) - resource/session verb separation, relay scope gating (nonempty, never CRUD), provenance-bound wildcard, Operation/Command ref type gating, Principal/Seccomp ref gating, umask > 0o777 refusal, capability/mount bounds + mount-path shapes, full posture canonical round-trip, facet defaults.
- role_binding.rs (7 tests) - role-ref + nonempty-subject requirement, duplicate-subject rejection, scope-narrowing subset, ref/zone/execution bounds, relay-authority spellings + unknown rejection, full-facet canonical round-trip, facet defaults.
- resource_bundle.rs (7 tests) - binary-path mismatch rejection, dynamic template without bundle process, row sorting + zone_uid + round-trip, omitted optional metadata, forbidden private fields, content-tamper rejection, system-minijail provider requirement.
- component_session.rs (7 tests) - identifier/channel/handshake Debug redaction, named-stream queue invariant, zone-link profile validation matrix (10 mutations), zero reconnect-generation rejection, frozen wire enum vectors.
- zone.rs (5 tests) - exactly-empty spec, self-resource identity check, handler sorting + mandatory-ready, frozen hyphenated handler wire names, mandatory-unique system-core pair.
- resource_export.rs (3 tests) - strict construction + list canonicalization, wrong-type/duplicate rejection, quota bounds + policy Debug redaction.
- services.rs (3 tests) - fingerprint sensitivity to methods, frozen package wire values, audit-gap detection.
- resource_import.rs (2 tests) - same-type projection + validation against export, remote-ref/capability-widening rejection.
- zone_link.rs (2 tests) - closed child/transport-provider naming, intent-queue bound.
- emergency_policy.rs (1 test) - restrictive union + tightest deadline.
