# d2b-contracts-broker — unit-test audit
tests: 49 · src files: 5
net: -6 tests, -100 lines

## Findings (biggest net first)
- duplicate: `broker_request_envelope_round_trips_with_admin` (src/broker_wire.rs:3181) — covered by `envelope_invoke_round_trips_root_and_nested` (src/broker_wire.rs:4298). Both pin the root `BrokerRequestEnvelope` carrying `EnvelopeInvoke("signal-pidfd", zone-a, AdminUid)` round-tripping through canonical frames; the keeper additionally pins the nested request, response-result, and response-refusal variants. 3181's only extra (non-empty `fd_indexes`/`fd_kinds` on the envelope request) is the same fd-declaration mechanism pinned by `forwarded_fd_declarations_round_trip` (src/broker_wire.rs:4357) and `spawn_runner_response_round_trips` (src/broker_wire.rs:4240).
- duplicate: `set_bridge_port_flags_rejects_raw_bridge_field` (src/broker_wire.rs:3436) — covered by `set_bridge_port_flags_rejects_each_legacy_authority_field` (src/broker_wire.rs:3613). Both pin that raw bridge/port/isolated/neighSuppress fields make the `SetBridgePortFlagsRequest` typed payload unparseable (`deny_unknown_fields`); the keeper asserts each field individually (plus `rule`) instead of all four in one payload.
- duplicate: `create_persistent_tap_rejects_raw_ifname_field` (src/broker_wire.rs:3460) — covered by `create_persistent_tap_rejects_each_legacy_authority_field` (src/broker_wire.rs:3565). Both pin that a raw `ifnameDerived` field fails the `CreatePersistentTapRequest` typed parse; the keeper loops the same field among seven.
- duplicate: `create_tap_fd_rejects_invalid_ifname` (src/broker_wire.rs:3803) — covered by `create_tap_fd_rejects_each_legacy_authority_field` (src/broker_wire.rs:3589). Both pin that a raw `ifnameDerived` field fails the `CreateTapFdRequest` typed parse; the keeper loops the same field among seven.
- duplicate: `usbip_bind_firewall_rule_rejects_raw_bus_id_field` (src/broker_wire.rs:3481) — covered by `usbip_bind_firewall_rule_rejects_each_legacy_authority_field` (src/broker_wire.rs:3629). Both pin that a raw `busId` field is refused by `UsbipBindFirewallRuleRequest`'s `deny_unknown_fields` (3481 via direct variant decode, 3629 via envelope payload parse of the same struct); the keeper also covers `ruleHash`.
- duplicate: `broker_caller_role_default_is_not_authorized` (src/broker_wire.rs:3135) — covered by `broker_request_envelope_default_caller_role_is_not_authorized` (src/broker_wire.rs:3205). Both pin "default caller role is NotAuthorized": the envelope field carries `#[serde(default)]`, so the wire-level default routes through `Default::default()` — any change to the `#[default]` variant breaks the wire test too.
- gap: RunnerRole kebab-case wire tokens for `ProviderController`, `QemuMedia`, `ActivationNixos` unpinned (`spawn_runner_role_kebab_case_serialization` covers 11 of 14 variants; enum at src/broker_wire.rs:2377) — the test exists to pin tokens against rename, so a rename of any of these three would silently change the wire code.
- gap: `for_display` audit labels for `RootUid` and `HostShutdownUid` unpinned (`broker_caller_role_display_uses_stable_audit_labels` covers 3 of 5 variants; impl at src/broker_wire.rs:2919) — these labels are wire-visible audit contract; `RootUid` currently returns the non-conforming literal "RootUid".
- cross-check: unit serde roundtrips (envelope/forward/context families) may duplicate contract or golden wire tests — no `tests/golden` broker fixtures and no `d2b-contracts` re-tests of `broker_wire` shapes found in this crate; C2 to confirm against `tests/golden/` and sibling contract crates.
- route-out: `RunnerRole::ActivationNixos` serde kebab-case token is "activation-nixos" but `as_str()` returns "activation-nixos-runner" (src/broker_wire.rs:2377/2404) — the wire token and the role_id string disagree for the same role.
- route-out: `BrokerCallerRole::for_display` returns "RootUid" for `RootUid` (src/broker_wire.rs:2922) — inconsistent with the stable `d2b-*` audit-label convention of every sibling variant.

## Keep
- remaining 15 tests pin positive wire shapes: `pipewire_audio_request_is_opaque_and_closed`, `storage_and_sync_requests_are_opaque_only`, `usbip_bind_firewall_rule_round_trips`, `usbip_proxy_reconcile_carries_optional_trace_context`, `create_persistent_tap_request_requires_admitted_provenance`, `create_tap_fd_request_requires_admitted_provenance`, `set_bridge_port_flags_request_is_opaque_only`, `apply_nftables_request_is_opaque_only`, `apply_nftables_projection_requires_installed_generation_fence`, `delete_persistent_tap_requires_opaque_id_and_both_generation_fences`, `launch_minijail_child_carries_only_role_and_vm`, `spawn_runner_request_is_opaque_only`, `signal_runner_request_round_trips`, `cgroup_kill_request_round_trips`, `user_manager_check_round_trips` — each opaque-only typed request decodes from its wire frame with the admitted fields intact.
- remaining 10 tests pin fail-closed rejection: `tap_create_rejects_missing_provenance_fields`, `tap_create_rejects_null_provenance_fields` (distinct failure modes, neither covers the other), `launch_minijail_child_rejects_inline_authority_fields`, `spawn_runner_rejects_each_legacy_authority_field`, `spawn_runner_runtime_allocation_unknown_kind_rejected`, `create_persistent_tap_rejects_each_legacy_authority_field`, `create_tap_fd_rejects_each_legacy_authority_field`, `set_bridge_port_flags_rejects_each_legacy_authority_field`, `usbip_bind_firewall_rule_rejects_each_legacy_authority_field`, `unknown_broker_variant_fails_closed` — legacy authority fields, null/missing provenance, unknown allocation kinds, and unknown variants all fail closed.
- remaining 8 tests pin role/envelope contracts: `broker_caller_role_admin_passes_predicate`, `broker_caller_role_display_uses_stable_audit_labels`, `broker_caller_role_round_trips`, `broker_request_envelope_default_caller_role_is_not_authorized`, `envelope_invoke_round_trips_root_and_nested`, `profile_spawn_runner_role_matrix_is_closed`, `guest_profile_requires_a_complete_target_execution_binding`, `spawn_runner_role_kebab_case_serialization` — caller-role predicate/display/roundtrip, wire-level default denial, envelope root/nested/response roundtrips, profile admission, kebab tokens.
- remaining 10 tests pin response/forwarding/context contracts: `signal_runner_response_round_trips`, `spawn_runner_response_round_trips`, `forwarded_fd_declarations_round_trip`, `forwarded_frames_without_fd_declarations_decode_as_empty_sets`, `a_minted_context_round_trips_through_canonical_json`, `a_context_round_trips_inside_the_forward_request`, `a_frame_without_a_context_decodes_as_absent`, `the_daemon_publication_round_trips_and_names_the_shared_code`, `the_fd_leg_refusal_code_is_the_shared_carrier_code`, `the_stale_context_code_is_the_shared_carrier_code` — typed envelope responses, forward-carrier fd/context defaults (old-sender compat), canonical context block, and shared wire-code constant pins.