# Historical Gateway migration

**Diataxis category:** historical reference.

The former Gateway/Realm workflow is retained for migration context only.
Current gateway-backed isolation uses a Zone-local Gateway Guest, ZoneLink
Resources, authenticated sessions, and Provider-owned credentials.

See [`configure-work-gateway.md`](./configure-work-gateway.md) for the current
resource shape. Do not use the retired enrollment commands or legacy
hierarchy options.
