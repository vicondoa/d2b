# `personal-dev` example alias

This is the doc-friendly alias for the checked
[`../minimal/`](../minimal/) flake.

Use it when you want the Rust-first command names from the README:

```bash
sudo d2b guest start personal-dev --apply
```

The actual flake, CI coverage, and end-to-end explanation still live
in [`../minimal/`](../minimal/).
