actions:
  - name: "build / check"
    container_image: "ubuntu-22.04"
    triggers:
      pull_request:
        branches:
          - v3
        merge_with_base: true
      push:
        branches:
          - v3
    steps:
      - run: >-
          make check
