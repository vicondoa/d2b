# This file is generated by `make layer1-workflow`.
# Source of truth: tests/layer1-jobs.json and tests/ci/layer1-workflow.template.yml.
# Do not edit .github/workflows/pr-l1-static-fast.yml directly.
name: pr-l1-static-fast
on:
  pull_request:
    branches: [main, v3]
  push:
    branches: [main, v3]

permissions:
  contents: read

defaults:
  run:
    shell: sh tests/tools/ci-shell {0}

jobs:
  tier0:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Tier 0 first-pass gate
        run: make check-tier0
      - name: ADR index coverage guard
        run: make test-adr-index-coverage
      - name: CI coverage structural guard
        run: make test-ci-coverage

  check-inventory:
    needs: [tier0]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Migration ledger drift gate
        run: make check-inventory

  test-performance-budgets:
    name: "Advisory - non-enforcing - Performance budget gate"
    needs: [tier0]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Performance budget gate
        run: make test-performance-budgets

  test-lint:
    needs: [tier0]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - uses: cachix/install-nix-action@23cf0fec1d55e0b1f2631aedd2a610c21ef8b077
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
      - name: Lint gate (preflight + parse + shellcheck)
        run: make test-lint

  test-changelog:
    if: github.event_name == 'pull_request'
    needs: [tier0]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
          fetch-depth: 0
      - name: Changelog policy gate
        run: make test-changelog

  test-rust-api-surface:
    needs: [tier0]
    runs-on: ubuntu-latest
    # Warm (rust-cache hit): ~8-12 min. Cold (no cache): ~43 min.
    timeout-minutes: 60
    env:
      # CI uses Swatinem/rust-cache (target-dir caching) and NOT sccache. That
      # is not a preference - sccache is incompatible with this workspace's
      # integration tests. Its server forwards only a whitelist of environment
      # variables to rustc, and CARGO_BIN_EXE_<name>, which Cargo injects so an
      # integration test can locate the binary it exercises, is not on it. Any
      # test using env!("CARGO_BIN_EXE_...") then fails to compile with
      # "environment variable ... not defined at compile time"; measured on this
      # gate, d2b-exec-runner/tests/tty_pty_integration.rs fails exactly that
      # way. It does not reproduce locally because CARGO_INCREMENTAL is on
      # there, which makes sccache treat those compilations as non-cacheable and
      # pass them through untouched.
      #
      # sccache remains enabled for local development, where the same shim is
      # reached through packages/.cargo/config.toml. Only CI opts out.
      #
      # CARGO_INCREMENTAL=0 is still set: incremental compilation artifacts are
      # non-deterministic and bloat the cache without benefit for CI (each PR
      # run starts from a different commit).
      CARGO_INCREMENTAL: "0"
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: cachix/install-nix-action@23cf0fec1d55e0b1f2631aedd2a610c21ef8b077
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
      - name: Free runner disk for Rust gate
        run: |
          df -h /
          avail_kib=$(df -Pk / | awk 'NR == 2 { print $4 }')
          # Reclaiming the preinstalled Android/dotnet/GHC trees and the Docker
          # image cache costs real wall time - measured across this workflow's
          # four Rust-profile jobs it ranged from 43 s to 3 min 48 s, the worst
          # of which sat on the critical path - and buys about 22 GiB on a
          # runner image that already arrives with 83-88 GiB free. Nothing in
          # this gate approaches that, so only pay the cost when an image
          # actually turns up short. This still fails safe: a fuller image
          # reclaims exactly as before.
          threshold_kib=$((70 * 1024 * 1024))
          if [ "$avail_kib" -ge "$threshold_kib" ]; then
            echo "free space $((avail_kib / 1024 / 1024)) GiB is at or above the $((threshold_kib / 1024 / 1024)) GiB threshold; skipping reclaim"
            exit 0
          fi
          echo "free space $((avail_kib / 1024 / 1024)) GiB is below the $((threshold_kib / 1024 / 1024)) GiB threshold; reclaiming"
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup /opt/hostedtoolcache/CodeQL || true
          docker system prune -af || true
          df -h /
      - name: Install pinned Rust toolchain + ripgrep + acl
        # MUST run BEFORE Swatinem/rust-cache: the cache action reads
        # `rustc --version` to compute its key hash, so the pinned
        # toolchain must be the active default when the cache step runs.
        # Without this, the runner's pre-installed 1.96.0 is hashed,
        # and the cache is keyed on the wrong compiler version.
        run: |
          PINNED=$(sed -n 's/^[[:space:]]*channel[[:space:]]*=[[:space:]]*"\([^"]*\)".*/\1/p' packages/rust-toolchain.toml | head -1)
          rustup toolchain install "$PINNED" --profile minimal --component rustfmt --component clippy
          rustup default "$PINNED"
          echo "Rust toolchain: $(rustc --version)"
          sudo apt-get update && sudo apt-get install -y ripgrep acl
      - name: Rust dependency cache (target dirs + cargo registry)
        # Swatinem/rust-cache caches dependency artifacts in target dirs
        # and the cargo registry. It performs all I/O in its own action
        # process (JavaScript pre/post steps) via @actions/cache - no
        # ACTIONS_RUNTIME_TOKEN or cache credentials are exposed to `run:`
        # steps where untrusted crate code (build scripts, proc-macros,
        # `cargo test`) executes.
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32
        with:
          workspaces: |
            packages -> target
            packages/d2b-priv-broker -> target
            packages/d2b-guest-shell-runner -> target
          cache-directories: |
            packages/d2b-priv-broker/target-layer1
            packages/d2b-priv-broker/target-fakebackends
            tests/tools/no-bash-ast-walker/target
            .scratch/rust-test-cache
          prefix-key: "v2-rust-api-json"
          shared-key: "test-rust-${{ runner.os }}"
          # The repository-local trees are keyed on rustc -vV by their owning
          # tests. Cargo fingerprints compiled inputs on restore, while failed
          # compile units are never cached and still rerun on every gate.
          # A single writer keeps concurrent saves from racing one shared key.
          # The main-workspace Rust shard is that writer; it produces both the
          # common Cargo target and the slow compiler/rustdoc scratch trees.
          # Every other Rust job restores the last complete entry. On a cold key
          # parallel shards may duplicate compilation, but only one entry wins.
          save-if: "false"
      - name: Rust compiler-derived API census
        env:
          D2B_SKIP_FIXTURE_BUILD: "1"
        run: make test-rust-api-surface


  test-rust-main:
    needs: [tier0]
    runs-on: ubuntu-latest
    # Warm (rust-cache hit): ~8-12 min. Cold (no cache): ~43 min.
    timeout-minutes: 60
    env:
      # CI uses Swatinem/rust-cache (target-dir caching) and NOT sccache. That
      # is not a preference - sccache is incompatible with this workspace's
      # integration tests. Its server forwards only a whitelist of environment
      # variables to rustc, and CARGO_BIN_EXE_<name>, which Cargo injects so an
      # integration test can locate the binary it exercises, is not on it. Any
      # test using env!("CARGO_BIN_EXE_...") then fails to compile with
      # "environment variable ... not defined at compile time"; measured on this
      # gate, d2b-exec-runner/tests/tty_pty_integration.rs fails exactly that
      # way. It does not reproduce locally because CARGO_INCREMENTAL is on
      # there, which makes sccache treat those compilations as non-cacheable and
      # pass them through untouched.
      #
      # sccache remains enabled for local development, where the same shim is
      # reached through packages/.cargo/config.toml. Only CI opts out.
      #
      # CARGO_INCREMENTAL=0 is still set: incremental compilation artifacts are
      # non-deterministic and bloat the cache without benefit for CI (each PR
      # run starts from a different commit).
      CARGO_INCREMENTAL: "0"
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: cachix/install-nix-action@23cf0fec1d55e0b1f2631aedd2a610c21ef8b077
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
      - name: Free runner disk for Rust gate
        run: |
          df -h /
          avail_kib=$(df -Pk / | awk 'NR == 2 { print $4 }')
          # Reclaiming the preinstalled Android/dotnet/GHC trees and the Docker
          # image cache costs real wall time - measured across this workflow's
          # four Rust-profile jobs it ranged from 43 s to 3 min 48 s, the worst
          # of which sat on the critical path - and buys about 22 GiB on a
          # runner image that already arrives with 83-88 GiB free. Nothing in
          # this gate approaches that, so only pay the cost when an image
          # actually turns up short. This still fails safe: a fuller image
          # reclaims exactly as before.
          threshold_kib=$((70 * 1024 * 1024))
          if [ "$avail_kib" -ge "$threshold_kib" ]; then
            echo "free space $((avail_kib / 1024 / 1024)) GiB is at or above the $((threshold_kib / 1024 / 1024)) GiB threshold; skipping reclaim"
            exit 0
          fi
          echo "free space $((avail_kib / 1024 / 1024)) GiB is below the $((threshold_kib / 1024 / 1024)) GiB threshold; reclaiming"
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup /opt/hostedtoolcache/CodeQL || true
          docker system prune -af || true
          df -h /
      - name: Install pinned Rust toolchain + ripgrep + acl
        # MUST run BEFORE Swatinem/rust-cache: the cache action reads
        # `rustc --version` to compute its key hash, so the pinned
        # toolchain must be the active default when the cache step runs.
        # Without this, the runner's pre-installed 1.96.0 is hashed,
        # and the cache is keyed on the wrong compiler version.
        run: |
          PINNED=$(sed -n 's/^[[:space:]]*channel[[:space:]]*=[[:space:]]*"\([^"]*\)".*/\1/p' packages/rust-toolchain.toml | head -1)
          rustup toolchain install "$PINNED" --profile minimal --component rustfmt --component clippy
          rustup default "$PINNED"
          echo "Rust toolchain: $(rustc --version)"
          sudo apt-get update && sudo apt-get install -y ripgrep acl
      - name: Rust dependency cache (target dirs + cargo registry)
        # Swatinem/rust-cache caches dependency artifacts in target dirs
        # and the cargo registry. It performs all I/O in its own action
        # process (JavaScript pre/post steps) via @actions/cache - no
        # ACTIONS_RUNTIME_TOKEN or cache credentials are exposed to `run:`
        # steps where untrusted crate code (build scripts, proc-macros,
        # `cargo test`) executes.
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32
        with:
          workspaces: |
            packages -> target
            packages/d2b-priv-broker -> target
            packages/d2b-guest-shell-runner -> target
          cache-directories: |
            packages/d2b-priv-broker/target-layer1
            packages/d2b-priv-broker/target-fakebackends
            tests/tools/no-bash-ast-walker/target
            .scratch/rust-test-cache
          prefix-key: "v2-rust-api-json"
          shared-key: "test-rust-${{ runner.os }}"
          # The repository-local trees are keyed on rustc -vV by their owning
          # tests. Cargo fingerprints compiled inputs on restore, while failed
          # compile units are never cached and still rerun on every gate.
          # A single writer keeps concurrent saves from racing one shared key.
          # The main-workspace Rust shard is that writer; it produces both the
          # common Cargo target and the slow compiler/rustdoc scratch trees.
          # Every other Rust job restores the last complete entry. On a cold key
          # parallel shards may duplicate compilation, but only one entry wins.
          save-if: "true"
      - name: Rust main workspace gate
        env:
          D2B_SKIP_FIXTURE_BUILD: "1"
        run: make test-rust-main


  test-rust-remaining:
    needs: [tier0]
    runs-on: ubuntu-latest
    # Warm (rust-cache hit): ~8-12 min. Cold (no cache): ~43 min.
    timeout-minutes: 60
    env:
      # CI uses Swatinem/rust-cache (target-dir caching) and NOT sccache. That
      # is not a preference - sccache is incompatible with this workspace's
      # integration tests. Its server forwards only a whitelist of environment
      # variables to rustc, and CARGO_BIN_EXE_<name>, which Cargo injects so an
      # integration test can locate the binary it exercises, is not on it. Any
      # test using env!("CARGO_BIN_EXE_...") then fails to compile with
      # "environment variable ... not defined at compile time"; measured on this
      # gate, d2b-exec-runner/tests/tty_pty_integration.rs fails exactly that
      # way. It does not reproduce locally because CARGO_INCREMENTAL is on
      # there, which makes sccache treat those compilations as non-cacheable and
      # pass them through untouched.
      #
      # sccache remains enabled for local development, where the same shim is
      # reached through packages/.cargo/config.toml. Only CI opts out.
      #
      # CARGO_INCREMENTAL=0 is still set: incremental compilation artifacts are
      # non-deterministic and bloat the cache without benefit for CI (each PR
      # run starts from a different commit).
      CARGO_INCREMENTAL: "0"
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: cachix/install-nix-action@23cf0fec1d55e0b1f2631aedd2a610c21ef8b077
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
      - name: Free runner disk for Rust gate
        run: |
          df -h /
          avail_kib=$(df -Pk / | awk 'NR == 2 { print $4 }')
          # Reclaiming the preinstalled Android/dotnet/GHC trees and the Docker
          # image cache costs real wall time - measured across this workflow's
          # four Rust-profile jobs it ranged from 43 s to 3 min 48 s, the worst
          # of which sat on the critical path - and buys about 22 GiB on a
          # runner image that already arrives with 83-88 GiB free. Nothing in
          # this gate approaches that, so only pay the cost when an image
          # actually turns up short. This still fails safe: a fuller image
          # reclaims exactly as before.
          threshold_kib=$((70 * 1024 * 1024))
          if [ "$avail_kib" -ge "$threshold_kib" ]; then
            echo "free space $((avail_kib / 1024 / 1024)) GiB is at or above the $((threshold_kib / 1024 / 1024)) GiB threshold; skipping reclaim"
            exit 0
          fi
          echo "free space $((avail_kib / 1024 / 1024)) GiB is below the $((threshold_kib / 1024 / 1024)) GiB threshold; reclaiming"
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup /opt/hostedtoolcache/CodeQL || true
          docker system prune -af || true
          df -h /
      - name: Install pinned Rust toolchain + ripgrep + acl
        # MUST run BEFORE Swatinem/rust-cache: the cache action reads
        # `rustc --version` to compute its key hash, so the pinned
        # toolchain must be the active default when the cache step runs.
        # Without this, the runner's pre-installed 1.96.0 is hashed,
        # and the cache is keyed on the wrong compiler version.
        run: |
          PINNED=$(sed -n 's/^[[:space:]]*channel[[:space:]]*=[[:space:]]*"\([^"]*\)".*/\1/p' packages/rust-toolchain.toml | head -1)
          rustup toolchain install "$PINNED" --profile minimal --component rustfmt --component clippy
          rustup default "$PINNED"
          echo "Rust toolchain: $(rustc --version)"
          sudo apt-get update && sudo apt-get install -y ripgrep acl
      - name: Rust dependency cache (target dirs + cargo registry)
        # Swatinem/rust-cache caches dependency artifacts in target dirs
        # and the cargo registry. It performs all I/O in its own action
        # process (JavaScript pre/post steps) via @actions/cache - no
        # ACTIONS_RUNTIME_TOKEN or cache credentials are exposed to `run:`
        # steps where untrusted crate code (build scripts, proc-macros,
        # `cargo test`) executes.
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32
        with:
          workspaces: |
            packages -> target
            packages/d2b-priv-broker -> target
            packages/d2b-guest-shell-runner -> target
          cache-directories: |
            packages/d2b-priv-broker/target-layer1
            packages/d2b-priv-broker/target-fakebackends
            tests/tools/no-bash-ast-walker/target
            .scratch/rust-test-cache
          prefix-key: "v2-rust-api-json"
          shared-key: "test-rust-${{ runner.os }}"
          # The repository-local trees are keyed on rustc -vV by their owning
          # tests. Cargo fingerprints compiled inputs on restore, while failed
          # compile units are never cached and still rerun on every gate.
          # A single writer keeps concurrent saves from racing one shared key.
          # The main-workspace Rust shard is that writer; it produces both the
          # common Cargo target and the slow compiler/rustdoc scratch trees.
          # Every other Rust job restores the last complete entry. On a cold key
          # parallel shards may duplicate compilation, but only one entry wins.
          save-if: "false"
      - name: Rust remaining suites gate
        env:
          D2B_SKIP_FIXTURE_BUILD: "1"
        run: make test-rust-remaining


  test-rust:
    needs: [test-rust-api-surface, test-rust-main, test-rust-remaining]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Require every Rust gate shard to pass
        run: |
          failed=0
          result='${{ needs.test-rust-api-surface.result }}'
          echo "test-rust-api-surface=$result"
          [ "$result" = success ] || failed=1
          result='${{ needs.test-rust-main.result }}'
          echo "test-rust-main=$result"
          [ "$result" = success ] || failed=1
          result='${{ needs.test-rust-remaining.result }}'
          echo "test-rust-remaining=$result"
          [ "$result" = success ] || failed=1
          [ "$failed" -eq 0 ] || exit 1
          echo "Both Rust gate shards passed."

  test-fixture-contracts:
    needs: [tier0]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: write
    # Warm (rust-cache hit): ~8-12 min. Cold (no cache): ~43 min.
    timeout-minutes: 60
    env:
      # CI uses Swatinem/rust-cache (target-dir caching) and NOT sccache. That
      # is not a preference - sccache is incompatible with this workspace's
      # integration tests. Its server forwards only a whitelist of environment
      # variables to rustc, and CARGO_BIN_EXE_<name>, which Cargo injects so an
      # integration test can locate the binary it exercises, is not on it. Any
      # test using env!("CARGO_BIN_EXE_...") then fails to compile with
      # "environment variable ... not defined at compile time"; measured on this
      # gate, d2b-exec-runner/tests/tty_pty_integration.rs fails exactly that
      # way. It does not reproduce locally because CARGO_INCREMENTAL is on
      # there, which makes sccache treat those compilations as non-cacheable and
      # pass them through untouched.
      #
      # sccache remains enabled for local development, where the same shim is
      # reached through packages/.cargo/config.toml. Only CI opts out.
      #
      # CARGO_INCREMENTAL=0 is still set: incremental compilation artifacts are
      # non-deterministic and bloat the cache without benefit for CI (each PR
      # run starts from a different commit).
      CARGO_INCREMENTAL: "0"
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: cachix/install-nix-action@23cf0fec1d55e0b1f2631aedd2a610c21ef8b077
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
      - name: Nix store cache
        # Without this the job rebuilds the Rust host-tool set from source,
        # which is what makes the fixture-contract gate cost half an hour here
        # while completing in seconds on a host with a substituter. Restore is
        # best-effort: a cache miss is slow, never incorrect. See
        # NIX_CACHED_JOBS for why only some jobs carry an entry.
        uses: nix-community/cache-nix-action@7df957e333c1e5da7721f60227dbba6d06080569
        with:
          # The format epoch rotates immutable Actions caches when retention
          # semantics change. Fixture jobs use a source-complete hash; other
          # cached Nix jobs hash the flake and Nix corpus they evaluate.
          primary-key: nix-v1-${{ runner.os }}-test-fixture-contracts-${{ hashFiles('flake.lock', '**/*.nix') }}
          restore-prefixes-first-match: nix-v1-${{ runner.os }}-test-fixture-contracts-
          gc-max-store-size-linux: 4G
          purge: true
          purge-prefixes: nix-v1-${{ runner.os }}-test-fixture-contracts-
          purge-created: 0
          purge-primary-key: never
      - name: Free runner disk for Rust gate
        run: |
          df -h /
          avail_kib=$(df -Pk / | awk 'NR == 2 { print $4 }')
          # Reclaiming the preinstalled Android/dotnet/GHC trees and the Docker
          # image cache costs real wall time - measured across this workflow's
          # four Rust-profile jobs it ranged from 43 s to 3 min 48 s, the worst
          # of which sat on the critical path - and buys about 22 GiB on a
          # runner image that already arrives with 83-88 GiB free. Nothing in
          # this gate approaches that, so only pay the cost when an image
          # actually turns up short. This still fails safe: a fuller image
          # reclaims exactly as before.
          threshold_kib=$((70 * 1024 * 1024))
          if [ "$avail_kib" -ge "$threshold_kib" ]; then
            echo "free space $((avail_kib / 1024 / 1024)) GiB is at or above the $((threshold_kib / 1024 / 1024)) GiB threshold; skipping reclaim"
            exit 0
          fi
          echo "free space $((avail_kib / 1024 / 1024)) GiB is below the $((threshold_kib / 1024 / 1024)) GiB threshold; reclaiming"
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup /opt/hostedtoolcache/CodeQL || true
          docker system prune -af || true
          df -h /
      - name: Install pinned Rust toolchain + ripgrep + acl
        # MUST run BEFORE Swatinem/rust-cache: the cache action reads
        # `rustc --version` to compute its key hash, so the pinned
        # toolchain must be the active default when the cache step runs.
        # Without this, the runner's pre-installed 1.96.0 is hashed,
        # and the cache is keyed on the wrong compiler version.
        run: |
          PINNED=$(sed -n 's/^[[:space:]]*channel[[:space:]]*=[[:space:]]*"\([^"]*\)".*/\1/p' packages/rust-toolchain.toml | head -1)
          rustup toolchain install "$PINNED" --profile minimal --component rustfmt --component clippy
          rustup default "$PINNED"
          echo "Rust toolchain: $(rustc --version)"
          sudo apt-get update && sudo apt-get install -y ripgrep acl
      - name: Rust dependency cache (target dirs + cargo registry)
        # Swatinem/rust-cache caches dependency artifacts in target dirs
        # and the cargo registry. It performs all I/O in its own action
        # process (JavaScript pre/post steps) via @actions/cache - no
        # ACTIONS_RUNTIME_TOKEN or cache credentials are exposed to `run:`
        # steps where untrusted crate code (build scripts, proc-macros,
        # `cargo test`) executes.
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32
        with:
          workspaces: |
            packages -> target
            packages/d2b-priv-broker -> target
            packages/d2b-guest-shell-runner -> target
          cache-directories: |
            packages/d2b-priv-broker/target-layer1
            packages/d2b-priv-broker/target-fakebackends
            tests/tools/no-bash-ast-walker/target
            .scratch/rust-test-cache
          prefix-key: "v2-rust-api-json"
          shared-key: "test-rust-${{ runner.os }}"
          # The repository-local trees are keyed on rustc -vV by their owning
          # tests. Cargo fingerprints compiled inputs on restore, while failed
          # compile units are never cached and still rerun on every gate.
          # A single writer keeps concurrent saves from racing one shared key.
          # The main-workspace Rust shard is that writer; it produces both the
          # common Cargo target and the slow compiler/rustdoc scratch trees.
          # Every other Rust job restores the last complete entry. On a cold key
          # parallel shards may duplicate compilation, but only one entry wins.
          save-if: "false"
      - name: Fixture-dependent Rust contract tests
        env:
          D2B_ENABLE_FIXTURE_BUILD: "1"
        run: make test-fixture-contracts


  test-proofs:
    needs: [tier0]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - uses: cachix/install-nix-action@23cf0fec1d55e0b1f2631aedd2a610c21ef8b077
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
      - name: Proof crates gate
        run: make test-proofs

  nix-unit-discover:
    needs: [tier0]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      checks: ${{ steps.list.outputs.nixunitchecks }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - uses: cachix/install-nix-action@23cf0fec1d55e0b1f2631aedd2a610c21ef8b077
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
      - id: list
        name: Enumerate Nix unit checks
        run: |
          # Same partition tool as flake-eval-discover, so this lane is handed
          # exactly the names that lane drops from its instantiate-only matrix.
          partition=$(make -s test-flake-partition)
          echo "$partition"
          echo "$partition" >> "$GITHUB_OUTPUT"


  nix-unit-shards:
    needs: [nix-unit-discover]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        check: ${{ fromJSON(needs.nix-unit-discover.outputs.checks) }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - uses: cachix/install-nix-action@23cf0fec1d55e0b1f2631aedd2a610c21ef8b077
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
      - name: Nix unit shard
        env:
          # The matrix value is data, not shell source. The driver validates it
          # against both the safe-name grammar and the discovered check set.
          D2B_NIX_UNIT_CHECK: ${{ matrix.check }}
          D2B_NIX_UNIT_JOBS: "1"
        run: make test-nix-unit

  test-nix-unit:
    needs: [nix-unit-discover, nix-unit-shards]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Require every Nix unit shard to pass
        run: |
          discover='${{ needs.nix-unit-discover.result }}'
          shards='${{ needs.nix-unit-shards.result }}'
          echo "nix-unit-discover=$discover  nix-unit-shards=$shards"
          if [ "$discover" = success ] && [ "$shards" = success ]; then
            echo "Every discovered Nix-unit shard passed."
          else
            echo "::error::Nix-unit gate failed (discover=$discover, shards=$shards)"
            exit 1
          fi

  flake-eval-discover:
    needs: [tier0]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      evalchecks: ${{ steps.list.outputs.evalchecks }}
      realizedchecks: ${{ steps.list.outputs.realizedchecks }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - uses: cachix/install-nix-action@23cf0fec1d55e0b1f2631aedd2a610c21ef8b077
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
      - id: list
        name: Enumerate flake checks (x86_64-linux)
        run: |
          # One enumeration produces every dispatch class, so the names dropped
          # from the eval matrix are provably the names another lane realizes.
          # The tool fails closed on an empty enumeration or a realized name
          # that is not a discovered check.
          partition=$(make -s test-flake-partition)
          echo "$partition"
          echo "$partition" >> "$GITHUB_OUTPUT"


  flake-eval-x86:
    needs: [flake-eval-discover]
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        check: ${{ fromJSON(needs.flake-eval-discover.outputs.evalchecks) }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Add swap (insurance for the heaviest single check)
        run: |
          # A single check instantiates in its own process and fits a 16 GB
          # runner (heaviest measured ~12 GB), so unlike the old monolith this
          # rarely touches swap. Add a modest swapfile purely as OOM insurance.
          SWAP=/mnt/d2b-ci-swap
          sudo swapoff "$SWAP" 2>/dev/null || true
          sudo rm -f "$SWAP"
          sudo fallocate -l 8G "$SWAP" || sudo dd if=/dev/zero of="$SWAP" bs=1M count=8192
          sudo chmod 600 "$SWAP"
          sudo mkswap "$SWAP"
          sudo swapon "$SWAP"
      - uses: cachix/install-nix-action@23cf0fec1d55e0b1f2631aedd2a610c21ef8b077
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
      - name: Install flake shard diagnostics
        run: sudo apt-get update && sudo apt-get install -y gdb
      - name: Flake check shard (x86_64-linux)
        # D2B_FLAKE_CHECK is passed via the step environment, NOT interpolated
        # into the shell command: a flake check attr name is PR-controlled, so
        # `D2B_FLAKE_CHECK='${{ matrix.check }}' ...` would be a shell-injection
        # vector. test-flake.sh additionally rejects names outside [A-Za-z0-9._-].
        env:
          D2B_FLAKE_CHECK: ${{ matrix.check }}
        run: make test-flake

  flake-eval-x86-realized:
    needs: [flake-eval-discover]
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        check: ${{ fromJSON(needs.flake-eval-discover.outputs.realizedchecks) }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - uses: cachix/install-nix-action@23cf0fec1d55e0b1f2631aedd2a610c21ef8b077
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
      - name: Realized-check input cache
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830
        with:
          path: ${{ runner.temp }}/d2b-realized-cache
          key: d2b-realized-v2-${{ runner.os }}-${{ matrix.check }}-${{ hashFiles('flake.lock', 'flake.nix', 'pkgs/**') }}
          restore-keys: |
            d2b-realized-v2-${{ runner.os }}-${{ matrix.check }}-
      - name: Realized-check cache self-test
        # Two seconds, and it runs where Nix is guaranteed. It pins the copy
        # shape the restore depends on, whose last regression was invisible:
        # the entry restored, the copy failed, the shard rebuilt from scratch,
        # and the step still reported success.
        run: bash tests/tools/realized-check-cache.sh self-test
      - name: Restore prebuilt check inputs
        # Best-effort: a miss costs the build this cache exists to avoid, and
        # can never produce a wrong result, so it must not fail the shard.
        env:
          D2B_FLAKE_CHECK: ${{ matrix.check }}
        run: bash tests/tools/realized-check-cache.sh import "$D2B_FLAKE_CHECK" ${{ runner.temp }}/d2b-realized-cache
      - name: Flake check shard (x86_64-linux, realized)
        # D2B_FLAKE_CHECK is passed via the step environment, NOT interpolated
        # into the shell command: a flake check attr name is PR-controlled, so
        # `D2B_FLAKE_CHECK='${{ matrix.check }}' ...` would be a shell-injection
        # vector. test-flake.sh additionally rejects names outside [A-Za-z0-9._-].
        env:
          D2B_FLAKE_CHECK: ${{ matrix.check }}
        run: make test-flake
      - name: Publish built check inputs
        env:
          D2B_FLAKE_CHECK: ${{ matrix.check }}
        run: bash tests/tools/realized-check-cache.sh export "$D2B_FLAKE_CHECK" ${{ runner.temp }}/d2b-realized-cache

  flake-eval-x86-outputs:
    needs: [tier0]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - uses: cachix/install-nix-action@23cf0fec1d55e0b1f2631aedd2a610c21ef8b077
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
      - name: Flake non-checks outputs (x86_64-linux)
        env:
          D2B_FLAKE_OUTPUTS: "1"
        run: make test-flake

  test-flake-x86:
    needs: [flake-eval-discover, flake-eval-x86, flake-eval-x86-realized, flake-eval-x86-outputs]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Require all x86 flake jobs to pass
        run: |
          discover='${{ needs.flake-eval-discover.result }}'
          shards='${{ needs.flake-eval-x86.result }}'
          realized='${{ needs.flake-eval-x86-realized.result }}'
          outputs='${{ needs.flake-eval-x86-outputs.result }}'
          echo "flake-eval-discover=$discover  flake-eval-x86=$shards  flake-eval-x86-realized=$realized  flake-eval-x86-outputs=$outputs"
          if [ "$discover" = success ] && [ "$shards" = success ] \
            && [ "$realized" = success ] && [ "$outputs" = success ]; then
            echo "All x86_64-linux flake checks + outputs passed."
          else
            echo "::error::x86_64 flake gate failed (discover=$discover, shards=$shards, realized=$realized, outputs=$outputs)"
            exit 1
          fi

  test-flake-aarch64:
    needs: [tier0]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - uses: cachix/install-nix-action@23cf0fec1d55e0b1f2631aedd2a610c21ef8b077
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
      - name: aarch64 smoke eval
        run: |
          nix-instantiate --eval --strict \
            -E 'let f = import ./tests/unit/smoke/smoke-eval-aarch64.nix; r = f {}; in r.drvPath' \
            >/dev/null

  test-drift:
    needs: [tier0]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - uses: cachix/install-nix-action@23cf0fec1d55e0b1f2631aedd2a610c21ef8b077
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
      - name: Drift gate
        run: make test-drift

  test-policy:
    needs: [tier0]
    runs-on: ubuntu-latest
    # Warm (rust-cache hit): ~8-12 min. Cold (no cache): ~43 min.
    timeout-minutes: 60
    env:
      # CI uses Swatinem/rust-cache (target-dir caching) and NOT sccache. That
      # is not a preference - sccache is incompatible with this workspace's
      # integration tests. Its server forwards only a whitelist of environment
      # variables to rustc, and CARGO_BIN_EXE_<name>, which Cargo injects so an
      # integration test can locate the binary it exercises, is not on it. Any
      # test using env!("CARGO_BIN_EXE_...") then fails to compile with
      # "environment variable ... not defined at compile time"; measured on this
      # gate, d2b-exec-runner/tests/tty_pty_integration.rs fails exactly that
      # way. It does not reproduce locally because CARGO_INCREMENTAL is on
      # there, which makes sccache treat those compilations as non-cacheable and
      # pass them through untouched.
      #
      # sccache remains enabled for local development, where the same shim is
      # reached through packages/.cargo/config.toml. Only CI opts out.
      #
      # CARGO_INCREMENTAL=0 is still set: incremental compilation artifacts are
      # non-deterministic and bloat the cache without benefit for CI (each PR
      # run starts from a different commit).
      CARGO_INCREMENTAL: "0"
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: cachix/install-nix-action@23cf0fec1d55e0b1f2631aedd2a610c21ef8b077
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
      - name: Free runner disk for Rust gate
        run: |
          df -h /
          avail_kib=$(df -Pk / | awk 'NR == 2 { print $4 }')
          # Reclaiming the preinstalled Android/dotnet/GHC trees and the Docker
          # image cache costs real wall time - measured across this workflow's
          # four Rust-profile jobs it ranged from 43 s to 3 min 48 s, the worst
          # of which sat on the critical path - and buys about 22 GiB on a
          # runner image that already arrives with 83-88 GiB free. Nothing in
          # this gate approaches that, so only pay the cost when an image
          # actually turns up short. This still fails safe: a fuller image
          # reclaims exactly as before.
          threshold_kib=$((70 * 1024 * 1024))
          if [ "$avail_kib" -ge "$threshold_kib" ]; then
            echo "free space $((avail_kib / 1024 / 1024)) GiB is at or above the $((threshold_kib / 1024 / 1024)) GiB threshold; skipping reclaim"
            exit 0
          fi
          echo "free space $((avail_kib / 1024 / 1024)) GiB is below the $((threshold_kib / 1024 / 1024)) GiB threshold; reclaiming"
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup /opt/hostedtoolcache/CodeQL || true
          docker system prune -af || true
          df -h /
      - name: Install pinned Rust toolchain + ripgrep + acl
        # MUST run BEFORE Swatinem/rust-cache: the cache action reads
        # `rustc --version` to compute its key hash, so the pinned
        # toolchain must be the active default when the cache step runs.
        # Without this, the runner's pre-installed 1.96.0 is hashed,
        # and the cache is keyed on the wrong compiler version.
        run: |
          PINNED=$(sed -n 's/^[[:space:]]*channel[[:space:]]*=[[:space:]]*"\([^"]*\)".*/\1/p' packages/rust-toolchain.toml | head -1)
          rustup toolchain install "$PINNED" --profile minimal --component rustfmt --component clippy
          rustup default "$PINNED"
          echo "Rust toolchain: $(rustc --version)"
          sudo apt-get update && sudo apt-get install -y ripgrep acl
      - name: Rust dependency cache (target dirs + cargo registry)
        # Swatinem/rust-cache caches dependency artifacts in target dirs
        # and the cargo registry. It performs all I/O in its own action
        # process (JavaScript pre/post steps) via @actions/cache - no
        # ACTIONS_RUNTIME_TOKEN or cache credentials are exposed to `run:`
        # steps where untrusted crate code (build scripts, proc-macros,
        # `cargo test`) executes.
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32
        with:
          workspaces: |
            packages -> target
            packages/d2b-priv-broker -> target
            packages/d2b-guest-shell-runner -> target
          cache-directories: |
            packages/d2b-priv-broker/target-layer1
            packages/d2b-priv-broker/target-fakebackends
            tests/tools/no-bash-ast-walker/target
            .scratch/rust-test-cache
          prefix-key: "v2-rust-api-json"
          shared-key: "test-rust-${{ runner.os }}"
          # The repository-local trees are keyed on rustc -vV by their owning
          # tests. Cargo fingerprints compiled inputs on restore, while failed
          # compile units are never cached and still rerun on every gate.
          # A single writer keeps concurrent saves from racing one shared key.
          # The main-workspace Rust shard is that writer; it produces both the
          # common Cargo target and the slow compiler/rustdoc scratch trees.
          # Every other Rust job restores the last complete entry. On a cold key
          # parallel shards may duplicate compilation, but only one entry wins.
          save-if: "false"
      - name: Policy meta-gates
        env:
          {}
        run: make test-policy


  test-runtime-ledger:
    needs: [tier0]
    runs-on: ubuntu-latest
    # Warm (rust-cache hit): ~8-12 min. Cold (no cache): ~43 min.
    timeout-minutes: 30
    env:
      # CI uses Swatinem/rust-cache (target-dir caching) and NOT sccache. That
      # is not a preference - sccache is incompatible with this workspace's
      # integration tests. Its server forwards only a whitelist of environment
      # variables to rustc, and CARGO_BIN_EXE_<name>, which Cargo injects so an
      # integration test can locate the binary it exercises, is not on it. Any
      # test using env!("CARGO_BIN_EXE_...") then fails to compile with
      # "environment variable ... not defined at compile time"; measured on this
      # gate, d2b-exec-runner/tests/tty_pty_integration.rs fails exactly that
      # way. It does not reproduce locally because CARGO_INCREMENTAL is on
      # there, which makes sccache treat those compilations as non-cacheable and
      # pass them through untouched.
      #
      # sccache remains enabled for local development, where the same shim is
      # reached through packages/.cargo/config.toml. Only CI opts out.
      #
      # CARGO_INCREMENTAL=0 is still set: incremental compilation artifacts are
      # non-deterministic and bloat the cache without benefit for CI (each PR
      # run starts from a different commit).
      CARGO_INCREMENTAL: "0"
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: cachix/install-nix-action@23cf0fec1d55e0b1f2631aedd2a610c21ef8b077
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
      - name: Free runner disk for Rust gate
        run: |
          df -h /
          avail_kib=$(df -Pk / | awk 'NR == 2 { print $4 }')
          # Reclaiming the preinstalled Android/dotnet/GHC trees and the Docker
          # image cache costs real wall time - measured across this workflow's
          # four Rust-profile jobs it ranged from 43 s to 3 min 48 s, the worst
          # of which sat on the critical path - and buys about 22 GiB on a
          # runner image that already arrives with 83-88 GiB free. Nothing in
          # this gate approaches that, so only pay the cost when an image
          # actually turns up short. This still fails safe: a fuller image
          # reclaims exactly as before.
          threshold_kib=$((70 * 1024 * 1024))
          if [ "$avail_kib" -ge "$threshold_kib" ]; then
            echo "free space $((avail_kib / 1024 / 1024)) GiB is at or above the $((threshold_kib / 1024 / 1024)) GiB threshold; skipping reclaim"
            exit 0
          fi
          echo "free space $((avail_kib / 1024 / 1024)) GiB is below the $((threshold_kib / 1024 / 1024)) GiB threshold; reclaiming"
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup /opt/hostedtoolcache/CodeQL || true
          docker system prune -af || true
          df -h /
      - name: Install pinned Rust toolchain + ripgrep + acl
        # MUST run BEFORE Swatinem/rust-cache: the cache action reads
        # `rustc --version` to compute its key hash, so the pinned
        # toolchain must be the active default when the cache step runs.
        # Without this, the runner's pre-installed 1.96.0 is hashed,
        # and the cache is keyed on the wrong compiler version.
        run: |
          PINNED=$(sed -n 's/^[[:space:]]*channel[[:space:]]*=[[:space:]]*"\([^"]*\)".*/\1/p' packages/rust-toolchain.toml | head -1)
          rustup toolchain install "$PINNED" --profile minimal --component rustfmt --component clippy
          rustup default "$PINNED"
          echo "Rust toolchain: $(rustc --version)"
          sudo apt-get update && sudo apt-get install -y ripgrep acl
      - name: Rust dependency cache (target dirs + cargo registry)
        # Swatinem/rust-cache caches dependency artifacts in target dirs
        # and the cargo registry. It performs all I/O in its own action
        # process (JavaScript pre/post steps) via @actions/cache - no
        # ACTIONS_RUNTIME_TOKEN or cache credentials are exposed to `run:`
        # steps where untrusted crate code (build scripts, proc-macros,
        # `cargo test`) executes.
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32
        with:
          workspaces: |
            packages -> target
            packages/d2b-priv-broker -> target
            packages/d2b-guest-shell-runner -> target
          cache-directories: |
            packages/d2b-priv-broker/target-layer1
            packages/d2b-priv-broker/target-fakebackends
            tests/tools/no-bash-ast-walker/target
            .scratch/rust-test-cache
          prefix-key: "v2-rust-api-json"
          shared-key: "test-rust-${{ runner.os }}"
          # The repository-local trees are keyed on rustc -vV by their owning
          # tests. Cargo fingerprints compiled inputs on restore, while failed
          # compile units are never cached and still rerun on every gate.
          # A single writer keeps concurrent saves from racing one shared key.
          # The main-workspace Rust shard is that writer; it produces both the
          # common Cargo target and the slow compiler/rustdoc scratch trees.
          # Every other Rust job restores the last complete entry. On a cold key
          # parallel shards may duplicate compilation, but only one entry wins.
          save-if: "false"
      - name: Runtime execution-budget ledger gate
        env:
          {}
        run: make test-runtime-ledger


  check:
    needs: [tier0, check-inventory, test-performance-budgets, test-lint, test-changelog, test-rust, test-fixture-contracts, test-proofs, test-nix-unit, test-flake-x86, test-flake-aarch64, test-drift, test-policy, test-runtime-ledger]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Require generated Layer-1 gate graph to pass
        run: |
          failed=0
          require_success() {
            name="$1"
            result="$2"
            echo "$name=$result"
            if [ "$result" != success ]; then
              echo "::error::$name did not pass (result=$result)"
              failed=1
            fi
          }
          allow_success_or_skipped() {
            name="$1"
            result="$2"
            echo "$name=$result"
            if [ "$result" != success ] && [ "$result" != skipped ]; then
              echo "::error::$name did not pass (result=$result)"
              failed=1
            fi
          }
          require_advisory_success() {
            name="$1"
            result="$2"
            echo "advisory:$name=$result (not an enforcing pass)"
            if [ "$result" != success ]; then
              echo "::error::required advisory $name did not complete successfully (result=$result)"
              failed=1
            fi
          }
          require_success tier0 '${{ needs.tier0.result }}'
          require_success check-inventory '${{ needs.check-inventory.result }}'
          require_advisory_success test-performance-budgets '${{ needs.test-performance-budgets.result }}'
          require_success test-lint '${{ needs.test-lint.result }}'
          allow_success_or_skipped test-changelog '${{ needs.test-changelog.result }}'
          require_success test-rust '${{ needs.test-rust.result }}'
          require_success test-fixture-contracts '${{ needs.test-fixture-contracts.result }}'
          require_success test-proofs '${{ needs.test-proofs.result }}'
          require_success test-nix-unit '${{ needs.test-nix-unit.result }}'
          require_success test-flake-x86 '${{ needs.test-flake-x86.result }}'
          require_success test-flake-aarch64 '${{ needs.test-flake-aarch64.result }}'
          require_success test-drift '${{ needs.test-drift.result }}'
          require_success test-policy '${{ needs.test-policy.result }}'
          require_success test-runtime-ledger '${{ needs.test-runtime-ledger.result }}'
          if [ "$failed" -ne 0 ]; then
            exit 1
          fi
          echo "All generated enforcing Layer-1 jobs passed."
          echo "Required advisory jobs completed (not enforcing passes): test-performance-budgets"
