package(default_visibility = ["//visibility:public"])

load(":manifest.bzl", "d2b_evidence_manifest")

exports_files([
    "defs.bzl",
    "manifest.bzl",
    "toolchains.bzl",
    "rules/sandboxed_action.bzl",
])

d2b_evidence_manifest(
    name = "rules-rust-evidence-manifest",
    targets = [
        "//bazel/evidence:rules-rust-evidence-build",
    ],
)

alias(
    name = "policy-probe",
    actual = ":rules-rust-evidence-manifest",
)
