//! Descriptor-policy validation reused from the authenticated session layer.

pub use d2b_session_unix::{
    AcceptedAttachment, DescriptorPolicy, ObjectIdentity, PeerCredentials, PidfdEvidence,
    PidfdIdentityPolicy, PidfdIdentityVerifier, VerifiedPacket,
};
