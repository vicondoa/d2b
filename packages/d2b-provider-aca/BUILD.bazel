load("@rules_rust//rust:defs.bzl", "rust_binary", "rust_library", "rust_test")

rust_library(
    name = "d2b_provider_aca",
    srcs = ["src/lib.rs"],
    compile_data = [
        "Cargo.toml",
        "src/lib.rs",
    ],
    proc_macro_deps = ["@crates//:async-trait"],
    visibility = ["//visibility:public"],
    deps = [
        "//packages/d2b-realm-core:d2b_realm_core",
        "//packages/d2b-realm-provider:d2b_realm_provider",
        "@crates//:azure_core",
        "@crates//:azure_identity",
        "@crates//:reqwest",
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:sha2",
        "@crates//:tokio",
        "@crates//:tracing",
    ],
)

rust_binary(
    name = "aca_live",
    srcs = ["examples/aca_live.rs"],
    compile_data = ["Cargo.toml"],
    tags = ["example"],
    visibility = ["//visibility:public"],
    deps = [
        ":d2b_provider_aca",
        "//packages/d2b-realm-core:d2b_realm_core",
        "@crates//:azure_identity",
        "@crates//:tokio",
    ],
)

rust_test(
    name = "d2b_provider_aca_test",
    compile_data = ["Cargo.toml"],
    crate = ":d2b_provider_aca",
)
