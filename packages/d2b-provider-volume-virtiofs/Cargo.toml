[package]
name = "d2b-provider-volume-virtiofs"
version = "0.0.0-bootstrap"
edition = "2024"
publish = false
license.workspace = true

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
disallowed_methods = "deny"
await_holding_lock = "deny"
await_holding_refcell_ref = "deny"

[dependencies]
d2b-contracts-resource = { path = "../d2b-contracts-resource", version = "0.0.0-bootstrap" }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
tokio = { workspace = true, features = ["sync"] }
tracing = "0.1"

[dev-dependencies]
# The suite's no-op-waker driver lives in `d2b_core::test_support` (KTD2).
d2b-core = { path = "../d2b-core", version = "0.0.0-bootstrap", features = ["test-support"] }
