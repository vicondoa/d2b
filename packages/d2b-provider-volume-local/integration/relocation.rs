//! integration-target: host-integration
//!
//! Declaration only. This file is not executable test coverage.
//! The scenario requires two real Host placements, adapter-routed anchored
//! source and destination views, midpoint crash injection, destination
//! activation, and a volume-virtiofs Export re-point before source deletion.
//! Those cross-Provider composition points are not wired, so the required
//! Host-to-Host relocation cannot yet be executed here.
