Compare the original judgment with the worktree after native `ce-work`
completes.

Dispatch `contributor.d2b-pr-comment-verifier` as a read-only review lane.
Require repository evidence for every accepted finding and retain unresolved
or operator-decision findings explicitly.  A missing or failed targeted check
must not be reported as verified.
