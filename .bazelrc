try-import %workspace%/.bazelrc.user

common --lockfile_mode=error
common --@protobuf//bazel/toolchains:prefer_prebuilt_protoc
common --per_file_copt=external/.*protobuf.*@--PROTOBUF_WAS_NOT_SUPPOSED_TO_BE_BUILT
common --host_per_file_copt=external/.*protobuf.*@--PROTOBUF_WAS_NOT_SUPPOSED_TO_BE_BUILT
common --per_file_copt=external/.*grpc.*@--GRPC_WAS_NOT_SUPPOSED_TO_BE_BUILT
common --host_per_file_copt=external/.*grpc.*@--GRPC_WAS_NOT_SUPPOSED_TO_BE_BUILT
common --remote_download_outputs=minimal
common --remote_retries=0
common --remote_timeout=60s

# Keep ordinary local/test actions unstamped and avoid unstable workspace data.
build --stamp=no
build --noslim_profile
build --experimental_profile_include_target_label
build --experimental_profile_include_primary_output
# The local profile is the exact same target graph without any provider
# endpoints. The bazel-check facade supplies the local repository-content
# option outside the remote profiles.
build:local --remote_executor=
build:local --remote_cache=
build:local --bes_backend=

# Remote profiles use the d2b BuildBuddy workspace. Authentication is
# the credential helper only. Direct API-key headers leak into BEP.
build:remote --remote_executor=grpcs://d2b.buildbuddy.io
build:remote --remote_cache=grpcs://d2b.buildbuddy.io
build:remote --bes_backend=grpcs://d2b.buildbuddy.io
build:remote --bes_results_url=https://d2b.buildbuddy.io/invocation/
build:remote --credential_helper=d2b.buildbuddy.io=%workspace%/tests/tools/bazel-check
build:remote --remote_timeout=10m
build:remote --remote_instance_name=d2b/developer/linux-x86_64/rules_rs/worker-v1/minimal/lock-v1
build:remote --platforms=@toolchains_buildbuddy//platforms:linux_x86_64
build:remote --extra_execution_platforms=@toolchains_buildbuddy//platforms:linux_x86_64
build:remote --extra_toolchains=@toolchains_buildbuddy//toolchains/cc:ubuntu_gcc_x86_64
build:remote --remote_download_minimal
build:remote --jobs=50
build:remote --remote_cache_compression
# Rustc and C build scripts both run on BuildBuddy so locally compiled
# glibc objects are not linked on the Ubuntu worker.
# Rustc uses BuildBuddy RBE plus the action cache.

build:trusted-seed --remote_executor=grpcs://d2b.buildbuddy.io
build:trusted-seed --remote_cache=grpcs://d2b.buildbuddy.io
build:trusted-seed --bes_backend=grpcs://d2b.buildbuddy.io
build:trusted-seed --bes_results_url=https://d2b.buildbuddy.io/invocation/
build:trusted-seed --credential_helper=d2b.buildbuddy.io=%workspace%/tests/tools/bazel-check
build:trusted-seed --remote_timeout=10m
build:trusted-seed --remote_instance_name=d2b/trusted-seed/linux-x86_64/rules_rs/worker-v1/minimal/lock-v1
build:trusted-seed --platforms=@toolchains_buildbuddy//platforms:linux_x86_64
build:trusted-seed --extra_execution_platforms=@toolchains_buildbuddy//platforms:linux_x86_64
build:trusted-seed --extra_toolchains=@toolchains_buildbuddy//toolchains/cc:ubuntu_gcc_x86_64
build:trusted-seed --remote_upload_local_results=true
build:trusted-seed --remote_cache_async=false
build:trusted-seed --remote_download_minimal
build:trusted-seed --jobs=50
build:trusted-seed --remote_cache_compression
# Build scripts execute remotely with the Ubuntu toolchain.

build:qualification --remote_executor=grpcs://d2b.buildbuddy.io
build:qualification --remote_cache=grpcs://d2b.buildbuddy.io
build:qualification --bes_backend=grpcs://d2b.buildbuddy.io
build:qualification --bes_results_url=https://d2b.buildbuddy.io/invocation/
build:qualification --credential_helper=d2b.buildbuddy.io=%workspace%/tests/tools/bazel-check
build:qualification --remote_timeout=10m
build:qualification --remote_instance_name=d2b/qualification/linux-x86_64/rules_rs/worker-v1/minimal/lock-v1
build:qualification --platforms=@toolchains_buildbuddy//platforms:linux_x86_64
build:qualification --extra_execution_platforms=@toolchains_buildbuddy//platforms:linux_x86_64
build:qualification --extra_toolchains=@toolchains_buildbuddy//toolchains/cc:ubuntu_gcc_x86_64
build:qualification --remote_download_minimal
build:qualification --jobs=50
build:qualification --remote_cache_compression
# Build scripts execute remotely with the Ubuntu toolchain.
