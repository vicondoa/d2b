# Deferred findings

This artifact records the current finding policy for the feature. Findings remain in the
shared discovery ledger until fixed and verified; classification does not change because of
elapsed review cycles. Late nonblocking observations remain informational history, while
blocking findings remain release-blocking.
