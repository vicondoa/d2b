### Changed

- Added fixed, local-only Bazel labels for Nix-unit groups, flake evaluation,
  realization, output checks, and aarch64 smoke evaluation with bounded RSS
  guards and cacheable declared inputs.
