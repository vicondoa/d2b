### Changed

- Replaced the controller reaction placeholder with a hermetic benchmark over
  the production redb Resource-API watch and Process Provider path, enforcing
  5 ms handler and 20 ms launch p95 budgets at 1/10/100 Process concurrency.
