### Changed

- Panel reviews now run on `gpt-5.6-sol` at `xhigh`, and the architecture
  agent uses the same model and effort with the 1M context tier.
- Existing `gemini-3.1-pro-preview` panel records at `high` remain accepted as
  an exact compatibility pair.
