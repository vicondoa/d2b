### Changed

- Credential, Device-GPU, and Device-USBIP provider crate lock
  acquisitions are now covered by per-site lint allows whose recorded
  reasons name the two genuine classes: the Device-GPU authority-lease
  cache is a synchronous provider port, and the recording test doubles
  are test-only helpers. The locks, the shared recording-double types
  the daemon builds its facet sets from, and every crate dependency are
  unchanged; the recorded reasons are what the banned-API allow policy
  validates.
