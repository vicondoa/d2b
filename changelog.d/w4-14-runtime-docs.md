### Fixed

- Documented the broker process entry point: `runtime` now carries a module
  doc, and `ServerConfig`, `BrokerMode`, `RunError`, `parse_command`, and
  `run` have doc comments with `# Errors` sections on the fallible functions.