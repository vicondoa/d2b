### Fixed

- Armed the async-gate scanner's method-call lock detection (issue #590): a `lock()`/`read()`/`write()` method call inside an `async fn` not followed by `.await` now fails `check-async-gate`, closing the blind spot where the production lock shape (`mutex.lock()`) was invisible to the deny list's qualified paths. Awaited tokio lock sites stay legitimate (the `.await` exclusion), and the escape hatch is the source-level marker `// async-gate-allow: <reason>` recorded in `packages/xtask/data/async-gate-inventory.json`; a marker without an inventory entry fails the gate, and a stale inventory entry fails it too.
- Reconciled the Cargo.toml and clippy.toml comments with the landed gate (they claimed the source-level policy check "is not landed here"), and re-baselined `blocking-census-baseline.json` to the census's clippy-visible counts, which the per-site allow conversion had left stale.