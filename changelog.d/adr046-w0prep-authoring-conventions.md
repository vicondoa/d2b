### Added

- Documented the authoring shapes the ADR 0046 spec registry actually
  tolerates: the optional work-item heading title and its four introducer
  forms, shortest-match ID anchoring for prefixes that themselves contain
  hyphens, the varying enclosing section title, escaped pipes in table cells,
  and the reserved range-expansion keyword in dependency cells.

### Fixed

- Corrected the work-item ID row, which described the heading as exact while
  187 of the 543 declared headings carry a title suffix.
