### Added

- Add fail-closed BuildBuddy qualification evidence with commit, closure,
  namespace, provider-transfer, cache, fallback, redaction, and latency
  bindings. Require independent fresh-worktree samples and protect the live
  wrapper with the trusted security digest. Missing provider-accounted
  transfer remains non-qualifying; caller-authored evidence is quarantined
  until a provider attestation or generated collector exists.
