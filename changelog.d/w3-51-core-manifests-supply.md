### Changed

- d2b-telemetry: drop the unused `rustix` dependency (the workspace entry stays for other members).
- d2b-bus: drop the unused `tempfile` dev-dependency.
- d2b: drop the unused `d2b-zone-routing` dependency from the manifest and the Bazel deps list.